# Drawing（梵天神策摇签）进度记录

> 每次实质更新在此追加一条（最新在上）。上级记录见 [`../progress.md`](../progress.md)，Obsidian 同步副本：`obsidian/Projects/zhanbu`。

## 2026-07-23 · 建立子项目 README 与独立进度记录

- **改了什么**：把原先混在 `Drawing/README.md` 里的站点级说明（两个应用总表、安装、发布）上移到仓库根 `zhanbu/README.md`，本 README 改为只讲 Drawing 自己；新建本文件作为 Drawing 的独立进度记录。
- **为什么改**：`zhanbu` 下实为两个独立小项目，此前只有一份混合 README、且没有任何子项目级记录；仓库顶层 README 缺失也是既有待办。
- **顺带修正了一处文档错误**：旧 README 写「二百签，每签两句五言偈」，与代码不符。核对 `data.js` 后更正为 **99 签、每签 8 句五言 + 白话译文**。
- **如何验证**：`python3` 解析 `Drawing-PWA/data.js` 的 `LOTS`，得条目数 **99**、`id` 1–99、每条 `lines` 长度均为 **8**、字段为 `id`/`lines`/`translation`；`摇签.txt` 编号偈句 `grep -cE "^\s*[0-9]+\."` = **398**，最大编号 398。

## 当前状态（2026-07-23，可用）

**已完成**

- 99 签数据（`data.js`），每签 8 句五言 + 白话译文
- 摇签流程：点击签筒/按钮或摇晃手机（`devicemotion`）触发 → 签筒摇动 → 签支飞出 → 签牌翻转 → 卷轴展开
- 一轮 7 策机制；每轮首次摇签前须诵「灌顶无上偈颂」；满 3 签后可「刷新」开新轮
- 本轮签文/译文侧栏记录，可回看；移动端签文↔译文视图切换
- `crypto.getRandomValues` 等概率随机；`navigator.vibrate` 震动反馈
- 内置《灌顶梵天神策经》卷第十开篇原文（「经文缘起」）
- 竖屏/横屏两套背景；自定义元素 `programmatic-scroll` 实现卷轴
- Service Worker 离线缓存（含法华文楷字体）；GitHub Pages 已发布

**待确认 / 未完成**

- ⚠️ **第 100 签疑似缺失**：经文自述「出梵天结愿一百偈颂」，`摇签.txt` 共 398 条编号偈句，按每签 4 组切分得 99 签（99 × 4 = 396），**第 397、398 两条未进入 `data.js`**。需核对是原经文本就缺 2 句，还是抄录时漏了——补齐才能凑满第 100 签。
- `make_excel.py` 内 `SRC`/`OUT` 仍是旧机器的 Windows 绝对路径（`D:\OneDrive\...`），在当前 Mac 上不能直接跑，需重新生成数据时得先改路径
- `art/` 下部分源素材（如 `签文背景2.webp`、`无花纹.png`）未见被 PWA 引用，是否清理待定

> 2026-07-23 之前的详细过程无记录，以上为按代码与数据核对后的回填基线。
