/**
 * Angular Separation Calculation
 *
 * Core functions for calculating angular distances between celestial bodies.
 *
 * @module aspects/angular-separation
 *
 * @remarks
 * All calculations use the shortest arc principle:
 * - Angular separation is always 0-180°
 * - Handles 360°/0° boundary correctly
 * - Uses floating-point arithmetic with appropriate precision
 *
 * @see IMPL.md Section 4 for algorithm specification
 */
/**
 * Normalize an angle to the range [0, 360).
 *
 * @param angle - Angle in degrees (can be any value)
 * @returns Normalized angle in range [0, 360)
 *
 * @example
 * ```typescript
 * normalizeAngle(370)   // 10
 * normalizeAngle(-10)   // 350
 * normalizeAngle(360)   // 0
 * normalizeAngle(0)     // 0
 * ```
 */
declare function normalizeAngle$3(angle: number): number;
/**
 * Calculate the angular separation between two ecliptic longitudes.
 *
 * @param lon1 - First ecliptic longitude in degrees
 * @param lon2 - Second ecliptic longitude in degrees
 * @returns Shortest arc separation in degrees (0-180)
 *
 * @remarks
 * This function always returns the shortest arc between the two points.
 * The order of arguments doesn't matter (separation is symmetric).
 *
 * This is the foundation for all aspect detection.
 *
 * @example
 * ```typescript
 * // Simple cases
 * angularSeparation(0, 90)    // 90
 * angularSeparation(0, 180)   // 180
 * angularSeparation(45, 45)   // 0
 *
 * // Crossing 0°/360° boundary
 * angularSeparation(350, 10)  // 20
 * angularSeparation(359, 1)   // 2
 *
 * // Always shortest arc
 * angularSeparation(0, 270)   // 90 (not 270)
 * angularSeparation(10, 200)  // 170 (not 190)
 * ```
 */
declare function angularSeparation$1(lon1: number, lon2: number): number;
/**
 * Calculate the signed angle from lon1 to lon2.
 *
 * @param lon1 - First ecliptic longitude in degrees
 * @param lon2 - Second ecliptic longitude in degrees
 * @returns Signed angle in degrees (-180 to +180)
 *
 * @remarks
 * Positive = lon2 is ahead of lon1 (going counterclockwise/eastward)
 * Negative = lon2 is behind lon1 (going clockwise/westward)
 *
 * This is needed for determining applying vs separating aspects.
 *
 * @example
 * ```typescript
 * signedAngle(0, 10)    // +10  (lon2 is 10° ahead)
 * signedAngle(10, 0)    // -10  (lon2 is 10° behind)
 * signedAngle(350, 10)  // +20  (crossing 0°)
 * signedAngle(10, 350)  // -20  (crossing 0°)
 * ```
 */
declare function signedAngle(lon1: number, lon2: number): number;
/**
 * Check if two angles are equal within epsilon tolerance.
 *
 * @param angle1 - First angle in degrees
 * @param angle2 - Second angle in degrees
 * @param epsilon - Tolerance in degrees
 * @returns True if angles are equal within tolerance
 *
 * @remarks
 * Handles wraparound at 360°/0° boundary correctly.
 */
declare function anglesAreEqual(angle1: number, angle2: number, epsilon?: number): boolean;
/**
 * Calculate the midpoint between two longitudes.
 *
 * @param lon1 - First ecliptic longitude in degrees
 * @param lon2 - Second ecliptic longitude in degrees
 * @returns Midpoint longitude in degrees (0-360)
 *
 * @remarks
 * Takes the midpoint of the shorter arc.
 * Handles 0°/360° boundary correctly.
 *
 * @example
 * ```typescript
 * midpoint(0, 90)     // 45
 * midpoint(350, 10)   // 0 (midpoint of 20° arc crossing 0°)
 * midpoint(90, 270)   // 180 or 0 (either midpoint is valid for opposition)
 * ```
 */
declare function midpoint(lon1: number, lon2: number): number;
/**
 * Get the sign (0-11) for a given ecliptic longitude.
 *
 * @param longitude - Ecliptic longitude in degrees
 * @returns Sign index (0 = Aries, 11 = Pisces)
 *
 * @example
 * ```typescript
 * getSignIndex(0)     // 0 (Aries)
 * getSignIndex(45)    // 1 (Taurus)
 * getSignIndex(359)   // 11 (Pisces)
 * ```
 */
declare function getSignIndex$1(longitude: number): number;
/**
 * Calculate the sign separation between two longitudes.
 *
 * @param lon1 - First ecliptic longitude in degrees
 * @param lon2 - Second ecliptic longitude in degrees
 * @returns Sign separation (0-11)
 *
 * @remarks
 * Used for detecting out-of-sign aspects.
 * Returns the absolute sign difference (always positive).
 *
 * @example
 * ```typescript
 * signSeparation(0, 90)     // 3 (Aries to Cancer)
 * signSeparation(0, 120)    // 4 (Aries to Leo)
 * signSeparation(350, 10)   // 0 (both in Pisces/Aries area)
 * ```
 */
declare function signSeparation(lon1: number, lon2: number): number;

/**
 * Aspects Module - Type Definitions
 *
 * Core types for astrological aspect calculations.
 *
 * @module aspects/types
 *
 * @remarks
 * All angles are in degrees (0-360 for longitudes, 0-180 for separations).
 * Aspects are angular relationships between celestial bodies along the ecliptic.
 *
 * @see KNOWLEDGE.md Section 9 for conceptual foundations
 * @see IMPL.md for full implementation specification
 */
/**
 * Supported aspect types.
 *
 * @remarks
 * Major aspects (Ptolemaic) are the five traditional aspects used since antiquity.
 * Minor aspects provide additional nuance with tighter orbs.
 */
declare enum AspectType {
    /** Conjunction: 0° - planets at same position, energies merge */
    Conjunction = "conjunction",
    /** Sextile: 60° - opportunity, harmonious support */
    Sextile = "sextile",
    /** Square: 90° - tension, challenge, dynamic action */
    Square = "square",
    /** Trine: 120° - harmony, ease, natural flow */
    Trine = "trine",
    /** Opposition: 180° - polarity, awareness, balance */
    Opposition = "opposition",
    /** Semi-sextile: 30° - subtle friction, minor adjustment */
    SemiSextile = "semi-sextile",
    /** Semi-square: 45° - irritation, minor tension */
    SemiSquare = "semi-square",
    /** Quintile: 72° - creativity, talent, special gifts */
    Quintile = "quintile",
    /** Sesquiquadrate: 135° - frustration, agitation */
    Sesquiquadrate = "sesquiquadrate",
    /** Biquintile: 144° - creative mastery, refined talent */
    Biquintile = "biquintile",
    /** Quincunx: 150° - adjustment needed, inconjunct, "blind spot" */
    Quincunx = "quincunx",
    /** Septile: 360°/7 ≈ 51.43° - fate, karma, spiritual connection */
    Septile = "septile",
    /** Novile: 360°/9 = 40° - spiritual completion, divine order */
    Novile = "novile",
    /** Decile: 360°/10 = 36° - growth, skill development */
    Decile = "decile"
}
/**
 * Classification of aspect types.
 */
type AspectClassification = 'major' | 'minor';
/**
 * Nature/quality of an aspect.
 *
 * @remarks
 * - Harmonious (soft): Easy energy flow (trine, sextile)
 * - Dynamic (hard): Tension requiring action (square, opposition)
 * - Neutral: Nature depends on planets involved (conjunction)
 */
type AspectNature = 'harmonious' | 'dynamic' | 'neutral';
/**
 * Definition of an aspect type with its properties.
 *
 * @remarks
 * This defines the static properties of an aspect type (angle, symbol, etc.)
 * as opposed to a detected aspect between two specific bodies.
 */
interface AspectDefinition {
    /** The aspect type identifier */
    type: AspectType;
    /** Exact angle in degrees (0-180) */
    angle: number;
    /** Unicode symbol for the aspect */
    symbol: string;
    /** Default orb in degrees */
    defaultOrb: number;
    /** Major or minor classification */
    classification: AspectClassification;
    /** Harmonious, dynamic, or neutral */
    nature: AspectNature;
    /** Human-readable name */
    name: string;
    /** Harmonic number (360 / angle) */
    harmonic: number;
}
/**
 * A detected aspect between two celestial bodies.
 *
 * @remarks
 * This represents an actual aspect found between two bodies at a specific time.
 * All angular values are in degrees.
 *
 * @example
 * ```typescript
 * const aspect: Aspect = {
 *   body1: 'Sun',
 *   body2: 'Saturn',
 *   type: AspectType.Trine,
 *   angle: 120,
 *   separation: 119.92,
 *   deviation: 0.08,
 *   orb: 8,
 *   strength: 99,
 *   isApplying: true,
 *   isOutOfSign: false,
 *   symbol: '△'
 * };
 * ```
 */
interface Aspect {
    /** First celestial body (by convention, the faster-moving body) */
    body1: string;
    /** Second celestial body */
    body2: string;
    /** Type of aspect */
    type: AspectType;
    /** Exact angle for this aspect type (e.g., 120 for trine) */
    angle: number;
    /** Actual angular separation between the bodies (0-180°) */
    separation: number;
    /** Deviation from exact aspect angle (always positive) */
    deviation: number;
    /** Orb used for this detection */
    orb: number;
    /** Aspect strength as percentage (100 = exact, 0 = at orb edge) */
    strength: number;
    /** Whether the aspect is applying (getting tighter) or separating */
    isApplying: boolean | null;
    /** Whether this is an out-of-sign (dissociate) aspect */
    isOutOfSign: boolean;
    /** The aspect symbol */
    symbol: string;
}
/**
 * Configuration options for aspect calculations.
 *
 * @remarks
 * Allows customization of which aspects to detect and how orbs are applied.
 */
interface AspectConfig$1 {
    /**
     * Which aspect types to detect.
     * @default Major aspects only (conjunction, sextile, square, trine, opposition)
     */
    aspectTypes?: AspectType[];
    /**
     * Custom orbs per aspect type (overrides defaults).
     * Values in degrees.
     */
    orbs?: Partial<Record<AspectType, number>>;
    /**
     * Whether to detect out-of-sign (dissociate) aspects.
     * @default true
     */
    includeOutOfSign?: boolean;
    /**
     * Strength multiplier for out-of-sign aspects (0-1).
     * Applied as: finalStrength = strength * (1 - outOfSignPenalty)
     * @default 0 (no penalty)
     */
    outOfSignPenalty?: number;
    /**
     * Minimum strength (0-100) to include in results.
     * @default 0 (include all within orb)
     */
    minimumStrength?: number;
    /**
     * Whether to calculate and include applying/separating info.
     * Requires longitudeSpeed data for the bodies.
     * @default true
     */
    includeApplying?: boolean;
}
/**
 * Input for aspect calculation: a body's position and optional speed.
 */
interface AspectBody {
    /** Identifier for the celestial body */
    name: string;
    /** Ecliptic longitude in degrees (0-360) */
    longitude: number;
    /** Longitude speed in degrees per day (positive = direct, negative = retrograde) */
    longitudeSpeed?: number;
}
/**
 * Result of calculating all aspects for a chart.
 */
interface AspectCalculationResult {
    /** All detected aspects */
    aspects: Aspect[];
    /** Configuration used for calculation */
    config: AspectConfig$1;
    /** Bodies that were analyzed */
    bodies: string[];
    /** Number of body pairs checked */
    pairsChecked: number;
}
/**
 * Types of aspect patterns (configurations of 3+ bodies).
 */
declare enum PatternType {
    /** T-Square: 2 squares + 1 opposition forming a T shape */
    TSquare = "T-Square",
    /** Grand Trine: 3 trines forming an equilateral triangle */
    GrandTrine = "Grand Trine",
    /** Grand Cross: 4 squares + 2 oppositions forming a cross */
    GrandCross = "Grand Cross",
    /** Yod (Finger of God): 2 quincunxes + 1 sextile */
    Yod = "Yod",
    /** Kite: Grand Trine + 1 opposition */
    Kite = "Kite",
    /** Mystic Rectangle: 2 trines + 2 sextiles + 2 oppositions */
    MysticRectangle = "Mystic Rectangle",
    /** Stellium: 3+ planets conjunct */
    Stellium = "Stellium"
}
/**
 * A detected aspect pattern.
 */
interface AspectPattern {
    /** Type of pattern */
    type: PatternType;
    /** Bodies involved in the pattern */
    bodies: string[];
    /** Aspects forming the pattern */
    aspects: Aspect[];
    /** Human-readable description */
    description: string;
}

/**
 * Aspect Detection
 *
 * Core functions for detecting aspects between celestial bodies.
 *
 * @module aspects/aspect-detection
 *
 * @remarks
 * This module combines angular separation with orb checking to detect
 * actual aspects between planetary positions.
 *
 * @see IMPL.md Section 4 for algorithm specification
 */

/**
 * Check if an aspect is out-of-sign (dissociate).
 *
 * @param lon1 - First body's ecliptic longitude
 * @param lon2 - Second body's ecliptic longitude
 * @param aspectType - The type of aspect
 * @returns True if the aspect is out-of-sign
 *
 * @remarks
 * An out-of-sign aspect occurs when two bodies form an aspect by degree
 * but are NOT in signs that traditionally form that aspect.
 *
 * Example: A trine at 28° Aries to 2° Virgo is out-of-sign because
 * Aries-Virgo don't form a traditional trine relationship (Fire-Earth).
 *
 * @example
 * ```typescript
 * // In-sign trine (Aries to Leo = Fire to Fire)
 * isOutOfSign(15, 135, AspectType.Trine) // false
 *
 * // Out-of-sign trine (Aries to Virgo)
 * isOutOfSign(28, 152, AspectType.Trine) // true
 * ```
 */
declare function isOutOfSign$1(lon1: number, lon2: number, aspectType: AspectType): boolean;
/**
 * Detect an aspect between two bodies.
 *
 * @param body1 - First celestial body
 * @param body2 - Second celestial body
 * @param config - Configuration for aspect detection
 * @returns Detected aspect or null if no aspect within orb
 *
 * @example
 * ```typescript
 * const sun = { name: 'Sun', longitude: 280.37, longitudeSpeed: 1.02 };
 * const moon = { name: 'Moon', longitude: 223.32, longitudeSpeed: 13.18 };
 *
 * const aspect = detectAspect(sun, moon);
 * // { type: 'sextile', separation: 57.05, deviation: 2.95, ... }
 * ```
 */
declare function detectAspect(body1: AspectBody, body2: AspectBody, config?: AspectConfig$1): Aspect | null;
/**
 * Find all aspects between a set of bodies.
 *
 * @param bodies - Array of celestial bodies with positions
 * @param config - Configuration for aspect detection
 * @returns All detected aspects
 *
 * @remarks
 * Checks all unique pairs of bodies. Does not check self-aspects.
 * Results are sorted by strength (strongest first).
 *
 * @example
 * ```typescript
 * const bodies = [
 *   { name: 'Sun', longitude: 280.37 },
 *   { name: 'Moon', longitude: 223.32 },
 *   { name: 'Mercury', longitude: 271.89 },
 * ];
 *
 * const aspects = findAllAspects(bodies);
 * ```
 */
declare function findAllAspects(bodies: AspectBody[], config?: AspectConfig$1): Aspect[];
/**
 * Calculate aspects with full result metadata.
 *
 * @param bodies - Array of celestial bodies with positions
 * @param config - Configuration for aspect detection
 * @returns Complete calculation result with metadata
 */
declare function calculateAspects(bodies: AspectBody[], config?: AspectConfig$1): AspectCalculationResult;
/**
 * Get a summary of aspects by type.
 *
 * @param aspects - Array of detected aspects
 * @returns Count of each aspect type
 */
declare function getAspectSummary(aspects: Aspect[]): Record<AspectType, number>;
/**
 * Filter aspects by type(s).
 *
 * @param aspects - Array of aspects to filter
 * @param types - Aspect type(s) to include
 * @returns Filtered aspects
 */
declare function filterAspectsByType(aspects: Aspect[], types: AspectType | AspectType[]): Aspect[];
/**
 * Filter aspects by body.
 *
 * @param aspects - Array of aspects to filter
 * @param bodyName - Name of body to filter for
 * @returns Aspects involving the specified body
 */
declare function filterAspectsByBody(aspects: Aspect[], bodyName: string): Aspect[];
/**
 * Get the strongest aspect for a body.
 *
 * @param aspects - Array of aspects
 * @param bodyName - Name of body
 * @returns Strongest aspect involving the body, or null
 */
declare function getStrongestAspect$2(aspects: Aspect[], bodyName: string): Aspect | null;
/**
 * Format an aspect for display.
 *
 * @param aspect - The aspect to format
 * @returns Human-readable string
 *
 * @example
 * ```typescript
 * formatAspect(aspect)
 * // "Sun △ Saturn (0°05', 99%, applying)"
 * ```
 */
declare function formatAspect$1(aspect: Aspect): string;

/**
 * Aspects Module - Constants
 *
 * Aspect definitions, symbols, and default orb values.
 *
 * @module aspects/constants
 *
 * @remarks
 * All aspect angles and orbs are verified against authoritative sources:
 * - Ptolemy's "Tetrabiblos" (major aspects)
 * - Lilly's "Christian Astrology" (traditional orbs)
 * - Modern consensus (Astrograph, Astro.com)
 *
 * @see IMPL.md Section 2 and 3 for detailed specifications
 */

/**
 * Default orb values for each aspect type in degrees.
 *
 * @remarks
 * These are consensus values from modern astrological practice:
 * - Major aspects: 6-8° orbs
 * - Minor aspects: 2-3° orbs
 *
 * Sources:
 * - Astrograph.com aspect orb tables
 * - Astro.com default settings
 * - Robert Hand's recommendations
 *
 * Note: Luminaries (Sun/Moon) often use wider orbs (+2°).
 * This is handled via configuration, not built into these defaults.
 */
declare const DEFAULT_ORBS: Record<AspectType, number>;
/**
 * Major aspects (Ptolemaic) - the five traditional aspects.
 */
declare const MAJOR_ASPECTS$1: AspectType[];
/**
 * Minor aspects - additional aspects with tighter orbs.
 */
declare const MINOR_ASPECTS: AspectType[];
/**
 * Kepler aspects - harmonic aspects from Johannes Kepler's "Harmonices Mundi" (1619).
 *
 * @remarks
 * These aspects are derived from dividing the circle by 7, 9, and 10.
 * They have very tight orbs (1°) and are considered subtle influences.
 *
 * Mathematical derivation:
 * - Septile: 360°/7 = 51.42857142857143°
 * - Novile: 360°/9 = 40° exactly
 * - Decile: 360°/10 = 36° exactly
 */
declare const KEPLER_ASPECTS: AspectType[];
/**
 * All supported aspect types.
 */
declare const ALL_ASPECTS$1: AspectType[];
/**
 * Complete definitions for all supported aspects.
 *
 * @remarks
 * This combines all aspect properties into a single reference object.
 * Use this for looking up aspect properties by type.
 *
 * @example
 * ```typescript
 * const trine = ASPECT_DEFINITIONS[AspectType.Trine];
 * console.log(trine.angle); // 120
 * console.log(trine.symbol); // '△'
 * console.log(trine.nature); // 'harmonious'
 * ```
 */
declare const ASPECT_DEFINITIONS: Record<AspectType, AspectDefinition>;
/**
 * Expected sign separations for each aspect type.
 *
 * @remarks
 * Used to detect out-of-sign (dissociate) aspects.
 * An aspect is "out of sign" when the planets are in aspect by degree
 * but NOT in signs that traditionally form that aspect.
 *
 * Sign separation = (sign2 - sign1 + 12) % 12
 *
 * Example for Square (3 signs apart):
 * - Aries (0) to Cancer (3) = 3 ✓
 * - Aries (0) to Capricorn (9) = 9 (= 12 - 3) ✓
 */
declare const ASPECT_SIGN_SEPARATIONS: Record<AspectType, number[]>;

/**
 * Orb Handling and Configuration
 *
 * Functions for managing aspect orbs and calculating aspect strength.
 *
 * @module aspects/orbs
 *
 * @remarks
 * Orbs define the allowable deviation from exact aspect angles.
 * Wider orbs = more aspects detected; tighter orbs = fewer, stronger aspects.
 *
 * @see IMPL.md Section 3 for orb system specification
 */

/**
 * Get the effective orb for an aspect type, considering configuration.
 *
 * @param aspectType - The type of aspect
 * @param config - Optional configuration with custom orbs
 * @returns Effective orb in degrees
 *
 * @example
 * ```typescript
 * // Default orb
 * getOrb(AspectType.Trine) // 8
 *
 * // Custom orb
 * getOrb(AspectType.Trine, { orbs: { trine: 6 } }) // 6
 * ```
 */
declare function getOrb(aspectType: AspectType, config?: AspectConfig$1): number;
/**
 * Calculate aspect strength based on deviation from exact.
 *
 * @param deviation - Angular deviation from exact aspect in degrees (always positive)
 * @param orb - The orb being used for this aspect
 * @returns Strength as percentage (100 = exact, 0 = at orb edge)
 *
 * @remarks
 * Strength decreases linearly from 100% at exact to 0% at orb boundary.
 * This is a simple linear model; some traditions use other decay curves.
 *
 * Formula: strength = 100 × (1 - deviation/orb)
 *
 * @example
 * ```typescript
 * calculateStrength(0, 8)   // 100 (exact)
 * calculateStrength(4, 8)   // 50  (halfway)
 * calculateStrength(8, 8)   // 0   (at orb edge)
 * calculateStrength(2, 8)   // 75
 * ```
 */
declare function calculateStrength(deviation: number, orb: number): number;
/**
 * Check if a separation falls within orb of an aspect angle.
 *
 * @param separation - Angular separation between two bodies (0-180°)
 * @param aspectAngle - The exact angle for the aspect type
 * @param orb - The orb to use
 * @returns True if the separation is within orb of the aspect angle
 *
 * @example
 * ```typescript
 * isWithinOrb(88, 90, 7)   // true (square with 2° deviation)
 * isWithinOrb(85, 90, 3)   // false (5° deviation, only 3° orb)
 * isWithinOrb(120, 120, 8) // true (exact trine)
 * ```
 */
declare function isWithinOrb(separation: number, aspectAngle: number, orb: number): boolean;
/**
 * Find which aspect (if any) a separation matches.
 *
 * @param separation - Angular separation between two bodies (0-180°)
 * @param config - Configuration specifying which aspects to check and orbs
 * @returns The matching aspect definition and deviation, or null if no match
 *
 * @remarks
 * When multiple aspects could match (rare with reasonable orbs),
 * returns the one with smallest deviation (closest to exact).
 *
 * @example
 * ```typescript
 * const result = findMatchingAspect(88, { aspectTypes: [AspectType.Square] });
 * // { aspect: { type: 'square', angle: 90, ... }, deviation: 2 }
 * ```
 */
declare function findMatchingAspect(separation: number, config?: AspectConfig$1): {
    aspect: AspectDefinition;
    deviation: number;
} | null;
/**
 * Get all aspects that a separation could match (for debugging/analysis).
 *
 * @param separation - Angular separation between two bodies (0-180°)
 * @param config - Configuration specifying which aspects to check and orbs
 * @returns Array of matching aspects with deviations, sorted by deviation
 *
 * @remarks
 * Normally you'd use findMatchingAspect which returns only the best match.
 * This function is useful for understanding edge cases where multiple
 * aspects are within orb.
 */
declare function findAllMatchingAspects(separation: number, config?: AspectConfig$1): Array<{
    aspect: AspectDefinition;
    deviation: number;
}>;
/**
 * Apply out-of-sign penalty to strength.
 *
 * @param strength - Original strength (0-100)
 * @param isOutOfSign - Whether the aspect is out-of-sign
 * @param penalty - Penalty factor (0-1), e.g., 0.2 = 20% reduction
 * @returns Adjusted strength
 *
 * @remarks
 * Out-of-sign aspects are traditionally considered weaker.
 * This function applies a configurable penalty.
 *
 * Formula: adjustedStrength = strength × (1 - penalty) if out-of-sign
 *
 * @example
 * ```typescript
 * applyOutOfSignPenalty(80, false, 0.2) // 80 (no penalty)
 * applyOutOfSignPenalty(80, true, 0.2)  // 64 (20% reduction)
 * applyOutOfSignPenalty(80, true, 0)    // 80 (no penalty configured)
 * ```
 */
declare function applyOutOfSignPenalty(strength: number, isOutOfSign: boolean, penalty: number): number;
/**
 * Create an aspect configuration with defaults filled in.
 *
 * @param partial - Partial configuration to merge with defaults
 * @returns Complete configuration with all values defined
 */
declare function createAspectConfig(partial?: Partial<AspectConfig$1>): Required<AspectConfig$1>;

/**
 * Aspect Pattern Detection
 *
 * Detection of complex aspect configurations (T-Square, Grand Trine, etc.)
 *
 * @module aspects/patterns
 *
 * @remarks
 * Aspect patterns are geometric configurations formed by 3+ celestial bodies.
 * Each pattern has specific requirements for the aspects that form it.
 *
 * Pattern definitions (mathematical, verifiable):
 * - **T-Square**: 2 squares (90°) + 1 opposition (180°) forming a T
 * - **Grand Trine**: 3 trines (120°) forming an equilateral triangle
 * - **Grand Cross**: 4 squares (90°) + 2 oppositions (180°) forming a cross
 * - **Yod**: 2 quincunxes (150°) + 1 sextile (60°) forming a Y
 * - **Kite**: Grand Trine + 1 opposition to one vertex
 * - **Mystic Rectangle**: 2 trines + 2 sextiles + 2 oppositions
 * - **Stellium**: 3+ bodies within conjunction orb
 *
 * @see IMPL.md Section 7 for pattern specifications
 */

/**
 * Find all aspect patterns in a set of aspects.
 *
 * @param aspects - Array of detected aspects
 * @returns Array of detected patterns, sorted by significance
 *
 * @remarks
 * Patterns are detected in order of rarity/significance:
 * 1. Grand Cross (rarest)
 * 2. Kite
 * 3. Mystic Rectangle
 * 4. Grand Trine
 * 5. T-Square
 * 6. Yod
 * 7. Stellium
 *
 * @example
 * ```typescript
 * const aspects = findAllAspects(bodies);
 * const patterns = findPatterns(aspects);
 *
 * for (const pattern of patterns) {
 *   console.log(`${pattern.type}: ${pattern.bodies.join(', ')}`);
 * }
 * ```
 */
declare function findPatterns(aspects: Aspect[]): AspectPattern[];
/**
 * Detect T-Square patterns.
 *
 * @remarks
 * A T-Square consists of:
 * - 1 opposition (180°)
 * - 2 squares (90°) from each end of the opposition to a common apex
 *
 * Mathematical verification:
 * - Body A at 0°, Body B at 180° (opposition)
 * - Body C at 90° (apex)
 * - A-C = 90° (square), B-C = 90° (square) ✓
 */
declare function detectTSquare(aspects: Aspect[]): AspectPattern[];
/**
 * Detect Grand Trine patterns.
 *
 * @remarks
 * A Grand Trine consists of:
 * - 3 trines (120°) connecting 3 bodies in an equilateral triangle
 *
 * Mathematical verification:
 * - Body A at 0°, Body B at 120°, Body C at 240°
 * - A-B = 120°, B-C = 120°, A-C = 120° ✓
 */
declare function detectGrandTrine(aspects: Aspect[]): AspectPattern[];
/**
 * Detect Grand Cross patterns.
 *
 * @remarks
 * A Grand Cross consists of:
 * - 4 squares (90°) forming a square
 * - 2 oppositions (180°) as diagonals
 *
 * Mathematical verification:
 * - Bodies at 0°, 90°, 180°, 270°
 * - 4 squares + 2 oppositions ✓
 */
declare function detectGrandCross(aspects: Aspect[]): AspectPattern[];
/**
 * Detect Yod (Finger of God) patterns.
 *
 * @remarks
 * A Yod consists of:
 * - 2 quincunxes (150°) from two planets to a common apex
 * - 1 sextile (60°) between the base planets
 *
 * Mathematical verification:
 * - Body A at 0°, Body B at 60° (sextile base)
 * - Body C at 150° (apex)
 * - A-C = 150° (quincunx), B-C = 150° (quincunx) ✓
 */
declare function detectYod(aspects: Aspect[]): AspectPattern[];
/**
 * Detect Kite patterns.
 *
 * @remarks
 * A Kite consists of:
 * - Grand Trine (3 trines)
 * - 1 opposition from one vertex to a 4th planet
 * - 2 sextiles from the 4th planet to the other two vertices
 *
 * Mathematical verification:
 * - Bodies at 0°, 120°, 240° (Grand Trine)
 * - Body at 180° (opposite to 0°)
 * - Forms sextiles to 120° and 240° ✓
 */
declare function detectKite(aspects: Aspect[]): AspectPattern[];
/**
 * Detect Mystic Rectangle patterns.
 *
 * @remarks
 * A Mystic Rectangle consists of:
 * - 2 oppositions (180°)
 * - 2 trines (120°)
 * - 2 sextiles (60°)
 *
 * Mathematical verification:
 * - Bodies at 0°, 60°, 180°, 240°
 * - Oppositions: 0°-180°, 60°-240° ✓
 * - Trines: 0°-240°, 60°-180° ✓
 * - Sextiles: 0°-60°, 180°-240° ✓
 */
declare function detectMysticRectangle(aspects: Aspect[]): AspectPattern[];
/**
 * Detect Stellium patterns.
 *
 * @remarks
 * A Stellium consists of:
 * - 3 or more planets in conjunction (all within orb of each other)
 *
 * Different traditions define stellium as:
 * - Modern: 3+ planets within ~10° span
 * - Traditional: 3+ planets in same sign
 *
 * This implementation uses the conjunction aspect approach.
 */
declare function detectStellium(aspects: Aspect[]): AspectPattern[];
/**
 * Get a summary of detected patterns by type.
 */
declare function getPatternSummary(patterns: AspectPattern[]): Record<PatternType, number>;
/**
 * Format a pattern for display.
 */
declare function formatPattern(pattern: AspectPattern): string;

/**
 * Aspects Module
 *
 * Angular relationships between celestial bodies used in astrological interpretation.
 *
 * @module aspects
 *
 * @remarks
 * This module provides comprehensive aspect calculation capabilities:
 *
 * - **Aspect Detection**: Find aspects between any pair of bodies
 * - **Orb Configuration**: Customizable orbs for each aspect type
 * - **Strength Calculation**: Linear decay from exact to orb edge
 * - **Applying/Separating**: Determine if aspects are forming or waning
 * - **Out-of-Sign Detection**: Identify dissociate aspects
 *
 * @example
 * ```typescript
 * import { calculateAspects, AspectType } from 'celestine/aspects';
 *
 * const bodies = [
 *   { name: 'Sun', longitude: 280.37, longitudeSpeed: 1.02 },
 *   { name: 'Moon', longitude: 223.32, longitudeSpeed: 13.18 },
 *   { name: 'Mercury', longitude: 271.89, longitudeSpeed: 1.56 },
 * ];
 *
 * const result = calculateAspects(bodies);
 *
 * for (const aspect of result.aspects) {
 *   console.log(`${aspect.body1} ${aspect.symbol} ${aspect.body2} (${aspect.strength}%)`);
 * }
 * ```
 *
 * @see IMPL.md for detailed implementation specification
 */

declare const index$7_ASPECT_DEFINITIONS: typeof ASPECT_DEFINITIONS;
declare const index$7_ASPECT_SIGN_SEPARATIONS: typeof ASPECT_SIGN_SEPARATIONS;
type index$7_Aspect = Aspect;
type index$7_AspectBody = AspectBody;
type index$7_AspectCalculationResult = AspectCalculationResult;
type index$7_AspectDefinition = AspectDefinition;
type index$7_AspectPattern = AspectPattern;
type index$7_AspectType = AspectType;
declare const index$7_AspectType: typeof AspectType;
declare const index$7_DEFAULT_ORBS: typeof DEFAULT_ORBS;
declare const index$7_KEPLER_ASPECTS: typeof KEPLER_ASPECTS;
declare const index$7_MINOR_ASPECTS: typeof MINOR_ASPECTS;
type index$7_PatternType = PatternType;
declare const index$7_PatternType: typeof PatternType;
declare const index$7_anglesAreEqual: typeof anglesAreEqual;
declare const index$7_applyOutOfSignPenalty: typeof applyOutOfSignPenalty;
declare const index$7_calculateAspects: typeof calculateAspects;
declare const index$7_calculateStrength: typeof calculateStrength;
declare const index$7_createAspectConfig: typeof createAspectConfig;
declare const index$7_detectAspect: typeof detectAspect;
declare const index$7_detectGrandCross: typeof detectGrandCross;
declare const index$7_detectGrandTrine: typeof detectGrandTrine;
declare const index$7_detectKite: typeof detectKite;
declare const index$7_detectMysticRectangle: typeof detectMysticRectangle;
declare const index$7_detectStellium: typeof detectStellium;
declare const index$7_detectTSquare: typeof detectTSquare;
declare const index$7_detectYod: typeof detectYod;
declare const index$7_filterAspectsByBody: typeof filterAspectsByBody;
declare const index$7_filterAspectsByType: typeof filterAspectsByType;
declare const index$7_findAllAspects: typeof findAllAspects;
declare const index$7_findAllMatchingAspects: typeof findAllMatchingAspects;
declare const index$7_findMatchingAspect: typeof findMatchingAspect;
declare const index$7_findPatterns: typeof findPatterns;
declare const index$7_formatPattern: typeof formatPattern;
declare const index$7_getAspectSummary: typeof getAspectSummary;
declare const index$7_getOrb: typeof getOrb;
declare const index$7_getPatternSummary: typeof getPatternSummary;
declare const index$7_isWithinOrb: typeof isWithinOrb;
declare const index$7_midpoint: typeof midpoint;
declare const index$7_signSeparation: typeof signSeparation;
declare const index$7_signedAngle: typeof signedAngle;
declare namespace index$7 {
  export { ALL_ASPECTS$1 as ALL_ASPECTS, index$7_ASPECT_DEFINITIONS as ASPECT_DEFINITIONS, index$7_ASPECT_SIGN_SEPARATIONS as ASPECT_SIGN_SEPARATIONS, type index$7_Aspect as Aspect, type index$7_AspectBody as AspectBody, type index$7_AspectCalculationResult as AspectCalculationResult, type AspectConfig$1 as AspectConfig, type index$7_AspectDefinition as AspectDefinition, type index$7_AspectPattern as AspectPattern, index$7_AspectType as AspectType, index$7_DEFAULT_ORBS as DEFAULT_ORBS, index$7_KEPLER_ASPECTS as KEPLER_ASPECTS, MAJOR_ASPECTS$1 as MAJOR_ASPECTS, index$7_MINOR_ASPECTS as MINOR_ASPECTS, index$7_PatternType as PatternType, index$7_anglesAreEqual as anglesAreEqual, angularSeparation$1 as angularSeparation, index$7_applyOutOfSignPenalty as applyOutOfSignPenalty, index$7_calculateAspects as calculateAspects, index$7_calculateStrength as calculateStrength, index$7_createAspectConfig as createAspectConfig, index$7_detectAspect as detectAspect, index$7_detectGrandCross as detectGrandCross, index$7_detectGrandTrine as detectGrandTrine, index$7_detectKite as detectKite, index$7_detectMysticRectangle as detectMysticRectangle, index$7_detectStellium as detectStellium, index$7_detectTSquare as detectTSquare, index$7_detectYod as detectYod, index$7_filterAspectsByBody as filterAspectsByBody, index$7_filterAspectsByType as filterAspectsByType, index$7_findAllAspects as findAllAspects, index$7_findAllMatchingAspects as findAllMatchingAspects, index$7_findMatchingAspect as findMatchingAspect, index$7_findPatterns as findPatterns, formatAspect$1 as formatAspect, index$7_formatPattern as formatPattern, index$7_getAspectSummary as getAspectSummary, index$7_getOrb as getOrb, index$7_getPatternSummary as getPatternSummary, getSignIndex$1 as getSignIndex, getStrongestAspect$2 as getStrongestAspect, isOutOfSign$1 as isOutOfSign, index$7_isWithinOrb as isWithinOrb, index$7_midpoint as midpoint, normalizeAngle$3 as normalizeAngle, index$7_signSeparation as signSeparation, index$7_signedAngle as signedAngle };
}

/**
 * Ephemeris Module - Type Definitions
 *
 * Core types for planetary position calculations.
 *
 * @module ephemeris/types
 *
 * @remarks
 * All angles are in degrees unless otherwise specified.
 * All distances are in Astronomical Units (AU) unless otherwise specified.
 *
 * @see Jean Meeus, "Astronomical Algorithms" (2nd Ed., 1998) for algorithm foundations
 * @see IAU 2012 Resolution B2 for astronomical unit definition (149,597,870.700 km)
 */
/**
 * Celestial bodies supported by the ephemeris.
 *
 * @remarks
 * Priority levels for implementation:
 * - P0: Sun, Moon (foundation)
 * - P1: Classical planets (Mercury-Saturn) + North Node
 * - P2: Modern planets (Uranus-Pluto) + True Node
 * - P3: Minor bodies (Lilith, Chiron)
 */
declare enum Planet$1 {
    /** The Sun - center of our solar system */
    Sun = "Sun",
    /** The Moon - Earth's natural satellite */
    Moon = "Moon",
    /** Mercury - innermost planet */
    Mercury = "Mercury",
    /** Venus - second planet from Sun */
    Venus = "Venus",
    /** Mars - fourth planet, first superior planet */
    Mars = "Mars",
    /** Jupiter - largest planet */
    Jupiter = "Jupiter",
    /** Saturn - ringed gas giant */
    Saturn = "Saturn",
    /** Uranus - first modern planet (discovered 1781) */
    Uranus = "Uranus",
    /** Neptune - eighth planet (discovered 1846) */
    Neptune = "Neptune",
    /** Pluto - dwarf planet (discovered 1930) */
    Pluto = "Pluto",
    /**
     * North Lunar Node (Mean) - ascending node of Moon's orbit
     *
     * @remarks
     * The point where the Moon's orbit crosses the ecliptic going northward.
     * Mean node uses average motion; True node includes oscillations.
     */
    NorthNode = "NorthNode",
    /**
     * South Lunar Node - descending node of Moon's orbit
     *
     * @remarks
     * Always exactly opposite (180°) from North Node.
     */
    SouthNode = "SouthNode",
    /**
     * Mean Black Moon Lilith - lunar apogee
     *
     * @remarks
     * The point where the Moon is farthest from Earth in its orbit.
     * Not a physical body, but an orbital point.
     */
    Lilith = "Lilith",
    /**
     * Chiron - centaur orbiting between Saturn and Uranus
     *
     * @remarks
     * Discovered 1977. Orbital period ~50 years.
     * Highly elliptical orbit makes calculation more complex.
     */
    Chiron = "Chiron",
    /**
     * Ceres - dwarf planet in the asteroid belt
     *
     * @remarks
     * Largest object in asteroid belt. Orbital period ~4.6 years.
     * Represents nurturing and motherhood in astrology.
     */
    Ceres = "Ceres",
    /**
     * Pallas - second largest asteroid
     *
     * @remarks
     * Highest inclination (34.8°) of major asteroids.
     * Represents wisdom and creative intelligence.
     */
    Pallas = "Pallas",
    /**
     * Juno - asteroid representing partnerships
     *
     * @remarks
     * Orbital period ~4.4 years.
     * Represents committed relationships and equality.
     */
    Juno = "Juno",
    /**
     * Vesta - brightest asteroid
     *
     * @remarks
     * Second largest asteroid. Shortest period ~3.6 years.
     * Represents devotion and sacred service.
     */
    Vesta = "Vesta"
}
/**
 * Position of a celestial body at a given moment.
 *
 * @remarks
 * All positions are geocentric (as seen from Earth's center)
 * in the tropical zodiac system (ecliptic coordinates).
 *
 * @example
 * ```typescript
 * const sunPosition: PlanetPosition = {
 *   longitude: 280.458,      // ~10° Capricorn
 *   latitude: 0.0001,        // Sun is always near ecliptic
 *   distance: 0.9833,        // ~0.98 AU in January
 *   longitudeSpeed: 1.0194,  // ~1°/day
 *   isRetrograde: false,     // Sun never retrogrades
 * };
 * ```
 */
interface PlanetPosition {
    /**
     * Ecliptic longitude in degrees (0-360).
     *
     * @remarks
     * This is the primary output - the position along the zodiac.
     * 0° = Vernal Equinox (0° Aries in tropical zodiac)
     * Measured along the ecliptic plane.
     */
    longitude: number;
    /**
     * Ecliptic latitude in degrees (-90 to +90).
     *
     * @remarks
     * Angular distance north (+) or south (-) of the ecliptic plane.
     * For Sun: always ~0° (by definition, Sun defines the ecliptic)
     * For Moon: ranges from -5.3° to +5.3°
     * For planets: typically within ±3° of ecliptic
     */
    latitude: number;
    /**
     * Distance from Earth in Astronomical Units (AU).
     *
     * @remarks
     * 1 AU = 149,597,870.700 km (IAU 2012 definition)
     * Sun distance: ~0.983 AU (perihelion) to ~1.017 AU (aphelion)
     * Moon distance: ~0.0024 AU (~356,500 km) to ~0.0027 AU (~406,700 km)
     */
    distance: number;
    /**
     * Rate of change of longitude in degrees per day.
     *
     * @remarks
     * Positive = direct motion (normal, eastward through zodiac)
     * Negative = retrograde motion (apparent backward motion)
     * Zero = stationary (at station point)
     *
     * Typical values:
     * - Sun: ~0.95° to ~1.02°/day (varies with Earth's orbital speed)
     * - Moon: ~11.5° to ~14.5°/day
     * - Mercury: -1.4° to +2.2°/day
     * - Mars: -0.4° to +0.8°/day
     * - Jupiter: -0.13° to +0.23°/day
     */
    longitudeSpeed: number;
    /**
     * True if the body is in apparent retrograde motion.
     *
     * @remarks
     * Convenience flag: equivalent to `longitudeSpeed < 0`
     *
     * Sun and Moon never retrograde.
     * Mercury retrogrades ~3 times per year for ~3 weeks each.
     * Outer planets retrograde once per year for several months.
     */
    isRetrograde: boolean;
}
/**
 * Options for ephemeris calculations.
 *
 * @remarks
 * Default options provide arcminute-level accuracy sufficient for astrology.
 * Enable additional corrections for higher precision requirements.
 */
interface EphemerisOptions {
    /**
     * Calculate and include daily motion (speed).
     *
     * @default true
     *
     * @remarks
     * When true, calculates position for JD and JD+1 to determine speed.
     * Disable for slight performance improvement when speed not needed.
     */
    includeSpeed?: boolean;
    /**
     * Apply nutation correction to longitude.
     *
     * @default false
     *
     * @remarks
     * Nutation is a small periodic oscillation in Earth's axis (~±17").
     * For arcminute precision: not needed (error < 0.3')
     * For arcsecond precision: required
     *
     * @see Meeus Ch. 22 for nutation calculation
     */
    includeNutation?: boolean;
    /**
     * Apply aberration correction.
     *
     * @default false (except Sun where it's always applied)
     *
     * @remarks
     * Aberration is the apparent shift due to Earth's orbital velocity
     * combined with the finite speed of light. Effect up to ~20".
     *
     * For Sun: always included as it's significant (~20")
     * For planets: optional, typically < 1"
     */
    includeAberration?: boolean;
    /**
     * Apply light-time correction.
     *
     * @default false
     *
     * @remarks
     * We see planets where they were, not where they are, due to
     * the time light takes to travel from planet to Earth.
     *
     * For inner planets: effect < 1"
     * For outer planets: can be several arcseconds
     */
    includeLightTime?: boolean;
}
/**
 * Result of calculating positions for multiple bodies.
 *
 * @example
 * ```typescript
 * const positions = getAllPositions(julianDate);
 * const sun = positions.get(Planet.Sun);
 * const moon = positions.get(Planet.Moon);
 * ```
 */
type PlanetPositions$1 = Map<Planet$1, PlanetPosition>;
/**
 * Rectangular (Cartesian) coordinates in 3D space.
 *
 * @remarks
 * Used internally for coordinate transformations.
 * Heliocentric: X-Y plane = ecliptic, X-axis = vernal equinox
 * Geocentric: Same orientation, but centered on Earth
 *
 * @internal
 */
interface RectangularCoordinates {
    /** X coordinate in AU (toward vernal equinox) */
    x: number;
    /** Y coordinate in AU (90° ahead in ecliptic) */
    y: number;
    /** Z coordinate in AU (toward north ecliptic pole) */
    z: number;
}
/**
 * Spherical coordinates (longitude, latitude, distance).
 *
 * @remarks
 * Used internally before final conversion to PlanetPosition.
 *
 * @internal
 */
interface SphericalCoordinates {
    /** Longitude in degrees (0-360) */
    longitude: number;
    /** Latitude in degrees (-90 to +90) */
    latitude: number;
    /** Distance in AU */
    distance: number;
}
/**
 * Orbital elements for a celestial body.
 *
 * @remarks
 * Six elements fully describe an orbit. Used for planet position calculations.
 * Elements are typically given as polynomials in Julian centuries (T).
 *
 * @see Meeus Ch. 31 for orbital element definitions
 * @see Meeus Table 31.a for planetary orbital elements
 *
 * @internal
 */
interface OrbitalElements {
    /**
     * Mean longitude at epoch (L₀) in degrees.
     *
     * @remarks
     * The mean position of the body if it moved uniformly.
     */
    meanLongitude: number;
    /**
     * Semi-major axis (a) in AU.
     *
     * @remarks
     * Half the longest diameter of the elliptical orbit.
     * For simplified calculations, often treated as constant.
     */
    semiMajorAxis: number;
    /**
     * Eccentricity (e) - dimensionless.
     *
     * @remarks
     * Shape of the orbit: 0 = circle, 0-1 = ellipse
     * Earth: ~0.0167, Mercury: ~0.2056, Pluto: ~0.2488
     */
    eccentricity: number;
    /**
     * Inclination (i) in degrees.
     *
     * @remarks
     * Tilt of orbital plane relative to the ecliptic.
     * For major planets: typically < 3° (except Pluto ~17°)
     */
    inclination: number;
    /**
     * Longitude of ascending node (Ω) in degrees.
     *
     * @remarks
     * Where the orbit crosses the ecliptic going northward.
     */
    longitudeOfAscendingNode: number;
    /**
     * Longitude of perihelion (ω̃) in degrees.
     *
     * @remarks
     * Direction from Sun to the closest point in orbit.
     * ω̃ = Ω + ω (where ω is argument of perihelion)
     */
    longitudeOfPerihelion: number;
}
/**
 * Validation result for ephemeris input parameters.
 *
 * @internal
 */
interface EphemerisValidationResult {
    /** Whether the input is valid */
    valid: boolean;
    /** Error message if invalid */
    error?: string;
    /** Warning messages for edge cases */
    warnings?: string[];
}

/**
 * Unified Position Calculator
 *
 * Provides a single entry point for getting any celestial body's position.
 *
 * @module ephemeris/positions
 *
 * @example
 * ```ts
 * import { getPosition, getAllPositions, Planet } from 'celestine';
 *
 * const jd = 2451545.0; // J2000.0
 *
 * // Get single body position
 * const sun = getPosition(Planet.Sun, jd);
 *
 * // Get all positions at once
 * const chart = getAllPositions(jd);
 * console.log(chart.get(Planet.Moon)?.longitude);
 * ```
 */

/**
 * Extended body identifiers including variants (Mean/True).
 */
declare enum CelestialBody {
    Sun = "Sun",
    Moon = "Moon",
    Mercury = "Mercury",
    Venus = "Venus",
    Mars = "Mars",
    Jupiter = "Jupiter",
    Saturn = "Saturn",
    Uranus = "Uranus",
    Neptune = "Neptune",
    Pluto = "Pluto",
    /** Mean North Node (default) */
    NorthNode = "NorthNode",
    /** True North Node */
    TrueNorthNode = "TrueNorthNode",
    /** Mean South Node */
    SouthNode = "SouthNode",
    /** True South Node */
    TrueSouthNode = "TrueSouthNode",
    /** Mean Lilith (default) */
    Lilith = "Lilith",
    /** True Lilith (Osculating) */
    TrueLilith = "TrueLilith",
    Chiron = "Chiron",
    /** Ceres - dwarf planet */
    Ceres = "Ceres",
    /** Pallas - wisdom asteroid */
    Pallas = "Pallas",
    /** Juno - partnership asteroid */
    Juno = "Juno",
    /** Vesta - devotion asteroid */
    Vesta = "Vesta"
}
/**
 * Get the position of any celestial body.
 *
 * @param body - The celestial body (Planet enum or CelestialBody enum)
 * @param jd - Julian Date
 * @param options - Calculation options
 * @returns Position of the body
 * @throws Error if body is not recognized
 *
 * @example
 * ```ts
 * const sun = getPosition(Planet.Sun, 2451545.0);
 * const trueNode = getPosition(CelestialBody.TrueNorthNode, 2451545.0);
 * ```
 */
declare function getPosition(body: Planet$1 | CelestialBody | string, jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Default bodies to include in getAllPositions().
 * Includes the 10 traditional astrological bodies + Chiron.
 */
declare const DEFAULT_BODIES: readonly Planet$1[];
/**
 * Get positions of all celestial bodies at once.
 *
 * @param jd - Julian Date
 * @param bodies - Bodies to include (defaults to all standard bodies)
 * @param options - Calculation options
 * @returns Map of body to position
 *
 * @example
 * ```ts
 * const positions = getAllPositions(2451545.0);
 *
 * // Iterate all positions
 * for (const [body, pos] of positions) {
 *   console.log(`${body}: ${pos.longitude.toFixed(2)}°`);
 * }
 *
 * // Get specific body
 * const moon = positions.get(Planet.Moon);
 * ```
 */
declare function getAllPositions(jd: number, bodies?: readonly (Planet$1 | CelestialBody)[], options?: EphemerisOptions): PlanetPositions$1;
/**
 * Get positions of all bodies as a plain object.
 * Useful for JSON serialization.
 *
 * @param jd - Julian Date
 * @param bodies - Bodies to include
 * @param options - Calculation options
 * @returns Object mapping body name to position
 */
declare function getAllPositionsObject(jd: number, bodies?: readonly (Planet$1 | CelestialBody)[], options?: EphemerisOptions): Record<string, PlanetPosition>;
/**
 * Check if a body is currently retrograde.
 *
 * @param body - The celestial body
 * @param jd - Julian Date
 * @returns True if retrograde
 *
 * @example
 * ```ts
 * if (isRetrograde(Planet.Mercury, jd)) {
 *   console.log('Mercury is retrograde!');
 * }
 * ```
 */
declare function isRetrograde$2(body: Planet$1 | CelestialBody, jd: number): boolean;
/**
 * Get the zodiac sign (0-11) for a body.
 *
 * @param body - The celestial body
 * @param jd - Julian Date
 * @returns Sign index (0=Aries, 1=Taurus, ..., 11=Pisces)
 */
declare function getSign(body: Planet$1 | CelestialBody, jd: number): number;
/**
 * Get the degree within sign (0-30) for a body.
 *
 * @param body - The celestial body
 * @param jd - Julian Date
 * @returns Degree within sign (0.0 to 29.999...)
 */
declare function getDegreeInSign(body: Planet$1 | CelestialBody, jd: number): number;

/**
 * Houses module type definitions
 *
 * Core types for astrological house calculations, including geographic locations,
 * house cusps, angles, and house systems.
 */
/**
 * Geographic location for house calculations
 *
 * Specifies the observer's position on Earth. Latitude and longitude are required;
 * elevation is optional and rarely significant for astrological calculations
 * (affects horizon parallax, which is negligible for house cusps).
 *
 * @property latitude - Degrees north (positive) or south (negative) of equator.
 *   Valid range: -90 to +90. Latitude 0 = equator, +90 = North Pole, -90 = South Pole.
 *
 * @property longitude - Degrees east (positive) or west (negative) of prime meridian.
 *   Can be expressed as -180 to +180 or 0 to 360 (both are supported and normalized internally).
 *   Longitude 0 = Greenwich, +180 or -180 = International Date Line.
 *
 * @property elevation - Optional elevation in meters above sea level.
 *   Only affects parallax corrections (not implemented in basic house calculations).
 *   Default: 0 (sea level).
 *
 * @example
 * London, UK:
 * ```typescript
 * { latitude: 51.5074, longitude: -0.1278 }
 * ```
 *
 * @example
 * New York, USA:
 * ```typescript
 * { latitude: 40.7128, longitude: -74.0060 }
 * ```
 *
 * @example
 * Sydney, Australia (Southern hemisphere, Eastern longitude):
 * ```typescript
 * { latitude: -33.8688, longitude: 151.2093 }
 * ```
 */
interface GeographicLocation {
    latitude: number;
    longitude: number;
    elevation?: number;
}
/**
 * The four angles of the chart
 *
 * The angles are the four cardinal points of the chart, marking the intersections
 * of the ecliptic with the horizon (ASC/DSC) and meridian (MC/IC).
 *
 * @property ascendant - Ascendant (ASC): ecliptic degree rising on eastern horizon.
 *   This is the cusp of the 1st house in most house systems.
 *
 * @property midheaven - Midheaven (MC, Medium Coeli): ecliptic degree culminating
 *   (crossing the meridian) at the highest point. This is the cusp of the 10th house.
 *
 * @property descendant - Descendant (DSC): ecliptic degree setting on western horizon.
 *   Always exactly opposite the Ascendant (ASC + 180°). Cusp of the 7th house.
 *
 * @property imumCoeli - Imum Coeli (IC): lowest point, opposite the Midheaven (MC + 180°).
 *   Cusp of the 4th house.
 *
 * @remarks
 * All values are ecliptic longitudes in degrees (0-360):
 * - 0° = 0° Aries
 * - 90° = 0° Cancer
 * - 180° = 0° Libra
 * - 270° = 0° Capricorn
 */
interface Angles {
    ascendant: number;
    midheaven: number;
    descendant: number;
    imumCoeli: number;
}
/**
 * House cusps for all twelve houses
 *
 * The cusps are the starting degrees of each house on the ecliptic.
 * Represented as a fixed-length tuple of 12 numbers (houses 1-12).
 *
 * @remarks
 * House relationships:
 * - House 1 cusp = Ascendant
 * - House 4 cusp = Imum Coeli (in most systems)
 * - House 7 cusp = Descendant
 * - House 10 cusp = Midheaven
 *
 * The intermediate houses (2, 3, 5, 6, 8, 9, 11, 12) are calculated
 * differently depending on the house system used.
 *
 * All values are ecliptic longitudes in degrees (0-360).
 *
 * @example
 * ```typescript
 * const cusps: HouseCusps = {
 *   cusps: [
 *     0,    // House 1 (ASC)
 *     30,   // House 2
 *     60,   // House 3
 *     90,   // House 4 (IC)
 *     120,  // House 5
 *     150,  // House 6
 *     180,  // House 7 (DSC)
 *     210,  // House 8
 *     240,  // House 9
 *     270,  // House 10 (MC)
 *     300,  // House 11
 *     330   // House 12
 *   ]
 * };
 * ```
 */
interface HouseCusps {
    cusps: [
        number,
        number,
        number,
        number,
        number,
        number,
        number,
        number,
        number,
        number,
        number,
        number
    ];
}
/**
 * Available house systems
 *
 * Different methods for dividing the celestial sphere into twelve houses.
 * Each system has different geometric rationale and works best in different contexts.
 *
 * @remarks
 * System characteristics:
 *
 * - **placidus**: Most popular in modern Western astrology. Divides diurnal/nocturnal
 *   arcs into thirds. Fails at latitudes above ~66° (Arctic/Antarctic circles).
 *
 * - **koch**: Similar to Placidus but uses the Ascendant's pole. Popular in Europe.
 *   Also fails at extreme latitudes.
 *
 * - **equal**: Simplest system. Each house is exactly 30° from the previous, starting
 *   from the Ascendant. MC floats independently. Works at all latitudes.
 *
 * - **whole-sign**: Ancient system from Hellenistic astrology. Each house corresponds
 *   to one zodiac sign. House 1 starts at 0° of the sign containing the Ascendant.
 *   Works everywhere.
 *
 * - **porphyry**: Simple trisection of quadrants between angles. Works at all latitudes.
 *
 * - **regiomontanus**: Divides the celestial equator into 30° segments, then projects
 *   onto the ecliptic. Works at most latitudes.
 *
 * - **campanus**: Divides the prime vertical into 30° arcs. Complex spherical geometry.
 *   Works at most latitudes.
 */
type HouseSystem = 'placidus' | 'koch' | 'equal' | 'whole-sign' | 'porphyry' | 'regiomontanus' | 'campanus';
/**
 * Complete house calculation result
 *
 * Contains all computed house data: angles, cusps, and the input parameters
 * used for the calculation.
 *
 * @property system - The house system used for calculation
 * @property angles - The four angles (ASC, MC, DSC, IC)
 * @property cusps - All twelve house cusps
 * @property latitude - Geographic latitude used (degrees)
 * @property longitude - Geographic longitude used (degrees)
 * @property lst - Local Sidereal Time used (degrees, 0-360)
 * @property obliquity - Obliquity of the ecliptic at calculation time (degrees)
 *
 * @example
 * ```typescript
 * const houseData: HouseData = {
 *   system: 'placidus',
 *   angles: {
 *     ascendant: 15.3,
 *     midheaven: 285.7,
 *     descendant: 195.3,
 *     imumCoeli: 105.7
 *   },
 *   cusps: {
 *     cusps: [15.3, 42.1, 68.9, 105.7, 142.5, 169.3, 195.3, 222.1, 248.9, 285.7, 322.5, 349.3]
 *   },
 *   latitude: 51.5074,
 *   longitude: -0.1278,
 *   lst: 180.5,
 *   obliquity: 23.4368
 * };
 * ```
 */
interface HouseData {
    system: HouseSystem;
    angles: Angles;
    cusps: HouseCusps;
    latitude: number;
    longitude: number;
    lst: number;
    obliquity: number;
}
/**
 * Validation result for location validation
 *
 * @property valid - Whether the input is valid
 * @property errors - Array of error messages if invalid
 */
interface LocationValidationResult {
    valid: boolean;
    errors: string[];
}

/**
 * Zodiac module types
 *
 * Type definitions for zodiac signs, planetary dignities, and related astrological concepts.
 * All types are based on the tropical zodiac system (0° Aries = vernal equinox).
 *
 * @module zodiac/types
 */
/**
 * The twelve zodiac signs
 *
 * Enumerated 0-11 corresponding to 30° segments of the ecliptic,
 * starting from the vernal equinox (0° Aries).
 *
 * @enum {number}
 */
declare enum Sign {
    Aries = 0,
    Taurus = 1,
    Gemini = 2,
    Cancer = 3,
    Leo = 4,
    Virgo = 5,
    Libra = 6,
    Scorpio = 7,
    Sagittarius = 8,
    Capricorn = 9,
    Aquarius = 10,
    Pisces = 11
}
/**
 * The four classical elements
 *
 * Each element is associated with three signs (triplicities).
 *
 * @enum {string}
 */
declare enum Element {
    Fire = "Fire",
    Earth = "Earth",
    Air = "Air",
    Water = "Water"
}
/**
 * The three modalities (qualities)
 *
 * Each modality is associated with four signs (quadruplicities).
 *
 * @enum {string}
 */
declare enum Modality {
    Cardinal = "Cardinal",
    Fixed = "Fixed",
    Mutable = "Mutable"
}
/**
 * Polarity (masculine/feminine, yang/yin)
 *
 * Alternates through the zodiac: Aries (Positive), Taurus (Negative), etc.
 *
 * @enum {string}
 */
declare enum Polarity {
    Positive = "Positive",// Yang, Active, Masculine
    Negative = "Negative"
}
/**
 * The ten planets used in Western astrology
 *
 * Includes the traditional seven planets (Sun through Saturn) and
 * the modern outer planets (Uranus, Neptune, Pluto).
 *
 * @enum {string}
 */
declare enum Planet {
    Sun = "Sun",
    Moon = "Moon",
    Mercury = "Mercury",
    Venus = "Venus",
    Mars = "Mars",
    Jupiter = "Jupiter",
    Saturn = "Saturn",
    Uranus = "Uranus",
    Neptune = "Neptune",
    Pluto = "Pluto"
}
/**
 * Essential dignity states
 *
 * Describes a planet's strength or weakness based on the sign it occupies.
 *
 * @enum {string}
 */
declare enum DignityState {
    /** Planet rules the sign (+5 points) */
    Domicile = "Domicile",
    /** Planet is exalted in the sign (+4 points) */
    Exaltation = "Exaltation",
    /** Planet has no special dignity (0 points) */
    Peregrine = "Peregrine",
    /** Planet is opposite its domicile (-5 points) */
    Detriment = "Detriment",
    /** Planet is opposite its exaltation (-4 points) */
    Fall = "Fall"
}
/**
 * Position in the zodiac
 *
 * Represents a specific point on the ecliptic converted to zodiac notation.
 *
 * @interface
 */
interface ZodiacPosition {
    /** The zodiac sign (0-11) */
    sign: Sign;
    /** Sign name as string */
    signName: string;
    /** Normalized ecliptic longitude (0-360°) */
    longitude: number;
    /** Degree within the sign (0.0-29.999...) */
    degreeInSign: number;
    /** Integer degree within sign (0-29) */
    degree: number;
    /** Minutes (0-59) */
    minute: number;
    /** Seconds (0-59) */
    second: number;
    /** Formatted string representation */
    formatted: string;
}
/**
 * Complete information about a zodiac sign
 *
 * @interface
 */
interface SignInfo {
    /** The sign enum value */
    sign: Sign;
    /** Sign name */
    name: string;
    /** Unicode symbol (e.g., ♈ for Aries) */
    symbol: string;
    /** Element (Fire, Earth, Air, Water) */
    element: Element;
    /** Modality (Cardinal, Fixed, Mutable) */
    modality: Modality;
    /** Polarity (Positive/Yang or Negative/Yin) */
    polarity: Polarity;
    /** Primary ruler (modern if applicable, otherwise traditional) */
    ruler: Planet;
    /** Traditional ruler (pre-1781) */
    traditionalRuler: Planet;
    /** Modern ruler (for Aquarius, Pisces, Scorpio only) */
    modernRuler?: Planet;
    /** Starting ecliptic longitude (0, 30, 60, etc.) */
    startDegree: number;
    /** Ending ecliptic longitude (30, 60, 90, etc.) */
    endDegree: number;
}
/**
 * Planetary dignity information
 *
 * Describes a planet's essential dignity in a specific sign.
 *
 * @interface
 */
interface Dignity {
    /** The planet being evaluated */
    planet: Planet;
    /** The sign the planet is in */
    sign: Sign;
    /** Dignity state */
    state: DignityState;
    /** Numerical strength (+5, +4, 0, -4, -5) */
    strength: number;
    /** Human-readable description */
    description: string;
    /** Exact exaltation degree (if in exaltation state) */
    exaltationDegree?: number;
}
/**
 * Exaltation data for a planet
 *
 * @interface
 */
interface ExaltationData {
    /** Sign where planet is exalted */
    sign: Sign;
    /** Exact degree of maximum exaltation (0-29) */
    degree: number;
}
/**
 * Options for formatting zodiac positions
 *
 * @interface
 */
interface FormatOptions {
    /** Include seconds in output (default: true) */
    includeSeconds?: boolean;
    /** Include sign name/symbol in output (default: true) */
    includeSign?: boolean;
    /** Use symbol instead of name (default: false) */
    useSymbol?: boolean;
    /** Use decimal degrees instead of DMS (default: false) */
    decimalDegrees?: boolean;
}

/**
 * Chart Module Type Definitions
 *
 * All TypeScript interfaces and types for the birth chart module.
 *
 * @module chart/types
 */

/**
 * Birth data required to calculate a chart.
 *
 * @remarks
 * All times should be in local time; timezone offset handles conversion to UTC.
 */
interface BirthData {
    /** Birth year (Gregorian calendar) */
    year: number;
    /** Birth month (1-12) */
    month: number;
    /** Birth day (1-31) */
    day: number;
    /** Birth hour in local time (0-23) */
    hour: number;
    /** Birth minute (0-59) */
    minute: number;
    /** Birth second (0-59), defaults to 0 */
    second?: number;
    /**
     * Timezone offset from UTC in hours.
     * Positive = east of Greenwich, negative = west.
     * Examples: -5 (EST), +1 (CET), +5.5 (IST)
     */
    timezone: number;
    /** Geographic latitude (-90 to +90, positive = north) */
    latitude: number;
    /** Geographic longitude (-180 to +180, positive = east) */
    longitude: number;
}
/**
 * Optional configuration for chart calculation.
 */
interface ChartOptions {
    /** House system to use (default: 'placidus') */
    houseSystem?: HouseSystem;
    /** Include major asteroids: Ceres, Pallas, Juno, Vesta (default: true) */
    includeAsteroids?: boolean;
    /** Include Chiron (default: true) */
    includeChiron?: boolean;
    /**
     * Lilith calculation method.
     * - 'mean': Mean Black Moon Lilith (smooth motion)
     * - 'true': True/Oscillating Lilith (includes perturbations)
     * - 'both': Include both
     * - false: Exclude Lilith
     * Default: 'mean'
     */
    includeLilith?: 'mean' | 'true' | 'both' | false;
    /**
     * Lunar node calculation method.
     * - 'mean': Mean Node (smooth motion)
     * - 'true': True Node (includes oscillations)
     * - 'both': Include both
     * - false: Don't include nodes
     * Default: 'true'
     */
    includeNodes?: 'mean' | 'true' | 'both' | false;
    /** Include Part of Fortune and Part of Spirit (default: true) */
    includeLots?: boolean;
    /** Aspect types to calculate (default: major aspects) */
    aspectTypes?: AspectType[];
    /** Custom orbs per aspect type */
    aspectOrbs?: Partial<Record<AspectType, number>>;
    /** Detect aspect patterns like T-Square, Grand Trine (default: true) */
    includePatterns?: boolean;
    /** Minimum aspect strength to include (0-100, default: 0) */
    minimumAspectStrength?: number;
}
/**
 * Intermediate calculated values used during chart generation.
 */
interface CalculatedData {
    /** Julian Date of birth moment (UT) */
    julianDate: number;
    /** Julian Centuries from J2000.0 */
    julianCenturies: number;
    /** Local Sidereal Time in degrees (0-360) */
    localSiderealTime: number;
    /** Greenwich Mean Sidereal Time in degrees */
    greenwichSiderealTime: number;
    /** Obliquity of the ecliptic in degrees */
    obliquity: number;
    /** Birth date/time converted to UTC */
    utcDateTime: {
        year: number;
        month: number;
        day: number;
        hour: number;
        minute: number;
        second: number;
    };
    /** Whether Sun is above the horizon (for day/night chart) */
    isDaytime: boolean;
}
/**
 * Complete position and analysis for a celestial body.
 */
interface ChartPlanet {
    /** Display name (e.g., "Sun", "Moon", "Mercury") */
    name: string;
    /** Internal body identifier */
    body: CelestialBody;
    /** Ecliptic longitude (0-360°) */
    longitude: number;
    /** Ecliptic latitude (-90 to +90°) */
    latitude: number;
    /** Distance from Earth (AU for planets, Earth radii for Moon) */
    distance: number;
    /** Daily motion in longitude (degrees/day) */
    longitudeSpeed: number;
    /** True if currently in retrograde motion */
    isRetrograde: boolean;
    /** Zodiac sign containing this body */
    sign: Sign;
    /** Sign name as string */
    signName: string;
    /** Degree within sign (0-29) */
    degree: number;
    /** Arc minute within degree (0-59) */
    minute: number;
    /** Arc second within minute (0-59) */
    second: number;
    /** Formatted position string (e.g., "15°24'30\" Aries") */
    formatted: string;
    /** House number containing this body (1-12) */
    house: number;
    /** Essential dignity information */
    dignity: {
        /** Dignity state (Domicile, Exaltation, etc.) */
        state: DignityState;
        /** Numerical strength (-5 to +5) */
        strength: number;
        /** Human-readable description */
        description: string;
    };
}
/**
 * Lunar node position (North or South Node).
 */
interface ChartNode {
    /** "North Node" or "South Node" */
    name: string;
    /** Calculation type ("Mean" or "True") */
    type: 'Mean' | 'True';
    /** Ecliptic longitude (0-360°) */
    longitude: number;
    /** Zodiac sign */
    sign: Sign;
    /** Sign name */
    signName: string;
    /** Degree within sign */
    degree: number;
    /** Minute within degree */
    minute: number;
    /** Formatted position */
    formatted: string;
    /** House placement */
    house: number;
}
/**
 * Lilith (Black Moon) position.
 */
interface ChartLilith {
    /** "Mean Lilith" or "True Lilith" */
    name: string;
    /** Calculation type */
    type: 'Mean' | 'True';
    /** Ecliptic longitude */
    longitude: number;
    /** Zodiac sign */
    sign: Sign;
    /** Sign name */
    signName: string;
    /** Degree within sign */
    degree: number;
    /** Minute within degree */
    minute: number;
    /** Formatted position */
    formatted: string;
    /** House placement */
    house: number;
}
/**
 * Arabic Part/Lot position (e.g., Part of Fortune).
 */
interface ChartLot {
    /** Lot name (e.g., "Part of Fortune") */
    name: string;
    /** Formula used (e.g., "ASC + Moon - Sun") */
    formula: string;
    /** Ecliptic longitude */
    longitude: number;
    /** Zodiac sign */
    sign: Sign;
    /** Sign name */
    signName: string;
    /** Degree within sign */
    degree: number;
    /** Minute within degree */
    minute: number;
    /** Formatted position */
    formatted: string;
    /** House placement */
    house: number;
}
/**
 * A chart angle (ASC, MC, DSC, IC).
 */
interface ChartAngle {
    /** Angle name */
    name: 'Ascendant' | 'Midheaven' | 'Descendant' | 'Imum Coeli';
    /** Abbreviation */
    abbrev: 'ASC' | 'MC' | 'DSC' | 'IC';
    /** Ecliptic longitude */
    longitude: number;
    /** Zodiac sign */
    sign: Sign;
    /** Sign name */
    signName: string;
    /** Degree within sign */
    degree: number;
    /** Minute within degree */
    minute: number;
    /** Second within minute */
    second: number;
    /** Formatted position */
    formatted: string;
}
/**
 * All four chart angles.
 */
interface ChartAngles {
    ascendant: ChartAngle;
    midheaven: ChartAngle;
    descendant: ChartAngle;
    imumCoeli: ChartAngle;
}
/**
 * A single house cusp.
 */
interface ChartHouseCusp {
    /** House number (1-12) */
    house: number;
    /** Ecliptic longitude of cusp */
    longitude: number;
    /** Zodiac sign on cusp */
    sign: Sign;
    /** Sign name */
    signName: string;
    /** Degree within sign */
    degree: number;
    /** Minute within degree */
    minute: number;
    /** Formatted position */
    formatted: string;
    /** Size of this house in degrees */
    size: number;
}
/**
 * Complete house data.
 */
interface ChartHouses {
    /** House system used */
    system: HouseSystem;
    /** Human-readable system name */
    systemName: string;
    /** Array of 12 house cusps */
    cusps: ChartHouseCusp[];
}
/**
 * Complete aspect analysis for the chart.
 */
interface ChartAspects {
    /** All detected aspects */
    all: Aspect[];
    /** Aspects grouped by body */
    byBody: Record<string, Aspect[]>;
    /** Aspects grouped by type */
    byType: Record<AspectType, Aspect[]>;
    /** Total aspect count */
    count: number;
    /** Summary counts by aspect type */
    summary: {
        conjunctions: number;
        sextiles: number;
        squares: number;
        trines: number;
        oppositions: number;
        minor: number;
    };
}
/**
 * Element distribution in the chart.
 */
interface ElementDistribution {
    /** Planets in fire signs */
    fire: string[];
    /** Planets in earth signs */
    earth: string[];
    /** Planets in air signs */
    air: string[];
    /** Planets in water signs */
    water: string[];
}
/**
 * Modality distribution in the chart.
 */
interface ModalityDistribution {
    /** Planets in cardinal signs */
    cardinal: string[];
    /** Planets in fixed signs */
    fixed: string[];
    /** Planets in mutable signs */
    mutable: string[];
}
/**
 * Hemisphere distribution (by planet count).
 */
interface HemisphereDistribution {
    /** Planets in houses 1-6 (below horizon) */
    north: number;
    /** Planets in houses 7-12 (above horizon) */
    south: number;
    /** Planets in houses 10-3 (eastern/rising) */
    east: number;
    /** Planets in houses 4-9 (western/setting) */
    west: number;
}
/**
 * Quadrant distribution.
 */
interface QuadrantDistribution {
    /** Houses 1-3: Self, identity */
    first: string[];
    /** Houses 4-6: Personal, private */
    second: string[];
    /** Houses 7-9: Others, relationships */
    third: string[];
    /** Houses 10-12: Public, career */
    fourth: string[];
}
/**
 * Comprehensive chart summary.
 */
interface ChartSummary {
    /** Element distribution */
    elements: ElementDistribution;
    /** Modality distribution */
    modalities: ModalityDistribution;
    /** Hemisphere emphasis */
    hemispheres: HemisphereDistribution;
    /** Quadrant distribution */
    quadrants: QuadrantDistribution;
    /** Polarity balance */
    polarity: {
        positive: number;
        negative: number;
    };
    /** List of retrograde planets */
    retrograde: string[];
    /** Detected aspect patterns */
    patterns: string[];
    /** Dignified planet groups */
    dignified: {
        domicile: string[];
        exalted: string[];
        detriment: string[];
        fall: string[];
        peregrine: string[];
    };
}
/**
 * A complete astrological birth chart.
 */
interface Chart {
    /** Original birth data input */
    input: BirthData;
    /** Configuration options used */
    options: Required<ChartOptions>;
    /** Intermediate calculated values */
    calculated: CalculatedData;
    /** All planetary positions (Sun, Moon, planets, asteroids, Chiron) */
    planets: ChartPlanet[];
    /** Lunar nodes */
    nodes: ChartNode[];
    /** Lilith positions */
    lilith: ChartLilith[];
    /** Arabic Parts/Lots */
    lots: ChartLot[];
    /** Chart angles (ASC, MC, DSC, IC) */
    angles: ChartAngles;
    /** House cusps */
    houses: ChartHouses;
    /** Aspect data */
    aspects: ChartAspects;
    /** Aspect patterns */
    patterns: AspectPattern[];
    /** Chart summary and statistics */
    summary: ChartSummary;
}
/**
 * Error thrown when birth data validation fails.
 */
declare class ValidationError extends Error {
    field: string;
    value: unknown;
    constructor(message: string, field: string, value: unknown);
}
/**
 * Error thrown when a calculation fails.
 */
declare class CalculationError extends Error {
    step: string;
    details?: unknown | undefined;
    constructor(message: string, step: string, details?: unknown | undefined);
}
/**
 * Result of birth data validation.
 */
interface ValidationResult$1 {
    /** Whether data is valid */
    valid: boolean;
    /** List of validation errors */
    errors: ValidationErrorDetail[];
    /** List of warnings (non-fatal issues) */
    warnings: string[];
    /** Normalized birth data (if valid) */
    normalized?: BirthData;
}
/**
 * Details about a validation error.
 */
interface ValidationErrorDetail {
    /** Field that failed validation */
    field: string;
    /** Error message */
    message: string;
    /** Invalid value */
    value: unknown;
    /** Suggested fix (if applicable) */
    suggestion?: string;
}

/**
 * Aspect Calculation for Charts
 *
 * Wrapper around the aspects module for chart calculation.
 *
 * @module chart/aspect-calculation
 */

/**
 * Build aspect bodies from chart planets.
 *
 * @param planets - Chart planets
 * @returns Array of AspectBody objects for aspect calculation
 */
declare function buildAspectBodies(planets: ChartPlanet[]): AspectBody[];
/**
 * Calculate all aspects for a chart.
 *
 * @param planets - Chart planets
 * @param options - Chart options with aspect configuration
 * @param nodes - Optional lunar nodes
 * @param lilith - Optional Lilith positions
 * @returns Complete aspect data for the chart
 */
declare function calculateChartAspects(planets: ChartPlanet[], options: Required<ChartOptions>, nodes?: ChartNode[], lilith?: ChartLilith[]): ChartAspects;
/**
 * Detect aspect patterns in the chart.
 *
 * @param aspects - All calculated aspects
 * @returns Detected patterns
 */
declare function detectChartPatterns(aspects: Aspect[]): AspectPattern[];
/**
 * Get aspects for a specific planet.
 *
 * @param chartAspects - Chart aspects data
 * @param planetName - Name of the planet
 * @returns Aspects involving this planet
 */
declare function getAspectsForPlanet(chartAspects: ChartAspects, planetName: string): Aspect[];
/**
 * Get the strongest aspect in the chart.
 *
 * @param chartAspects - Chart aspects data
 * @returns The aspect with highest strength, or null if none
 */
declare function getStrongestAspect$1(chartAspects: ChartAspects): Aspect | null;
/**
 * Get applying aspects (still forming).
 *
 * @param chartAspects - Chart aspects data
 * @returns Aspects that are applying
 */
declare function getApplyingAspects(chartAspects: ChartAspects): Aspect[];
/**
 * Get separating aspects (moving apart).
 *
 * @param chartAspects - Chart aspects data
 * @returns Aspects that are separating
 */
declare function getSeparatingAspects(chartAspects: ChartAspects): Aspect[];
/**
 * Check if two bodies are in aspect.
 *
 * @param chartAspects - Chart aspects data
 * @param body1 - First body name
 * @param body2 - Second body name
 * @returns The aspect between them, or null if none
 */
declare function getAspectBetween(chartAspects: ChartAspects, body1: string, body2: string): Aspect | null;

/**
 * Input Validation for Birth Data
 *
 * Validates and normalizes birth data before chart calculation.
 *
 * @module chart/input-validation
 */

/**
 * Validate birth data for chart calculation.
 *
 * @param birth - Birth data to validate
 * @returns Validation result with errors and warnings
 *
 * @example
 * ```typescript
 * const result = validateBirthData({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0,
 *   timezone: 0,
 *   latitude: 51.5, longitude: -0.1
 * });
 *
 * if (!result.valid) {
 *   console.error(result.errors);
 * }
 * ```
 */
declare function validateBirthData(birth: BirthData): ValidationResult$1;
/**
 * Validate and adjust chart options for the given latitude.
 *
 * @param options - Chart options (may be modified)
 * @param latitude - Geographic latitude
 * @returns Adjusted options and any warnings
 */
declare function validateChartOptions(options: ChartOptions | undefined, latitude: number): {
    options: ChartOptions;
    warnings: string[];
};
/**
 * Check if the given date/time results in day rollover when converting to UTC.
 *
 * @param birth - Birth data
 * @returns Object describing any day/month/year rollover
 */
declare function checkDayRollover(birth: BirthData): {
    rollsOver: boolean;
    direction: 'previous' | 'next' | 'none';
};
/**
 * Get available house systems for a given latitude.
 *
 * @param latitude - Geographic latitude
 * @returns Array of house systems that work at this latitude
 */
declare function getAvailableHouseSystems$1(latitude: number): string[];

/**
 * Main Chart Calculation
 *
 * The primary entry point for calculating complete birth charts.
 *
 * @module chart/chart
 *
 * @example
 * ```typescript
 * import { calculateChart } from 'celestine/chart';
 *
 * const chart = calculateChart({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0,
 *   timezone: 0,
 *   latitude: 51.5074,
 *   longitude: -0.1278
 * });
 *
 * console.log(chart.planets[0].formatted); // Sun position
 * console.log(chart.angles.ascendant.signName); // Rising sign
 * ```
 */

/**
 * Calculate a complete astrological birth chart.
 *
 * @param birth - Birth date, time, and location
 * @param options - Optional configuration
 * @returns Complete chart object
 *
 * @throws {ValidationError} If birth data is invalid
 * @throws {CalculationError} If calculation fails
 *
 * @example
 * ```typescript
 * const chart = calculateChart({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0,
 *   timezone: 0,
 *   latitude: 51.5074,
 *   longitude: -0.1278
 * });
 *
 * // Access planets
 * for (const planet of chart.planets) {
 *   console.log(`${planet.name}: ${planet.formatted} (House ${planet.house})`);
 * }
 *
 * // Access angles
 * console.log(`ASC: ${chart.angles.ascendant.formatted}`);
 * console.log(`MC: ${chart.angles.midheaven.formatted}`);
 *
 * // Access aspects
 * for (const aspect of chart.aspects.all) {
 *   console.log(`${aspect.body1} ${aspect.symbol} ${aspect.body2}`);
 * }
 * ```
 */
declare function calculateChart(birth: BirthData, options?: ChartOptions): Chart;
/**
 * Validate birth data without calculating a chart.
 *
 * @param birth - Birth data to validate
 * @returns Validation result
 */
declare function validateBirth(birth: BirthData): ValidationResult$1;

/**
 * Calculate only planetary positions without houses or aspects.
 *
 * @param birth - Birth data
 * @param options - Optional configuration
 * @returns Array of chart planets with zodiac positions
 *
 * @example
 * ```typescript
 * const planets = calculatePlanets({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0,
 *   timezone: 0, latitude: 0, longitude: 0
 * });
 *
 * for (const planet of planets) {
 *   console.log(`${planet.name}: ${planet.formatted}`);
 * }
 * ```
 */
declare function calculatePlanets(birth: BirthData, options?: Partial<ChartOptions>): ChartPlanet[];
/**
 * Calculate only house cusps and angles.
 *
 * @param birth - Birth data
 * @param options - Optional house system
 * @returns House cusps and angles
 *
 * @example
 * ```typescript
 * const houses = calculateHouseCusps({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0,
 *   timezone: 0, latitude: 51.5, longitude: -0.1
 * });
 *
 * console.log(`Ascendant: ${houses.angles.ascendant.formatted}`);
 * for (const cusp of houses.cusps) {
 *   console.log(`House ${cusp.house}: ${cusp.formatted}`);
 * }
 * ```
 */
declare function calculateHouseCusps(birth: BirthData, options?: {
    houseSystem?: HouseSystem;
}): {
    houses: ChartHouses;
    angles: ChartAngles;
    warnings: string[];
};
/**
 * Format a chart for display as text.
 *
 * @param chart - Chart to format
 * @returns Formatted chart string
 *
 * @example
 * ```typescript
 * const chart = calculateChart(birthData);
 * console.log(formatChart(chart));
 * ```
 */
declare function formatChart(chart: Chart): string;

/**
 * Chart Summary Generation
 *
 * Generates statistical summaries and distributions for a chart.
 *
 * @module chart/chart-summary
 */

/**
 * Generate a complete chart summary.
 *
 * @param planets - All chart planets
 * @param patterns - Detected aspect patterns
 * @returns Complete chart summary
 */
declare function generateChartSummary(planets: ChartPlanet[], patterns: AspectPattern[]): ChartSummary;
/**
 * Calculate element distribution.
 *
 * @param planets - Chart planets
 * @returns Element distribution with planet names
 */
declare function calculateElementDistribution(planets: ChartPlanet[]): ElementDistribution;
/**
 * Calculate modality distribution.
 *
 * @param planets - Chart planets
 * @returns Modality distribution with planet names
 */
declare function calculateModalityDistribution(planets: ChartPlanet[]): ModalityDistribution;
/**
 * Calculate hemisphere distribution.
 *
 * @param planets - Chart planets
 * @returns Hemisphere counts
 */
declare function calculateHemisphereDistribution(planets: ChartPlanet[]): HemisphereDistribution;
/**
 * Calculate quadrant distribution.
 *
 * @param planets - Chart planets
 * @returns Quadrant distribution with planet names
 */
declare function calculateQuadrantDistribution(planets: ChartPlanet[]): QuadrantDistribution;
/**
 * Calculate polarity balance.
 *
 * @param planets - Chart planets
 * @returns Positive/negative counts
 */
declare function calculatePolarityBalance(planets: ChartPlanet[]): {
    positive: number;
    negative: number;
};
/**
 * Get list of retrograde planets.
 *
 * @param planets - Chart planets
 * @returns Names of retrograde planets
 */
declare function getRetrogradePlanets(planets: ChartPlanet[]): string[];
/**
 * Categorize planets by dignity state.
 *
 * @param planets - Chart planets
 * @returns Categorized planet names
 */
declare function categorizeByDignity(planets: ChartPlanet[]): {
    domicile: string[];
    exalted: string[];
    detriment: string[];
    fall: string[];
    peregrine: string[];
};
/**
 * Get the dominant element in the chart.
 *
 * @param elements - Element distribution
 * @returns The most common element
 */
declare function getDominantElement(elements: ElementDistribution): 'fire' | 'earth' | 'air' | 'water';
/**
 * Get the dominant modality in the chart.
 *
 * @param modalities - Modality distribution
 * @returns The most common modality
 */
declare function getDominantModality(modalities: ModalityDistribution): 'cardinal' | 'fixed' | 'mutable';
/**
 * Check if the chart has a specific emphasis.
 *
 * @param summary - Chart summary
 * @returns Description of chart emphasis
 */
declare function getChartEmphasis(summary: ChartSummary): string[];

/**
 * Chart Module Constants
 *
 * Default values, limits, and configuration constants.
 *
 * @module chart/constants
 */

/**
 * Minimum supported year for chart calculations.
 * Limited by ephemeris accuracy and historical calendar changes.
 */
declare const MIN_YEAR = -4000;
/**
 * Maximum supported year for chart calculations.
 * Limited by ephemeris extrapolation accuracy.
 */
declare const MAX_YEAR = 4000;
/**
 * Recommended minimum year for accurate calculations.
 * Before this, ephemeris accuracy degrades.
 */
declare const RECOMMENDED_MIN_YEAR = 1800;
/**
 * Recommended maximum year for accurate calculations.
 * After this, ephemeris is extrapolated.
 */
declare const RECOMMENDED_MAX_YEAR = 2200;
/**
 * Default house system.
 */
declare const DEFAULT_HOUSE_SYSTEM: HouseSystem;
/**
 * Major aspects (Ptolemaic).
 */
declare const MAJOR_ASPECTS: AspectType[];
/**
 * All aspects including minor and Kepler aspects.
 */
declare const ALL_ASPECTS: AspectType[];
/**
 * Default chart calculation options.
 */
declare const DEFAULT_OPTIONS: Required<ChartOptions>;
/**
 * Standard names for celestial bodies in the chart.
 */
declare const BODY_NAMES$1: {
    readonly sun: "Sun";
    readonly moon: "Moon";
    readonly mercury: "Mercury";
    readonly venus: "Venus";
    readonly mars: "Mars";
    readonly jupiter: "Jupiter";
    readonly saturn: "Saturn";
    readonly uranus: "Uranus";
    readonly neptune: "Neptune";
    readonly pluto: "Pluto";
    readonly chiron: "Chiron";
    readonly ceres: "Ceres";
    readonly pallas: "Pallas";
    readonly juno: "Juno";
    readonly vesta: "Vesta";
    readonly northNode: "North Node";
    readonly southNode: "South Node";
    readonly meanLilith: "Mean Lilith";
    readonly trueLilith: "True Lilith";
    readonly partOfFortune: "Part of Fortune";
    readonly partOfSpirit: "Part of Spirit";
};
/**
 * Order of planets in chart output (traditional order).
 */
declare const PLANET_ORDER: readonly ["Sun", "Moon", "Mercury", "Venus", "Mars", "Jupiter", "Saturn", "Uranus", "Neptune", "Pluto", "Chiron", "Ceres", "Pallas", "Juno", "Vesta"];
/**
 * Human-readable names for house systems.
 */
declare const HOUSE_SYSTEM_NAMES$1: Record<HouseSystem, string>;

/**
 * House Calculation for Charts
 *
 * Wrapper around the houses module for chart calculation.
 *
 * @module chart/house-calculation
 */

/**
 * Result of house calculation.
 */
interface HouseCalculationResult {
    /** The house system actually used (may differ from requested) */
    system: HouseSystem;
    /** Human-readable system name */
    systemName: string;
    /** All four angles */
    angles: ChartAngles;
    /** All 12 house cusps */
    houses: ChartHouses;
    /** Warnings (e.g., if system was changed due to latitude) */
    warnings: string[];
}
/**
 * Calculate house cusps and angles for a chart.
 *
 * @param location - Geographic coordinates
 * @param lst - Local Sidereal Time in degrees
 * @param T - Julian Centuries from J2000.0
 * @param requestedSystem - Desired house system
 * @returns House calculation result with angles and cusps
 *
 * @throws {CalculationError} If house calculation fails
 */
declare function calculateChartHouses(location: GeographicLocation, lst: number, T: number, requestedSystem: HouseSystem): HouseCalculationResult;
/**
 * Determine which house a longitude falls in.
 *
 * @param longitude - Ecliptic longitude (0-360)
 * @param cusps - Array of 12 house cusp longitudes
 * @returns House number (1-12)
 */
declare function getHouseNumber(longitude: number, cusps: number[]): number;
/**
 * Get the cusps as a simple array of longitudes.
 */
declare function getCuspLongitudes(houses: ChartHouses): number[];
/**
 * Check if a planet is on an angle (within orb).
 *
 * @param longitude - Planet longitude
 * @param angles - Chart angles
 * @param orb - Orb in degrees (default: 3°)
 * @returns The angle name if on an angle, null otherwise
 */
declare function getAngleConjunction(longitude: number, angles: ChartAngles, orb?: number): 'ASC' | 'MC' | 'DSC' | 'IC' | null;

/**
 * Planetary Position Calculation
 *
 * Wrapper around the ephemeris module to calculate all planetary positions
 * for a birth chart.
 *
 * @module chart/planet-positions
 */

/**
 * Result of planetary position calculations.
 */
interface PlanetPositions {
    /** Main planets (Sun through Pluto) */
    planets: Map<CelestialBody, PlanetPosition>;
    /** Chiron position (if included) */
    chiron?: PlanetPosition;
    /** Asteroid positions (if included) */
    asteroids?: {
        ceres?: PlanetPosition;
        pallas?: PlanetPosition;
        juno?: PlanetPosition;
        vesta?: PlanetPosition;
    };
    /** Lunar nodes */
    nodes?: {
        meanNorth?: {
            longitude: number;
        };
        trueNorth?: {
            longitude: number;
        };
        meanSouth?: {
            longitude: number;
        };
        trueSouth?: {
            longitude: number;
        };
    };
    /** Lilith positions */
    lilith?: {
        mean?: {
            longitude: number;
        };
        true?: {
            longitude: number;
        };
    };
    /** Arabic Parts/Lots (calculated after we have Sun, Moon, ASC) */
    lots?: {
        fortune?: {
            longitude: number;
        };
        spirit?: {
            longitude: number;
        };
    };
}
/**
 * Calculate all planetary positions for a given Julian Date.
 *
 * @param jd - Julian Date
 * @param options - Chart options determining which bodies to include
 * @returns All calculated positions
 */
declare function calculatePlanetPositions(jd: number, options: Required<ChartOptions>): PlanetPositions;
/**
 * Calculate Arabic Parts/Lots after we have ASC.
 *
 * @param sunLongitude - Sun's ecliptic longitude
 * @param moonLongitude - Moon's ecliptic longitude
 * @param ascendant - Ascendant longitude
 * @param isDaytime - Whether the chart is a daytime chart
 * @param includeLots - Whether to include lots
 * @returns Lots positions
 */
declare function calculateLots(sunLongitude: number, moonLongitude: number, ascendant: number, isDaytime: boolean, includeLots: boolean): {
    fortune?: {
        longitude: number;
    };
    spirit?: {
        longitude: number;
    };
};
/**
 * Check if a celestial body is retrograde.
 *
 * @param position - Celestial position with speed
 * @returns True if retrograde (negative speed)
 */
declare function isRetrograde$1(position: PlanetPosition): boolean;
/**
 * Check if a celestial body is stationary.
 *
 * @param position - Celestial position with speed
 * @returns True if nearly stationary
 */
declare function isStationary(position: PlanetPosition): boolean;
/**
 * Get the display name for a celestial body.
 *
 * @param body - Celestial body
 * @returns Human-readable name
 */
declare function getBodyName(body: CelestialBody): string;
/**
 * Get ordered list of planet names for chart display.
 *
 * @returns Planet names in traditional order
 */
declare function getPlanetOrder(): readonly string[];

/**
 * Time module type definitions
 *
 * Core types for astronomical time calculations, including calendar dates,
 * Julian dates, and sidereal time representations.
 */
/**
 * Gregorian calendar date and time representation
 *
 * Used as the primary input format for astronomical calculations.
 * All components must be valid according to the Gregorian calendar rules.
 *
 * @property year - Full year (e.g., 2025, -100 for 101 BCE). Note: Year 0 doesn't exist historically.
 * @property month - Month of year, 1-12 (January = 1, December = 12)
 * @property day - Day of month, 1-31 (depending on month and year)
 * @property hour - Hour of day, 0-23 (0 = midnight, 12 = noon)
 * @property minute - Minute of hour, 0-59
 * @property second - Second of minute, 0-59.999... (fractional seconds allowed)
 * @property timezone - Optional hours offset from UTC (e.g., -5 for EST, +1 for CET)
 */
interface CalendarDateTime {
    year: number;
    month: number;
    day: number;
    hour: number;
    minute: number;
    second: number;
    timezone?: number;
}
/**
 * Julian Date representation
 *
 * A continuous count of days and fractional days since noon (12:00 UT)
 * on January 1, 4713 BCE in the proleptic Julian calendar.
 * This is the standard time measure in astronomical calculations.
 *
 * @property jd - Julian Date value (fractional days)
 *
 * @example
 * // J2000.0 epoch (January 1, 2000, 12:00 TT)
 * { jd: 2451545.0 }
 *
 * @example
 * // December 18, 2025, 18:00 UTC
 * { jd: 2460665.25 }
 */
interface JulianDate {
    jd: number;
}
/**
 * Julian Centuries from J2000.0 epoch
 *
 * Time expressed as centuries from the J2000.0 epoch (JD 2451545.0).
 * Most modern ephemeris formulas use T as their time variable.
 *
 * @property T - Centuries from J2000.0 (can be negative for dates before 2000)
 *
 * @example
 * // J2000.0 epoch (January 1, 2000, 12:00 TT)
 * { T: 0 }
 *
 * @example
 * // One century after J2000.0
 * { T: 1.0 }
 *
 * @example
 * // One century before J2000.0
 * { T: -1.0 }
 */
interface JulianCenturies {
    T: number;
}
/**
 * Sidereal time representation
 *
 * Time measured by Earth's rotation relative to the fixed stars
 * rather than the Sun. A sidereal day is ~23h 56m 4s.
 *
 * Can be expressed in either degrees (0-360) or hours (0-24).
 *
 * @property degrees - Sidereal time in degrees (0-360, where 360° = 24 hours)
 * @property hours - Optional sidereal time in hours (0-24)
 */
interface SiderealTime {
    degrees: number;
    hours?: number;
}
/**
 * Time system classification
 *
 * Different time scales used in astronomy and daily life:
 * - UT: Universal Time (based on Earth's rotation)
 * - UTC: Coordinated Universal Time (civil time standard with leap seconds)
 * - TT: Terrestrial Time (uniform time scale for ephemeris, TT = UTC + ΔT)
 * - local: Local civil time (includes timezone offset)
 */
type TimeSystem = 'UT' | 'UTC' | 'TT' | 'local';
/**
 * Complete time context for astronomical calculations
 *
 * Bundles all time representations together for convenience.
 * Useful when you need multiple time formats for a single moment.
 *
 * @property calendar - Calendar date and time representation
 * @property jd - Julian Date
 * @property T - Julian Centuries from J2000.0
 * @property gmst - Optional Greenwich Mean Sidereal Time in degrees
 * @property lst - Optional Local Sidereal Time in degrees
 * @property system - Which time system the calendar time is expressed in
 */
interface TimeContext {
    calendar: CalendarDateTime;
    jd: number;
    T: number;
    gmst?: number;
    lst?: number;
    system: TimeSystem;
}
/**
 * Validation result for date/time validation functions
 *
 * @property valid - Whether the input is valid
 * @property errors - Array of error messages if invalid
 */
interface ValidationResult {
    valid: boolean;
    errors: string[];
}

/**
 * Time Conversion for Chart Calculation
 *
 * Converts birth data to astronomical time values needed for chart calculation.
 *
 * @module chart/time-conversion
 */

/**
 * Convert local birth time to UTC.
 *
 * @param birth - Birth data with local time and timezone
 * @returns UTC datetime
 *
 * @remarks
 * Handles day rollover when timezone conversion crosses midnight.
 *
 * @example
 * ```typescript
 * // 18:00 EST (-5) = 23:00 UTC
 * const utc = localToUTC({
 *   year: 2000, month: 1, day: 1,
 *   hour: 18, minute: 0, second: 0,
 *   timezone: -5, latitude: 0, longitude: 0
 * });
 * // { year: 2000, month: 1, day: 1, hour: 23, minute: 0, second: 0 }
 * ```
 */
declare function localToUTC(birth: BirthData): CalendarDateTime;
/**
 * Calculate all time-related values needed for chart calculation.
 *
 * @param birth - Validated birth data
 * @returns Calculated time values (JD, T, LST, obliquity, etc.)
 *
 * @example
 * ```typescript
 * const data = calculateTimeData({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0,
 *   timezone: 0,
 *   latitude: 51.5, longitude: -0.1
 * });
 *
 * console.log(data.julianDate); // 2451545.0
 * console.log(data.localSiderealTime); // ~degrees
 * ```
 */
declare function calculateTimeData(birth: BirthData): CalculatedData;
/**
 * Recalculate isDaytime based on actual Sun position.
 *
 * @param sunLongitude - Sun's ecliptic longitude
 * @param ascendant - Ascendant longitude
 * @returns True if Sun is above horizon (in houses 7-12)
 *
 * @remarks
 * Sun is above horizon when it's in the upper half of the chart.
 * This is determined by whether it's in houses 7-12 (above ASC-DSC axis).
 */
declare function calculateIsDaytime(sunLongitude: number, ascendant: number): boolean;

/**
 * Zodiac Placement for Charts
 *
 * Places celestial bodies in signs and houses, calculates dignities.
 *
 * @module chart/zodiac-placement
 */

/**
 * Build a ChartPlanet from a celestial position.
 *
 * @param body - Celestial body identifier
 * @param position - Position data from ephemeris
 * @param houses - House cusps for house placement
 * @returns Complete ChartPlanet object
 */
declare function buildChartPlanet(body: CelestialBody, position: PlanetPosition, houses: ChartHouses): ChartPlanet;
/**
 * Build a ChartNode from a node position.
 *
 * @param name - "North Node" or "South Node"
 * @param type - "Mean" or "True"
 * @param longitude - Ecliptic longitude
 * @param houses - House cusps for house placement
 * @returns ChartNode object
 */
declare function buildChartNode(name: string, type: 'Mean' | 'True', longitude: number, houses: ChartHouses): ChartNode;
/**
 * Build a ChartLilith from a Lilith position.
 *
 * @param type - "Mean" or "True"
 * @param longitude - Ecliptic longitude
 * @param houses - House cusps for house placement
 * @returns ChartLilith object
 */
declare function buildChartLilith(type: 'Mean' | 'True', longitude: number, houses: ChartHouses): ChartLilith;
/**
 * Build a ChartLot from an Arabic Part position.
 *
 * @param name - Lot name (e.g., "Part of Fortune")
 * @param formula - Formula used (e.g., "ASC + Moon - Sun")
 * @param longitude - Ecliptic longitude
 * @param houses - House cusps for house placement
 * @returns ChartLot object
 */
declare function buildChartLot(name: string, formula: string, longitude: number, houses: ChartHouses): ChartLot;
/**
 * Get the zodiac sign for a longitude.
 *
 * @param longitude - Ecliptic longitude
 * @returns Sign enum value
 */
declare function getSignForLongitude(longitude: number): Sign;
/**
 * Get element for a sign.
 */
declare function getElement(sign: Sign): 'fire' | 'earth' | 'air' | 'water';
/**
 * Get modality for a sign.
 */
declare function getModality(sign: Sign): 'cardinal' | 'fixed' | 'mutable';
/**
 * Get polarity for a sign.
 */
declare function getPolarity(sign: Sign): 'positive' | 'negative';
/**
 * Get quadrant for a house number.
 *
 * @param house - House number (1-12)
 * @returns Quadrant number (1-4)
 */
declare function getQuadrant(house: number): 1 | 2 | 3 | 4;
/**
 * Get hemisphere for a house number.
 *
 * @param house - House number (1-12)
 * @returns Object with north/south and east/west
 */
declare function getHemisphere(house: number): {
    vertical: 'north' | 'south';
    horizontal: 'east' | 'west';
};

/**
 * Chart Module
 *
 * Calculate complete astrological birth charts by integrating all
 * Celestine modules: time, ephemeris, houses, zodiac, and aspects.
 *
 * @module chart
 *
 * @example
 * ```typescript
 * import { calculateChart } from 'celestine/chart';
 *
 * const chart = calculateChart({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0,
 *   timezone: 0,
 *   latitude: 51.5074,  // London
 *   longitude: -0.1278
 * });
 *
 * // Access planetary positions
 * for (const planet of chart.planets) {
 *   console.log(`${planet.name}: ${planet.formatted} in House ${planet.house}`);
 * }
 *
 * // Access chart angles
 * console.log(`Rising: ${chart.angles.ascendant.signName}`);
 * console.log(`Midheaven: ${chart.angles.midheaven.formatted}`);
 *
 * // Access aspects
 * for (const aspect of chart.aspects.all) {
 *   console.log(`${aspect.body1} ${aspect.symbol} ${aspect.body2}`);
 * }
 *
 * // Check summary
 * console.log('Fire planets:', chart.summary.elements.fire);
 * console.log('Retrograde:', chart.summary.retrograde);
 * ```
 */

declare const index$6_ALL_ASPECTS: typeof ALL_ASPECTS;
type index$6_BirthData = BirthData;
type index$6_CalculatedData = CalculatedData;
type index$6_CalculationError = CalculationError;
declare const index$6_CalculationError: typeof CalculationError;
type index$6_Chart = Chart;
type index$6_ChartAngle = ChartAngle;
type index$6_ChartAngles = ChartAngles;
type index$6_ChartAspects = ChartAspects;
type index$6_ChartHouseCusp = ChartHouseCusp;
type index$6_ChartHouses = ChartHouses;
type index$6_ChartLilith = ChartLilith;
type index$6_ChartLot = ChartLot;
type index$6_ChartNode = ChartNode;
type index$6_ChartOptions = ChartOptions;
type index$6_ChartPlanet = ChartPlanet;
type index$6_ChartSummary = ChartSummary;
declare const index$6_DEFAULT_HOUSE_SYSTEM: typeof DEFAULT_HOUSE_SYSTEM;
declare const index$6_DEFAULT_OPTIONS: typeof DEFAULT_OPTIONS;
type index$6_ElementDistribution = ElementDistribution;
type index$6_HemisphereDistribution = HemisphereDistribution;
declare const index$6_MAJOR_ASPECTS: typeof MAJOR_ASPECTS;
declare const index$6_MAX_YEAR: typeof MAX_YEAR;
declare const index$6_MIN_YEAR: typeof MIN_YEAR;
type index$6_ModalityDistribution = ModalityDistribution;
declare const index$6_PLANET_ORDER: typeof PLANET_ORDER;
type index$6_QuadrantDistribution = QuadrantDistribution;
declare const index$6_RECOMMENDED_MAX_YEAR: typeof RECOMMENDED_MAX_YEAR;
declare const index$6_RECOMMENDED_MIN_YEAR: typeof RECOMMENDED_MIN_YEAR;
type index$6_ValidationError = ValidationError;
declare const index$6_ValidationError: typeof ValidationError;
type index$6_ValidationErrorDetail = ValidationErrorDetail;
declare const index$6_buildAspectBodies: typeof buildAspectBodies;
declare const index$6_buildChartLilith: typeof buildChartLilith;
declare const index$6_buildChartLot: typeof buildChartLot;
declare const index$6_buildChartNode: typeof buildChartNode;
declare const index$6_buildChartPlanet: typeof buildChartPlanet;
declare const index$6_calculateChart: typeof calculateChart;
declare const index$6_calculateChartAspects: typeof calculateChartAspects;
declare const index$6_calculateChartHouses: typeof calculateChartHouses;
declare const index$6_calculateElementDistribution: typeof calculateElementDistribution;
declare const index$6_calculateHemisphereDistribution: typeof calculateHemisphereDistribution;
declare const index$6_calculateHouseCusps: typeof calculateHouseCusps;
declare const index$6_calculateIsDaytime: typeof calculateIsDaytime;
declare const index$6_calculateLots: typeof calculateLots;
declare const index$6_calculateModalityDistribution: typeof calculateModalityDistribution;
declare const index$6_calculatePlanetPositions: typeof calculatePlanetPositions;
declare const index$6_calculatePlanets: typeof calculatePlanets;
declare const index$6_calculatePolarityBalance: typeof calculatePolarityBalance;
declare const index$6_calculateQuadrantDistribution: typeof calculateQuadrantDistribution;
declare const index$6_calculateTimeData: typeof calculateTimeData;
declare const index$6_categorizeByDignity: typeof categorizeByDignity;
declare const index$6_checkDayRollover: typeof checkDayRollover;
declare const index$6_detectChartPatterns: typeof detectChartPatterns;
declare const index$6_formatChart: typeof formatChart;
declare const index$6_generateChartSummary: typeof generateChartSummary;
declare const index$6_getAngleConjunction: typeof getAngleConjunction;
declare const index$6_getApplyingAspects: typeof getApplyingAspects;
declare const index$6_getAspectBetween: typeof getAspectBetween;
declare const index$6_getAspectsForPlanet: typeof getAspectsForPlanet;
declare const index$6_getBodyName: typeof getBodyName;
declare const index$6_getChartEmphasis: typeof getChartEmphasis;
declare const index$6_getCuspLongitudes: typeof getCuspLongitudes;
declare const index$6_getDominantElement: typeof getDominantElement;
declare const index$6_getDominantModality: typeof getDominantModality;
declare const index$6_getElement: typeof getElement;
declare const index$6_getHemisphere: typeof getHemisphere;
declare const index$6_getHouseNumber: typeof getHouseNumber;
declare const index$6_getModality: typeof getModality;
declare const index$6_getPlanetOrder: typeof getPlanetOrder;
declare const index$6_getPolarity: typeof getPolarity;
declare const index$6_getQuadrant: typeof getQuadrant;
declare const index$6_getRetrogradePlanets: typeof getRetrogradePlanets;
declare const index$6_getSeparatingAspects: typeof getSeparatingAspects;
declare const index$6_getSignForLongitude: typeof getSignForLongitude;
declare const index$6_isStationary: typeof isStationary;
declare const index$6_localToUTC: typeof localToUTC;
declare const index$6_validateBirth: typeof validateBirth;
declare const index$6_validateBirthData: typeof validateBirthData;
declare const index$6_validateChartOptions: typeof validateChartOptions;
declare namespace index$6 {
  export { index$6_ALL_ASPECTS as ALL_ASPECTS, BODY_NAMES$1 as BODY_NAMES, type index$6_BirthData as BirthData, type index$6_CalculatedData as CalculatedData, index$6_CalculationError as CalculationError, type index$6_Chart as Chart, type index$6_ChartAngle as ChartAngle, type index$6_ChartAngles as ChartAngles, type index$6_ChartAspects as ChartAspects, type index$6_ChartHouseCusp as ChartHouseCusp, type index$6_ChartHouses as ChartHouses, type index$6_ChartLilith as ChartLilith, type index$6_ChartLot as ChartLot, type index$6_ChartNode as ChartNode, type index$6_ChartOptions as ChartOptions, type index$6_ChartPlanet as ChartPlanet, type index$6_ChartSummary as ChartSummary, index$6_DEFAULT_HOUSE_SYSTEM as DEFAULT_HOUSE_SYSTEM, index$6_DEFAULT_OPTIONS as DEFAULT_OPTIONS, type index$6_ElementDistribution as ElementDistribution, HOUSE_SYSTEM_NAMES$1 as HOUSE_SYSTEM_NAMES, type index$6_HemisphereDistribution as HemisphereDistribution, index$6_MAJOR_ASPECTS as MAJOR_ASPECTS, index$6_MAX_YEAR as MAX_YEAR, index$6_MIN_YEAR as MIN_YEAR, type index$6_ModalityDistribution as ModalityDistribution, index$6_PLANET_ORDER as PLANET_ORDER, type index$6_QuadrantDistribution as QuadrantDistribution, index$6_RECOMMENDED_MAX_YEAR as RECOMMENDED_MAX_YEAR, index$6_RECOMMENDED_MIN_YEAR as RECOMMENDED_MIN_YEAR, index$6_ValidationError as ValidationError, type index$6_ValidationErrorDetail as ValidationErrorDetail, type ValidationResult$1 as ValidationResult, index$6_buildAspectBodies as buildAspectBodies, index$6_buildChartLilith as buildChartLilith, index$6_buildChartLot as buildChartLot, index$6_buildChartNode as buildChartNode, index$6_buildChartPlanet as buildChartPlanet, index$6_calculateChart as calculateChart, index$6_calculateChartAspects as calculateChartAspects, index$6_calculateChartHouses as calculateChartHouses, index$6_calculateElementDistribution as calculateElementDistribution, index$6_calculateHemisphereDistribution as calculateHemisphereDistribution, index$6_calculateHouseCusps as calculateHouseCusps, index$6_calculateIsDaytime as calculateIsDaytime, index$6_calculateLots as calculateLots, index$6_calculateModalityDistribution as calculateModalityDistribution, index$6_calculatePlanetPositions as calculatePlanetPositions, index$6_calculatePlanets as calculatePlanets, index$6_calculatePolarityBalance as calculatePolarityBalance, index$6_calculateQuadrantDistribution as calculateQuadrantDistribution, index$6_calculateTimeData as calculateTimeData, index$6_categorizeByDignity as categorizeByDignity, index$6_checkDayRollover as checkDayRollover, index$6_detectChartPatterns as detectChartPatterns, index$6_formatChart as formatChart, index$6_generateChartSummary as generateChartSummary, index$6_getAngleConjunction as getAngleConjunction, index$6_getApplyingAspects as getApplyingAspects, index$6_getAspectBetween as getAspectBetween, index$6_getAspectsForPlanet as getAspectsForPlanet, getAvailableHouseSystems$1 as getAvailableHouseSystems, index$6_getBodyName as getBodyName, index$6_getChartEmphasis as getChartEmphasis, index$6_getCuspLongitudes as getCuspLongitudes, index$6_getDominantElement as getDominantElement, index$6_getDominantModality as getDominantModality, index$6_getElement as getElement, index$6_getHemisphere as getHemisphere, index$6_getHouseNumber as getHouseNumber, index$6_getModality as getModality, index$6_getPlanetOrder as getPlanetOrder, index$6_getPolarity as getPolarity, index$6_getQuadrant as getQuadrant, index$6_getRetrogradePlanets as getRetrogradePlanets, index$6_getSeparatingAspects as getSeparatingAspects, index$6_getSignForLongitude as getSignForLongitude, getStrongestAspect$1 as getStrongestAspect, isRetrograde$1 as isRetrograde, index$6_isStationary as isStationary, index$6_localToUTC as localToUTC, index$6_validateBirth as validateBirth, index$6_validateBirthData as validateBirthData, index$6_validateChartOptions as validateChartOptions };
}

/**
 * Ephemeris Module - Astronomical Constants
 *
 * Fundamental constants for ephemeris calculations with authoritative sources.
 *
 * @module ephemeris/constants
 *
 * @remarks
 * All constants are defined with their primary source cited.
 * Where multiple sources exist, the most authoritative (IAU, IERS) is preferred.
 */
/**
 * Julian Date of J2000.0 epoch (January 1, 2000, 12:00 TT).
 *
 * @remarks
 * The fundamental reference epoch for modern astronomical calculations.
 * All orbital elements and positions are typically referred to this epoch.
 *
 * @source IAU 1976 System of Astronomical Constants
 * @source Meeus, "Astronomical Algorithms", p. 62
 */
declare const J2000_EPOCH$1 = 2451545;
/**
 * Number of days in a Julian century.
 *
 * @remarks
 * Julian centuries (T) are the standard time unit for ephemeris polynomials.
 * T = (JD - J2000_EPOCH) / DAYS_PER_JULIAN_CENTURY
 *
 * @source Definition: 100 Julian years × 365.25 days/year
 */
declare const DAYS_PER_JULIAN_CENTURY = 36525;
/**
 * Number of days in a Julian millennium (1000 Julian years).
 *
 * @remarks
 * Used in some VSOP87 formulas that use millennia instead of centuries.
 *
 * @source Definition: 1000 Julian years × 365.25 days/year
 */
declare const DAYS_PER_JULIAN_MILLENNIUM = 365250;
/**
 * Degrees in a full circle.
 *
 * @source Euclidean geometry definition
 */
declare const DEGREES_PER_CIRCLE$1 = 360;
/**
 * Arcminutes in one degree.
 *
 * @source Definition: 1° = 60'
 */
declare const ARCMINUTES_PER_DEGREE$1 = 60;
/**
 * Arcseconds in one degree.
 *
 * @source Definition: 1° = 3600"
 */
declare const ARCSECONDS_PER_DEGREE$1 = 3600;
/**
 * Arcseconds in one arcminute.
 *
 * @source Definition: 1' = 60"
 */
declare const ARCSECONDS_PER_ARCMINUTE = 60;
/**
 * Conversion factor: degrees to radians.
 *
 * @source Definition: π/180
 */
declare const DEG_TO_RAD: number;
/**
 * Conversion factor: radians to degrees.
 *
 * @source Definition: 180/π
 */
declare const RAD_TO_DEG: number;
/**
 * Conversion factor: arcseconds to radians.
 *
 * @remarks
 * Useful for small angle calculations where angles are given in arcseconds.
 *
 * @source Definition: π / (180 × 3600)
 */
declare const ARCSEC_TO_RAD: number;
/**
 * Astronomical Unit in kilometers.
 *
 * @remarks
 * The IAU 2012 definition is exact (by definition), not measured.
 *
 * @source IAU 2012 Resolution B2
 * @see https://www.iau.org/static/resolutions/IAU2012_English.pdf
 */
declare const AU_IN_KM = 149597870.7;
/**
 * Speed of light in km/s.
 *
 * @remarks
 * Exact value by SI definition since 1983.
 *
 * @source SI definition (exact)
 * @source CODATA 2018
 */
declare const SPEED_OF_LIGHT_KM_S = 299792.458;
/**
 * Light-time for 1 AU in days.
 *
 * @remarks
 * Time for light to travel one Astronomical Unit.
 * τ = AU / c = 149,597,870.7 km / (299,792.458 km/s × 86400 s/day)
 *
 * @source Derived from IAU 2012 AU and SI speed of light
 */
declare const LIGHT_TIME_PER_AU_DAYS: number;
/**
 * Mean obliquity of the ecliptic at J2000.0 (degrees).
 *
 * @remarks
 * The angle between the equator and the ecliptic.
 * This is the mean value; true obliquity includes nutation.
 *
 * Value: 23°26'21.448" = 23.4392911°
 *
 * @source IAU 1976 System of Astronomical Constants
 * @source Lieske et al. (1977), Astronomy & Astrophysics 58, 1
 * @source Meeus, "Astronomical Algorithms", p. 147
 */
declare const OBLIQUITY_J2000_DEG = 23.439291111;
/**
 * Coefficients for mean obliquity polynomial (degrees).
 *
 * @remarks
 * ε = ε₀ + ε₁·T + ε₂·T² + ε₃·T³
 * where T is Julian centuries from J2000.0
 *
 * More precise than just using the constant J2000 value.
 *
 * @source IAU 1976 / Lieske et al. (1977)
 * @source Meeus, "Astronomical Algorithms", Eq. 22.2, p. 147
 */
declare const OBLIQUITY_COEFFICIENTS$1: {
    /** Constant term: 23°26'21.448" in degrees */
    c0: number;
    /** Linear term: -46.8150"/century in degrees/century */
    c1: number;
    /** Quadratic term: -0.00059"/century² in degrees/century² */
    c2: number;
    /** Cubic term: +0.001813"/century³ in degrees/century³ */
    c3: number;
};
/**
 * Minimum valid Julian Date for this implementation.
 *
 * @remarks
 * Meeus simplified formulas are reliable from ~1800 CE.
 * JD 2378497 ≈ January 1, 1800
 */
declare const MIN_JULIAN_DATE = 2378497;
/**
 * Maximum valid Julian Date for this implementation.
 *
 * @remarks
 * Meeus simplified formulas are reliable to ~2200 CE.
 * JD 2524594 ≈ January 1, 2200
 */
declare const MAX_JULIAN_DATE = 2524594;

/**
 * Sun Position Calculator
 *
 * Calculates the apparent geocentric position of the Sun.
 *
 * @module ephemeris/sun
 *
 * @remarks
 * The Sun is the foundation for all astrological calculations:
 * - Defines the ecliptic plane
 * - Determines day/night (for Part of Fortune calculation)
 * - Primary reference for all planetary aspects
 *
 * Algorithm accuracy: ±1 arcminute (±0.017°) for years 1800-2200.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 25, pp. 163-169
 */

/**
 * Calculates the Sun's geometric mean longitude.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Mean longitude L₀ in degrees (not normalized)
 *
 * @remarks
 * L₀ = 280.46646 + 36000.76983·T + 0.0003032·T²
 *
 * The geometric mean longitude is where the Sun would be if Earth's
 * orbit were perfectly circular and unperturbed.
 *
 * @source Meeus, "Astronomical Algorithms", Table 25.a, p. 163
 * @internal
 */
declare function sunMeanLongitude(T: number): number;
/**
 * Calculates the Sun's mean anomaly.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Mean anomaly M in degrees (not normalized)
 *
 * @remarks
 * M = 357.52911 + 35999.05029·T - 0.0001537·T²
 *
 * The mean anomaly is the angle from perihelion if the body
 * moved uniformly in a circular orbit.
 *
 * @source Meeus, "Astronomical Algorithms", Table 25.a, p. 163
 * @internal
 */
declare function sunMeanAnomaly(T: number): number;
/**
 * Calculates Earth's orbital eccentricity.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Eccentricity e (dimensionless)
 *
 * @remarks
 * e = 0.016708634 - 0.000042037·T - 0.0000001267·T²
 *
 * Earth's eccentricity is slowly decreasing.
 *
 * @source Meeus, "Astronomical Algorithms", Eq. 25.4, p. 163
 * @internal
 */
declare function earthEccentricity(T: number): number;
/**
 * Calculates the equation of center for the Sun.
 *
 * @param M - Mean anomaly in degrees
 * @param T - Julian centuries from J2000.0
 * @returns Equation of center C in degrees
 *
 * @remarks
 * The equation of center converts mean anomaly to true anomaly
 * for the Sun. It's the difference between where the Sun actually
 * is and where it would be in a circular orbit.
 *
 * C = (1.914602 - 0.004817·T - 0.000014·T²)·sin(M)
 *   + (0.019993 - 0.000101·T)·sin(2M)
 *   + 0.000289·sin(3M)
 *
 * @source Meeus, "Astronomical Algorithms", Eq. 25.4, p. 164
 * @internal
 */
declare function sunEquationOfCenter(M: number, T: number): number;
/**
 * Calculates the Sun's true longitude.
 *
 * @param T - Julian centuries from J2000.0
 * @returns True longitude ☉ in degrees (not normalized)
 *
 * @remarks
 * ☉ = L₀ + C
 *
 * True longitude is the actual geometric position of the Sun
 * along the ecliptic.
 *
 * @internal
 */
declare function sunTrueLongitude(T: number): number;
/**
 * Calculates the simplified nutation in longitude.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Nutation Δψ in degrees
 *
 * @remarks
 * This is a simplified formula using only the dominant term.
 * Full nutation has many more terms but this gives ±0.5" accuracy
 * which is within our arcminute target.
 *
 * Δψ ≈ -0.00478·sin(Ω)
 *
 * where Ω is the longitude of the Moon's ascending node.
 *
 * @source Meeus, "Astronomical Algorithms", simplified from Ch. 22
 * @internal
 */
declare function nutationInLongitude(T: number): number;
/**
 * Calculates the Sun's apparent longitude.
 *
 * @param T - Julian centuries from J2000.0
 * @param options - Calculation options
 * @returns Apparent longitude in degrees (not normalized)
 *
 * @remarks
 * Apparent longitude = True longitude + Nutation + Aberration
 *
 * Aberration is the apparent shift due to Earth's orbital velocity
 * combined with the finite speed of light.
 *
 * For the Sun: aberration ≈ -20.4898"/R where R is Earth-Sun distance in AU.
 * Since R ≈ 1 AU, aberration ≈ -20.5" ≈ -0.00569°
 *
 * @source Meeus, "Astronomical Algorithms", pp. 167-168
 * @internal
 */
declare function sunApparentLongitude(T: number, options?: EphemerisOptions): number;
/**
 * Calculates the Sun's distance from Earth (radius vector).
 *
 * @param T - Julian centuries from J2000.0
 * @returns Distance R in Astronomical Units (AU)
 *
 * @remarks
 * R = 1.000001018 · (1 - e²) / (1 + e·cos(ν))
 *
 * where e is eccentricity and ν is true anomaly.
 *
 * Simplified: R ≈ 1.00014 - 0.01671·cos(M) - 0.00014·cos(2M)
 *
 * Range: ~0.983 AU (perihelion, ~Jan 3) to ~1.017 AU (aphelion, ~Jul 4)
 *
 * @source Meeus, "Astronomical Algorithms", Eq. 25.5, p. 164
 * @internal
 */
declare function sunDistance(T: number): number;
/**
 * Calculates the geocentric position of the Sun.
 *
 * @param jd - Julian Date
 * @param options - Optional calculation parameters
 * @returns Sun's position including longitude, latitude, distance, and speed
 *
 * @remarks
 * The Sun's ecliptic latitude is always essentially 0° (by definition,
 * the Sun defines the ecliptic plane). Small deviations (~0.0001°) are
 * due to the barycenter of the solar system not being at the Sun's center.
 *
 * Accuracy: ±1 arcminute for years 1800-2200.
 *
 * @example
 * ```typescript
 * import { getSunPosition } from 'celestine/ephemeris';
 *
 * // J2000.0 epoch
 * const jd = 2451545.0;
 * const sun = getSunPosition(jd);
 * // sun.longitude ≈ 280.46° (Capricorn)
 * // sun.latitude ≈ 0°
 * // sun.distance ≈ 0.983 AU
 * // sun.longitudeSpeed ≈ 1.02°/day
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 25
 */
declare function getSunPosition(jd: number, options?: EphemerisOptions): PlanetPosition;

/**
 * Moon Position Calculator
 *
 * Calculates the apparent geocentric position of the Moon.
 *
 * @module ephemeris/moon
 *
 * @remarks
 * The Moon is the most complex celestial body to calculate due to:
 * - Strong perturbations from the Sun
 * - Over 60 periodic terms for longitude
 * - Fast motion (~13°/day vs Sun's ~1°/day)
 *
 * Algorithm accuracy: ±10 arcseconds for longitude, ±4 arcseconds for latitude.
 * This is significantly better than our 1 arcminute target.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 47, pp. 337-345
 */

/**
 * Calculates the Moon's mean longitude (L').
 *
 * @param T - Julian centuries from J2000.0
 * @returns Mean longitude in degrees (not normalized)
 *
 * @source Meeus, "Astronomical Algorithms", Table 47.a, p. 338
 * @internal
 */
declare function moonMeanLongitude(T: number): number;
/**
 * Calculates the mean elongation of the Moon from the Sun (D).
 *
 * @param T - Julian centuries from J2000.0
 * @returns Mean elongation in degrees
 *
 * @source Meeus, "Astronomical Algorithms", Table 47.a, p. 338
 * @internal
 */
declare function moonMeanElongation(T: number): number;
/**
 * Calculates the Sun's mean anomaly (M) for Moon calculations.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Sun's mean anomaly in degrees
 *
 * @source Meeus, "Astronomical Algorithms", Table 47.a, p. 338
 * @internal
 */
declare function sunMeanAnomalyForMoon(T: number): number;
/**
 * Calculates the Moon's mean anomaly (M').
 *
 * @param T - Julian centuries from J2000.0
 * @returns Moon's mean anomaly in degrees
 *
 * @source Meeus, "Astronomical Algorithms", Table 47.a, p. 338
 * @internal
 */
declare function moonMeanAnomaly(T: number): number;
/**
 * Calculates the Moon's argument of latitude (F).
 *
 * @param T - Julian centuries from J2000.0
 * @returns Argument of latitude in degrees
 *
 * @source Meeus, "Astronomical Algorithms", Table 47.a, p. 338
 * @internal
 */
declare function moonArgumentOfLatitude(T: number): number;
/**
 * Calculates the Moon's ecliptic longitude.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Ecliptic longitude in degrees (not normalized)
 *
 * @remarks
 * Uses 60 periodic terms from Meeus Table 47.A.
 * Accuracy: approximately ±10 arcseconds.
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 47
 * @internal
 */
declare function moonLongitude(T: number): number;
/**
 * Calculates the Moon's ecliptic latitude.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Ecliptic latitude in degrees
 *
 * @remarks
 * Uses 60 periodic terms from Meeus Table 47.B.
 * Accuracy: approximately ±4 arcseconds.
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 47
 * @internal
 */
declare function moonLatitude(T: number): number;
/**
 * Calculates the Moon's distance from Earth.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Distance in kilometers
 *
 * @remarks
 * Uses periodic terms from Meeus Table 47.A.
 * Mean distance: 385,000 km
 * Range: ~356,500 km (perigee) to ~406,700 km (apogee)
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 47
 * @internal
 */
declare function moonDistance(T: number): number;
/**
 * Calculates the Moon's distance in AU.
 *
 * @param T - Julian centuries from J2000.0
 * @returns Distance in Astronomical Units
 *
 * @internal
 */
declare function moonDistanceAU(T: number): number;
/**
 * Calculates the geocentric position of the Moon.
 *
 * @param jd - Julian Date
 * @param options - Optional calculation parameters
 * @returns Moon's position including longitude, latitude, distance, and speed
 *
 * @remarks
 * The Moon's position is calculated using Meeus' algorithm from Chapter 47,
 * which is based on the ELP-2000/82 lunar theory (simplified).
 *
 * Accuracy:
 * - Longitude: ±10 arcseconds (~0.003°)
 * - Latitude: ±4 arcseconds (~0.001°)
 *
 * This is well within our 1 arcminute (0.017°) target.
 *
 * @example
 * ```typescript
 * import { getMoonPosition } from 'celestine/ephemeris';
 *
 * // J2000.0 epoch
 * const jd = 2451545.0;
 * const moon = getMoonPosition(jd);
 * // moon.longitude ≈ 218.32° (Scorpio)
 * // moon.latitude ≈ 5.15°
 * // moon.distance ≈ 0.00257 AU (~385,000 km)
 * // moon.longitudeSpeed ≈ 13.2°/day
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 47
 */
declare function getMoonPosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Calculates the Moon's longitude of the ascending node (mean node).
 *
 * @param T - Julian centuries from J2000.0
 * @returns Longitude of ascending node in degrees
 *
 * @remarks
 * The ascending node is where the Moon's orbit crosses the ecliptic going north.
 * This is the "mean" node - it doesn't include short-period oscillations.
 *
 * The node regresses (moves backward) through the zodiac, completing one
 * cycle in about 18.6 years (~19°/year or ~1.5°/month).
 *
 * @source Meeus, "Astronomical Algorithms", Eq. 47.7, p. 343
 */
declare function moonMeanAscendingNode(T: number): number;
/**
 * Calculates the Moon's mean perigee (mean Lilith / Black Moon).
 *
 * @param T - Julian centuries from J2000.0
 * @returns Longitude of mean perigee in degrees
 *
 * @remarks
 * The lunar apogee (mean Black Moon Lilith) is 180° from the perigee.
 * The perigee advances through the zodiac, completing one cycle in about 8.85 years.
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 50
 */
declare function moonMeanPerigee(T: number): number;

/**
 * Jupiter Position Calculator
 *
 * Calculates the apparent geocentric position of Jupiter.
 *
 * @module ephemeris/planets/jupiter
 *
 * @remarks
 * Jupiter is the largest planet with an orbital period of ~12 years.
 * It spends about 1 year in each zodiac sign.
 * Jupiter's opposition occurs every ~399 days.
 *
 * Algorithm accuracy: ±1 arcminute for longitude.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 33
 */

/**
 * Calculates Jupiter's heliocentric longitude.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric longitude in radians
 * @internal
 */
declare function jupiterHeliocentricLongitude(tau: number): number;
/**
 * Calculates Jupiter's heliocentric latitude.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric latitude in radians
 * @internal
 */
declare function jupiterHeliocentricLatitude(tau: number): number;
/**
 * Calculates Jupiter's heliocentric distance (radius vector).
 * @param tau - Julian millennia from J2000.0
 * @returns Distance in AU
 * @internal
 */
declare function jupiterHeliocentricDistance(tau: number): number;
/**
 * Calculates the geocentric position of Jupiter.
 *
 * @param jd - Julian Date
 * @param options - Optional calculation parameters
 * @returns Jupiter's position including longitude, latitude, distance, and speed
 *
 * @remarks
 * Jupiter is the largest planet with a ~12 year orbital period.
 * It spends about 1 year in each zodiac sign, making it important
 * for yearly astrological transits.
 *
 * Accuracy: ±1 arcminute for years 1800-2200.
 *
 * @example
 * ```typescript
 * import { getJupiterPosition } from 'celestine/ephemeris';
 *
 * const jd = 2451545.0; // J2000.0
 * const jupiter = getJupiterPosition(jd);
 * // jupiter.longitude ≈ 34° (Taurus)
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 33
 */
declare function getJupiterPosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Jupiter's orbital elements (mean values at J2000.0).
 */
declare const JUPITER_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU */
    readonly semiMajorAxis: 5.202887;
    /** Orbital eccentricity */
    readonly eccentricity: 0.04838624;
    /** Orbital inclination in degrees */
    readonly inclination: 1.30439695;
    /** Longitude of ascending node in degrees */
    readonly ascendingNode: 100.47390909;
    /** Longitude of perihelion in degrees */
    readonly perihelion: 14.72847983;
    /** Mean longitude at J2000.0 in degrees */
    readonly meanLongitude: 34.39644051;
    /** Orbital period in days */
    readonly orbitalPeriod: 4332.59;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 11.862;
    /** Synodic period in days (Jupiter-Earth) */
    readonly synodicPeriod: 398.88;
};

/**
 * Mars Position Calculator
 *
 * Calculates the apparent geocentric position of Mars.
 *
 * @module ephemeris/planets/mars
 *
 * @remarks
 * Mars is the first outer planet (orbit larger than Earth's).
 * It has notable eccentricity (0.0934) and an orbital period of ~687 days.
 * Mars opposition occurs every ~780 days when Earth passes between Mars and Sun.
 *
 * Algorithm accuracy: ±1 arcminute for longitude.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 33
 */

/**
 * Calculates Mars's heliocentric longitude.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric longitude in radians
 * @internal
 */
declare function marsHeliocentricLongitude(tau: number): number;
/**
 * Calculates Mars's heliocentric latitude.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric latitude in radians
 * @internal
 */
declare function marsHeliocentricLatitude(tau: number): number;
/**
 * Calculates Mars's heliocentric distance (radius vector).
 * @param tau - Julian millennia from J2000.0
 * @returns Distance in AU
 * @internal
 */
declare function marsHeliocentricDistance(tau: number): number;
/**
 * Calculates the geocentric position of Mars.
 *
 * @param jd - Julian Date
 * @param options - Optional calculation parameters
 * @returns Mars's position including longitude, latitude, distance, and speed
 *
 * @remarks
 * Mars is the first outer planet (orbit larger than Earth's). Its opposition
 * occurs every ~780 days when Earth passes between Mars and Sun, making Mars
 * brightest and closest to Earth.
 *
 * Accuracy: ±1 arcminute for years 1800-2200.
 *
 * @example
 * ```typescript
 * import { getMarsPosition } from 'celestine/ephemeris';
 *
 * const jd = 2451545.0; // J2000.0
 * const mars = getMarsPosition(jd);
 * // mars.longitude ≈ 355° (Pisces)
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 33
 */
declare function getMarsPosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Mars's orbital elements (mean values at J2000.0).
 */
declare const MARS_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU */
    readonly semiMajorAxis: 1.52371034;
    /** Orbital eccentricity */
    readonly eccentricity: 0.0933941;
    /** Orbital inclination in degrees */
    readonly inclination: 1.84969142;
    /** Longitude of ascending node in degrees */
    readonly ascendingNode: 49.55953891;
    /** Longitude of perihelion in degrees */
    readonly perihelion: 336.04084219;
    /** Mean longitude at J2000.0 in degrees */
    readonly meanLongitude: 355.45332;
    /** Orbital period in days */
    readonly orbitalPeriod: 686.98;
    /** Synodic period in days (Mars-Earth) */
    readonly synodicPeriod: 779.94;
};

/**
 * Mercury Position Calculator
 *
 * Calculates the apparent geocentric position of Mercury.
 *
 * @module ephemeris/planets/mercury
 *
 * @remarks
 * Mercury is the fastest planet and has the highest eccentricity (0.2056)
 * among the inner planets. It orbits the Sun in ~88 days.
 *
 * Algorithm accuracy: ±1 arcminute for longitude.
 *
 * The calculation involves:
 * 1. Heliocentric position using VSOP87-derived series (Meeus Tables 33.a)
 * 2. Conversion to geocentric coordinates
 * 3. Aberration correction
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 33
 */

/**
 * Calculates Mercury's heliocentric longitude.
 *
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric longitude in radians
 * @internal
 */
declare function mercuryHeliocentricLongitude(tau: number): number;
/**
 * Calculates Mercury's heliocentric latitude.
 *
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric latitude in radians
 * @internal
 */
declare function mercuryHeliocentricLatitude(tau: number): number;
/**
 * Calculates Mercury's heliocentric distance (radius vector).
 *
 * @param tau - Julian millennia from J2000.0
 * @returns Distance in AU
 * @internal
 */
declare function mercuryHeliocentricDistance(tau: number): number;
/**
 * Calculates the geocentric position of Mercury.
 *
 * @param jd - Julian Date
 * @param options - Optional calculation parameters
 * @returns Mercury's position including longitude, latitude, distance, and speed
 *
 * @remarks
 * Mercury's position is calculated using VSOP87-derived series from Meeus.
 * The calculation provides heliocentric coordinates which are then converted
 * to geocentric ecliptic coordinates.
 *
 * Accuracy: ±1 arcminute for years 1800-2200.
 *
 * @example
 * ```typescript
 * import { getMercuryPosition } from 'celestine/ephemeris';
 *
 * const jd = 2451545.0; // J2000.0
 * const mercury = getMercuryPosition(jd);
 * // mercury.longitude ≈ 250.2° (Sagittarius)
 * // mercury.isRetrograde depends on orbital position
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 33
 */
declare function getMercuryPosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Mercury's orbital elements (mean values at J2000.0).
 *
 * @remarks
 * These are provided for reference and educational purposes.
 */
declare const MERCURY_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU */
    readonly semiMajorAxis: 0.38709927;
    /** Orbital eccentricity */
    readonly eccentricity: 0.20563593;
    /** Orbital inclination in degrees */
    readonly inclination: 7.00497902;
    /** Longitude of ascending node in degrees */
    readonly ascendingNode: 48.33076593;
    /** Longitude of perihelion in degrees */
    readonly perihelion: 77.45779628;
    /** Mean longitude at J2000.0 in degrees */
    readonly meanLongitude: 252.2503235;
    /** Orbital period in days */
    readonly orbitalPeriod: 87.9691;
    /** Synodic period in days (Mercury-Earth) */
    readonly synodicPeriod: 115.88;
};

/**
 * Neptune Position Calculator
 *
 * Calculates the apparent geocentric position of Neptune.
 *
 * @module ephemeris/planets/neptune
 *
 * @remarks
 * Neptune is the 8th and outermost classical planet with an orbital period of ~165 years.
 * It spends about 14 years in each zodiac sign.
 * Neptune was discovered in 1846 through mathematical prediction - a triumph of celestial mechanics.
 * It has the most circular orbit of all planets (e = 0.0086).
 *
 * Algorithm accuracy: ±1 arcminute for longitude.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 33
 */

/**
 * Calculate heliocentric longitude of Neptune (L) in radians.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric longitude in radians
 */
declare function neptuneHeliocentricLongitude(tau: number): number;
/**
 * Calculate heliocentric latitude of Neptune (B) in radians.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric latitude in radians
 */
declare function neptuneHeliocentricLatitude(tau: number): number;
/**
 * Calculate heliocentric distance of Neptune (R) in AU.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric distance in AU
 */
declare function neptuneHeliocentricDistance(tau: number): number;
/**
 * Calculate apparent geocentric position of Neptune.
 *
 * @param jd - Julian Date
 * @param options - Calculation options
 * @returns Neptune's apparent geocentric position
 *
 * @example
 * ```ts
 * import { getNeptunePosition } from 'celestine';
 *
 * const jd = 2451545.0; // J2000.0
 * const neptune = getNeptunePosition(jd);
 * console.log(neptune.longitude); // ~303.2° (Aquarius)
 * ```
 */
declare function getNeptunePosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Neptune's mean orbital elements (for reference).
 */
declare const NEPTUNE_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU */
    readonly semiMajorAxis: 30.07;
    /** Orbital eccentricity (nearly circular - lowest of all planets!) */
    readonly eccentricity: 0.0086;
    /** Orbital inclination in degrees */
    readonly inclination: 1.77;
    /** Orbital period in days */
    readonly orbitalPeriod: 60190;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 164.8;
    /** Synodic period in days (time between oppositions) */
    readonly synodicPeriod: 367.49;
};

/**
 * Pluto Position Calculator
 *
 * Calculates the apparent geocentric position of Pluto.
 *
 * @module ephemeris/planets/pluto
 *
 * @remarks
 * Pluto is a dwarf planet (reclassified in 2006) but remains essential in astrology.
 * It has a highly eccentric orbit (e = 0.2488) and high inclination (17.16°).
 * At perihelion, Pluto is closer to the Sun than Neptune!
 * Orbital period is ~248 years; it spends 12-32 years in each zodiac sign (varies due to eccentricity).
 *
 * This uses Meeus's algorithm from Chapter 37, which provides accuracy of ±1 arcminute
 * for the period 1885-2099.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 37
 */

/**
 * Calculate apparent geocentric position of Pluto.
 *
 * @param jd - Julian Date
 * @param options - Calculation options
 * @returns Pluto's apparent geocentric position
 *
 * @example
 * ```ts
 * import { getPlutoPosition } from 'celestine';
 *
 * const jd = 2451545.0; // J2000.0
 * const pluto = getPlutoPosition(jd);
 * console.log(pluto.longitude); // ~251.5° (Sagittarius)
 * ```
 */
declare function getPlutoPosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Get Pluto's heliocentric longitude in degrees.
 * @param jd - Julian Date
 * @returns Heliocentric longitude in degrees
 */
declare function plutoHeliocentricLongitude(jd: number): number;
/**
 * Get Pluto's heliocentric latitude in degrees.
 * @param jd - Julian Date
 * @returns Heliocentric latitude in degrees
 */
declare function plutoHeliocentricLatitude(jd: number): number;
/**
 * Get Pluto's heliocentric distance in AU.
 * @param jd - Julian Date
 * @returns Heliocentric distance in AU
 */
declare function plutoHeliocentricDistance(jd: number): number;
/**
 * Pluto's mean orbital elements (for reference).
 */
declare const PLUTO_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU */
    readonly semiMajorAxis: 39.48;
    /** Orbital eccentricity (highly eccentric!) */
    readonly eccentricity: 0.2488;
    /** Orbital inclination in degrees (highly inclined!) */
    readonly inclination: 17.16;
    /** Orbital period in days */
    readonly orbitalPeriod: 90560;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 248;
    /** Synodic period in days (time between oppositions) */
    readonly synodicPeriod: 366.73;
    /** Perihelion distance in AU (inside Neptune's orbit!) */
    readonly perihelion: 29.66;
    /** Aphelion distance in AU */
    readonly aphelion: 49.31;
};

/**
 * Saturn Position Calculator
 *
 * Calculates the apparent geocentric position of Saturn.
 *
 * @module ephemeris/planets/saturn
 *
 * @remarks
 * Saturn is the second largest planet with an orbital period of ~29.46 years.
 * It spends about 2.5 years in each zodiac sign.
 * Saturn's opposition occurs every ~378 days.
 * Saturn is famous for its ring system, which affects its apparent brightness.
 *
 * Algorithm accuracy: ±1 arcminute for longitude.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 33
 */

/**
 * Calculate heliocentric longitude of Saturn (L) in radians.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric longitude in radians
 */
declare function saturnHeliocentricLongitude(tau: number): number;
/**
 * Calculate heliocentric latitude of Saturn (B) in radians.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric latitude in radians
 */
declare function saturnHeliocentricLatitude(tau: number): number;
/**
 * Calculate heliocentric distance of Saturn (R) in AU.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric distance in AU
 */
declare function saturnHeliocentricDistance(tau: number): number;
/**
 * Calculate apparent geocentric position of Saturn.
 *
 * @param jd - Julian Date
 * @param options - Calculation options
 * @returns Saturn's apparent geocentric position
 *
 * @example
 * ```ts
 * import { getSaturnPosition } from 'celestine';
 *
 * const jd = 2451545.0; // J2000.0
 * const saturn = getSaturnPosition(jd);
 * console.log(saturn.longitude); // ~40.4° (Taurus)
 * ```
 */
declare function getSaturnPosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Saturn's mean orbital elements (for reference).
 */
declare const SATURN_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU */
    readonly semiMajorAxis: 9.537;
    /** Orbital eccentricity */
    readonly eccentricity: 0.0539;
    /** Orbital inclination in degrees */
    readonly inclination: 2.49;
    /** Orbital period in days */
    readonly orbitalPeriod: 10759;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 29.46;
    /** Synodic period in days (time between oppositions) */
    readonly synodicPeriod: 378.09;
};

/**
 * Uranus Position Calculator
 *
 * Calculates the apparent geocentric position of Uranus.
 *
 * @module ephemeris/planets/uranus
 *
 * @remarks
 * Uranus is the 7th planet from the Sun with an orbital period of ~84 years.
 * It spends about 7 years in each zodiac sign.
 * Uranus is unique for its extreme axial tilt (97.77°) - it essentially rotates on its side.
 * Discovered by William Herschel in 1781.
 *
 * Algorithm accuracy: ±1 arcminute for longitude.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 33
 */

/**
 * Calculate heliocentric longitude of Uranus (L) in radians.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric longitude in radians
 */
declare function uranusHeliocentricLongitude(tau: number): number;
/**
 * Calculate heliocentric latitude of Uranus (B) in radians.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric latitude in radians
 */
declare function uranusHeliocentricLatitude(tau: number): number;
/**
 * Calculate heliocentric distance of Uranus (R) in AU.
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric distance in AU
 */
declare function uranusHeliocentricDistance(tau: number): number;
/**
 * Calculate apparent geocentric position of Uranus.
 *
 * @param jd - Julian Date
 * @param options - Calculation options
 * @returns Uranus's apparent geocentric position
 *
 * @example
 * ```ts
 * import { getUranusPosition } from 'celestine';
 *
 * const jd = 2451545.0; // J2000.0
 * const uranus = getUranusPosition(jd);
 * console.log(uranus.longitude); // ~314.8° (Aquarius)
 * ```
 */
declare function getUranusPosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Uranus's mean orbital elements (for reference).
 */
declare const URANUS_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU */
    readonly semiMajorAxis: 19.19;
    /** Orbital eccentricity */
    readonly eccentricity: 0.0472;
    /** Orbital inclination in degrees */
    readonly inclination: 0.77;
    /** Orbital period in days */
    readonly orbitalPeriod: 30687;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 84.01;
    /** Synodic period in days (time between oppositions) */
    readonly synodicPeriod: 369.66;
    /** Axial tilt in degrees (unique: rotates on its side) */
    readonly axialTilt: 97.77;
};

/**
 * Venus Position Calculator
 *
 * Calculates the apparent geocentric position of Venus.
 *
 * @module ephemeris/planets/venus
 *
 * @remarks
 * Venus is the brightest planet and has the most circular orbit
 * of any planet (eccentricity 0.0067). It orbits the Sun in ~225 days.
 *
 * Algorithm accuracy: ±1 arcminute for longitude.
 *
 * The calculation involves:
 * 1. Heliocentric position using VSOP87-derived series (Meeus Tables 33.a)
 * 2. Conversion to geocentric coordinates
 * 3. Aberration correction
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 33
 */

/**
 * Calculates Venus's heliocentric longitude.
 *
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric longitude in radians
 * @internal
 */
declare function venusHeliocentricLongitude(tau: number): number;
/**
 * Calculates Venus's heliocentric latitude.
 *
 * @param tau - Julian millennia from J2000.0
 * @returns Heliocentric latitude in radians
 * @internal
 */
declare function venusHeliocentricLatitude(tau: number): number;
/**
 * Calculates Venus's heliocentric distance (radius vector).
 *
 * @param tau - Julian millennia from J2000.0
 * @returns Distance in AU
 * @internal
 */
declare function venusHeliocentricDistance(tau: number): number;
/**
 * Calculates the geocentric position of Venus.
 *
 * @param jd - Julian Date
 * @param options - Optional calculation parameters
 * @returns Venus's position including longitude, latitude, distance, and speed
 *
 * @remarks
 * Venus's position is calculated using VSOP87-derived series from Meeus.
 * Venus has the most circular orbit of any planet (e = 0.0067).
 *
 * Accuracy: ±1 arcminute for years 1800-2200.
 *
 * @example
 * ```typescript
 * import { getVenusPosition } from 'celestine/ephemeris';
 *
 * const jd = 2451545.0; // J2000.0
 * const venus = getVenusPosition(jd);
 * // venus.longitude ≈ 241.5° (Sagittarius)
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Chapter 33
 */
declare function getVenusPosition(jd: number, options?: EphemerisOptions): PlanetPosition;
/**
 * Venus's orbital elements (mean values at J2000.0).
 */
declare const VENUS_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU */
    readonly semiMajorAxis: 0.72333566;
    /** Orbital eccentricity (lowest of all planets) */
    readonly eccentricity: 0.00677672;
    /** Orbital inclination in degrees */
    readonly inclination: 3.39467605;
    /** Longitude of ascending node in degrees */
    readonly ascendingNode: 76.67984255;
    /** Longitude of perihelion in degrees */
    readonly perihelion: 131.60246718;
    /** Mean longitude at J2000.0 in degrees */
    readonly meanLongitude: 181.9790995;
    /** Orbital period in days */
    readonly orbitalPeriod: 224.701;
    /** Synodic period in days (Venus-Earth) */
    readonly synodicPeriod: 583.92;
    /** Maximum elongation from Sun in degrees */
    readonly maxElongation: 47.8;
};

/**
 * Lunar Nodes Calculator
 *
 * Calculates the positions of the Moon's orbital nodes (North Node / South Node).
 *
 * @module ephemeris/nodes
 *
 * @remarks
 * The Lunar Nodes are the two points where the Moon's orbit crosses the ecliptic:
 * - **North Node (Ascending)**: Where Moon crosses ecliptic going northward
 * - **South Node (Descending)**: Where Moon crosses ecliptic going southward (always 180° opposite)
 *
 * The nodes precess **westward** (retrograde) with a period of ~18.6 years.
 *
 * Two calculation methods are provided:
 * - **Mean Node**: Uses mean orbital elements, moves smoothly retrograde
 * - **True Node**: Includes perturbations, oscillates around mean position
 *
 * Both are used in astrology - some traditions prefer Mean, others True.
 *
 * Algorithm accuracy: ±1 arcminute for longitude.
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 47
 */
/**
 * Lunar Node position result.
 */
interface LunarNodePosition {
    /** Longitude of North Node in degrees [0, 360) */
    northNode: number;
    /** Longitude of South Node in degrees [0, 360) - always northNode + 180° */
    southNode: number;
    /** Daily motion in degrees/day (negative = retrograde) */
    speed: number;
    /** Whether the node is retrograde (always true for lunar nodes) */
    isRetrograde: boolean;
}
/**
 * Calculate the Mean North Node longitude.
 *
 * The mean node moves smoothly retrograde at ~0.0529°/day.
 * This is the "averaged" position without perturbations.
 *
 * @param jd - Julian Date
 * @returns Mean North Node longitude in degrees [0, 360)
 *
 * @example
 * ```ts
 * import { getMeanNodeLongitude } from 'celestine';
 *
 * const jd = 2451545.0; // J2000.0
 * const meanNode = getMeanNodeLongitude(jd);
 * console.log(meanNode); // ~125.04°
 * ```
 */
declare function getMeanNodeLongitude(jd: number): number;
/**
 * Calculate the Mean Lunar Node position.
 *
 * @param jd - Julian Date
 * @returns Mean Lunar Node position with north/south nodes and speed
 *
 * @example
 * ```ts
 * import { getMeanNode } from 'celestine';
 *
 * const jd = 2451545.0;
 * const node = getMeanNode(jd);
 * console.log(node.northNode); // ~125.04° (Leo)
 * console.log(node.southNode); // ~305.04° (Aquarius)
 * ```
 */
declare function getMeanNode(jd: number): LunarNodePosition;
/**
 * Calculate the True North Node longitude.
 *
 * The true node includes perturbations from the Sun, causing it to
 * oscillate around the mean node by up to ~1.5°.
 *
 * @param jd - Julian Date
 * @returns True North Node longitude in degrees [0, 360)
 *
 * @example
 * ```ts
 * import { getTrueNodeLongitude } from 'celestine';
 *
 * const jd = 2451545.0; // J2000.0
 * const trueNode = getTrueNodeLongitude(jd);
 * console.log(trueNode); // ~123.95°
 * ```
 */
declare function getTrueNodeLongitude(jd: number): number;
/**
 * Calculate the True Lunar Node position.
 *
 * @param jd - Julian Date
 * @returns True Lunar Node position with north/south nodes and speed
 *
 * @example
 * ```ts
 * import { getTrueNode } from 'celestine';
 *
 * const jd = 2451545.0;
 * const node = getTrueNode(jd);
 * console.log(node.northNode); // ~123.95° (Leo)
 * console.log(node.southNode); // ~303.95° (Aquarius)
 * ```
 */
declare function getTrueNode(jd: number): LunarNodePosition;
/**
 * Get the North Node position (alias for getTrueNode).
 * In astrology, "North Node" typically refers to the True Node.
 *
 * @param jd - Julian Date
 * @returns True Lunar Node position
 */
declare function getNorthNode(jd: number): LunarNodePosition;
/**
 * Get the South Node longitude.
 * The South Node is always exactly 180° opposite the North Node.
 *
 * @param jd - Julian Date
 * @returns South Node longitude in degrees [0, 360)
 */
declare function getSouthNodeLongitude(jd: number): number;
/**
 * Lunar Node orbital characteristics (for reference).
 */
declare const LUNAR_NODE_CHARACTERISTICS: {
    /** Nodal period in days */
    readonly nodalPeriod: 6798.38;
    /** Nodal period in years */
    readonly nodalPeriodYears: 18.6;
    /** Mean daily motion in degrees (negative = retrograde) */
    readonly meanDailyMotion: -0.0529;
    /** Maximum oscillation of true node from mean (degrees) */
    readonly maxOscillation: 1.5;
};

/**
 * Lilith (Black Moon) Calculator
 *
 * Calculates the position of the Moon's apogee, known as Black Moon Lilith.
 *
 * @module ephemeris/lilith
 *
 * @remarks
 * Black Moon Lilith is the lunar apogee - the point in the Moon's elliptical
 * orbit that is farthest from Earth. It's not a physical body but a
 * mathematical point, significant in some astrological traditions.
 *
 * Two calculation methods are provided:
 * - **Mean Lilith**: Uses mean orbital elements, moves smoothly prograde
 * - **True Lilith**: Uses osculating (actual) elements, oscillates significantly
 *
 * Mean Lilith is more commonly used in astrology due to its predictability.
 *
 * Period: ~8.85 years (3232 days) for complete zodiac traversal.
 * Mean daily motion: ~0.111° (always prograde for Mean Lilith).
 *
 * @see Meeus, "Astronomical Algorithms", 2nd Ed., Chapter 47
 */
/**
 * Lilith position result.
 */
interface LilithPosition {
    /** Longitude in degrees [0, 360) */
    longitude: number;
    /** Daily motion in degrees/day */
    speed: number;
    /** Whether Lilith is retrograde (Mean Lilith is never retrograde) */
    isRetrograde: boolean;
}
/**
 * Calculate the Mean Black Moon Lilith longitude.
 *
 * Mean Lilith uses the Moon's mean orbital elements and moves smoothly
 * through the zodiac at ~0.111°/day (prograde).
 *
 * @param jd - Julian Date
 * @returns Mean Lilith longitude in degrees [0, 360)
 */
declare function getMeanLilithLongitude(jd: number): number;
/**
 * Calculate the Mean Black Moon Lilith position.
 *
 * @param jd - Julian Date
 * @returns Mean Lilith position with longitude and speed
 */
declare function getMeanLilith(jd: number): LilithPosition;
/**
 * Calculate the True (Osculating) Black Moon Lilith longitude.
 *
 * True Lilith uses the actual instantaneous orbital elements, which causes
 * it to oscillate significantly around the mean position (up to ±30°).
 *
 * @param jd - Julian Date
 * @returns True Lilith longitude in degrees [0, 360)
 */
declare function getTrueLilithLongitude(jd: number): number;
/**
 * Calculate the True (Osculating) Black Moon Lilith position.
 *
 * @param jd - Julian Date
 * @returns True Lilith position with longitude and speed
 */
declare function getTrueLilith(jd: number): LilithPosition;
/**
 * Get Black Moon Lilith position (alias for getMeanLilith).
 * In astrology, "Lilith" typically refers to Mean Lilith.
 *
 * @param jd - Julian Date
 * @returns Mean Lilith position
 */
declare function getLilith(jd: number): LilithPosition;
/**
 * Black Moon Lilith characteristics (for reference).
 */
declare const LILITH_CHARACTERISTICS: {
    /** Period in days for full zodiac traversal */
    readonly period: 3232;
    /** Period in years */
    readonly periodYears: 8.85;
    /** Mean daily motion in degrees (prograde) */
    readonly meanDailyMotion: 0.111;
    /** Maximum oscillation of true Lilith from mean (degrees) */
    readonly maxOscillation: 30;
};

/**
 * Arabic Parts / Lots Calculator
 *
 * Calculates the positions of Arabic Parts (also called Lots), which are
 * sensitive points derived from the positions of planets and chart angles.
 *
 * @module ephemeris/lots
 *
 * @remarks
 * Arabic Parts (or Lots) are calculated points in astrology, not physical bodies.
 * The most famous is the **Part of Fortune** (Lot of Fortune), calculated from
 * the Sun, Moon, and Ascendant.
 *
 * The traditional formula varies by day/night:
 * - **Day chart** (Sun above horizon): ASC + Moon - Sun
 * - **Night chart** (Sun below horizon): ASC + Sun - Moon
 *
 * Some modern astrologers use the day formula for all charts.
 */
/**
 * Part of Fortune calculation options.
 */
interface PartOfFortuneOptions {
    /** Whether to use the traditional day/night formula distinction. @default true */
    useDayNightFormula?: boolean;
    /** Force day or night formula. Only used if useDayNightFormula is true. */
    isDayChart?: boolean;
}
/**
 * Part of Fortune result.
 */
interface PartOfFortuneResult {
    /** Longitude in degrees [0, 360) */
    longitude: number;
    /** Whether day formula was used */
    isDayChart: boolean;
    /** Sun longitude used in calculation */
    sunLongitude: number;
    /** Moon longitude used in calculation */
    moonLongitude: number;
    /** Ascendant longitude used in calculation */
    ascendant: number;
}
/**
 * Calculate the Part of Fortune longitude.
 *
 * @param sunLon - Sun longitude in degrees
 * @param moonLon - Moon longitude in degrees
 * @param ascendant - Ascendant longitude in degrees
 * @param isDayChart - Whether it's a day chart (Sun above horizon)
 * @returns Part of Fortune longitude in degrees [0, 360)
 */
declare function calculatePartOfFortune(sunLon: number, moonLon: number, ascendant: number, isDayChart: boolean): number;
/**
 * Calculate the Part of Fortune from Julian Date and Ascendant.
 *
 * @param jd - Julian Date
 * @param ascendant - Ascendant longitude in degrees
 * @param options - Calculation options
 * @returns Part of Fortune result with all components
 */
declare function getPartOfFortune(jd: number, ascendant: number, options?: PartOfFortuneOptions): PartOfFortuneResult;
/**
 * Calculate the Part of Fortune using only the day formula.
 *
 * @param sunLon - Sun longitude in degrees
 * @param moonLon - Moon longitude in degrees
 * @param ascendant - Ascendant longitude in degrees
 * @returns Part of Fortune longitude in degrees [0, 360)
 */
declare function calculatePartOfFortuneDayFormula(sunLon: number, moonLon: number, ascendant: number): number;
/**
 * Calculate the Part of Spirit (Lot of Spirit) longitude.
 * The Part of Spirit uses the opposite formula from Part of Fortune.
 *
 * @param sunLon - Sun longitude in degrees
 * @param moonLon - Moon longitude in degrees
 * @param ascendant - Ascendant longitude in degrees
 * @param isDayChart - Whether it's a day chart
 * @returns Part of Spirit longitude in degrees [0, 360)
 */
declare function calculatePartOfSpirit(sunLon: number, moonLon: number, ascendant: number, isDayChart: boolean): number;
/**
 * Get the Part of Spirit from Julian Date and Ascendant.
 *
 * @param jd - Julian Date
 * @param ascendant - Ascendant longitude in degrees
 * @param options - Calculation options
 * @returns Part of Spirit result
 */
declare function getPartOfSpirit(jd: number, ascendant: number, options?: PartOfFortuneOptions): PartOfFortuneResult;
/**
 * Arabic Parts/Lots characteristics (for reference).
 */
declare const LOTS_CHARACTERISTICS: {
    /** Part of Fortune moves approximately this many degrees per day (tied to Moon) */
    readonly dailyMotion: 13;
    /** Part of Fortune completes zodiac cycle in approximately this many days */
    readonly period: 27.3;
};

/**
 * Chiron Position Calculator
 *
 * Calculates the apparent geocentric position of Chiron.
 *
 * @module ephemeris/chiron
 *
 * @remarks
 * Chiron (2060 Chiron) is a centaur - a minor planet with an unstable orbit
 * between Saturn and Uranus. Discovered in 1977 by Charles Kowal, it has
 * become important in modern astrology as the "Wounded Healer."
 *
 * Orbital characteristics:
 * - Semi-major axis: 13.65 AU
 * - Eccentricity: 0.379 (highly eccentric)
 * - Orbital period: ~50.4 years
 * - Perihelion: 8.46 AU (inside Saturn's orbit)
 * - Aphelion: 18.83 AU (near Uranus' orbit)
 * - Inclination: 6.93°
 *
 * This implementation uses Keplerian orbital elements with perturbation
 * corrections for reasonable accuracy.
 *
 * @see JPL Small-Body Database for orbital elements
 */

/**
 * Chiron's orbital elements at J2000.0 epoch.
 * Source: JPL Horizons EPHEM_TYPE='ELEMENTS' at 2000-01-01 12:00 TT
 * Retrieved: 2025-Dec-19
 * These are AUTHORITATIVE values - do not modify without verification!
 */
declare const CHIRON_ORBITAL_ELEMENTS: {
    /** Semi-major axis in AU (from A = 2.035289944812818E+09 km / 149597870.7) */
    readonly semiMajorAxis: 13.607;
    /** Orbital eccentricity */
    readonly eccentricity: 0.3793438805668402;
    /** Orbital inclination in degrees */
    readonly inclination: 6.941566489467262;
    /** Longitude of ascending node at J2000.0 (degrees) */
    readonly longitudeOfAscendingNode: 209.39667025637;
    /** Argument of perihelion at J2000.0 (degrees) */
    readonly argumentOfPerihelion: 339.1420138434362;
    /** Mean anomaly at J2000.0 (degrees) */
    readonly meanAnomalyAtEpoch: 27.99722096634838;
    /** Mean daily motion (degrees/day) - from N = 2.273207179302781E-07 deg/s */
    readonly meanDailyMotion: number;
    /** Orbital period in days */
    readonly orbitalPeriod: 18400;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 50.4;
    /** Synodic period in days */
    readonly synodicPeriod: 377;
    /** Perihelion distance in AU */
    readonly perihelion: 8.44;
    /** Aphelion distance in AU */
    readonly aphelion: 18.77;
};
/**
 * Get Chiron's heliocentric longitude in degrees.
 * @param jd - Julian Date
 * @returns Heliocentric longitude in degrees
 */
declare function chironHeliocentricLongitude(jd: number): number;
/**
 * Get Chiron's heliocentric latitude in degrees.
 * @param jd - Julian Date
 * @returns Heliocentric latitude in degrees
 */
declare function chironHeliocentricLatitude(jd: number): number;
/**
 * Get Chiron's heliocentric distance in AU.
 * @param jd - Julian Date
 * @returns Heliocentric distance in AU
 */
declare function chironHeliocentricDistance(jd: number): number;
/**
 * Calculate apparent geocentric position of Chiron.
 *
 * @param jd - Julian Date
 * @param options - Calculation options
 * @returns Chiron's apparent geocentric position
 *
 * @example
 * ```ts
 * import { getChironPosition } from 'celestine';
 *
 * const jd = 2451545.0; // J2000.0
 * const chiron = getChironPosition(jd);
 * console.log(chiron.longitude); // Position in degrees
 * ```
 */
declare function getChironPosition(jd: number, options?: EphemerisOptions): PlanetPosition;

/**
 * Ceres Position Calculator
 *
 * @module ephemeris/asteroids/ceres
 * @description
 * Calculates geocentric position of Ceres (dwarf planet, largest object in asteroid belt).
 *
 * Uses JPL Horizons orbital elements at J2000.0 epoch for optimal accuracy
 * across the date range commonly used in astrology.
 *
 * @remarks
 * Ceres was the first asteroid discovered (1801) and is now classified as a dwarf planet.
 * In astrology, Ceres represents nurturing, motherhood, and harvest.
 *
 * Orbital characteristics:
 * - Semi-major axis: 2.77 AU
 * - Orbital period: 4.60 years
 * - Eccentricity: 0.078 (nearly circular)
 * - Inclination: 10.6°
 */
/**
 * JPL Horizons orbital elements for Ceres at J2000.0 epoch
 * Source: JPL Horizons EPHEM_TYPE='ELEMENTS' at 2000-01-01 12:00 TT
 * Retrieved: 2025-Dec-19
 * These are AUTHORITATIVE values - do not modify without verification!
 */
declare const CERES_ORBITAL_ELEMENTS: {
    /** Reference epoch (Julian Date TDB) - J2000.0 */
    readonly epoch: 2451545;
    /** Semi-major axis in AU */
    readonly semiMajorAxis: number;
    /** Eccentricity (dimensionless) */
    readonly eccentricity: 0.0783756264716304;
    /** Inclination in degrees */
    readonly inclination: 10.58336045805628;
    /** Longitude of ascending node in degrees */
    readonly longitudeOfAscendingNode: 80.49435747295276;
    /** Argument of perihelion in degrees */
    readonly argumentOfPerihelion: 73.92286274285223;
    /** Mean anomaly at epoch in degrees */
    readonly meanAnomalyAtEpoch: 6.176654513180486;
    /** Mean daily motion in degrees/day */
    readonly meanDailyMotion: number;
    /** Orbital period in days */
    readonly orbitalPeriod: number;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 4.6;
};
/**
 * Gets Ceres heliocentric ecliptic longitude
 */
declare function ceresHeliocentricLongitude(jd: number): number;
/**
 * Gets Ceres heliocentric ecliptic latitude
 */
declare function ceresHeliocentricLatitude(jd: number): number;
/**
 * Gets Ceres heliocentric distance
 */
declare function ceresHeliocentricDistance(jd: number): number;
/**
 * Calculates the complete geocentric position of Ceres
 */
declare function getCeresPosition(jd: number, options?: {
    includeSpeed?: boolean;
}): {
    longitude: number;
    latitude: number;
    distance: number;
    longitudeSpeed: number;
    isRetrograde: boolean;
};

/**
 * Juno Position Calculator
 *
 * @module ephemeris/asteroids/juno
 * @description
 * Calculates geocentric position of Juno (third asteroid discovered).
 *
 * Uses JPL Horizons orbital elements at J2000.0 epoch for optimal accuracy.
 *
 * @remarks
 * Juno was discovered in 1804 and is the 11th largest asteroid.
 * In astrology, Juno represents committed partnerships, marriage, and equality.
 *
 * Orbital characteristics:
 * - Semi-major axis: 2.67 AU
 * - Orbital period: 4.36 years
 * - Eccentricity: 0.258 (moderate)
 * - Inclination: 13.0°
 */
/**
 * JPL Horizons orbital elements for Juno at J2000.0 epoch
 * Source: JPL Horizons EPHEM_TYPE='ELEMENTS' at 2000-01-01 12:00 TT
 * Retrieved: 2025-Dec-19
 * These are AUTHORITATIVE values - do not modify without verification!
 */
declare const JUNO_ORBITAL_ELEMENTS: {
    /** Reference epoch (Julian Date TDB) - J2000.0 */
    readonly epoch: 2451545;
    /** Semi-major axis in AU */
    readonly semiMajorAxis: number;
    /** Eccentricity (dimensionless) */
    readonly eccentricity: 0.2584434725349585;
    /** Inclination in degrees */
    readonly inclination: 12.96742544139138;
    /** Longitude of ascending node in degrees */
    readonly longitudeOfAscendingNode: 170.1725855275966;
    /** Argument of perihelion in degrees */
    readonly argumentOfPerihelion: 248.031724336219;
    /** Mean anomaly at epoch in degrees */
    readonly meanAnomalyAtEpoch: 240.2686465739974;
    /** Mean daily motion in degrees/day */
    readonly meanDailyMotion: number;
    /** Orbital period in days */
    readonly orbitalPeriod: number;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 4.36;
};
declare function junoHeliocentricLongitude(jd: number): number;
declare function junoHeliocentricLatitude(jd: number): number;
declare function junoHeliocentricDistance(jd: number): number;
declare function getJunoPosition(jd: number, options?: {
    includeSpeed?: boolean;
}): {
    longitude: number;
    latitude: number;
    distance: number;
    longitudeSpeed: number;
    isRetrograde: boolean;
};

/**
 * Pallas Position Calculator
 *
 * @module ephemeris/asteroids/pallas
 * @description
 * Calculates geocentric position of Pallas (second-largest asteroid).
 *
 * Uses JPL Horizons orbital elements at J2000.0 epoch for optimal accuracy.
 *
 * @remarks
 * Pallas has the highest inclination (34.8°) of any major asteroid,
 * causing it to reach high ecliptic latitudes.
 * In astrology, Pallas represents wisdom, creative intelligence, and strategy.
 *
 * Orbital characteristics:
 * - Semi-major axis: 2.77 AU
 * - Orbital period: 4.61 years
 * - Eccentricity: 0.230 (moderate)
 * - Inclination: 34.8° (highest of major asteroids)
 */
/**
 * JPL Horizons orbital elements for Pallas at J2000.0 epoch
 * Source: JPL Horizons EPHEM_TYPE='ELEMENTS' at 2000-01-01 12:00 TT
 * Retrieved: 2025-Dec-19
 * These are AUTHORITATIVE values - do not modify without verification!
 */
declare const PALLAS_ORBITAL_ELEMENTS: {
    /** Reference epoch (Julian Date TDB) - J2000.0 */
    readonly epoch: 2451545;
    /** Semi-major axis in AU */
    readonly semiMajorAxis: number;
    /** Eccentricity (dimensionless) */
    readonly eccentricity: 0.2296435321725964;
    /** Inclination in degrees */
    readonly inclination: 34.8461400333228;
    /** Longitude of ascending node in degrees */
    readonly longitudeOfAscendingNode: 173.197799126349;
    /** Argument of perihelion in degrees */
    readonly argumentOfPerihelion: 310.2656378931236;
    /** Mean anomaly at epoch in degrees */
    readonly meanAnomalyAtEpoch: 352.9602856376183;
    /** Mean daily motion in degrees/day */
    readonly meanDailyMotion: number;
    /** Orbital period in days */
    readonly orbitalPeriod: number;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 4.61;
};
declare function pallasHeliocentricLongitude(jd: number): number;
declare function pallasHeliocentricLatitude(jd: number): number;
declare function pallasHeliocentricDistance(jd: number): number;
declare function getPallasPosition(jd: number, options?: {
    includeSpeed?: boolean;
}): {
    longitude: number;
    latitude: number;
    distance: number;
    longitudeSpeed: number;
    isRetrograde: boolean;
};

/**
 * Vesta Position Calculator
 *
 * @module ephemeris/asteroids/vesta
 * @description
 * Calculates geocentric position of Vesta (brightest asteroid).
 *
 * Uses JPL Horizons orbital elements at J2000.0 epoch for optimal accuracy.
 *
 * @remarks
 * Vesta is the second-largest asteroid and the brightest as seen from Earth.
 * It has the shortest orbital period of the major asteroids (~3.6 years).
 * In astrology, Vesta represents devotion, dedication, and sacred service.
 *
 * Orbital characteristics:
 * - Semi-major axis: 2.36 AU (innermost major asteroid)
 * - Orbital period: 3.63 years (shortest of major asteroids)
 * - Eccentricity: 0.090 (nearly circular)
 * - Inclination: 7.1°
 */
/**
 * JPL Horizons orbital elements for Vesta at J2000.0 epoch
 * Source: JPL Horizons EPHEM_TYPE='ELEMENTS' at 2000-01-01 12:00 TT
 * Retrieved: 2025-Dec-19
 * These are AUTHORITATIVE values - do not modify without verification!
 */
declare const VESTA_ORBITAL_ELEMENTS: {
    /** Reference epoch (Julian Date TDB) - J2000.0 */
    readonly epoch: 2451545;
    /** Semi-major axis in AU */
    readonly semiMajorAxis: number;
    /** Eccentricity (dimensionless) */
    readonly eccentricity: 0.09002244561937413;
    /** Inclination in degrees */
    readonly inclination: 7.133935828421654;
    /** Longitude of ascending node in degrees */
    readonly longitudeOfAscendingNode: 103.9514370845001;
    /** Argument of perihelion in degrees */
    readonly argumentOfPerihelion: 149.5866679599199;
    /** Mean anomaly at epoch in degrees */
    readonly meanAnomalyAtEpoch: 341.0238343838706;
    /** Mean daily motion in degrees/day */
    readonly meanDailyMotion: number;
    /** Orbital period in days */
    readonly orbitalPeriod: number;
    /** Orbital period in years */
    readonly orbitalPeriodYears: 3.63;
};
declare function vestaHeliocentricLongitude(jd: number): number;
declare function vestaHeliocentricLatitude(jd: number): number;
declare function vestaHeliocentricDistance(jd: number): number;
declare function getVestaPosition(jd: number, options?: {
    includeSpeed?: boolean;
}): {
    longitude: number;
    latitude: number;
    distance: number;
    longitudeSpeed: number;
    isRetrograde: boolean;
};

/**
 * Kepler Equation Solver
 *
 * Solves Kepler's equation to find eccentric anomaly from mean anomaly.
 * This is fundamental to converting orbital elements to positions.
 *
 * @module ephemeris/utils/kepler
 *
 * @remarks
 * Kepler's equation: M = E - e·sin(E)
 * where:
 *   M = mean anomaly (known)
 *   E = eccentric anomaly (to find)
 *   e = orbital eccentricity
 *
 * This transcendental equation has no closed-form solution and must be
 * solved iteratively using Newton-Raphson method.
 *
 * @see Meeus, "Astronomical Algorithms", Chapter 30, pp. 195-197
 * @see Danby, J.M.A., "Fundamentals of Celestial Mechanics", Chapter 6
 */
/**
 * Solves Kepler's equation for eccentric anomaly.
 *
 * @param meanAnomaly - Mean anomaly M in degrees
 * @param eccentricity - Orbital eccentricity e (0 ≤ e < 1 for ellipse)
 * @returns Eccentric anomaly E in degrees
 *
 * @throws {Error} If eccentricity is out of range [0, 1)
 * @throws {Error} If iteration does not converge
 *
 * @remarks
 * Uses Newton-Raphson iteration:
 * E_{n+1} = E_n - (E_n - e·sin(E_n) - M) / (1 - e·cos(E_n))
 *
 * Convergence is typically achieved in 3-5 iterations for planetary
 * eccentricities (all < 0.25 except Pluto at ~0.25).
 *
 * @example
 * ```typescript
 * // Earth's eccentricity ~0.0167, mean anomaly 90°
 * const E = solveKepler(90, 0.0167);
 * // E ≈ 90.955° (slightly ahead of mean due to eccentricity)
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Eq. 30.7, p. 196
 */
declare function solveKepler(meanAnomaly: number, eccentricity: number): number;
/**
 * Calculates true anomaly from eccentric anomaly and eccentricity.
 *
 * @param eccentricAnomaly - Eccentric anomaly E in degrees
 * @param eccentricity - Orbital eccentricity e
 * @returns True anomaly ν (nu) in degrees
 *
 * @remarks
 * The true anomaly is the actual angular position of the body as seen
 * from the focus of the ellipse (where the Sun is).
 *
 * Formula: tan(ν/2) = √((1+e)/(1-e)) · tan(E/2)
 *
 * Using atan2 for proper quadrant handling.
 *
 * @example
 * ```typescript
 * // If E = 90.955° and e = 0.0167
 * const nu = eccentricToTrue(90.955, 0.0167);
 * // nu ≈ 91.91° (true anomaly leads eccentric anomaly)
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Eq. 30.1, p. 195
 */
declare function eccentricToTrue(eccentricAnomaly: number, eccentricity: number): number;
/**
 * Calculates true anomaly directly from mean anomaly and eccentricity.
 *
 * @param meanAnomaly - Mean anomaly M in degrees
 * @param eccentricity - Orbital eccentricity e
 * @returns True anomaly ν (nu) in degrees
 *
 * @remarks
 * Convenience function that combines solveKepler and eccentricToTrue.
 * This is the most commonly used function for orbital calculations.
 *
 * @example
 * ```typescript
 * // Convert mean anomaly to true anomaly for Earth
 * const trueAnomaly = meanToTrue(90, 0.0167);
 * // trueAnomaly ≈ 91.91°
 * ```
 */
declare function meanToTrue(meanAnomaly: number, eccentricity: number): number;
/**
 * Calculates the radius vector (distance from focus) given eccentric anomaly.
 *
 * @param eccentricAnomaly - Eccentric anomaly E in degrees
 * @param semiMajorAxis - Semi-major axis a (in any unit, typically AU)
 * @param eccentricity - Orbital eccentricity e
 * @returns Distance r from the focus (in same units as semiMajorAxis)
 *
 * @remarks
 * r = a · (1 - e·cos(E))
 *
 * At perihelion (E = 0°): r = a(1-e)
 * At aphelion (E = 180°): r = a(1+e)
 *
 * @example
 * ```typescript
 * // Earth at E = 0° (perihelion)
 * const r = radiusVector(0, 1.0, 0.0167);
 * // r ≈ 0.9833 AU
 * ```
 *
 * @source Meeus, "Astronomical Algorithms", Eq. 30.2, p. 195
 */
declare function radiusVector(eccentricAnomaly: number, semiMajorAxis: number, eccentricity: number): number;

/**
 * Ephemeris Module
 *
 * Calculate positions of celestial bodies (Sun, Moon, planets) at any moment.
 *
 * @module ephemeris
 *
 * @remarks
 * This module provides the core planetary position calculations needed for
 * birth chart generation and transit calculations.
 *
 * **Accuracy**: ±1 arcminute for years 1800-2200 (simplified formulas).
 *
 * **Algorithm source**: Jean Meeus, "Astronomical Algorithms" (2nd Ed., 1998)
 *
 * @example
 * ```typescript
 * import { getSunPosition, Planet } from 'celestine/ephemeris';
 *
 * // Get Sun position at J2000.0 epoch
 * const jd = 2451545.0; // January 1, 2000, 12:00 TT
 * const sun = getSunPosition(jd);
 *
 * console.log(sun.longitude);    // ~280.46° (Capricorn)
 * console.log(sun.distance);     // ~0.983 AU
 * console.log(sun.isRetrograde); // false (Sun never retrogrades)
 * ```
 */

declare const index$5_ARCSECONDS_PER_ARCMINUTE: typeof ARCSECONDS_PER_ARCMINUTE;
declare const index$5_ARCSEC_TO_RAD: typeof ARCSEC_TO_RAD;
declare const index$5_AU_IN_KM: typeof AU_IN_KM;
declare const index$5_CERES_ORBITAL_ELEMENTS: typeof CERES_ORBITAL_ELEMENTS;
declare const index$5_CHIRON_ORBITAL_ELEMENTS: typeof CHIRON_ORBITAL_ELEMENTS;
type index$5_CelestialBody = CelestialBody;
declare const index$5_CelestialBody: typeof CelestialBody;
declare const index$5_DAYS_PER_JULIAN_CENTURY: typeof DAYS_PER_JULIAN_CENTURY;
declare const index$5_DAYS_PER_JULIAN_MILLENNIUM: typeof DAYS_PER_JULIAN_MILLENNIUM;
declare const index$5_DEFAULT_BODIES: typeof DEFAULT_BODIES;
declare const index$5_DEG_TO_RAD: typeof DEG_TO_RAD;
type index$5_EphemerisOptions = EphemerisOptions;
type index$5_EphemerisValidationResult = EphemerisValidationResult;
declare const index$5_JUNO_ORBITAL_ELEMENTS: typeof JUNO_ORBITAL_ELEMENTS;
declare const index$5_JUPITER_ORBITAL_ELEMENTS: typeof JUPITER_ORBITAL_ELEMENTS;
declare const index$5_LIGHT_TIME_PER_AU_DAYS: typeof LIGHT_TIME_PER_AU_DAYS;
declare const index$5_LILITH_CHARACTERISTICS: typeof LILITH_CHARACTERISTICS;
declare const index$5_LOTS_CHARACTERISTICS: typeof LOTS_CHARACTERISTICS;
declare const index$5_LUNAR_NODE_CHARACTERISTICS: typeof LUNAR_NODE_CHARACTERISTICS;
type index$5_LilithPosition = LilithPosition;
type index$5_LunarNodePosition = LunarNodePosition;
declare const index$5_MARS_ORBITAL_ELEMENTS: typeof MARS_ORBITAL_ELEMENTS;
declare const index$5_MAX_JULIAN_DATE: typeof MAX_JULIAN_DATE;
declare const index$5_MERCURY_ORBITAL_ELEMENTS: typeof MERCURY_ORBITAL_ELEMENTS;
declare const index$5_MIN_JULIAN_DATE: typeof MIN_JULIAN_DATE;
declare const index$5_NEPTUNE_ORBITAL_ELEMENTS: typeof NEPTUNE_ORBITAL_ELEMENTS;
declare const index$5_OBLIQUITY_J2000_DEG: typeof OBLIQUITY_J2000_DEG;
type index$5_OrbitalElements = OrbitalElements;
declare const index$5_PALLAS_ORBITAL_ELEMENTS: typeof PALLAS_ORBITAL_ELEMENTS;
declare const index$5_PLUTO_ORBITAL_ELEMENTS: typeof PLUTO_ORBITAL_ELEMENTS;
type index$5_PartOfFortuneOptions = PartOfFortuneOptions;
type index$5_PartOfFortuneResult = PartOfFortuneResult;
type index$5_PlanetPosition = PlanetPosition;
declare const index$5_RAD_TO_DEG: typeof RAD_TO_DEG;
type index$5_RectangularCoordinates = RectangularCoordinates;
declare const index$5_SATURN_ORBITAL_ELEMENTS: typeof SATURN_ORBITAL_ELEMENTS;
declare const index$5_SPEED_OF_LIGHT_KM_S: typeof SPEED_OF_LIGHT_KM_S;
type index$5_SphericalCoordinates = SphericalCoordinates;
declare const index$5_URANUS_ORBITAL_ELEMENTS: typeof URANUS_ORBITAL_ELEMENTS;
declare const index$5_VENUS_ORBITAL_ELEMENTS: typeof VENUS_ORBITAL_ELEMENTS;
declare const index$5_VESTA_ORBITAL_ELEMENTS: typeof VESTA_ORBITAL_ELEMENTS;
declare const index$5_calculatePartOfFortune: typeof calculatePartOfFortune;
declare const index$5_calculatePartOfFortuneDayFormula: typeof calculatePartOfFortuneDayFormula;
declare const index$5_calculatePartOfSpirit: typeof calculatePartOfSpirit;
declare const index$5_ceresHeliocentricDistance: typeof ceresHeliocentricDistance;
declare const index$5_ceresHeliocentricLatitude: typeof ceresHeliocentricLatitude;
declare const index$5_ceresHeliocentricLongitude: typeof ceresHeliocentricLongitude;
declare const index$5_chironHeliocentricDistance: typeof chironHeliocentricDistance;
declare const index$5_chironHeliocentricLatitude: typeof chironHeliocentricLatitude;
declare const index$5_chironHeliocentricLongitude: typeof chironHeliocentricLongitude;
declare const index$5_earthEccentricity: typeof earthEccentricity;
declare const index$5_eccentricToTrue: typeof eccentricToTrue;
declare const index$5_getAllPositions: typeof getAllPositions;
declare const index$5_getAllPositionsObject: typeof getAllPositionsObject;
declare const index$5_getCeresPosition: typeof getCeresPosition;
declare const index$5_getChironPosition: typeof getChironPosition;
declare const index$5_getDegreeInSign: typeof getDegreeInSign;
declare const index$5_getJunoPosition: typeof getJunoPosition;
declare const index$5_getJupiterPosition: typeof getJupiterPosition;
declare const index$5_getLilith: typeof getLilith;
declare const index$5_getMarsPosition: typeof getMarsPosition;
declare const index$5_getMeanLilith: typeof getMeanLilith;
declare const index$5_getMeanLilithLongitude: typeof getMeanLilithLongitude;
declare const index$5_getMeanNode: typeof getMeanNode;
declare const index$5_getMeanNodeLongitude: typeof getMeanNodeLongitude;
declare const index$5_getMercuryPosition: typeof getMercuryPosition;
declare const index$5_getMoonPosition: typeof getMoonPosition;
declare const index$5_getNeptunePosition: typeof getNeptunePosition;
declare const index$5_getNorthNode: typeof getNorthNode;
declare const index$5_getPallasPosition: typeof getPallasPosition;
declare const index$5_getPartOfFortune: typeof getPartOfFortune;
declare const index$5_getPartOfSpirit: typeof getPartOfSpirit;
declare const index$5_getPlutoPosition: typeof getPlutoPosition;
declare const index$5_getPosition: typeof getPosition;
declare const index$5_getSaturnPosition: typeof getSaturnPosition;
declare const index$5_getSign: typeof getSign;
declare const index$5_getSouthNodeLongitude: typeof getSouthNodeLongitude;
declare const index$5_getSunPosition: typeof getSunPosition;
declare const index$5_getTrueLilith: typeof getTrueLilith;
declare const index$5_getTrueLilithLongitude: typeof getTrueLilithLongitude;
declare const index$5_getTrueNode: typeof getTrueNode;
declare const index$5_getTrueNodeLongitude: typeof getTrueNodeLongitude;
declare const index$5_getUranusPosition: typeof getUranusPosition;
declare const index$5_getVenusPosition: typeof getVenusPosition;
declare const index$5_getVestaPosition: typeof getVestaPosition;
declare const index$5_junoHeliocentricDistance: typeof junoHeliocentricDistance;
declare const index$5_junoHeliocentricLatitude: typeof junoHeliocentricLatitude;
declare const index$5_junoHeliocentricLongitude: typeof junoHeliocentricLongitude;
declare const index$5_jupiterHeliocentricDistance: typeof jupiterHeliocentricDistance;
declare const index$5_jupiterHeliocentricLatitude: typeof jupiterHeliocentricLatitude;
declare const index$5_jupiterHeliocentricLongitude: typeof jupiterHeliocentricLongitude;
declare const index$5_marsHeliocentricDistance: typeof marsHeliocentricDistance;
declare const index$5_marsHeliocentricLatitude: typeof marsHeliocentricLatitude;
declare const index$5_marsHeliocentricLongitude: typeof marsHeliocentricLongitude;
declare const index$5_meanToTrue: typeof meanToTrue;
declare const index$5_mercuryHeliocentricDistance: typeof mercuryHeliocentricDistance;
declare const index$5_mercuryHeliocentricLatitude: typeof mercuryHeliocentricLatitude;
declare const index$5_mercuryHeliocentricLongitude: typeof mercuryHeliocentricLongitude;
declare const index$5_moonArgumentOfLatitude: typeof moonArgumentOfLatitude;
declare const index$5_moonDistance: typeof moonDistance;
declare const index$5_moonDistanceAU: typeof moonDistanceAU;
declare const index$5_moonLatitude: typeof moonLatitude;
declare const index$5_moonLongitude: typeof moonLongitude;
declare const index$5_moonMeanAnomaly: typeof moonMeanAnomaly;
declare const index$5_moonMeanAscendingNode: typeof moonMeanAscendingNode;
declare const index$5_moonMeanElongation: typeof moonMeanElongation;
declare const index$5_moonMeanLongitude: typeof moonMeanLongitude;
declare const index$5_moonMeanPerigee: typeof moonMeanPerigee;
declare const index$5_neptuneHeliocentricDistance: typeof neptuneHeliocentricDistance;
declare const index$5_neptuneHeliocentricLatitude: typeof neptuneHeliocentricLatitude;
declare const index$5_neptuneHeliocentricLongitude: typeof neptuneHeliocentricLongitude;
declare const index$5_nutationInLongitude: typeof nutationInLongitude;
declare const index$5_pallasHeliocentricDistance: typeof pallasHeliocentricDistance;
declare const index$5_pallasHeliocentricLatitude: typeof pallasHeliocentricLatitude;
declare const index$5_pallasHeliocentricLongitude: typeof pallasHeliocentricLongitude;
declare const index$5_plutoHeliocentricDistance: typeof plutoHeliocentricDistance;
declare const index$5_plutoHeliocentricLatitude: typeof plutoHeliocentricLatitude;
declare const index$5_plutoHeliocentricLongitude: typeof plutoHeliocentricLongitude;
declare const index$5_radiusVector: typeof radiusVector;
declare const index$5_saturnHeliocentricDistance: typeof saturnHeliocentricDistance;
declare const index$5_saturnHeliocentricLatitude: typeof saturnHeliocentricLatitude;
declare const index$5_saturnHeliocentricLongitude: typeof saturnHeliocentricLongitude;
declare const index$5_solveKepler: typeof solveKepler;
declare const index$5_sunApparentLongitude: typeof sunApparentLongitude;
declare const index$5_sunDistance: typeof sunDistance;
declare const index$5_sunEquationOfCenter: typeof sunEquationOfCenter;
declare const index$5_sunMeanAnomaly: typeof sunMeanAnomaly;
declare const index$5_sunMeanAnomalyForMoon: typeof sunMeanAnomalyForMoon;
declare const index$5_sunMeanLongitude: typeof sunMeanLongitude;
declare const index$5_sunTrueLongitude: typeof sunTrueLongitude;
declare const index$5_uranusHeliocentricDistance: typeof uranusHeliocentricDistance;
declare const index$5_uranusHeliocentricLatitude: typeof uranusHeliocentricLatitude;
declare const index$5_uranusHeliocentricLongitude: typeof uranusHeliocentricLongitude;
declare const index$5_venusHeliocentricDistance: typeof venusHeliocentricDistance;
declare const index$5_venusHeliocentricLatitude: typeof venusHeliocentricLatitude;
declare const index$5_venusHeliocentricLongitude: typeof venusHeliocentricLongitude;
declare const index$5_vestaHeliocentricDistance: typeof vestaHeliocentricDistance;
declare const index$5_vestaHeliocentricLatitude: typeof vestaHeliocentricLatitude;
declare const index$5_vestaHeliocentricLongitude: typeof vestaHeliocentricLongitude;
declare namespace index$5 {
  export { ARCMINUTES_PER_DEGREE$1 as ARCMINUTES_PER_DEGREE, index$5_ARCSECONDS_PER_ARCMINUTE as ARCSECONDS_PER_ARCMINUTE, ARCSECONDS_PER_DEGREE$1 as ARCSECONDS_PER_DEGREE, index$5_ARCSEC_TO_RAD as ARCSEC_TO_RAD, index$5_AU_IN_KM as AU_IN_KM, index$5_CERES_ORBITAL_ELEMENTS as CERES_ORBITAL_ELEMENTS, index$5_CHIRON_ORBITAL_ELEMENTS as CHIRON_ORBITAL_ELEMENTS, index$5_CelestialBody as CelestialBody, index$5_DAYS_PER_JULIAN_CENTURY as DAYS_PER_JULIAN_CENTURY, index$5_DAYS_PER_JULIAN_MILLENNIUM as DAYS_PER_JULIAN_MILLENNIUM, index$5_DEFAULT_BODIES as DEFAULT_BODIES, DEGREES_PER_CIRCLE$1 as DEGREES_PER_CIRCLE, index$5_DEG_TO_RAD as DEG_TO_RAD, type index$5_EphemerisOptions as EphemerisOptions, type index$5_EphemerisValidationResult as EphemerisValidationResult, J2000_EPOCH$1 as J2000_EPOCH, index$5_JUNO_ORBITAL_ELEMENTS as JUNO_ORBITAL_ELEMENTS, index$5_JUPITER_ORBITAL_ELEMENTS as JUPITER_ORBITAL_ELEMENTS, index$5_LIGHT_TIME_PER_AU_DAYS as LIGHT_TIME_PER_AU_DAYS, index$5_LILITH_CHARACTERISTICS as LILITH_CHARACTERISTICS, index$5_LOTS_CHARACTERISTICS as LOTS_CHARACTERISTICS, index$5_LUNAR_NODE_CHARACTERISTICS as LUNAR_NODE_CHARACTERISTICS, type index$5_LilithPosition as LilithPosition, type index$5_LunarNodePosition as LunarNodePosition, index$5_MARS_ORBITAL_ELEMENTS as MARS_ORBITAL_ELEMENTS, index$5_MAX_JULIAN_DATE as MAX_JULIAN_DATE, index$5_MERCURY_ORBITAL_ELEMENTS as MERCURY_ORBITAL_ELEMENTS, index$5_MIN_JULIAN_DATE as MIN_JULIAN_DATE, index$5_NEPTUNE_ORBITAL_ELEMENTS as NEPTUNE_ORBITAL_ELEMENTS, OBLIQUITY_COEFFICIENTS$1 as OBLIQUITY_COEFFICIENTS, index$5_OBLIQUITY_J2000_DEG as OBLIQUITY_J2000_DEG, type index$5_OrbitalElements as OrbitalElements, index$5_PALLAS_ORBITAL_ELEMENTS as PALLAS_ORBITAL_ELEMENTS, index$5_PLUTO_ORBITAL_ELEMENTS as PLUTO_ORBITAL_ELEMENTS, type index$5_PartOfFortuneOptions as PartOfFortuneOptions, type index$5_PartOfFortuneResult as PartOfFortuneResult, Planet$1 as Planet, type index$5_PlanetPosition as PlanetPosition, type PlanetPositions$1 as PlanetPositions, index$5_RAD_TO_DEG as RAD_TO_DEG, type index$5_RectangularCoordinates as RectangularCoordinates, index$5_SATURN_ORBITAL_ELEMENTS as SATURN_ORBITAL_ELEMENTS, index$5_SPEED_OF_LIGHT_KM_S as SPEED_OF_LIGHT_KM_S, type index$5_SphericalCoordinates as SphericalCoordinates, index$5_URANUS_ORBITAL_ELEMENTS as URANUS_ORBITAL_ELEMENTS, index$5_VENUS_ORBITAL_ELEMENTS as VENUS_ORBITAL_ELEMENTS, index$5_VESTA_ORBITAL_ELEMENTS as VESTA_ORBITAL_ELEMENTS, index$5_calculatePartOfFortune as calculatePartOfFortune, index$5_calculatePartOfFortuneDayFormula as calculatePartOfFortuneDayFormula, index$5_calculatePartOfSpirit as calculatePartOfSpirit, index$5_ceresHeliocentricDistance as ceresHeliocentricDistance, index$5_ceresHeliocentricLatitude as ceresHeliocentricLatitude, index$5_ceresHeliocentricLongitude as ceresHeliocentricLongitude, index$5_chironHeliocentricDistance as chironHeliocentricDistance, index$5_chironHeliocentricLatitude as chironHeliocentricLatitude, index$5_chironHeliocentricLongitude as chironHeliocentricLongitude, index$5_earthEccentricity as earthEccentricity, index$5_eccentricToTrue as eccentricToTrue, index$5_getAllPositions as getAllPositions, index$5_getAllPositionsObject as getAllPositionsObject, index$5_getCeresPosition as getCeresPosition, index$5_getChironPosition as getChironPosition, index$5_getDegreeInSign as getDegreeInSign, index$5_getJunoPosition as getJunoPosition, index$5_getJupiterPosition as getJupiterPosition, index$5_getLilith as getLilith, index$5_getMarsPosition as getMarsPosition, index$5_getMeanLilith as getMeanLilith, index$5_getMeanLilithLongitude as getMeanLilithLongitude, index$5_getMeanNode as getMeanNode, index$5_getMeanNodeLongitude as getMeanNodeLongitude, index$5_getMercuryPosition as getMercuryPosition, index$5_getMoonPosition as getMoonPosition, index$5_getNeptunePosition as getNeptunePosition, index$5_getNorthNode as getNorthNode, index$5_getPallasPosition as getPallasPosition, index$5_getPartOfFortune as getPartOfFortune, index$5_getPartOfSpirit as getPartOfSpirit, index$5_getPlutoPosition as getPlutoPosition, index$5_getPosition as getPosition, index$5_getSaturnPosition as getSaturnPosition, index$5_getSign as getSign, index$5_getSouthNodeLongitude as getSouthNodeLongitude, index$5_getSunPosition as getSunPosition, index$5_getTrueLilith as getTrueLilith, index$5_getTrueLilithLongitude as getTrueLilithLongitude, index$5_getTrueNode as getTrueNode, index$5_getTrueNodeLongitude as getTrueNodeLongitude, index$5_getUranusPosition as getUranusPosition, index$5_getVenusPosition as getVenusPosition, index$5_getVestaPosition as getVestaPosition, isRetrograde$2 as isRetrograde, index$5_junoHeliocentricDistance as junoHeliocentricDistance, index$5_junoHeliocentricLatitude as junoHeliocentricLatitude, index$5_junoHeliocentricLongitude as junoHeliocentricLongitude, index$5_jupiterHeliocentricDistance as jupiterHeliocentricDistance, index$5_jupiterHeliocentricLatitude as jupiterHeliocentricLatitude, index$5_jupiterHeliocentricLongitude as jupiterHeliocentricLongitude, index$5_marsHeliocentricDistance as marsHeliocentricDistance, index$5_marsHeliocentricLatitude as marsHeliocentricLatitude, index$5_marsHeliocentricLongitude as marsHeliocentricLongitude, index$5_meanToTrue as meanToTrue, index$5_mercuryHeliocentricDistance as mercuryHeliocentricDistance, index$5_mercuryHeliocentricLatitude as mercuryHeliocentricLatitude, index$5_mercuryHeliocentricLongitude as mercuryHeliocentricLongitude, index$5_moonArgumentOfLatitude as moonArgumentOfLatitude, index$5_moonDistance as moonDistance, index$5_moonDistanceAU as moonDistanceAU, index$5_moonLatitude as moonLatitude, index$5_moonLongitude as moonLongitude, index$5_moonMeanAnomaly as moonMeanAnomaly, index$5_moonMeanAscendingNode as moonMeanAscendingNode, index$5_moonMeanElongation as moonMeanElongation, index$5_moonMeanLongitude as moonMeanLongitude, index$5_moonMeanPerigee as moonMeanPerigee, index$5_neptuneHeliocentricDistance as neptuneHeliocentricDistance, index$5_neptuneHeliocentricLatitude as neptuneHeliocentricLatitude, index$5_neptuneHeliocentricLongitude as neptuneHeliocentricLongitude, index$5_nutationInLongitude as nutationInLongitude, index$5_pallasHeliocentricDistance as pallasHeliocentricDistance, index$5_pallasHeliocentricLatitude as pallasHeliocentricLatitude, index$5_pallasHeliocentricLongitude as pallasHeliocentricLongitude, index$5_plutoHeliocentricDistance as plutoHeliocentricDistance, index$5_plutoHeliocentricLatitude as plutoHeliocentricLatitude, index$5_plutoHeliocentricLongitude as plutoHeliocentricLongitude, index$5_radiusVector as radiusVector, index$5_saturnHeliocentricDistance as saturnHeliocentricDistance, index$5_saturnHeliocentricLatitude as saturnHeliocentricLatitude, index$5_saturnHeliocentricLongitude as saturnHeliocentricLongitude, index$5_solveKepler as solveKepler, index$5_sunApparentLongitude as sunApparentLongitude, index$5_sunDistance as sunDistance, index$5_sunEquationOfCenter as sunEquationOfCenter, index$5_sunMeanAnomaly as sunMeanAnomaly, index$5_sunMeanAnomalyForMoon as sunMeanAnomalyForMoon, index$5_sunMeanLongitude as sunMeanLongitude, index$5_sunTrueLongitude as sunTrueLongitude, index$5_uranusHeliocentricDistance as uranusHeliocentricDistance, index$5_uranusHeliocentricLatitude as uranusHeliocentricLatitude, index$5_uranusHeliocentricLongitude as uranusHeliocentricLongitude, index$5_venusHeliocentricDistance as venusHeliocentricDistance, index$5_venusHeliocentricLatitude as venusHeliocentricLatitude, index$5_venusHeliocentricLongitude as venusHeliocentricLongitude, index$5_vestaHeliocentricDistance as vestaHeliocentricDistance, index$5_vestaHeliocentricLatitude as vestaHeliocentricLatitude, index$5_vestaHeliocentricLongitude as vestaHeliocentricLongitude };
}

/**
 * Calculate the four angles: Ascendant, Midheaven, Descendant, and Imum Coeli
 *
 * The angles are the four cardinal points of the astrological chart, marking
 * the intersections of the ecliptic with the horizon (ASC/DSC) and meridian (MC/IC).
 *
 * These calculations use spherical trigonometry to convert between coordinate systems.
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 13
 */

/**
 * Calculate the Midheaven (MC)
 *
 * The Midheaven is the ecliptic degree that is culminating (crossing the meridian)
 * at the given moment. It's the cusp of the 10th house.
 *
 * Formula from Meeus: tan(MC) = tan(LST) / cos(ε)
 *
 * @param lst - Local Sidereal Time in degrees (0-360)
 * @param obliquity - Obliquity of the ecliptic in degrees (~23.4°)
 * @returns Midheaven position in ecliptic longitude (0-360°)
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 13
 *
 * @example
 * ```typescript
 * // LST = 180° (sidereal noon), obliquity = 23.44°
 * const mc = calculateMidheaven(180, 23.44);
 * console.log(mc);  // ~180° (0° Libra)
 * ```
 */
declare function calculateMidheaven(lst: number, obliquity: number): number;
/**
 * Calculate the Ascendant (ASC)
 *
 * The Ascendant is the ecliptic degree rising on the eastern horizon at the
 * given moment and location. It's the cusp of the 1st house in most house systems.
 *
 * Formula from Meeus:
 * tan(ASC) = cos(LST) / -(sin(ε) * tan(φ) + cos(ε) * sin(LST))
 *
 * where:
 * - LST = Local Sidereal Time
 * - ε = obliquity of the ecliptic
 * - φ = geographic latitude
 *
 * @param lst - Local Sidereal Time in degrees (0-360)
 * @param obliquity - Obliquity of the ecliptic in degrees (~23.4°)
 * @param latitude - Geographic latitude in degrees (-90 to +90)
 * @returns Ascendant position in ecliptic longitude (0-360°)
 *
 * @throws {Error} If latitude is at exact poles (±90°) where ASC is undefined
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 13
 *
 * @example
 * ```typescript
 * // London: lat = 51.5°, LST = 180°, obliquity = 23.44°
 * const asc = calculateAscendant(180, 23.44, 51.5);
 * console.log(asc);  // Ecliptic degree rising at this moment
 * ```
 */
declare function calculateAscendant(lst: number, obliquity: number, latitude: number): number;
/**
 * Calculate all four angles at once
 *
 * Computes ASC, MC, DSC (opposite ASC), and IC (opposite MC) in one call.
 * This is a convenience function that combines the individual calculations.
 *
 * @param lst - Local Sidereal Time in degrees (0-360)
 * @param obliquity - Obliquity of the ecliptic in degrees (~23.4°)
 * @param latitude - Geographic latitude in degrees (-90 to +90)
 * @returns All four angles
 *
 * @throws {Error} If latitude is at exact poles (±90°)
 *
 * @example
 * ```typescript
 * const angles = calculateAngles(180, 23.44, 51.5);
 * console.log(angles);
 * // {
 * //   ascendant: ...,
 * //   midheaven: ...,
 * //   descendant: ...,
 * //   imumCoeli: ...
 * // }
 * ```
 */
declare function calculateAngles(lst: number, obliquity: number, latitude: number): Angles;

/**
 * Houses module constants
 *
 * Constants for house calculations including house system names,
 * latitude thresholds, and calculation tolerances.
 */
/**
 * Maximum latitude for Placidus and Koch house systems
 *
 * Above this latitude (approximately the Arctic/Antarctic circles),
 * Placidus and Koch calculations fail because the mathematical solution
 * doesn't exist (some degrees never rise or set during certain times of year).
 *
 * Systems like Equal and Whole Sign work at all latitudes.
 *
 * @constant
 */
declare const MAX_LATITUDE_PLACIDUS = 66.5;
/**
 * Maximum absolute latitude for house calculations
 *
 * At the exact poles (±90°), the Ascendant becomes undefined because
 * all ecliptic degrees rise and set simultaneously. This is a hard limit
 * for most house systems.
 *
 * @constant
 */
declare const MAX_ABSOLUTE_LATITUDE = 90;
/**
 * Maximum absolute longitude
 *
 * Longitude can be expressed as -180 to +180 or 0 to 360.
 * We normalize to -180 to +180 internally.
 *
 * @constant
 */
declare const MAX_ABSOLUTE_LONGITUDE = 180;
/**
 * Epsilon for floating-point comparisons
 *
 * Used to check if values are "close enough" to zero or other targets
 * to avoid division-by-zero and other numerical instability issues.
 *
 * @constant
 */
declare const EPSILON = 1e-10;
/**
 * Maximum iterations for iterative house calculations
 *
 * Placidus and other complex systems use iteration to find house cusps.
 * This prevents infinite loops if convergence fails (rare but possible).
 *
 * @constant
 */
declare const MAX_ITERATIONS = 50;
/**
 * Convergence tolerance for iterative calculations
 *
 * When the change between iterations is less than this value (in degrees),
 * we consider the iteration converged.
 *
 * 0.0001° = 0.36 arcseconds (well below astrological precision requirements)
 *
 * @constant
 */
declare const CONVERGENCE_TOLERANCE = 0.0001;
/**
 * Names of all supported house systems
 *
 * Used for validation and display purposes.
 *
 * @constant
 */
declare const HOUSE_SYSTEM_NAMES: Record<string, string>;
/**
 * Obliquity of ecliptic constants (Laskar 1986 formula)
 *
 * These constants are used to calculate the obliquity of the ecliptic
 * (the angle between the ecliptic and celestial equator) as a function of time.
 *
 * Formula: ε₀ = C0 + C1*T + C2*T² + C3*T³
 * where T = Julian Centuries from J2000.0
 *
 * Reference: Laskar, J. (1986), "Secular terms of classical planetary theories
 * using the results of general theory", Astronomy and Astrophysics, 157, 59–70.
 *
 * Also documented in Meeus, "Astronomical Algorithms", Chapter 22
 *
 * @constant
 */
declare const OBLIQUITY_COEFFICIENTS: {
    C0: number;
    C1: number;
    C2: number;
    C3: number;
};

/**
 * Campanus House System
 *
 * Created by Giovanni Campano (Campanus) in the 13th century. One of the oldest
 * house systems still in use. Divides the prime vertical (great circle through
 * east and west points) into 30° segments.
 *
 * Great circles through these division points and the celestial poles intersect
 * the ecliptic to form house cusps.
 *
 * Works at all latitudes, though interpretation becomes unusual near the poles.
 *
 * **Mathematical Foundation:**
 * - Divides the prime vertical into 12 equal parts (30° each)
 * - Pole heights calculated from latitude and prime vertical divisions
 * - Projects onto ecliptic using spherical trigonometry
 *
 * **Reference:**
 * Swiss Ephemeris swehouse.c (lines 1028-1082)
 * https://github.com/aloistr/swisseph/blob/master/swehouse.c
 *
 * @see `.references/swisseph/swehouse.c` for original C implementation
 */

/**
 * Calculate Campanus house cusps
 *
 * Uses prime vertical divisions with varying pole heights and equatorial positions.
 *
 * @param ascendant - Ascendant in degrees (0-360)
 * @param midheaven - Midheaven in degrees (0-360)
 * @param latitude - Geographic latitude in degrees
 * @param obliquity - Obliquity of ecliptic in degrees
 * @returns House cusps (1-12)
 *
 * @example
 * ```typescript
 * const asc = 15;
 * const mc = 285;
 * const cusps = campanusHouses(asc, mc, 51.5, 23.44);
 * console.log(cusps.cusps[0]);  // 15 (House 1 = ASC)
 * ```
 */
declare function campanusHouses(ascendant: number, midheaven: number, latitude: number, obliquity: number): HouseCusps;

/**
 * Equal House System
 *
 * The simplest house system. Each house is exactly 30° from the previous,
 * starting from the Ascendant.
 *
 * The MC floats independently and is not necessarily on the 10th house cusp.
 * This system works at all latitudes except the exact poles.
 *
 * @see "The Houses: Temples of the Sky" by Deborah Houlding
 */

/**
 * Calculate Equal house cusps
 *
 * Each house is exactly 30° from the previous, starting from the Ascendant.
 * House 1 cusp = Ascendant
 * House 2 cusp = Ascendant + 30°
 * House 3 cusp = Ascendant + 60°
 * ... and so on
 *
 * @param ascendant - Ascendant position in degrees (0-360)
 * @returns House cusps (1-12)
 *
 * @example
 * ```typescript
 * // Ascendant at 15° (15° Aries)
 * const cusps = equalHouses(15);
 * console.log(cusps.cusps[0]);  // 15 (House 1)
 * console.log(cusps.cusps[1]);  // 45 (House 2)
 * console.log(cusps.cusps[6]);  // 195 (House 7 = DSC)
 * ```
 */
declare function equalHouses(ascendant: number): HouseCusps;

/**
 * Koch House System (GOH - Birthplace System)
 *
 * Created by Walter Koch in the 1970s. Also known as "Geburtsortshäusersystem" (GOH).
 * Based on the birthplace's local space, using the "true" degree rising at birth.
 *
 * Like Placidus, Koch fails at latitudes above ~66° (near polar circles).
 * Falls back to Porphyry in those cases.
 *
 * **Mathematical Foundation:**
 * Uses spherical trigonometry to calculate cusps based on specific
 * angles measured from the sidereal time, adjusted by a factor `ad3`
 * that depends on the MC and latitude.
 *
 * **Reference:**
 * Swiss Ephemeris swehouse.c (lines 1250-1272)
 * https://github.com/aloistr/swisseph/blob/master/swehouse.c
 *
 * @see `.references/swisseph/swehouse.c` for original C implementation
 */

/**
 * Calculate Koch house cusps
 *
 * Uses the birthplace method with adjusted angles based on MC and latitude.
 * Falls back to Porphyry if latitude is too high (>66°).
 *
 * @param ascendant - Ascendant in degrees (0-360)
 * @param midheaven - Midheaven in degrees (0-360)
 * @param latitude - Geographic latitude in degrees
 * @param obliquity - Obliquity of ecliptic in degrees
 * @returns House cusps (1-12)
 *
 * @example
 * ```typescript
 * const asc = 15;
 * const mc = 285;
 * const cusps = kochHouses(asc, mc, 51.5, 23.44);
 * console.log(cusps.cusps[0]);  // 15 (House 1 = ASC)
 * ```
 */
declare function kochHouses(ascendant: number, midheaven: number, latitude: number, obliquity: number): HouseCusps;

/**
 * Placidus House System
 *
 * Implementation based on Swiss Ephemeris by Dieter Koch and Alois Treindl.
 * This is the most widely used house system in modern Western astrology.
 *
 * The Placidus system divides the diurnal and nocturnal semi-arcs into thirds.
 * It uses an iterative algorithm with "pole height" calculations.
 *
 * **Mathematical Foundation:**
 * - Uses "pole height" (f) to find points where semi-arcs are divided by 1/3 or 2/3
 * - Iteratively refines cusps using spherical trigonometry
 * - Falls back to Porphyry near polar circles (>66° latitude)
 *
 * **Reference:**
 * Swiss Ephemeris swehouse.c (lines 1830-1983)
 * https://github.com/aloistr/swisseph/blob/master/swehouse.c
 *
 * @see `.references/swisseph/swehouse.c` for original C implementation
 */

/**
 * Calculate Placidus house cusps
 *
 * Uses iterative pole-height method from Swiss Ephemeris.
 * Falls back to Porphyry if latitude is too high (>66°).
 *
 * @param ascendant - Ascendant in degrees (0-360)
 * @param midheaven - Midheaven in degrees (0-360)
 * @param latitude - Geographic latitude in degrees
 * @param obliquity - Obliquity of ecliptic in degrees
 * @returns House cusps (1-12)
 *
 * @throws {Error} If calculation fails (automatically falls back to Porphyry)
 */
declare function placidusHouses(ascendant: number, midheaven: number, latitude: number, obliquity: number): HouseCusps;

/**
 * Porphyry House System
 *
 * Simple trisection of the four quadrants defined by the angles.
 * Each quadrant (ASC to IC, IC to DSC, DSC to MC, MC to ASC) is divided
 * into three equal parts.
 *
 * Named after the philosopher Porphyry of Tyros (3rd century CE).
 * Works at all latitudes.
 *
 * @see Porphyry of Tyros (234-305 CE)
 */

/**
 * Calculate Porphyry house cusps
 *
 * Divides each quadrant into three equal parts:
 * - Quadrant 1 (ASC → IC): Houses 1, 2, 3
 * - Quadrant 2 (IC → DSC): Houses 4, 5, 6
 * - Quadrant 3 (DSC → MC): Houses 7, 8, 9
 * - Quadrant 4 (MC → ASC): Houses 10, 11, 12
 *
 * @param ascendant - Ascendant position in degrees (0-360)
 * @param midheaven - Midheaven position in degrees (0-360)
 * @returns House cusps (1-12)
 *
 * @example
 * ```typescript
 * const asc = 15;  // 15° Aries
 * const mc = 285;  // 15° Capricorn
 * const cusps = porphyryHouses(asc, mc);
 * console.log(cusps.cusps[0]);  // 15 (House 1 = ASC)
 * console.log(cusps.cusps[9]);  // 285 (House 10 = MC)
 * ```
 */
declare function porphyryHouses(ascendant: number, midheaven: number): HouseCusps;

/**
 * Regiomontanus House System
 *
 * One of the oldest house systems, created by Johannes Müller von Königsberg
 * (Regiomontanus) in the 15th century. Based on dividing the celestial equator
 * into 12 equal parts and projecting these divisions onto the ecliptic.
 *
 * Uses the equator as the reference circle and divides it into 30° segments.
 * Great circles through these points and the celestial poles intersect the
 * ecliptic to form house cusps.
 *
 * Works at all latitudes, including near the poles.
 *
 * **Reference:**
 * Swiss Ephemeris swehouse.c (lines 1381-1409)
 * https://github.com/aloistr/swisseph/blob/master/swehouse.c
 *
 * @see `.references/swisseph/swehouse.c` for original C implementation
 */

/**
 * Calculate Regiomontanus house cusps
 *
 * Divides the celestial equator into 30° segments and projects onto ecliptic.
 * Uses varying pole heights (fh1, fh2) based on latitude.
 *
 * @param ascendant - Ascendant in degrees (0-360)
 * @param midheaven - Midheaven in degrees (0-360)
 * @param latitude - Geographic latitude in degrees
 * @param obliquity - Obliquity of ecliptic in degrees
 * @returns House cusps (1-12)
 *
 * @example
 * ```typescript
 * const asc = 15;
 * const mc = 285;
 * const cusps = regiomontanusHouses(asc, mc, 51.5, 23.44);
 * console.log(cusps.cusps[0]);  // 15 (House 1 = ASC)
 * ```
 */
declare function regiomontanusHouses(ascendant: number, midheaven: number, latitude: number, obliquity: number): HouseCusps;

/**
 * Whole Sign House System
 *
 * Ancient system from Hellenistic astrology. Each house corresponds to one
 * complete zodiac sign. The house 1 cusp is the beginning of the sign
 * containing the Ascendant.
 *
 * This is the oldest house system, used by Ptolemy and other ancient astrologers.
 * Works at all latitudes except exact poles.
 *
 * @see Ptolemy, "Tetrabiblos"
 * @see Vettius Valens, "Anthologies"
 */

/**
 * Calculate Whole Sign house cusps
 *
 * House 1 starts at 0° of the sign containing the Ascendant.
 * Each subsequent house is the next sign (30° increments).
 *
 * Example: If Ascendant is at 15° Aries:
 * - House 1: 0° Aries - 30° Aries
 * - House 2: 0° Taurus - 30° Taurus
 * - House 3: 0° Gemini - 30° Gemini
 * ... etc
 *
 * @param ascendant - Ascendant position in degrees (0-360)
 * @returns House cusps (1-12)
 *
 * @example
 * ```typescript
 * // Ascendant at 15.5° (15°30' Aries)
 * const cusps = wholeSignHouses(15.5);
 * console.log(cusps.cusps[0]);  // 0 (House 1 = 0° Aries)
 * console.log(cusps.cusps[1]);  // 30 (House 2 = 0° Taurus)
 * console.log(cusps.cusps[11]); // 330 (House 12 = 0° Pisces)
 * ```
 *
 * @example
 * ```typescript
 * // Ascendant at 195° (15° Libra)
 * const cusps = wholeSignHouses(195);
 * console.log(cusps.cusps[0]);  // 180 (House 1 = 0° Libra)
 * ```
 */
declare function wholeSignHouses(ascendant: number): HouseCusps;

/**
 * Utility functions for house calculations
 *
 * Helper functions for working with ecliptic coordinates, house cusps,
 * and angular relationships.
 */
/**
 * Normalize an angle to the range [0, 360)
 *
 * Reduces any angle to its equivalent value between 0 (inclusive) and 360 (exclusive).
 * Handles both positive and negative input values.
 *
 * @param degrees - Angle in degrees (can be any value)
 * @returns Normalized angle in range [0, 360)
 *
 * @example
 * ```typescript
 * normalizeAngle(45)    // 45
 * normalizeAngle(365)   // 5
 * normalizeAngle(-30)   // 330
 * normalizeAngle(720)   // 0
 * ```
 */
declare function normalizeAngle$2(degrees: number): number;
/**
 * Calculate the opposite point on the ecliptic
 *
 * Returns the point 180° opposite the input position.
 * This is used to calculate DSC from ASC, and IC from MC.
 *
 * @param longitude - Ecliptic longitude in degrees (0-360)
 * @returns Opposite point in degrees (0-360)
 *
 * @example
 * ```typescript
 * oppositePoint(15)     // 195 (15° + 180°)
 * oppositePoint(195)    // 15  (195° + 180° = 375° = 15°)
 * oppositePoint(0)      // 180
 * oppositePoint(270)    // 90
 * ```
 */
declare function oppositePoint(longitude: number): number;
/**
 * Calculate the shortest angular distance between two points on the ecliptic
 *
 * Returns the smallest angle between two positions, taking into account
 * the circular nature of the ecliptic (0° = 360°).
 *
 * @param angle1 - First angle in degrees
 * @param angle2 - Second angle in degrees
 * @returns Angular distance in degrees (0-180)
 *
 * @example
 * ```typescript
 * angularDistance(10, 50)    // 40
 * angularDistance(350, 10)   // 20 (not 340 - shortest distance)
 * angularDistance(0, 180)    // 180
 * angularDistance(270, 90)   // 180
 * ```
 */
declare function angularDistance(angle1: number, angle2: number): number;
/**
 * Calculate the angular separation with sign (direction)
 *
 * Returns the signed angle from angle1 to angle2, moving in the positive
 * direction (counter-clockwise). Result is in range (-180, +180].
 *
 * Positive = angle2 is ahead (counter-clockwise)
 * Negative = angle2 is behind (clockwise)
 *
 * @param fromAngle - Starting angle in degrees
 * @param toAngle - Ending angle in degrees
 * @returns Signed angular separation in degrees (-180, +180]
 *
 * @example
 * ```typescript
 * signedAngularSeparation(10, 50)    // +40 (50 is ahead)
 * signedAngularSeparation(50, 10)    // -40 (10 is behind)
 * signedAngularSeparation(350, 10)   // +20 (crossing 0°)
 * signedAngularSeparation(10, 350)   // -20 (crossing 0° backwards)
 * ```
 */
declare function signedAngularSeparation(fromAngle: number, toAngle: number): number;
/**
 * Determine which house a given ecliptic position occupies
 *
 * Returns the house number (1-12) that contains the specified position.
 * Houses follow the natural order of the zodiac.
 *
 * @param longitude - Ecliptic longitude in degrees (0-360)
 * @param cusps - Array of 12 house cusps in degrees
 * @returns House number (1-12)
 *
 * @example
 * ```typescript
 * const cusps = [0, 30, 60, 90, 120, 150, 180, 210, 240, 270, 300, 330];
 * getHousePosition(15, cusps)    // 1 (between cusp 1 and cusp 2)
 * getHousePosition(45, cusps)    // 2 (between cusp 2 and cusp 3)
 * getHousePosition(350, cusps)   // 12 (between cusp 12 and cusp 1)
 * ```
 */
declare function getHousePosition(longitude: number, cusps: number[]): number;
/**
 * Check if a position is within a specified orb of an angle
 *
 * Used for determining if a planet is conjunct an angle (ASC, MC, DSC, IC).
 *
 * @param position - Position to check in degrees
 * @param angle - Angle (ASC, MC, etc.) in degrees
 * @param orb - Allowable orb in degrees (default: 10°)
 * @returns true if position is within orb of the angle
 *
 * @example
 * ```typescript
 * isOnAngle(15, 10, 10)    // true (5° from angle, within 10° orb)
 * isOnAngle(25, 10, 10)    // false (15° from angle, outside 10° orb)
 * isOnAngle(355, 5, 10)    // true (10° from angle, crossing 0°)
 * ```
 */
declare function isOnAngle(position: number, angle: number, orb?: number): boolean;
/**
 * Convert ecliptic longitude to zodiac sign and degree
 *
 * Returns which sign (0-11) and degree within that sign (0-30).
 *
 * @param longitude - Ecliptic longitude in degrees (0-360)
 * @returns Object with signIndex (0-11) and degreeInSign (0-30)
 *
 * @example
 * ```typescript
 * eclipticToZodiac(0)      // { signIndex: 0, degreeInSign: 0 } (0° Aries)
 * eclipticToZodiac(15.5)   // { signIndex: 0, degreeInSign: 15.5 } (15°30' Aries)
 * eclipticToZodiac(45)     // { signIndex: 1, degreeInSign: 15 } (15° Taurus)
 * eclipticToZodiac(359)    // { signIndex: 11, degreeInSign: 29 } (29° Pisces)
 * ```
 */
declare function eclipticToZodiac$1(longitude: number): {
    signIndex: number;
    degreeInSign: number;
};
/**
 * Get zodiac sign name from index
 *
 * @param signIndex - Sign index (0-11)
 * @returns Sign name
 *
 * @example
 * ```typescript
 * getSignName(0)   // 'Aries'
 * getSignName(6)   // 'Libra'
 * getSignName(11)  // 'Pisces'
 * ```
 */
declare function getSignName$1(signIndex: number): string;
/**
 * Format ecliptic longitude as zodiac position string
 *
 * Converts a raw ecliptic longitude into a human-readable zodiac position.
 *
 * @param longitude - Ecliptic longitude in degrees (0-360)
 * @returns Formatted string like "15°30' Aries"
 *
 * @example
 * ```typescript
 * formatZodiacPosition(0)       // "0°00' Aries"
 * formatZodiacPosition(15.5)    // "15°30' Aries"
 * formatZodiacPosition(45.75)   // "15°45' Taurus"
 * ```
 */
declare function formatZodiacPosition$1(longitude: number): string;

/**
 * High-Level Houses API
 *
 * Main entry point for calculating astrological house cusps.
 * Supports all major house systems with a unified interface.
 *
 * @module houses
 */

/**
 * Calculate house cusps for a given location, time, and house system
 *
 * This is the main function for calculating astrological houses.
 * It handles all the complexity of different house systems and provides
 * a unified interface.
 *
 * @param location - Geographic coordinates (latitude/longitude)
 * @param lst - Local Sidereal Time in degrees (0-360)
 * @param julianCenturies - Time in Julian centuries from J2000.0 (for obliquity)
 * @param system - House system to use (default: 'placidus')
 * @returns House data including cusps and angles
 *
 * @throws {Error} If location is invalid or calculations fail
 *
 * @example
 * ```typescript
 * import { calculateHouses } from './houses/index.js';
 *
 * const location = { latitude: 51.5074, longitude: -0.1278 }; // London
 * const lst = 180.5; // Local Sidereal Time
 * const T = 0.0; // J2000.0 epoch
 *
 * const houses = calculateHouses(location, lst, T, 'placidus');
 * console.log(houses.angles.ascendant); // Ascendant degree
 * console.log(houses.cusps.cusps[0]);   // House 1 cusp (same as ASC)
 * ```
 */
declare function calculateHouses(location: GeographicLocation, lst: number, julianCenturies: number, system?: HouseSystem): HouseData;
/**
 * Calculate just the angles (ASC, MC, DSC, IC) without house cusps
 *
 * Useful when you only need the four angles and not the full house division.
 *
 * @param location - Geographic coordinates
 * @param lst - Local Sidereal Time in degrees
 * @param julianCenturies - Time in Julian centuries from J2000.0
 * @returns The four angles
 *
 * @throws {Error} If location is invalid
 *
 * @example
 * ```typescript
 * const angles = calculateAnglesOnly(
 *   { latitude: 51.5074, longitude: -0.1278 },
 *   180.5,
 *   0.0
 * );
 * console.log(angles.ascendant);  // ASC
 * console.log(angles.midheaven);  // MC
 * ```
 */
declare function calculateAnglesOnly(location: GeographicLocation, lst: number, julianCenturies: number): Angles;
/**
 * Calculate house cusps for multiple systems at once
 *
 * Efficient when you need to compare different house systems.
 * Calculates angles once and reuses them for all systems.
 *
 * @param location - Geographic coordinates
 * @param lst - Local Sidereal Time in degrees
 * @param julianCenturies - Time in Julian centuries from J2000.0
 * @param systems - Array of house systems to calculate (default: all)
 * @returns Map of system name to house data
 *
 * @example
 * ```typescript
 * const comparison = calculateMultipleSystems(
 *   { latitude: 51.5074, longitude: -0.1278 },
 *   180.5,
 *   0.0,
 *   ['placidus', 'koch', 'equal']
 * );
 *
 * console.log(comparison.placidus.cusps.cusps[1]); // House 2 in Placidus
 * console.log(comparison.koch.cusps.cusps[1]);     // House 2 in Koch
 * ```
 */
declare function calculateMultipleSystems(location: GeographicLocation, lst: number, julianCenturies: number, systems?: HouseSystem[]): Record<HouseSystem, HouseData>;
/**
 * Get a list of all supported house systems
 *
 * @returns Array of house system identifiers
 */
declare function getSupportedHouseSystems(): HouseSystem[];
/**
 * Get a human-readable name for a house system
 *
 * @param system - House system identifier
 * @returns Display name
 *
 * @example
 * ```typescript
 * getHouseSystemName('placidus');     // "Placidus"
 * getHouseSystemName('whole-sign');   // "Whole Sign"
 * ```
 */
declare function getHouseSystemName(system: HouseSystem): string;
/**
 * Check if a house system requires latitude
 *
 * Simple systems like Equal and Whole Sign don't use latitude.
 * Complex systems like Placidus and Koch require it.
 *
 * @param system - House system identifier
 * @returns True if latitude is required for calculations
 */
declare function systemRequiresLatitude(system: HouseSystem): boolean;
/**
 * Check if a house system works at extreme latitudes (>66°)
 *
 * Placidus and Koch fail near polar circles and fall back to Porphyry.
 * Other systems work at all latitudes.
 *
 * @param system - House system identifier
 * @returns True if system works at extreme latitudes
 */
declare function systemWorksAtPolarCircle(system: HouseSystem): boolean;

/**
 * Obliquity of the ecliptic calculations
 *
 * The obliquity of the ecliptic (ε) is the angle between the ecliptic plane
 * (Earth's orbital plane) and the celestial equator (projection of Earth's equator).
 *
 * This angle is approximately 23.4° and changes slowly over time due to
 * gravitational perturbations from the Moon and planets.
 *
 * The obliquity is fundamental for:
 * - Converting between ecliptic and equatorial coordinates
 * - Calculating the Ascendant and Midheaven
 * - Understanding seasonal variations
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 22
 * @see Laskar, J. (1986), A&A, 157, 59-70
 */
/**
 * Calculate the mean obliquity of the ecliptic
 *
 * Uses the Laskar 1986 formula, which is accurate for ±10,000 years around J2000.
 * This is a simplified version of the full IAU formula, sufficient for astrological
 * calculations.
 *
 * Formula: ε₀ = 23.43929111° - 0.013004166°*T - 1.6388889×10⁻⁷°*T² + 5.0361111×10⁻⁷°*T³
 *
 * where T = Julian Centuries from J2000.0 = (JD - 2451545.0) / 36525
 *
 * @param T - Julian Centuries from J2000.0 epoch
 * @returns Mean obliquity of ecliptic in degrees
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 22, page 147
 *
 * @example
 * Calculate obliquity at J2000.0 epoch (T = 0):
 * ```typescript
 * const obliquity = meanObliquity(0);
 * console.log(obliquity);  // ~23.439° (23°26'21")
 * ```
 *
 * @example
 * Calculate obliquity one century after J2000:
 * ```typescript
 * const obliquity = meanObliquity(1.0);
 * console.log(obliquity);  // ~23.427° (slightly decreased)
 * ```
 *
 * @example
 * With Julian Centuries from time module:
 * ```typescript
 * import { toJulianCenturies } from '../time/julian-centuries.js';
 *
 * const T = toJulianCenturies(2460665.0);  // Some Julian Date
 * const obliquity = meanObliquity(T);
 * ```
 */
declare function meanObliquity(T: number): number;
/**
 * Calculate the mean obliquity of the ecliptic from Julian Date
 *
 * Convenience function that converts Julian Date to Julian Centuries
 * and calculates obliquity in one step.
 *
 * @param jd - Julian Date (UT)
 * @returns Mean obliquity of ecliptic in degrees
 *
 * @example
 * Calculate obliquity for J2000.0 epoch:
 * ```typescript
 * import { J2000_EPOCH } from '../time/constants.js';
 *
 * const obliquity = obliquityOfEcliptic(J2000_EPOCH);
 * console.log(obliquity);  // ~23.439° (23°26'21")
 * ```
 *
 * @example
 * Calculate for a specific date:
 * ```typescript
 * import { toJulianDate } from '../time/julian-date.js';
 *
 * const jd = toJulianDate({
 *   year: 2025, month: 12, day: 18,
 *   hour: 12, minute: 0, second: 0
 * });
 * const obliquity = obliquityOfEcliptic(jd);
 * ```
 */
declare function obliquityOfEcliptic(jd: number): number;

/**
 * Validation functions for geographic locations and house system compatibility
 *
 * Validates location coordinates and determines which house systems can be used
 * at a given latitude.
 */

/**
 * Normalize latitude to valid range (-90 to +90)
 *
 * Latitudes beyond ±90 are folded back into the valid range with appropriate
 * hemisphere flipping.
 *
 * @param lat - Input latitude in degrees
 * @returns Normalized latitude in range [-90, +90]
 *
 * @example
 * ```typescript
 * normalizeLatitude(45.5)     // 45.5 (already valid)
 * normalizeLatitude(95)       // 85 (folded back)
 * normalizeLatitude(-95)      // -85 (folded back)
 * normalizeLatitude(180)      // 0 (complete fold)
 * ```
 */
declare function normalizeLatitude(lat: number): number;
/**
 * Normalize longitude to valid range (-180 to +180)
 *
 * Accepts both -180/+180 and 0/360 formats and normalizes to -180/+180.
 *
 * @param lon - Input longitude in degrees
 * @returns Normalized longitude in range (-180, +180]
 *
 * @example
 * ```typescript
 * normalizeLongitude(45.5)      // 45.5 (already valid)
 * normalizeLongitude(185)       // -175 (wrapped)
 * normalizeLongitude(-185)      // 175 (wrapped)
 * normalizeLongitude(360)       // 0 (wrapped)
 * normalizeLongitude(720)       // 0 (multiple wraps)
 * ```
 */
declare function normalizeLongitude(lon: number): number;
/**
 * Validate a geographic location
 *
 * Checks if latitude and longitude are within valid ranges and returns
 * detailed error messages if invalid.
 *
 * Note: This does NOT normalize the values - it checks them as-is.
 * Use `normalizeLatitude`/`normalizeLongitude` first if needed.
 *
 * @param location - Geographic location to validate
 * @returns Validation result with error messages
 *
 * @example
 * ```typescript
 * validateLocation({ latitude: 51.5, longitude: -0.1 })
 * // { valid: true, errors: [] }
 *
 * validateLocation({ latitude: 95, longitude: -0.1 })
 * // { valid: false, errors: ['Latitude must be between -90 and +90, got 95'] }
 * ```
 */
declare function validateLocation(location: GeographicLocation): LocationValidationResult;
/**
 * Check if a house system is available at a given latitude
 *
 * Some house systems (Placidus, Koch) fail at extreme latitudes
 * (above ~66°, the Arctic/Antarctic circles) where some ecliptic degrees
 * never rise or set during certain times of year.
 *
 * @param system - House system to check
 * @param latitude - Geographic latitude in degrees
 * @returns true if the system can be used at this latitude
 *
 * @example
 * ```typescript
 * isHouseSystemAvailable('placidus', 51.5)   // true (London)
 * isHouseSystemAvailable('placidus', 70)     // false (Arctic)
 * isHouseSystemAvailable('equal', 70)        // true (works everywhere)
 * isHouseSystemAvailable('placidus', 90)     // false (North Pole)
 * ```
 */
declare function isHouseSystemAvailable(system: HouseSystem, latitude: number): boolean;
/**
 * Get list of available house systems for a given latitude
 *
 * Returns all house systems that can be calculated at the specified latitude.
 *
 * @param latitude - Geographic latitude in degrees
 * @returns Array of available house systems
 *
 * @example
 * ```typescript
 * getAvailableHouseSystems(51.5)   // All systems (London)
 * getAvailableHouseSystems(70)     // ['equal', 'whole-sign', 'porphyry', ...]
 * getAvailableHouseSystems(90)     // ['equal', 'whole-sign'] (North Pole)
 * ```
 */
declare function getAvailableHouseSystems(latitude: number): HouseSystem[];
/**
 * Get a fallback house system for extreme latitudes
 *
 * When Placidus or other systems fail at high latitudes, this suggests
 * an appropriate alternative system that works everywhere.
 *
 * @param preferredSystem - The originally requested system
 * @param latitude - Geographic latitude in degrees
 * @returns Original system if available, otherwise a suitable fallback
 *
 * @example
 * ```typescript
 * getFallbackHouseSystem('placidus', 51.5)   // 'placidus' (works here)
 * getFallbackHouseSystem('placidus', 70)     // 'porphyry' (fallback)
 * getFallbackHouseSystem('equal', 70)        // 'equal' (works here)
 * ```
 */
declare function getFallbackHouseSystem(preferredSystem: HouseSystem, latitude: number): HouseSystem;

/**
 * Houses Module
 *
 * Calculate astrological house cusps using various house systems.
 * Supports all major traditional and modern house systems.
 *
 * @module houses
 */

type index$4_Angles = Angles;
declare const index$4_CONVERGENCE_TOLERANCE: typeof CONVERGENCE_TOLERANCE;
declare const index$4_EPSILON: typeof EPSILON;
type index$4_GeographicLocation = GeographicLocation;
declare const index$4_HOUSE_SYSTEM_NAMES: typeof HOUSE_SYSTEM_NAMES;
type index$4_HouseCusps = HouseCusps;
type index$4_HouseData = HouseData;
type index$4_HouseSystem = HouseSystem;
type index$4_LocationValidationResult = LocationValidationResult;
declare const index$4_MAX_ABSOLUTE_LATITUDE: typeof MAX_ABSOLUTE_LATITUDE;
declare const index$4_MAX_ABSOLUTE_LONGITUDE: typeof MAX_ABSOLUTE_LONGITUDE;
declare const index$4_MAX_ITERATIONS: typeof MAX_ITERATIONS;
declare const index$4_MAX_LATITUDE_PLACIDUS: typeof MAX_LATITUDE_PLACIDUS;
declare const index$4_OBLIQUITY_COEFFICIENTS: typeof OBLIQUITY_COEFFICIENTS;
declare const index$4_angularDistance: typeof angularDistance;
declare const index$4_calculateAngles: typeof calculateAngles;
declare const index$4_calculateAnglesOnly: typeof calculateAnglesOnly;
declare const index$4_calculateAscendant: typeof calculateAscendant;
declare const index$4_calculateHouses: typeof calculateHouses;
declare const index$4_calculateMidheaven: typeof calculateMidheaven;
declare const index$4_calculateMultipleSystems: typeof calculateMultipleSystems;
declare const index$4_campanusHouses: typeof campanusHouses;
declare const index$4_equalHouses: typeof equalHouses;
declare const index$4_getAvailableHouseSystems: typeof getAvailableHouseSystems;
declare const index$4_getFallbackHouseSystem: typeof getFallbackHouseSystem;
declare const index$4_getHousePosition: typeof getHousePosition;
declare const index$4_getHouseSystemName: typeof getHouseSystemName;
declare const index$4_getSupportedHouseSystems: typeof getSupportedHouseSystems;
declare const index$4_isHouseSystemAvailable: typeof isHouseSystemAvailable;
declare const index$4_isOnAngle: typeof isOnAngle;
declare const index$4_kochHouses: typeof kochHouses;
declare const index$4_meanObliquity: typeof meanObliquity;
declare const index$4_normalizeLatitude: typeof normalizeLatitude;
declare const index$4_normalizeLongitude: typeof normalizeLongitude;
declare const index$4_obliquityOfEcliptic: typeof obliquityOfEcliptic;
declare const index$4_oppositePoint: typeof oppositePoint;
declare const index$4_placidusHouses: typeof placidusHouses;
declare const index$4_porphyryHouses: typeof porphyryHouses;
declare const index$4_regiomontanusHouses: typeof regiomontanusHouses;
declare const index$4_signedAngularSeparation: typeof signedAngularSeparation;
declare const index$4_systemRequiresLatitude: typeof systemRequiresLatitude;
declare const index$4_systemWorksAtPolarCircle: typeof systemWorksAtPolarCircle;
declare const index$4_validateLocation: typeof validateLocation;
declare const index$4_wholeSignHouses: typeof wholeSignHouses;
declare namespace index$4 {
  export { type index$4_Angles as Angles, index$4_CONVERGENCE_TOLERANCE as CONVERGENCE_TOLERANCE, index$4_EPSILON as EPSILON, type index$4_GeographicLocation as GeographicLocation, index$4_HOUSE_SYSTEM_NAMES as HOUSE_SYSTEM_NAMES, type index$4_HouseCusps as HouseCusps, type index$4_HouseData as HouseData, type index$4_HouseSystem as HouseSystem, type index$4_LocationValidationResult as LocationValidationResult, index$4_MAX_ABSOLUTE_LATITUDE as MAX_ABSOLUTE_LATITUDE, index$4_MAX_ABSOLUTE_LONGITUDE as MAX_ABSOLUTE_LONGITUDE, index$4_MAX_ITERATIONS as MAX_ITERATIONS, index$4_MAX_LATITUDE_PLACIDUS as MAX_LATITUDE_PLACIDUS, index$4_OBLIQUITY_COEFFICIENTS as OBLIQUITY_COEFFICIENTS, index$4_angularDistance as angularDistance, index$4_calculateAngles as calculateAngles, index$4_calculateAnglesOnly as calculateAnglesOnly, index$4_calculateAscendant as calculateAscendant, index$4_calculateHouses as calculateHouses, index$4_calculateMidheaven as calculateMidheaven, index$4_calculateMultipleSystems as calculateMultipleSystems, index$4_campanusHouses as campanusHouses, eclipticToZodiac$1 as eclipticToZodiac, index$4_equalHouses as equalHouses, formatZodiacPosition$1 as formatZodiacPosition, index$4_getAvailableHouseSystems as getAvailableHouseSystems, index$4_getFallbackHouseSystem as getFallbackHouseSystem, index$4_getHousePosition as getHousePosition, index$4_getHouseSystemName as getHouseSystemName, getSignName$1 as getSignName, index$4_getSupportedHouseSystems as getSupportedHouseSystems, index$4_isHouseSystemAvailable as isHouseSystemAvailable, index$4_isOnAngle as isOnAngle, index$4_kochHouses as kochHouses, index$4_meanObliquity as meanObliquity, normalizeAngle$2 as normalizeAngle, index$4_normalizeLatitude as normalizeLatitude, index$4_normalizeLongitude as normalizeLongitude, index$4_obliquityOfEcliptic as obliquityOfEcliptic, index$4_oppositePoint as oppositePoint, index$4_placidusHouses as placidusHouses, index$4_porphyryHouses as porphyryHouses, index$4_regiomontanusHouses as regiomontanusHouses, index$4_signedAngularSeparation as signedAngularSeparation, index$4_systemRequiresLatitude as systemRequiresLatitude, index$4_systemWorksAtPolarCircle as systemWorksAtPolarCircle, index$4_validateLocation as validateLocation, index$4_wholeSignHouses as wholeSignHouses };
}

/**
 * Progressions Module - Type Definitions
 *
 * Core types for astrological progression calculations.
 *
 * @module progressions/types
 *
 * @remarks
 * Progressions are predictive techniques where time is symbolically compressed.
 * The most common is secondary progressions where 1 day = 1 year.
 *
 * @see IMPL.md for full implementation specification
 * @see KNOWLEDGE.md Section 13 for birth chart foundations
 */

/**
 * Progression system type.
 *
 * @remarks
 * - 'secondary': Day-for-a-year (most common, 1 day = 1 year)
 * - 'solar-arc': All points move at Sun's rate
 * - 'minor': Month-for-a-year (1 tropical month = 1 year)
 * - 'tertiary': Day-for-a-month (1 day = 1 lunar month)
 */
type ProgressionType = 'secondary' | 'solar-arc' | 'minor' | 'tertiary';
/**
 * Method for calculating progressed angles (ASC, MC).
 *
 * @remarks
 * - 'solar-arc': ASC/MC advance by the solar arc (simpler, more common)
 * - 'time-based': Astronomically recalculate for progressed time
 * - 'true-angles': Alias for 'time-based' (legacy)
 */
type AngleProgressionMethod = 'solar-arc' | 'time-based' | 'true-angles';
/**
 * Phase of a progressed aspect.
 */
type ProgressedAspectPhase = 'applying' | 'exact' | 'separating';
/**
 * A progressed position (for angles, lots, etc.)
 */
interface ProgressedPosition {
    /** Progressed longitude (0-360°) */
    longitude: number;
    /** Natal longitude for reference */
    natalLongitude: number;
    /** Arc traveled since birth (degrees) */
    arcFromNatal: number;
    /** Zodiac sign index (0-11, 0=Aries) */
    signIndex: number;
    /** Zodiac sign name */
    signName: string;
    /** Degree within sign (0-29) */
    degree: number;
    /** Minute within degree (0-59) */
    minute: number;
    /** Second within minute (0-59) */
    second: number;
    /** Formatted position string (e.g., "15°32' Leo") */
    formatted: string;
    /** Has the sign changed from natal? */
    hasChangedSign: boolean;
}
/**
 * A progressed planet position with full details.
 */
interface ProgressedPlanet extends ProgressedPosition {
    /** Planet/body name */
    name: string;
    /** Celestial body enum value */
    body: CelestialBody;
    /** Direction of progressed planet */
    isRetrograde: boolean;
    /** Progressed daily motion (degrees/day) */
    longitudeSpeed: number;
    /** Did retrograde status change from natal? */
    retrogradeChanged: boolean;
    /** Natal retrograde status for reference */
    wasRetrograde: boolean;
    /** House placement (1-12) if available */
    house?: number;
}
/**
 * Progressed chart angles (ASC, MC, DSC, IC).
 */
interface ProgressedAngles$1 {
    /** Progressed Ascendant (1st house cusp) */
    ascendant: ProgressedPosition;
    /** Progressed Midheaven (10th house cusp) */
    midheaven: ProgressedPosition;
    /** Progressed Descendant (7th house cusp) */
    descendant: ProgressedPosition;
    /** Progressed Imum Coeli (4th house cusp) */
    imumCoeli: ProgressedPosition;
}
/**
 * Aspect between progressed and natal positions.
 *
 * @remarks
 * Progressed aspects use much tighter orbs than natal aspects,
 * typically 1° for major aspects and 0.5° for minor aspects.
 */
interface ProgressedAspect {
    /** What is aspecting (progressed body name) */
    progressedBody: string;
    /** Progressed body enum value */
    progressedBodyEnum?: CelestialBody;
    /** Progressed body longitude */
    progressedLongitude: number;
    /** What is being aspected (natal body name) */
    natalBody: string;
    /** Natal body enum value */
    natalBodyEnum?: CelestialBody;
    /** Natal body longitude */
    natalLongitude: number;
    /** Aspect type */
    aspectType: AspectType;
    /** Aspect symbol (e.g., '☌', '□', '△') */
    symbol: string;
    /** Exact angle for this aspect type (0, 60, 90, 120, 180, etc.) */
    exactAngle: number;
    /** Current angular separation */
    separation: number;
    /** Deviation from exact aspect (always positive) */
    deviation: number;
    /** Orb used for this detection */
    orb: number;
    /** Aspect strength as percentage (100 = exact, 0 = at orb edge) */
    strength: number;
    /** Is this aspect applying, exact, or separating? */
    phase: ProgressedAspectPhase;
    /** Is the progressed planet retrograde? */
    isRetrograde: boolean;
    /** When will/did this aspect perfect? (Julian Date) */
    exactJD?: number;
    /** When will/did this aspect perfect? (Calendar date) */
    exactDate?: ProgressedDate;
    /** Age at which aspect perfects */
    exactAge?: number;
}
/**
 * Calendar date representation for progressions.
 */
interface ProgressedDate {
    year: number;
    month: number;
    day: number;
    hour?: number;
    minute?: number;
    second?: number;
}
/**
 * Date information for a progressed chart.
 */
interface ProgressedChartDates {
    /** Natal chart Julian Date */
    natalJD: number;
    /** Natal calendar date */
    natalDate: ProgressedDate;
    /** Target date Julian Date (the date being progressed TO) */
    targetJD: number;
    /** Target calendar date */
    targetDate: ProgressedDate;
    /** Progressed chart Julian Date (the symbolic chart date) */
    progressedJD: number;
    /** Progressed calendar date */
    progressedDate: ProgressedDate;
    /** Days elapsed from birth to progressed chart date */
    daysFromBirth: number;
    /** Age at target date (in years, fractional) */
    ageInYears: number;
}
/**
 * Configuration options for progression calculations.
 *
 * @remarks
 * All options are optional - sensible defaults are used when not specified.
 */
interface ProgressionConfig {
    /**
     * Progression system to use.
     * @default 'secondary'
     */
    type?: ProgressionType;
    /**
     * Method for angle progression (ASC, MC).
     * @default 'solar-arc'
     */
    angleMethod?: AngleProgressionMethod;
    /**
     * Bodies to include in progressed chart.
     * @default Sun, Moon, Mercury, Venus, Mars, Jupiter, Saturn
     */
    bodies?: CelestialBody[];
    /**
     * Include progressed-to-progressed aspects.
     * @default false
     */
    includeProgressedAspects?: boolean;
    /**
     * Include progressed-to-natal aspects.
     * @default true
     */
    includeNatalAspects?: boolean;
    /**
     * Aspect types to detect.
     * @default Major aspects (conjunction, sextile, square, trine, opposition)
     */
    aspectTypes?: AspectType[];
    /**
     * Custom orbs per aspect type (overrides defaults).
     * Values in degrees. Progression orbs are much tighter than natal.
     */
    orbs?: Partial<Record<AspectType, number>>;
    /**
     * Include solar arc directed positions.
     * @default true
     */
    includeSolarArc?: boolean;
    /**
     * Minimum strength (0-100) to include aspects in results.
     * @default 0 (include all within orb)
     */
    minimumStrength?: number;
    /**
     * Threshold for marking aspect as 'exact' phase (degrees).
     * @default 0.1 (~6 arcminutes)
     */
    exactThreshold?: number;
}
/**
 * Information about a sign change in progressions.
 */
interface ProgressedSignChange {
    /** Body that changed signs */
    body: string;
    /** Body enum value */
    bodyEnum?: CelestialBody;
    /** Sign departed */
    fromSign: string;
    /** Sign entered */
    toSign: string;
    /** Approximate age when sign change occurred */
    approximateAge: number;
    /** Julian Date of sign change */
    changeJD?: number;
}
/**
 * Information about a retrograde status change.
 */
interface ProgressedRetrogradeChange {
    /** Body that changed direction */
    body: string;
    /** Body enum value */
    bodyEnum?: CelestialBody;
    /** Direction of change */
    direction: 'turned-retrograde' | 'turned-direct';
    /** Approximate age when change occurred */
    approximateAge: number;
    /** Julian Date of direction change */
    changeJD?: number;
    /** Longitude at direction change */
    longitude?: number;
}
/**
 * Information about the progressed Moon (most significant progressed body).
 */
interface ProgressedMoonInfo {
    /** Current progressed Moon sign */
    sign: string;
    /** Approximate months spent in current sign so far */
    monthsInSign: number;
    /** Approximate months until next sign change */
    monthsUntilNextSign: number;
    /** Last sign change information */
    lastSignChange: {
        fromSign: string;
        toSign: string;
        age: number;
    } | null;
    /** Next sign change information */
    nextSignChange: {
        fromSign: string;
        toSign: string;
        age: number;
    } | null;
}
/**
 * Summary of significant progressions.
 */
interface ProgressionSummary {
    /** Sign changes since birth */
    signChanges: ProgressedSignChange[];
    /** Retrograde status changes since birth */
    retrogradeChanges: ProgressedRetrogradeChange[];
    /** Currently exact aspects (within exactThreshold) */
    exactAspects: ProgressedAspect[];
    /** Approaching aspects (within 1°, applying) */
    approachingAspects: ProgressedAspect[];
    /** Progressed Moon analysis */
    progressedMoon: ProgressedMoonInfo | null;
    /** Most significant aspect (highest strength) */
    mostSignificantAspect: ProgressedAspect | null;
    /** Solar arc value in degrees */
    solarArc: number;
}
/**
 * Complete progressed chart result.
 *
 * @example
 * ```typescript
 * const progressed = calculateProgressedChart(birthData, targetDate);
 *
 * console.log(`Solar Arc: ${progressed.solarArc}°`);
 * console.log(`Progressed Sun: ${progressed.planets[0].formatted}`);
 *
 * for (const aspect of progressed.aspectsToNatal) {
 *   console.log(`P.${aspect.progressedBody} ${aspect.symbol} N.${aspect.natalBody}`);
 * }
 * ```
 */
interface ProgressedChart {
    /** Date information */
    dates: ProgressedChartDates;
    /** Progressed planetary positions */
    planets: ProgressedPlanet[];
    /** Progressed angles (ASC, MC, DSC, IC) */
    angles: ProgressedAngles$1;
    /** Solar arc value in degrees */
    solarArc: number;
    /** Solar arc directed positions (if includeSolarArc is true) */
    solarArcPositions: ProgressedPlanet[];
    /** Progressed-to-Natal aspects */
    aspectsToNatal: ProgressedAspect[];
    /** Progressed-to-Progressed aspects (if enabled) */
    aspectsProgressed: ProgressedAspect[];
    /** Summary information */
    summary: ProgressionSummary;
    /** Configuration used */
    config: Required<ProgressionConfig>;
}
/**
 * Birth data for progression calculations.
 * Matches the BirthData type from chart module.
 */
interface ProgressionBirthData {
    year: number;
    month: number;
    day: number;
    hour: number;
    minute: number;
    second?: number;
    timezone: number;
    latitude: number;
    longitude: number;
}
/**
 * Target date specification for progressions.
 */
interface ProgressionTargetDate {
    year: number;
    month: number;
    day: number;
    hour?: number;
    minute?: number;
    second?: number;
}
/**
 * Error thrown when progression calculation fails.
 */
declare class ProgressionCalculationError extends Error {
    step: string;
    details?: unknown | undefined;
    constructor(message: string, step: string, details?: unknown | undefined);
}
/**
 * Error thrown when progression input is invalid.
 */
declare class ProgressionValidationError extends Error {
    field: string;
    value: unknown;
    constructor(message: string, field: string, value: unknown);
}

/**
 * Progressions Module - Constants
 *
 * Progression rates, default orbs, and configuration constants.
 *
 * @module progressions/constants
 *
 * @remarks
 * All constants are verified against authoritative sources:
 * - Meeus "Astronomical Algorithms" for astronomical values
 * - Robert Hand & Noel Tyl for astrological practice
 *
 * @see IMPL.md Section 4 for detailed specifications
 */

/**
 * Days in a Julian year.
 * Used for calculating age in years from Julian Date differences.
 */
declare const DAYS_PER_YEAR = 365.25;
/**
 * Mean tropical month in days.
 * Used for minor progressions (month-for-a-year).
 *
 * @remarks
 * This is the average time for the Moon to return to the same
 * position relative to the vernal equinox.
 * Source: Meeus "Astronomical Algorithms"
 */
declare const TROPICAL_MONTH_DAYS = 27.321582;
/**
 * Mean synodic month in days.
 * The time between successive new moons.
 */
declare const SYNODIC_MONTH_DAYS = 29.530589;
/**
 * Sun's mean daily motion in degrees.
 * Source: Meeus "Astronomical Algorithms" Chapter 25
 */
declare const SUN_MEAN_DAILY_MOTION = 0.9856473;
/**
 * Moon's mean daily motion in degrees.
 * Source: Meeus "Astronomical Algorithms" Chapter 47
 */
declare const MOON_MEAN_DAILY_MOTION = 13.176358;
/**
 * Rate for secondary progressions.
 * 1 day of planetary motion = 1 year of life.
 */
declare const SECONDARY_PROGRESSION_RATE = 1;
/**
 * Rate for minor progressions.
 * 1 tropical month of motion = 1 year of life.
 */
declare const MINOR_PROGRESSION_RATE = 27.321582;
/**
 * Rate for tertiary progressions (days of motion per year of life).
 * 1 day of motion = 1 month of life.
 * So 12 days of motion = 1 year of life.
 */
declare const TERTIARY_PROGRESSION_RATE = 12;
/**
 * Progression rate lookup by type.
 *
 * @remarks
 * Value represents: how many days of motion per year of life.
 */
declare const PROGRESSION_RATES: Record<ProgressionType, number>;
/**
 * Default orbs for progressed aspects (in degrees).
 *
 * @remarks
 * Progressed aspects use much tighter orbs than natal aspects because:
 * 1. Each degree of orb represents about 1 year of time
 * 2. Tighter orbs increase precision of timing
 * 3. Traditional practice uses 1° for major, 0.5° for minor
 *
 * Source: Robert Hand's recommendations, Noel Tyl "Solar Arcs"
 */
declare const PROGRESSION_ORBS: Readonly<Record<AspectType, number>>;
/**
 * Threshold for marking an aspect as "exact" phase (degrees).
 * Aspects with deviation below this are considered exact.
 * ~6 arcminutes = very precise timing.
 */
declare const EXACT_THRESHOLD = 0.1;
/**
 * Default bodies to include in progressed charts.
 *
 * @remarks
 * Outer planets (Uranus, Neptune, Pluto) are excluded by default because
 * they move too slowly to produce meaningful progressions:
 * - Uranus: ~0.012°/year in progressions (barely noticeable)
 * - Neptune: ~0.006°/year
 * - Pluto: ~0.004°/year
 *
 * Inner planets and luminaries are the meaningful progressed bodies.
 */
declare const DEFAULT_PROGRESSION_BODIES: readonly CelestialBody[];
/**
 * Bodies that can meaningfully retrograde in progressions.
 *
 * @remarks
 * Mercury and Venus can turn retrograde or direct within a typical
 * human lifespan's worth of progressed time. Mars rarely does.
 * Outer planets effectively don't change direction in progressions.
 */
declare const RETROGRADE_CAPABLE_BODIES: readonly CelestialBody[];
/**
 * Major aspects for progressions (Ptolemaic).
 */
declare const MAJOR_PROGRESSION_ASPECTS: readonly AspectType[];
/**
 * Zodiac sign names by index (0-11).
 */
declare const SIGN_NAMES: readonly string[];
/**
 * Default progression configuration.
 *
 * @remarks
 * These defaults represent common astrological practice:
 * - Secondary progressions (most popular system)
 * - Solar arc for angles (simpler, widely used)
 * - Inner planets only (outer planets move too slowly)
 * - Major aspects only (with tight orbs)
 */
declare const DEFAULT_PROGRESSION_CONFIG: Required<ProgressionConfig>;

/**
 * Progression Date Calculations
 *
 * Core Julian Date calculations for astrological progressions.
 *
 * @module progressions/progression-date
 *
 * @remarks
 * The fundamental calculation: convert a target date (when you want
 * the progressed chart for) into a progressed Julian Date (the symbolic
 * chart date in the ephemeris).
 *
 * Secondary: 1 day after birth = 1 year of life
 * Solar Arc: Same timing as secondary (for Sun position)
 * Minor: 1 month after birth = 1 year of life
 * Tertiary: 1 day after birth = 1 month of life
 *
 * @see IMPL.md Section 2.1 for algorithm details
 */

/**
 * Convert birth data to Julian Date.
 *
 * @param birth - Birth data
 * @returns Julian Date
 */
declare function birthToJD(birth: ProgressionBirthData): number;
/**
 * Convert target date to Julian Date.
 *
 * @param target - Target date (assumes noon UT if time not specified)
 * @returns Julian Date
 */
declare function targetToJD(target: ProgressionTargetDate): number;
/**
 * Calculate age in years from birth to target date.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date of target
 * @returns Age in years (fractional)
 *
 * @example
 * ```ts
 * const age = calculateAge(birthJD, targetJD);
 * console.log(`Age: ${age.toFixed(2)} years`);
 * ```
 */
declare function calculateAge(birthJD: number, targetJD: number): number;
/**
 * Calculate age in days from birth to target date.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date of target
 * @returns Age in days
 */
declare function calculateAgeInDays(birthJD: number, targetJD: number): number;
/**
 * Calculate Julian Date from birth + age.
 *
 * @param birthJD - Julian Date of birth
 * @param ageInYears - Age in years
 * @returns Target Julian Date
 */
declare function ageToTargetJD(birthJD: number, ageInYears: number): number;
/**
 * Calculate the progressed Julian Date for secondary progressions.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @returns Progressed Julian Date
 *
 * @remarks
 * Secondary progressions: 1 day = 1 year
 * Progressed JD = Birth JD + (age in years × 1 day)
 *
 * @example
 * ```ts
 * // Age 30 = 30 days after birth
 * const progJD = getProgressedJD(birthJD, targetJD);
 * // progJD is birthJD + 30 (if age is 30)
 * ```
 */
declare function getProgressedJD(birthJD: number, targetJD: number, type?: ProgressionType): number;
/**
 * Calculate progressed Julian Date from birth data and target date.
 *
 * @param birth - Birth data
 * @param target - Target date
 * @param type - Progression type
 * @returns Progressed Julian Date
 */
declare function calculateProgressedJD(birth: ProgressionBirthData, target: ProgressionTargetDate, type?: ProgressionType): number;
/**
 * Get complete date information for a progression.
 *
 * @param birth - Birth data
 * @param target - Target date
 * @param type - Progression type
 * @returns Object with natal, target, and progressed dates
 */
declare function getProgressionDates(birth: ProgressionBirthData, target: ProgressionTargetDate, type?: ProgressionType): {
    natalJD: number;
    natalDate: ProgressedDate;
    targetJD: number;
    targetDate: ProgressedDate;
    progressedJD: number;
    progressedDate: ProgressedDate;
    daysFromBirth: number;
    ageInYears: number;
};
/**
 * Validate progression input dates.
 *
 * @param birth - Birth data
 * @param target - Target date
 * @throws ProgressionValidationError if validation fails
 */
declare function validateProgressionDates(birth: ProgressionBirthData, target: ProgressionTargetDate): void;

/**
 * Solar Arc Calculations
 *
 * Calculate solar arc for directions and apply to natal positions.
 *
 * @module progressions/solar-arc
 *
 * @remarks
 * Solar Arc is the arc (in degrees) that the Sun has traveled since birth.
 * All natal points can be "directed" by adding this arc.
 *
 * Formula: Solar Arc = Progressed Sun longitude - Natal Sun longitude
 *
 * @see IMPL.md Section 1.2 and 2.2 for solar arc theory
 * @see Noel Tyl "Solar Arcs" for authoritative reference
 */

/**
 * Calculate the solar arc for a given birth and target date.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to calculate arc for
 * @returns Solar arc in degrees
 *
 * @remarks
 * The solar arc is the difference between the progressed Sun position
 * and the natal Sun position. For secondary progressions, this is
 * approximately 1° per year of life.
 *
 * @example
 * ```ts
 * const birthJD = 2451545.0; // J2000.0
 * const targetJD = birthJD + 30 * 365.25; // 30 years later
 * const arc = calculateSolarArc(birthJD, targetJD);
 * console.log(arc); // ~30° (about 1° per year)
 * ```
 */
declare function calculateSolarArc(birthJD: number, targetJD: number): number;
/**
 * Calculate solar arc from birth data and target date.
 *
 * @param birth - Birth data
 * @param target - Target date
 * @returns Solar arc in degrees
 */
declare function calculateSolarArcFromDates(birth: ProgressionBirthData, target: ProgressionTargetDate): number;
/**
 * Estimate solar arc from age (quick approximation).
 *
 * @param ageInYears - Age in years
 * @returns Estimated solar arc in degrees
 *
 * @remarks
 * This uses the Sun's mean daily motion (~0.9856°/day = ~0.9856°/year in progressions).
 * Actual solar arc varies slightly due to Earth's elliptical orbit.
 */
declare function estimateSolarArc(ageInYears: number): number;
/**
 * Get the natal Sun longitude for reference.
 *
 * @param birthJD - Julian Date of birth
 * @returns Natal Sun longitude in degrees (0-360)
 */
declare function getNatalSunLongitude(birthJD: number): number;
/**
 * Get the progressed Sun longitude.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @returns Progressed Sun longitude in degrees (0-360)
 */
declare function getProgressedSunLongitude(birthJD: number, targetJD: number): number;
/**
 * Apply solar arc to a single natal longitude.
 *
 * @param natalLongitude - Natal longitude in degrees (0-360)
 * @param solarArc - Solar arc in degrees
 * @returns Directed longitude in degrees (0-360)
 *
 * @example
 * ```ts
 * // Natal Mars at 100°, solar arc of 30°
 * const directedMars = applySolarArc(100, 30);
 * console.log(directedMars); // 130°
 * ```
 */
declare function applySolarArc(natalLongitude: number, solarArc: number): number;
/**
 * Apply solar arc to multiple natal positions.
 *
 * @param natalPositions - Array of natal longitudes
 * @param solarArc - Solar arc in degrees
 * @returns Array of directed longitudes
 */
declare function applySolarArcToMany(natalPositions: number[], solarArc: number): number[];
/**
 * Estimate the age at which a specific solar arc will be reached.
 *
 * @param solarArc - Target solar arc in degrees
 * @returns Estimated age in years
 *
 * @example
 * ```ts
 * // When will solar arc be 30°?
 * const age = estimateAgeForSolarArc(30);
 * console.log(age); // ~30.4 years
 * ```
 */
declare function estimateAgeForSolarArc(solarArc: number): number;
/**
 * Find the age at which a natal point will be directed to a specific longitude.
 *
 * @param natalLongitude - Natal longitude
 * @param targetLongitude - Target longitude to direct to
 * @returns Estimated age in years, or null if not reachable
 *
 * @remarks
 * This finds the arc needed and converts to age.
 * Note: If target < natal, the arc will be (360 - (natal - target)).
 */
declare function estimateAgeForDirectedPosition(natalLongitude: number, targetLongitude: number): number;
/**
 * Calculate the solar arc at which a directed planet will aspect a natal point.
 *
 * @param natalDirecting - Natal longitude of body being directed
 * @param natalReceiving - Natal longitude of body receiving the aspect
 * @param aspectAngle - Aspect angle (0, 60, 90, 120, 180)
 * @returns Solar arc at which aspect is exact
 *
 * @example
 * ```ts
 * // When will directed Mars (natal 100°) square natal Sun (natal 50°)?
 * // Square = 90°, so Mars at 140° squares Sun at 50°
 * // Arc needed = 140 - 100 = 40°
 * const arc = solarArcForAspect(100, 50, 90);
 * console.log(arc); // 40°
 * ```
 */
declare function solarArcForAspect(natalDirecting: number, natalReceiving: number, aspectAngle: number): number[];
/**
 * Format solar arc for display.
 *
 * @param solarArc - Solar arc in degrees
 * @param precision - Decimal places (default 2)
 * @returns Formatted string like "30.44°"
 */
declare function formatSolarArc(solarArc: number, precision?: number): string;
/**
 * Format solar arc with DMS (degrees, minutes, seconds).
 *
 * @param solarArc - Solar arc in degrees
 * @returns Formatted string like "30°26'24\""
 */
declare function formatSolarArcDMS(solarArc: number): string;

/**
 * Progressed Angles Calculations
 *
 * Calculate progressed ASC and MC using solar arc or time-based methods.
 *
 * @module progressions/progressed-angles
 *
 * @remarks
 * There are two primary methods for progressing chart angles:
 *
 * 1. **Solar Arc Method** (most common): ASC and MC advance by the same
 *    arc as the progressed Sun. Simple and widely used.
 *
 * 2. **Time-Based Method** (more complex): Calculate the angles for the
 *    exact progressed time, accounting for birth location sidereal time.
 *
 * @see IMPL.md Section 2.3 for angle progression methodology
 */

/**
 * Complete progressed angles result.
 */
interface ProgressedAngles {
    ascendant: ProgressedPosition;
    midheaven: ProgressedPosition;
    descendant: ProgressedPosition;
    imumCoeli: ProgressedPosition;
    solarArc: number;
    method: AngleProgressionMethod;
}
/**
 * Get natal ASC and MC for a birth location.
 *
 * @param birthJD - Julian Date of birth
 * @param latitude - Birth latitude
 * @param longitude - Birth longitude
 * @param _houseSystem - House system (unused, angles are same for all systems)
 * @returns Object with ASC and MC longitudes
 */
declare function getNatalAngles(birthJD: number, latitude: number, longitude: number, _houseSystem?: HouseSystem): {
    ascendant: number;
    midheaven: number;
};
/**
 * Get natal angles from birth data.
 */
declare function getNatalAnglesFromBirth(birth: ProgressionBirthData, houseSystem?: HouseSystem): {
    ascendant: number;
    midheaven: number;
};
/**
 * Calculate progressed angles using solar arc method.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param latitude - Birth latitude
 * @param longitude - Birth longitude
 * @param houseSystem - House system
 * @returns Progressed angles
 *
 * @remarks
 * Solar arc method is the most common approach:
 * - Calculate the solar arc (progressed Sun - natal Sun)
 * - Apply this arc to natal ASC and MC
 */
declare function getProgressedAnglesSolarArc(birthJD: number, targetJD: number, latitude: number, longitude: number, houseSystem?: HouseSystem): ProgressedAngles;
/**
 * Calculate progressed angles using time-based method.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param latitude - Birth latitude
 * @param longitude - Birth longitude
 * @param progressionType - Type of progression for date calculation
 * @param houseSystem - House system
 * @returns Progressed angles
 *
 * @remarks
 * Time-based method calculates angles for the actual progressed moment:
 * - Get the progressed Julian Date
 * - Calculate angles for that date/time at birth location
 * - This gives angles that account for precession and time passage
 *
 * This method is more astronomically accurate but less commonly used
 * in astrological practice.
 */
declare function getProgressedAnglesTimeBased(birthJD: number, targetJD: number, latitude: number, longitude: number, progressionType?: ProgressionType, houseSystem?: HouseSystem): ProgressedAngles;
/**
 * Calculate progressed angles using specified method.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param latitude - Birth latitude
 * @param longitude - Birth longitude
 * @param method - Progression method ('solar-arc' or 'time-based')
 * @param progressionType - Type of progression (for time-based method)
 * @param houseSystem - House system
 * @returns Progressed angles
 */
declare function getProgressedAngles(birthJD: number, targetJD: number, latitude: number, longitude: number, method?: AngleProgressionMethod, progressionType?: ProgressionType, houseSystem?: HouseSystem): ProgressedAngles;
/**
 * Calculate progressed angles from birth data and target date.
 *
 * @param birth - Birth data
 * @param target - Target date
 * @param method - Progression method
 * @param progressionType - Type of progression
 * @param houseSystem - House system
 * @returns Progressed angles
 */
declare function calculateProgressedAngles(birth: ProgressionBirthData, target: ProgressionTargetDate, method?: AngleProgressionMethod, progressionType?: ProgressionType, houseSystem?: HouseSystem): ProgressedAngles;
/**
 * Get just the progressed ASC.
 */
declare function getProgressedASC(birthJD: number, targetJD: number, latitude: number, longitude: number, method?: AngleProgressionMethod): ProgressedPosition;
/**
 * Get just the progressed MC.
 */
declare function getProgressedMC(birthJD: number, targetJD: number, latitude: number, longitude: number, method?: AngleProgressionMethod): ProgressedPosition;
/**
 * Check if ASC has changed sign since birth.
 */
declare function hasASCChangedSign(birthJD: number, targetJD: number, latitude: number, longitude: number, method?: AngleProgressionMethod): boolean;
/**
 * Check if MC has changed sign since birth.
 */
declare function hasMCChangedSign(birthJD: number, targetJD: number, latitude: number, longitude: number, method?: AngleProgressionMethod): boolean;
/**
 * Estimate age at which ASC will reach a specific sign.
 *
 * @param natalASC - Natal ASC longitude
 * @param targetSignIndex - Index of target sign (0=Aries, 1=Taurus, etc.)
 * @returns Estimated age in years, or null if already past
 */
declare function estimateAgeForASCSign(natalASC: number, targetSignIndex: number): number | null;
/**
 * Estimate age at which MC will reach a specific sign.
 *
 * @param natalMC - Natal MC longitude
 * @param targetSignIndex - Index of target sign
 * @returns Estimated age in years
 */
declare function estimateAgeForMCSign(natalMC: number, targetSignIndex: number): number | null;
/**
 * Format progressed angles for display.
 */
declare function formatProgressedAngles(angles: ProgressedAngles): string;

/**
 * Progressed Positions Calculations
 *
 * Calculate progressed positions for planets and points.
 *
 * @module progressions/progressed-positions
 *
 * @remarks
 * This module provides functions to calculate progressed positions
 * for all celestial bodies using different progression types.
 *
 * For secondary progressions, all bodies are calculated for the progressed date.
 * For solar arc progressions, all bodies are directed by the solar arc.
 *
 * @see IMPL.md Section 2 for calculation methodology
 */

/**
 * Body names supported for progressed positions.
 */
type ProgressedBodyName = 'Sun' | 'Moon' | 'Mercury' | 'Venus' | 'Mars' | 'Jupiter' | 'Saturn' | 'Uranus' | 'Neptune' | 'Pluto' | 'Chiron' | 'TrueNode';
/**
 * Internal position result from ephemeris.
 */
interface EphemerisPosition {
    longitude: number;
    latitude?: number;
    distance?: number;
}
/**
 * Get the natal position of a body.
 *
 * @param bodyName - Name of the celestial body
 * @param birthJD - Julian Date of birth
 * @returns Natal position
 */
declare function getNatalPosition(bodyName: ProgressedBodyName, birthJD: number): EphemerisPosition;
/**
 * Get progressed position for a single body using secondary progression.
 *
 * @param bodyName - Name of the celestial body
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param progressionType - Type of progression (default: secondary)
 * @returns Progressed position information matching ProgressedPlanet interface
 */
declare function getProgressedPosition(bodyName: ProgressedBodyName, birthJD: number, targetJD: number, progressionType?: ProgressionType): ProgressedPlanet;
/**
 * Get progressed positions for multiple bodies.
 *
 * @param bodies - Array of body names to calculate
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param progressionType - Type of progression
 * @returns Array of progressed body positions
 */
declare function getProgressedPositions(bodies: ProgressedBodyName[], birthJD: number, targetJD: number, progressionType?: ProgressionType): ProgressedPlanet[];
/**
 * Get progressed positions for all default bodies.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param progressionType - Type of progression
 * @returns Array of progressed body positions
 */
declare function getAllProgressedPositions(birthJD: number, targetJD: number, progressionType?: ProgressionType): ProgressedPlanet[];
/**
 * Calculate progressed positions from birth data and target date.
 *
 * @param birth - Birth data
 * @param target - Target date
 * @param config - Optional configuration
 * @returns Array of progressed body positions
 */
declare function calculateProgressedPositions(birth: ProgressionBirthData, target: ProgressionTargetDate, config?: Partial<ProgressionConfig>): ProgressedPlanet[];
/**
 * Get a specific progressed body position from birth data and target date.
 *
 * @param bodyName - Name of body to calculate
 * @param birth - Birth data
 * @param target - Target date
 * @param progressionType - Type of progression
 * @returns Progressed body position
 */
declare function getProgressedBodyFromDates(bodyName: ProgressedBodyName, birth: ProgressionBirthData, target: ProgressionTargetDate, progressionType?: ProgressionType): ProgressedPlanet;
/**
 * Get bodies that have changed sign since birth.
 *
 * @param positions - Array of progressed positions
 * @returns Array of bodies that changed sign
 */
declare function getBodiesWithSignChange(positions: ProgressedPlanet[]): ProgressedPlanet[];
/**
 * Get bodies currently retrograde (in secondary progressions).
 *
 * @param positions - Array of progressed positions
 * @returns Array of retrograde bodies
 */
declare function getRetrogradeBodies(positions: ProgressedPlanet[]): ProgressedPlanet[];
/**
 * Find the body with the largest arc from natal.
 *
 * @param positions - Array of progressed positions
 * @returns Body with largest arc, or undefined if empty
 */
declare function getBodyWithLargestArc(positions: ProgressedPlanet[]): ProgressedPlanet | undefined;
/**
 * Get positions sorted by progressed longitude.
 *
 * @param positions - Array of progressed positions
 * @returns Sorted array
 */
declare function sortByLongitude(positions: ProgressedPlanet[]): ProgressedPlanet[];
/**
 * Get positions grouped by sign.
 *
 * @param positions - Array of progressed positions
 * @returns Map of sign name to bodies in that sign
 */
declare function groupBySign(positions: ProgressedPlanet[]): Map<string, ProgressedPlanet[]>;

/**
 * Progressed Moon Specializations
 *
 * The progressed Moon is the most active indicator in secondary progressions,
 * moving approximately 12-15° per year (about 1° per month).
 *
 * @module progressions/progressed-moon
 *
 * @remarks
 * The progressed Moon:
 * - Completes a full zodiac cycle in ~27-28 years
 * - Changes sign approximately every 2.5 years
 * - Forms new aspects more frequently than other progressed bodies
 * - Is a key timer for life events and emotional shifts
 *
 * @see IMPL.md Section 3.1 for Moon progression methodology
 */

/**
 * Lunar phase information for progressed Moon.
 */
interface ProgressedLunarPhase {
    /** Current phase name */
    phaseName: string;
    /** Phase angle (0-360°, 0 = New Moon) */
    phaseAngle: number;
    /** Days until next phase change */
    daysUntilNextPhase: number;
    /** Description of current phase meaning */
    description: string;
}
/**
 * Moon sign transit information.
 */
interface MoonSignTransit {
    /** Sign index (0-11) */
    signIndex: number;
    /** Sign name */
    signName: string;
    /** Entry date as years from birth */
    entryAge: number;
    /** Exit date as years from birth */
    exitAge: number;
    /** Duration in years */
    durationYears: number;
}
/**
 * Complete progressed Moon report.
 */
interface ProgressedMoonReport {
    /** Current progressed Moon position */
    current: ProgressedPlanet;
    /** Lunar phase relative to progressed Sun */
    phase: ProgressedLunarPhase;
    /** Timeline of sign transits */
    signTransits: MoonSignTransit[];
    /** Estimated age at next sign change */
    ageAtNextSignChange: number;
    /** Total zodiac cycles completed since birth */
    zodiacCyclesCompleted: number;
}
/**
 * Get progressed Moon position.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param progressionType - Type of progression
 * @returns Progressed Moon position matching ProgressedPlanet interface
 */
declare function getProgressedMoon(birthJD: number, targetJD: number, progressionType?: ProgressionType): ProgressedPlanet;
/**
 * Get progressed Moon from birth and target dates.
 */
declare function getProgressedMoonFromDates(birth: ProgressionBirthData, target: ProgressionTargetDate, progressionType?: ProgressionType): ProgressedPlanet;
/**
 * Calculate progressed lunar phase (Moon's relationship to progressed Sun).
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param progressionType - Type of progression
 * @returns Progressed lunar phase information
 */
declare function getProgressedLunarPhase(birthJD: number, targetJD: number, progressionType?: ProgressionType): ProgressedLunarPhase;
/**
 * Calculate when the progressed Moon enters each sign.
 *
 * @param birthJD - Julian Date of birth
 * @param yearsToCalculate - How many years ahead to calculate (default: 30)
 * @returns Array of Moon sign transits
 */
declare function calculateMoonSignTransits(birthJD: number, yearsToCalculate?: number): MoonSignTransit[];
/**
 * Calculate age at next Moon sign change.
 *
 * @param birthJD - Julian Date of birth
 * @param currentAge - Current age in years
 * @returns Age at next sign change
 */
declare function getAgeAtNextMoonSignChange(birthJD: number, currentAge: number): number;
/**
 * Calculate how many complete zodiac cycles the Moon has completed.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Target Julian Date
 * @returns Number of complete cycles (integer) and fractional progress
 */
declare function getMoonZodiacCycles(birthJD: number, targetJD: number): {
    complete: number;
    fractional: number;
    totalDegrees: number;
};
/**
 * Estimate age at which progressed Moon returns to natal position.
 *
 * @returns Array of ages at Moon returns
 */
declare function getMoonReturnAges(): number[];
/**
 * Generate a complete progressed Moon report.
 *
 * @param birth - Birth data
 * @param target - Target date
 * @param yearsToProject - Years to project for transits (default: 30)
 * @returns Complete progressed Moon report
 */
declare function getProgressedMoonReport(birth: ProgressionBirthData, target: ProgressionTargetDate, yearsToProject?: number): ProgressedMoonReport;
/**
 * Format Moon sign transit for display.
 */
declare function formatMoonTransit(transit: MoonSignTransit): string;
/**
 * Format progressed Moon report for display.
 */
declare function formatProgressedMoonReport(report: ProgressedMoonReport): string;

/**
 * Progressed Aspects Detection
 *
 * Detect aspects between progressed and natal positions.
 *
 * @module progressions/progressed-aspects
 *
 * @remarks
 * Progression aspects come in two primary types:
 * 1. **Progressed-to-Natal**: Progressed planet aspecting natal planet
 * 2. **Progressed-to-Progressed**: Aspect between two progressed bodies
 *
 * Orbs in progressions are typically very tight (0.5-1°) because
 * progressed positions change slowly over time.
 *
 * @see IMPL.md Section 4 for aspect detection methodology
 */

/**
 * Configuration for aspect detection.
 */
interface AspectConfig {
    /** Aspect types to detect */
    aspectTypes?: AspectType[];
    /** Orb overrides by aspect type */
    orbs?: Partial<Record<AspectType, number>>;
    /** Include progressed-to-progressed aspects */
    includeProgressedToProgressed?: boolean;
    /** Minimum orb strength to include (0-100) */
    minimumStrength?: number;
    /** Exact aspect threshold */
    exactThreshold?: number;
}
/**
 * Result of aspect detection.
 */
interface AspectDetectionResult {
    /** All detected aspects */
    aspects: ProgressedAspect[];
    /** Exact or near-exact aspects */
    exactAspects: ProgressedAspect[];
    /** Applying aspects (getting closer) */
    applyingAspects: ProgressedAspect[];
    /** Separating aspects (getting further) */
    separatingAspects: ProgressedAspect[];
    /** Summary statistics */
    summary: {
        total: number;
        exact: number;
        applying: number;
        separating: number;
    };
}
/**
 * Detect aspects between a progressed body and natal positions.
 *
 * @param progressedBody - Progressed body position
 * @param natalPositions - Array of natal positions
 * @param config - Aspect detection configuration
 * @returns Array of detected aspects
 */
declare function detectProgressedToNatalAspects(progressedBody: ProgressedPlanet, natalPositions: Array<{
    name: string;
    longitude: number;
}>, config?: AspectConfig): ProgressedAspect[];
/**
 * Detect aspects between two sets of progressed positions.
 *
 * @param positions - Array of progressed body positions
 * @param config - Aspect detection configuration
 * @returns Array of detected progressed-to-progressed aspects
 */
declare function detectProgressedToProgressedAspects(positions: ProgressedPlanet[], config?: AspectConfig): ProgressedAspect[];
/**
 * Detect all aspects for a progressed chart.
 *
 * @param birthJD - Julian Date of birth
 * @param targetJD - Julian Date to progress to
 * @param progressionType - Type of progression
 * @param config - Aspect detection configuration
 * @returns Complete aspect detection result
 */
declare function detectProgressedAspects(birthJD: number, targetJD: number, progressionType?: ProgressionType, config?: AspectConfig): AspectDetectionResult;
/**
 * Detect aspects from birth data and target date.
 */
declare function calculateProgressedAspects(birth: ProgressionBirthData, target: ProgressionTargetDate, progressionType?: ProgressionType, config?: AspectConfig): AspectDetectionResult;
/**
 * Find all aspects to a specific natal body.
 *
 * @param aspects - Array of aspects
 * @param bodyName - Name of natal body to filter for
 * @returns Aspects involving that natal body
 */
declare function getAspectsToNatalBody(aspects: ProgressedAspect[], bodyName: string): ProgressedAspect[];
/**
 * Find all aspects from a specific progressed body.
 *
 * @param aspects - Array of aspects
 * @param bodyName - Name of progressed body to filter for
 * @returns Aspects from that progressed body
 */
declare function getAspectsFromProgressedBody(aspects: ProgressedAspect[], bodyName: string): ProgressedAspect[];
/**
 * Get the strongest aspect.
 */
declare function getStrongestAspect(aspects: ProgressedAspect[]): ProgressedAspect | undefined;
/**
 * Get aspects by type.
 */
declare function getAspectsByType(aspects: ProgressedAspect[], aspectType: AspectType): ProgressedAspect[];
/**
 * Sort aspects by strength (strongest first).
 */
declare function sortByStrength(aspects: ProgressedAspect[]): ProgressedAspect[];
/**
 * Format aspect for display.
 */
declare function formatAspect(aspect: ProgressedAspect): string;
/**
 * Format all aspects for display.
 */
declare function formatAspects(result: AspectDetectionResult): string;

/**
 * Progression Summary
 *
 * Generate comprehensive summaries of progressed charts.
 *
 * @module progressions/progression-summary
 */

/**
 * Generate a complete progression result matching ProgressedChart interface.
 *
 * @param birth - Birth data
 * @param target - Target date
 * @param config - Optional configuration
 * @returns Complete progression result
 */
declare function calculateProgression(birth: ProgressionBirthData, target: ProgressionTargetDate, config?: Partial<ProgressionConfig>): ProgressedChart;
/**
 * Get a Moon-focused progression report.
 */
declare function getMoonProgressionReport(birth: ProgressionBirthData, target: ProgressionTargetDate): ProgressedMoonReport;
/**
 * Get exact aspects only.
 */
declare function getExactAspects(birth: ProgressionBirthData, target: ProgressionTargetDate, progressionType?: ProgressionType): AspectDetectionResult['exactAspects'];
/**
 * Get bodies that have changed sign.
 */
declare function getSignChanges(birth: ProgressionBirthData, target: ProgressionTargetDate, progressionType?: ProgressionType): ProgressedPlanet[];
/**
 * Format a complete progression result.
 */
declare function formatProgressedChart(result: ProgressedChart): string;

/**
 * Progressions Module
 *
 * Astrological progression calculations including secondary progressions,
 * solar arc directions, and related techniques.
 *
 * @module progressions
 *
 * @example
 * ```ts
 * import { calculateProgression, formatProgressionResult } from 'celestine';
 *
 * const birth = {
 *   year: 1990, month: 6, day: 15, hour: 14, minute: 30, second: 0,
 *   timezone: -5, latitude: 40.7128, longitude: -74.006
 * };
 *
 * const target = { year: 2024, month: 1, day: 1 };
 *
 * const result = calculateProgression(birth, target);
 * console.log(formatProgressionResult(result));
 * ```
 */

type index$3_AngleProgressionMethod = AngleProgressionMethod;
type index$3_AspectConfig = AspectConfig;
type index$3_AspectDetectionResult = AspectDetectionResult;
declare const index$3_DAYS_PER_YEAR: typeof DAYS_PER_YEAR;
declare const index$3_DEFAULT_PROGRESSION_BODIES: typeof DEFAULT_PROGRESSION_BODIES;
declare const index$3_DEFAULT_PROGRESSION_CONFIG: typeof DEFAULT_PROGRESSION_CONFIG;
declare const index$3_EXACT_THRESHOLD: typeof EXACT_THRESHOLD;
declare const index$3_MAJOR_PROGRESSION_ASPECTS: typeof MAJOR_PROGRESSION_ASPECTS;
declare const index$3_MINOR_PROGRESSION_RATE: typeof MINOR_PROGRESSION_RATE;
declare const index$3_MOON_MEAN_DAILY_MOTION: typeof MOON_MEAN_DAILY_MOTION;
type index$3_MoonSignTransit = MoonSignTransit;
declare const index$3_PROGRESSION_ORBS: typeof PROGRESSION_ORBS;
declare const index$3_PROGRESSION_RATES: typeof PROGRESSION_RATES;
type index$3_ProgressedAspect = ProgressedAspect;
type index$3_ProgressedAspectPhase = ProgressedAspectPhase;
type index$3_ProgressedBodyName = ProgressedBodyName;
type index$3_ProgressedChart = ProgressedChart;
type index$3_ProgressedDate = ProgressedDate;
type index$3_ProgressedLunarPhase = ProgressedLunarPhase;
type index$3_ProgressedMoonInfo = ProgressedMoonInfo;
type index$3_ProgressedMoonReport = ProgressedMoonReport;
type index$3_ProgressedPlanet = ProgressedPlanet;
type index$3_ProgressedPosition = ProgressedPosition;
type index$3_ProgressedRetrogradeChange = ProgressedRetrogradeChange;
type index$3_ProgressedSignChange = ProgressedSignChange;
type index$3_ProgressionBirthData = ProgressionBirthData;
type index$3_ProgressionCalculationError = ProgressionCalculationError;
declare const index$3_ProgressionCalculationError: typeof ProgressionCalculationError;
type index$3_ProgressionConfig = ProgressionConfig;
type index$3_ProgressionSummary = ProgressionSummary;
type index$3_ProgressionTargetDate = ProgressionTargetDate;
type index$3_ProgressionType = ProgressionType;
type index$3_ProgressionValidationError = ProgressionValidationError;
declare const index$3_ProgressionValidationError: typeof ProgressionValidationError;
declare const index$3_RETROGRADE_CAPABLE_BODIES: typeof RETROGRADE_CAPABLE_BODIES;
declare const index$3_SECONDARY_PROGRESSION_RATE: typeof SECONDARY_PROGRESSION_RATE;
declare const index$3_SIGN_NAMES: typeof SIGN_NAMES;
declare const index$3_SUN_MEAN_DAILY_MOTION: typeof SUN_MEAN_DAILY_MOTION;
declare const index$3_SYNODIC_MONTH_DAYS: typeof SYNODIC_MONTH_DAYS;
declare const index$3_TERTIARY_PROGRESSION_RATE: typeof TERTIARY_PROGRESSION_RATE;
declare const index$3_TROPICAL_MONTH_DAYS: typeof TROPICAL_MONTH_DAYS;
declare const index$3_ageToTargetJD: typeof ageToTargetJD;
declare const index$3_applySolarArc: typeof applySolarArc;
declare const index$3_applySolarArcToMany: typeof applySolarArcToMany;
declare const index$3_birthToJD: typeof birthToJD;
declare const index$3_calculateAge: typeof calculateAge;
declare const index$3_calculateAgeInDays: typeof calculateAgeInDays;
declare const index$3_calculateMoonSignTransits: typeof calculateMoonSignTransits;
declare const index$3_calculateProgressedAngles: typeof calculateProgressedAngles;
declare const index$3_calculateProgressedAspects: typeof calculateProgressedAspects;
declare const index$3_calculateProgressedJD: typeof calculateProgressedJD;
declare const index$3_calculateProgressedPositions: typeof calculateProgressedPositions;
declare const index$3_calculateProgression: typeof calculateProgression;
declare const index$3_calculateSolarArc: typeof calculateSolarArc;
declare const index$3_calculateSolarArcFromDates: typeof calculateSolarArcFromDates;
declare const index$3_detectProgressedAspects: typeof detectProgressedAspects;
declare const index$3_detectProgressedToNatalAspects: typeof detectProgressedToNatalAspects;
declare const index$3_detectProgressedToProgressedAspects: typeof detectProgressedToProgressedAspects;
declare const index$3_estimateAgeForASCSign: typeof estimateAgeForASCSign;
declare const index$3_estimateAgeForDirectedPosition: typeof estimateAgeForDirectedPosition;
declare const index$3_estimateAgeForMCSign: typeof estimateAgeForMCSign;
declare const index$3_estimateAgeForSolarArc: typeof estimateAgeForSolarArc;
declare const index$3_estimateSolarArc: typeof estimateSolarArc;
declare const index$3_formatAspect: typeof formatAspect;
declare const index$3_formatAspects: typeof formatAspects;
declare const index$3_formatMoonTransit: typeof formatMoonTransit;
declare const index$3_formatProgressedAngles: typeof formatProgressedAngles;
declare const index$3_formatProgressedChart: typeof formatProgressedChart;
declare const index$3_formatProgressedMoonReport: typeof formatProgressedMoonReport;
declare const index$3_formatSolarArc: typeof formatSolarArc;
declare const index$3_formatSolarArcDMS: typeof formatSolarArcDMS;
declare const index$3_getAgeAtNextMoonSignChange: typeof getAgeAtNextMoonSignChange;
declare const index$3_getAllProgressedPositions: typeof getAllProgressedPositions;
declare const index$3_getAspectsByType: typeof getAspectsByType;
declare const index$3_getAspectsFromProgressedBody: typeof getAspectsFromProgressedBody;
declare const index$3_getAspectsToNatalBody: typeof getAspectsToNatalBody;
declare const index$3_getBodiesWithSignChange: typeof getBodiesWithSignChange;
declare const index$3_getBodyWithLargestArc: typeof getBodyWithLargestArc;
declare const index$3_getExactAspects: typeof getExactAspects;
declare const index$3_getMoonProgressionReport: typeof getMoonProgressionReport;
declare const index$3_getMoonReturnAges: typeof getMoonReturnAges;
declare const index$3_getMoonZodiacCycles: typeof getMoonZodiacCycles;
declare const index$3_getNatalAngles: typeof getNatalAngles;
declare const index$3_getNatalAnglesFromBirth: typeof getNatalAnglesFromBirth;
declare const index$3_getNatalPosition: typeof getNatalPosition;
declare const index$3_getNatalSunLongitude: typeof getNatalSunLongitude;
declare const index$3_getProgressedASC: typeof getProgressedASC;
declare const index$3_getProgressedAngles: typeof getProgressedAngles;
declare const index$3_getProgressedAnglesSolarArc: typeof getProgressedAnglesSolarArc;
declare const index$3_getProgressedAnglesTimeBased: typeof getProgressedAnglesTimeBased;
declare const index$3_getProgressedBodyFromDates: typeof getProgressedBodyFromDates;
declare const index$3_getProgressedJD: typeof getProgressedJD;
declare const index$3_getProgressedLunarPhase: typeof getProgressedLunarPhase;
declare const index$3_getProgressedMC: typeof getProgressedMC;
declare const index$3_getProgressedMoon: typeof getProgressedMoon;
declare const index$3_getProgressedMoonFromDates: typeof getProgressedMoonFromDates;
declare const index$3_getProgressedMoonReport: typeof getProgressedMoonReport;
declare const index$3_getProgressedPosition: typeof getProgressedPosition;
declare const index$3_getProgressedPositions: typeof getProgressedPositions;
declare const index$3_getProgressedSunLongitude: typeof getProgressedSunLongitude;
declare const index$3_getProgressionDates: typeof getProgressionDates;
declare const index$3_getRetrogradeBodies: typeof getRetrogradeBodies;
declare const index$3_getSignChanges: typeof getSignChanges;
declare const index$3_getStrongestAspect: typeof getStrongestAspect;
declare const index$3_groupBySign: typeof groupBySign;
declare const index$3_hasASCChangedSign: typeof hasASCChangedSign;
declare const index$3_hasMCChangedSign: typeof hasMCChangedSign;
declare const index$3_solarArcForAspect: typeof solarArcForAspect;
declare const index$3_sortByLongitude: typeof sortByLongitude;
declare const index$3_sortByStrength: typeof sortByStrength;
declare const index$3_targetToJD: typeof targetToJD;
declare const index$3_validateProgressionDates: typeof validateProgressionDates;
declare namespace index$3 {
  export { type index$3_AngleProgressionMethod as AngleProgressionMethod, type index$3_AspectConfig as AspectConfig, type index$3_AspectDetectionResult as AspectDetectionResult, index$3_DAYS_PER_YEAR as DAYS_PER_YEAR, index$3_DEFAULT_PROGRESSION_BODIES as DEFAULT_PROGRESSION_BODIES, index$3_DEFAULT_PROGRESSION_CONFIG as DEFAULT_PROGRESSION_CONFIG, index$3_EXACT_THRESHOLD as EXACT_THRESHOLD, index$3_MAJOR_PROGRESSION_ASPECTS as MAJOR_PROGRESSION_ASPECTS, index$3_MINOR_PROGRESSION_RATE as MINOR_PROGRESSION_RATE, index$3_MOON_MEAN_DAILY_MOTION as MOON_MEAN_DAILY_MOTION, type index$3_MoonSignTransit as MoonSignTransit, index$3_PROGRESSION_ORBS as PROGRESSION_ORBS, index$3_PROGRESSION_RATES as PROGRESSION_RATES, type ProgressedAngles$1 as ProgressedAngles, type index$3_ProgressedAspect as ProgressedAspect, type index$3_ProgressedAspectPhase as ProgressedAspectPhase, type index$3_ProgressedBodyName as ProgressedBodyName, type index$3_ProgressedChart as ProgressedChart, type index$3_ProgressedDate as ProgressedDate, type index$3_ProgressedLunarPhase as ProgressedLunarPhase, type index$3_ProgressedMoonInfo as ProgressedMoonInfo, type index$3_ProgressedMoonReport as ProgressedMoonReport, type index$3_ProgressedPlanet as ProgressedPlanet, type index$3_ProgressedPosition as ProgressedPosition, type index$3_ProgressedRetrogradeChange as ProgressedRetrogradeChange, type index$3_ProgressedSignChange as ProgressedSignChange, type index$3_ProgressionBirthData as ProgressionBirthData, index$3_ProgressionCalculationError as ProgressionCalculationError, type index$3_ProgressionConfig as ProgressionConfig, type index$3_ProgressionSummary as ProgressionSummary, type index$3_ProgressionTargetDate as ProgressionTargetDate, type index$3_ProgressionType as ProgressionType, index$3_ProgressionValidationError as ProgressionValidationError, index$3_RETROGRADE_CAPABLE_BODIES as RETROGRADE_CAPABLE_BODIES, index$3_SECONDARY_PROGRESSION_RATE as SECONDARY_PROGRESSION_RATE, index$3_SIGN_NAMES as SIGN_NAMES, index$3_SUN_MEAN_DAILY_MOTION as SUN_MEAN_DAILY_MOTION, index$3_SYNODIC_MONTH_DAYS as SYNODIC_MONTH_DAYS, index$3_TERTIARY_PROGRESSION_RATE as TERTIARY_PROGRESSION_RATE, index$3_TROPICAL_MONTH_DAYS as TROPICAL_MONTH_DAYS, index$3_ageToTargetJD as ageToTargetJD, index$3_applySolarArc as applySolarArc, index$3_applySolarArcToMany as applySolarArcToMany, index$3_birthToJD as birthToJD, index$3_calculateAge as calculateAge, index$3_calculateAgeInDays as calculateAgeInDays, index$3_calculateMoonSignTransits as calculateMoonSignTransits, index$3_calculateProgressedAngles as calculateProgressedAngles, index$3_calculateProgressedAspects as calculateProgressedAspects, index$3_calculateProgressedJD as calculateProgressedJD, index$3_calculateProgressedPositions as calculateProgressedPositions, index$3_calculateProgression as calculateProgression, index$3_calculateSolarArc as calculateSolarArc, index$3_calculateSolarArcFromDates as calculateSolarArcFromDates, index$3_detectProgressedAspects as detectProgressedAspects, index$3_detectProgressedToNatalAspects as detectProgressedToNatalAspects, index$3_detectProgressedToProgressedAspects as detectProgressedToProgressedAspects, index$3_estimateAgeForASCSign as estimateAgeForASCSign, index$3_estimateAgeForDirectedPosition as estimateAgeForDirectedPosition, index$3_estimateAgeForMCSign as estimateAgeForMCSign, index$3_estimateAgeForSolarArc as estimateAgeForSolarArc, index$3_estimateSolarArc as estimateSolarArc, index$3_formatAspect as formatAspect, index$3_formatAspects as formatAspects, index$3_formatMoonTransit as formatMoonTransit, index$3_formatProgressedAngles as formatProgressedAngles, index$3_formatProgressedChart as formatProgressedChart, index$3_formatProgressedMoonReport as formatProgressedMoonReport, index$3_formatSolarArc as formatSolarArc, index$3_formatSolarArcDMS as formatSolarArcDMS, index$3_getAgeAtNextMoonSignChange as getAgeAtNextMoonSignChange, index$3_getAllProgressedPositions as getAllProgressedPositions, index$3_getAspectsByType as getAspectsByType, index$3_getAspectsFromProgressedBody as getAspectsFromProgressedBody, index$3_getAspectsToNatalBody as getAspectsToNatalBody, index$3_getBodiesWithSignChange as getBodiesWithSignChange, index$3_getBodyWithLargestArc as getBodyWithLargestArc, index$3_getExactAspects as getExactAspects, index$3_getMoonProgressionReport as getMoonProgressionReport, index$3_getMoonReturnAges as getMoonReturnAges, index$3_getMoonZodiacCycles as getMoonZodiacCycles, index$3_getNatalAngles as getNatalAngles, index$3_getNatalAnglesFromBirth as getNatalAnglesFromBirth, index$3_getNatalPosition as getNatalPosition, index$3_getNatalSunLongitude as getNatalSunLongitude, index$3_getProgressedASC as getProgressedASC, index$3_getProgressedAngles as getProgressedAngles, index$3_getProgressedAnglesSolarArc as getProgressedAnglesSolarArc, index$3_getProgressedAnglesTimeBased as getProgressedAnglesTimeBased, index$3_getProgressedBodyFromDates as getProgressedBodyFromDates, index$3_getProgressedJD as getProgressedJD, index$3_getProgressedLunarPhase as getProgressedLunarPhase, index$3_getProgressedMC as getProgressedMC, index$3_getProgressedMoon as getProgressedMoon, index$3_getProgressedMoonFromDates as getProgressedMoonFromDates, index$3_getProgressedMoonReport as getProgressedMoonReport, index$3_getProgressedPosition as getProgressedPosition, index$3_getProgressedPositions as getProgressedPositions, index$3_getProgressedSunLongitude as getProgressedSunLongitude, index$3_getProgressionDates as getProgressionDates, index$3_getRetrogradeBodies as getRetrogradeBodies, index$3_getSignChanges as getSignChanges, index$3_getStrongestAspect as getStrongestAspect, index$3_groupBySign as groupBySign, index$3_hasASCChangedSign as hasASCChangedSign, index$3_hasMCChangedSign as hasMCChangedSign, index$3_solarArcForAspect as solarArcForAspect, index$3_sortByLongitude as sortByLongitude, index$3_sortByStrength as sortByStrength, index$3_targetToJD as targetToJD, index$3_validateProgressionDates as validateProgressionDates };
}

/**
 * Calendar Date Module
 *
 * Converts Julian Date back to Gregorian calendar date (reverse conversion).
 * Uses the inverse Meeus algorithm from "Astronomical Algorithms" Chapter 7.
 *
 * @module time/calendar-date
 */

/**
 * Converts a Julian Date to a Gregorian calendar date and time
 *
 * This is the inverse operation of toJulianDate(). It extracts the calendar
 * date components (year, month, day) and time components (hour, minute, second)
 * from a Julian Date number.
 *
 * The algorithm automatically handles:
 * - Gregorian calendar (dates after Oct 15, 1582)
 * - Julian calendar (dates before Oct 5, 1582)
 * - Fractional days → time components
 * - Negative years (BCE dates)
 *
 * @param jd - Julian Date (can be fractional)
 * @returns CalendarDateTime object with year, month, day, hour, minute, second
 *
 * @example
 * ```typescript
 * // J2000.0 epoch
 * const date = fromJulianDate(2451545.0);
 * // Returns: { year: 2000, month: 1, day: 1, hour: 12, minute: 0, second: 0 }
 *
 * // Unix epoch
 * const unix = fromJulianDate(2440587.5);
 * // Returns: { year: 1970, month: 1, day: 1, hour: 0, minute: 0, second: 0 }
 * ```
 */
declare function fromJulianDate(jd: number): CalendarDateTime;
/**
 * Convenience function that returns CalendarDateTime from JD
 *
 * @param jd - Julian Date
 * @returns CalendarDateTime object
 *
 * @example
 * ```typescript
 * const date = calendarDate(2451545.0);
 * console.log(date); // { year: 2000, month: 1, day: 1, ... }
 * ```
 */
declare function calendarDate(jd: number): CalendarDateTime;

/**
 * Time module constants
 *
 * Fundamental constants for astronomical time calculations.
 * All values are based on IAU (International Astronomical Union) standards
 * and definitions from Jean Meeus "Astronomical Algorithms".
 */
/**
 * J2000.0 epoch: January 1, 2000, 12:00 TT (Terrestrial Time)
 *
 * This is the standard reference epoch for modern astronomical calculations.
 * Most ephemeris formulas and coordinate transformations are calibrated to this epoch.
 *
 * @constant
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 21
 */
declare const J2000_EPOCH = 2451545;
/**
 * Number of days in a Julian century
 *
 * Used to convert Julian Date to Julian Centuries: T = (JD - J2000_EPOCH) / DAYS_PER_CENTURY
 *
 * @constant
 */
declare const DAYS_PER_CENTURY = 36525;
/**
 * Modified Julian Date epoch offset
 *
 * MJD is defined as: MJD = JD - 2400000.5
 * Used in some modern systems to work with smaller numbers.
 * MJD 0 = JD 2400000.5 = November 17, 1858, 00:00 UT
 *
 * @constant
 */
declare const MJD_EPOCH = 2400000.5;
/**
 * Unix epoch in Julian Date
 *
 * January 1, 1970, 00:00:00 UTC in Julian Date representation.
 * Useful for converting between Unix timestamps and Julian Dates.
 *
 * @constant
 */
declare const UNIX_EPOCH_JD = 2440587.5;
/**
 * Number of seconds in a day
 *
 * @constant
 */
declare const SECONDS_PER_DAY = 86400;
/**
 * Number of minutes in a day
 *
 * @constant
 */
declare const MINUTES_PER_DAY = 1440;
/**
 * Number of hours in a day
 *
 * @constant
 */
declare const HOURS_PER_DAY = 24;
/**
 * Minutes per hour
 *
 * @constant
 */
declare const MINUTES_PER_HOUR = 60;
/**
 * Seconds per minute
 *
 * @constant
 */
declare const SECONDS_PER_MINUTE = 60;
/**
 * Seconds per hour
 *
 * @constant
 */
declare const SECONDS_PER_HOUR = 3600;
/**
 * Degrees per hour (for sidereal time conversions)
 *
 * Since a full rotation is 360° and 24 hours: 360° / 24h = 15°/h
 *
 * @constant
 */
declare const DEGREES_PER_HOUR = 15;
/**
 * Degrees in a full circle
 *
 * @constant
 */
declare const DEGREES_PER_CIRCLE = 360;
/**
 * Minutes of arc per degree
 *
 * @constant
 */
declare const ARCMINUTES_PER_DEGREE = 60;
/**
 * Seconds of arc per degree
 *
 * @constant
 */
declare const ARCSECONDS_PER_DEGREE = 3600;
/**
 * Ratio of sidereal day to solar day
 *
 * A sidereal day is the time it takes Earth to rotate 360° relative to the stars.
 * It's about 4 minutes shorter than a solar day (23h 56m 4.09s vs 24h).
 *
 * This ratio is: 1 sidereal day / 1 solar day ≈ 0.99726957
 *
 * @constant
 */
declare const SIDEREAL_DAY_RATIO = 0.99726957;
/**
 * Length of a sidereal day in solar days
 *
 * @constant
 */
declare const SIDEREAL_DAY_IN_DAYS = 0.99726957;
/**
 * Length of a solar day in sidereal days
 *
 * @constant
 */
declare const SOLAR_DAY_IN_SIDEREAL_DAYS = 1.00273791;
/**
 * Gregorian calendar reform date: October 15, 1582 (JD)
 *
 * The Gregorian calendar was adopted on October 15, 1582 (Gregorian),
 * which was October 4, 1582 in the Julian calendar (10 days were skipped).
 * Dates before this should use Julian calendar rules, after this use Gregorian.
 *
 * @constant
 */
declare const GREGORIAN_REFORM_JD = 2299160.5;
/**
 * Number of days in each month (non-leap year)
 *
 * Index 0 is unused, indices 1-12 correspond to January-December.
 *
 * @constant
 */
declare const DAYS_IN_MONTH: number[];
/**
 * Number of days in each month (leap year)
 *
 * Index 0 is unused, indices 1-12 correspond to January-December.
 *
 * @constant
 */
declare const DAYS_IN_MONTH_LEAP: number[];
/**
 * Month names
 *
 * Index 0 is unused, indices 1-12 correspond to January-December.
 *
 * @constant
 */
declare const MONTH_NAMES: string[];

/**
 * Delta T (ΔT) Module
 *
 * Calculates the difference between Terrestrial Time (TT) and Universal Time (UT):
 * ΔT = TT - UT
 *
 * This correction is needed for high-precision ephemeris calculations because
 * Earth's rotation is not uniform (it's slowing down irregularly due to tidal
 * friction and other factors).
 *
 * ## Authoritative Sources
 *
 * All polynomial expressions are from:
 * - **Fred Espenak & Jean Meeus (2006)**: "Five Millennium Canon of Solar Eclipses"
 * - **NASA Eclipse Website**: https://eclipse.gsfc.nasa.gov/SEcat5/deltatpoly.html
 * - **Historical Values**: https://eclipse.gsfc.nasa.gov/SEhelp/deltaT2.html
 * - **Morrison & Stephenson (2004)**: "Historical Values of the Earth's Clock Error"
 *
 * ## Verification
 *
 * Implementation verified against 17 NASA official reference values from -500 CE to 2020 CE.
 * Test accuracy: 16/17 within standard error (94.1%). See delta-t.test.ts for full test suite.
 *
 * The one outlier (year 2010) is at a polynomial boundary with 2.09% error, which is
 * acceptable given the uncertainty in Earth's rotation predictions.
 *
 * ## Important Notes
 *
 * - Time variable 't' is in **years** from epoch for most polynomials
 * - Exception: 2005-2050 uses 't' in **centuries** from 2005 (coefficients are ~100x larger)
 * - Do NOT modify polynomial coefficients - they are empirically derived from observations
 * - Polynomials are segmented by era to maintain accuracy across wide time ranges
 * - For dates after ~2150 CE, values are highly uncertain due to unpredictable Earth rotation
 *
 * @module time/delta-t
 */
/**
 * Calculate ΔT (TT - UT) in seconds for a given date
 *
 * This function uses polynomial approximations that vary by era.
 * The coefficients are empirically derived from historical observations
 * and extrapolated for future dates.
 *
 * Accuracy:
 * - Before 1600: ~100-1000 seconds uncertainty
 * - 1600-1800: ~10-100 seconds
 * - 1800-1900: ~1-10 seconds
 * - 1900-2000: ~0.1-1 second (measured values)
 * - 2000-2050: ~1-10 seconds (predicted)
 * - After 2050: Increasingly uncertain (extrapolated)
 *
 * @param year - Full year (e.g., 2025, -100 for 101 BCE)
 * @param month - Month (1-12, used for interpolation)
 * @returns ΔT in seconds
 *
 * @example
 * ```typescript
 * // Modern era (well-measured)
 * const dt2020 = deltaT(2020, 1); // ~69 seconds
 *
 * // Historical (estimated)
 * const dt1000 = deltaT(1000, 1); // ~1600 seconds
 *
 * // Ancient (extrapolated)
 * const dt100bce = deltaT(-100, 1); // ~17000 seconds
 * ```
 */
declare function deltaT(year: number, month: number): number;
/**
 * Convert TT (Terrestrial Time) to UT (Universal Time)
 *
 * @param ttJD - Julian Date in Terrestrial Time
 * @param year - Year (for ΔT calculation)
 * @param month - Month (for ΔT calculation)
 * @returns Julian Date in Universal Time
 */
declare function ttToUT(ttJD: number, year: number, month: number): number;
/**
 * Convert UT (Universal Time) to TT (Terrestrial Time)
 *
 * @param utJD - Julian Date in Universal Time
 * @param year - Year (for ΔT calculation)
 * @param month - Month (for ΔT calculation)
 * @returns Julian Date in Terrestrial Time
 */
declare function utToTT(utJD: number, year: number, month: number): number;

/**
 * Julian Centuries conversion
 *
 * Convert Julian Date to Julian Centuries from the J2000.0 epoch.
 * Most modern ephemeris formulas use T (Julian Centuries) as their time variable.
 *
 * Formula: T = (JD - 2451545.0) / 36525
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 22
 */
/**
 * Convert Julian Date to Julian Centuries from J2000.0 epoch
 *
 * The J2000.0 epoch is January 1, 2000, 12:00 TT (JD 2451545.0).
 * Most planetary position formulas use T as the time parameter.
 *
 * @param jd - Julian Date
 * @returns Julian Centuries from J2000.0 (can be negative for dates before 2000)
 *
 * @example
 * // J2000.0 epoch
 * toJulianCenturies(2451545.0)  // Returns: 0.0
 *
 * @example
 * // One century after J2000.0
 * toJulianCenturies(2488070.0)  // Returns: 1.0
 *
 * @example
 * // One century before J2000.0
 * toJulianCenturies(2415020.0)  // Returns: -1.0
 */
declare function toJulianCenturies(jd: number): number;
/**
 * Convert Julian Centuries to Julian Date
 *
 * Inverse of toJulianCenturies. Useful for converting T back to JD.
 *
 * @param T - Julian Centuries from J2000.0
 * @returns Julian Date
 *
 * @example
 * // J2000.0 epoch
 * fromJulianCenturies(0.0)  // Returns: 2451545.0
 *
 * @example
 * // One century after J2000.0
 * fromJulianCenturies(1.0)  // Returns: 2488070.0
 */
declare function fromJulianCenturies(T: number): number;

/**
 * Julian Date conversion
 *
 * Convert Gregorian calendar dates to Julian Date (JD), the standard
 * time measure in astronomical calculations.
 *
 * Algorithm from Jean Meeus, "Astronomical Algorithms" (2nd ed.), Chapter 7.
 *
 * Julian Date is a continuous count of days since noon Universal Time on
 * January 1, 4713 BCE (Julian calendar), which is November 24, 4714 BCE
 * in the Gregorian proleptic calendar.
 */

/**
 * Convert a calendar date/time to Julian Date
 *
 * Uses the Meeus algorithm which handles both Gregorian (post-1582) and
 * Julian (pre-1582) calendar dates automatically.
 *
 * The calendar reform occurred on October 15, 1582 (Gregorian), which was
 * October 4, 1582 (Julian). Days October 5-14, 1582 don't exist in the
 * Gregorian calendar.
 *
 * @param date - Calendar date and time (can include timezone)
 * @returns Julian Date (fractional days since JD epoch)
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 7
 *
 * @example
 * // J2000.0 epoch: January 1, 2000, 12:00 UTC
 * toJulianDate({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0
 * })
 * // Returns: 2451545.0
 *
 * @example
 * // With timezone (converted to UTC)
 * toJulianDate({
 *   year: 2000, month: 1, day: 1,
 *   hour: 12, minute: 0, second: 0,
 *   timezone: -5  // EST
 * })
 * // Returns: 2451545.208333... (17:00 UTC)
 */
declare function toJulianDate(date: CalendarDateTime): number;
/**
 * Create a Julian Date from individual components
 *
 * Convenience function for creating JD without constructing a CalendarDateTime object.
 *
 * @param year - Full year
 * @param month - Month (1-12)
 * @param day - Day of month
 * @param hour - Hour (0-23), defaults to 0
 * @param minute - Minute (0-59), defaults to 0
 * @param second - Second (0-59.999...), defaults to 0
 * @returns Julian Date
 *
 * @example
 * julianDate(2000, 1, 1, 12, 0, 0)  // J2000.0 epoch
 * // Returns: 2451545.0
 */
declare function julianDate(year: number, month: number, day: number, hour?: number, minute?: number, second?: number): number;

/**
 * Local Sidereal Time calculations
 *
 * Convert Greenwich Mean Sidereal Time (GMST) to Local Sidereal Time (LST)
 * for a given observer's longitude.
 *
 * LST is needed to calculate the Ascendant and house cusps for a specific location.
 */
/**
 * Calculate Local Sidereal Time from GMST and longitude
 *
 * LST is simply GMST adjusted for the observer's longitude.
 * Longitude is positive East, negative West (standard astronomical convention).
 *
 * Formula: LST = GMST + longitude
 *
 * @param gmst - Greenwich Mean Sidereal Time in degrees (0-360)
 * @param longitude - Observer's longitude in degrees (positive East, negative West)
 * @returns Local Sidereal Time in degrees (0-360)
 *
 * @example
 * // Greenwich (longitude = 0)
 * localSiderealTime(280, 0)  // Returns: 280 (LST = GMST)
 *
 * @example
 * // New York (longitude ≈ -74°)
 * localSiderealTime(280, -74)  // Returns: 206
 *
 * @example
 * // Tokyo (longitude ≈ 139.7°)
 * localSiderealTime(280, 139.7)  // Returns: 59.7
 */
declare function localSiderealTime(gmst: number, longitude: number): number;

/**
 * Sidereal Time calculations
 *
 * Calculate Greenwich Mean Sidereal Time (GMST) from Julian Date.
 * Sidereal time is based on Earth's rotation relative to the stars.
 *
 * A sidereal day is about 23h 56m 4s, roughly 4 minutes shorter than
 * a solar day. This is because Earth orbits the Sun, requiring slightly
 * more than 360° rotation to bring the Sun back to the same position.
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 12
 */
/**
 * Calculate Greenwich Mean Sidereal Time from Julian Date
 *
 * Uses the IAU 1982 formula from Jean Meeus "Astronomical Algorithms".
 * Returns GMST in degrees (0-360).
 *
 * GMST tells you what Right Ascension is currently on the Greenwich meridian.
 * It's needed for calculating the Ascendant and house cusps.
 *
 * @param jd - Julian Date (UT)
 * @returns Greenwich Mean Sidereal Time in degrees (0-360)
 *
 * @see "Astronomical Algorithms" by Jean Meeus, Chapter 12, page 88
 *
 * @example
 * // J2000.0 epoch (January 1, 2000, 12:00 UT)
 * greenwichMeanSiderealTime(2451545.0)
 * // Returns: ~280.46° (18h 42m)
 *
 * @example
 * // Calculate for a specific date
 * const jd = toJulianDate({ year: 2024, month: 6, day: 15, hour: 0, minute: 0, second: 0 });
 * const gmst = greenwichMeanSiderealTime(jd);
 */
declare function greenwichMeanSiderealTime(jd: number): number;
/**
 * Calculate Greenwich Mean Sidereal Time at 0h UT for a given Julian Date
 *
 * This gives the GMST at midnight (0h UT) on the given date, which is
 * useful as a reference point for calculating GMST at other times of day.
 *
 * @param jd - Julian Date
 * @returns GMST at 0h UT in degrees (0-360)
 *
 * @example
 * // GMST at midnight on J2000.0
 * greenwichMeanSiderealTimeAt0h(2451545.0)
 */
declare function greenwichMeanSiderealTimeAt0h(jd: number): number;
/**
 * Calculate the change in GMST per hour
 *
 * Due to Earth's rotation relative to the stars, GMST advances by about
 * 15.04° per hour (slightly more than 15° which would be for solar time).
 *
 * This is useful for interpolating GMST between known values.
 *
 * @returns Rate of change of GMST in degrees per hour
 */
declare function gmstRatePerHour(): number;

/**
 * Time utility functions
 *
 * Helper functions for time-related operations, including angle normalization,
 * unit conversions, and time component transformations.
 */
/**
 * Normalize an angle to the range [0, 360) degrees
 *
 * Reduces any angle to its equivalent value between 0 (inclusive) and 360 (exclusive).
 * Handles both positive and negative input values.
 *
 * @param degrees - Angle in degrees (can be any value)
 * @returns Normalized angle in range [0, 360)
 *
 * @example
 * normalizeAngle(45)    // 45
 * normalizeAngle(365)   // 5
 * normalizeAngle(-30)   // 330
 * normalizeAngle(720)   // 0
 */
declare function normalizeAngle$1(degrees: number): number;
/**
 * Convert degrees to hours
 *
 * Used for sidereal time conversions where 360° = 24 hours.
 *
 * @param degrees - Angle in degrees
 * @returns Equivalent in hours
 *
 * @example
 * degreesToHours(0)    // 0
 * degreesToHours(180)  // 12
 * degreesToHours(360)  // 24
 */
declare function degreesToHours(degrees: number): number;
/**
 * Convert hours to degrees
 *
 * Used for sidereal time conversions where 24 hours = 360°.
 *
 * @param hours - Time in hours
 * @returns Equivalent in degrees
 *
 * @example
 * hoursToDegrees(0)   // 0
 * hoursToDegrees(12)  // 180
 * hoursToDegrees(24)  // 360
 */
declare function hoursToDegrees(hours: number): number;
/**
 * Convert a fractional day to time components
 *
 * Extracts hours, minutes, and seconds from a fractional day value.
 * A full day (1.0) = 24 hours.
 *
 * @param fraction - Fractional day (0.0 to 1.0, where 0.5 = noon)
 * @returns Object with hour (0-23), minute (0-59), and second (0-59.999...)
 *
 * @example
 * fractionOfDayToTime(0.0)    // { hour: 0, minute: 0, second: 0 }
 * fractionOfDayToTime(0.5)    // { hour: 12, minute: 0, second: 0 }
 * fractionOfDayToTime(0.75)   // { hour: 18, minute: 0, second: 0 }
 */
declare function fractionOfDayToTime(fraction: number): {
    hour: number;
    minute: number;
    second: number;
};
/**
 * Convert time components to a fractional day
 *
 * Combines hours, minutes, and seconds into a fractional day value.
 * A full day = 1.0.
 *
 * @param hour - Hour of day (0-23)
 * @param minute - Minute of hour (0-59)
 * @param second - Second of minute (0-59.999...)
 * @returns Fractional day (0.0 to <1.0)
 *
 * @example
 * timeToFractionOfDay(0, 0, 0)     // 0.0
 * timeToFractionOfDay(12, 0, 0)    // 0.5
 * timeToFractionOfDay(18, 0, 0)    // 0.75
 * timeToFractionOfDay(6, 30, 0)    // 0.270833...
 */
declare function timeToFractionOfDay(hour: number, minute: number, second: number): number;
/**
 * Format a Julian Date as a string for debugging/display
 *
 * @param jd - Julian Date value
 * @returns Formatted string
 *
 * @example
 * formatJulianDate(2451545.0)  // "JD 2451545.0"
 */
declare function formatJulianDate(jd: number): string;
/**
 * Format sidereal time as a string
 *
 * Displays sidereal time in both degrees and hours format.
 *
 * @param degrees - Sidereal time in degrees (0-360)
 * @returns Formatted string
 *
 * @example
 * formatSiderealTime(180)  // "180.000000° (12.000000h)"
 */
declare function formatSiderealTime(degrees: number): string;

/**
 * Time validation functions
 *
 * Functions to validate calendar dates, time components, and detect leap years.
 * These are essential for ensuring input data is valid before astronomical calculations.
 */

/**
 * Check if a year is a leap year
 *
 * Leap year rules (Gregorian calendar):
 * - Divisible by 4: leap year
 * - EXCEPT divisible by 100: not a leap year
 * - EXCEPT divisible by 400: leap year
 *
 * Examples: 2000 (leap), 1900 (not leap), 2024 (leap), 2100 (not leap)
 *
 * @param year - Full year number (can be negative for BCE)
 * @returns true if leap year, false otherwise
 *
 * @example
 * isLeapYear(2000)  // true
 * isLeapYear(1900)  // false
 * isLeapYear(2024)  // true
 */
declare function isLeapYear(year: number): boolean;
/**
 * Get the number of days in a given month
 *
 * Takes leap years into account for February.
 *
 * @param year - Full year number
 * @param month - Month (1-12, where 1 = January)
 * @returns Number of days in the month (28-31), or 0 if month is invalid
 *
 * @example
 * daysInMonth(2024, 2)   // 29 (leap year)
 * daysInMonth(2023, 2)   // 28 (not leap year)
 * daysInMonth(2024, 4)   // 30 (April)
 */
declare function daysInMonth(year: number, month: number): number;
/**
 * Validate a calendar date
 *
 * Checks if the year, month, and day form a valid date.
 * Takes leap years into account.
 *
 * Note: Year 0 doesn't exist historically (1 BCE → 1 CE), but we allow it
 * for astronomical calculations (ISO 8601 uses year 0 = 1 BCE).
 *
 * @param year - Full year number
 * @param month - Month (1-12)
 * @param day - Day of month
 * @returns true if valid date, false otherwise
 *
 * @example
 * isValidDate(2024, 2, 29)   // true (leap year)
 * isValidDate(2023, 2, 29)   // false (not leap year)
 * isValidDate(2024, 13, 1)   // false (invalid month)
 * isValidDate(2024, 4, 31)   // false (April has 30 days)
 */
declare function isValidDate(year: number, month: number, day: number): boolean;
/**
 * Validate time components
 *
 * Checks if hour, minute, and second values are within valid ranges.
 * Allows fractional seconds.
 *
 * @param hour - Hour (0-23)
 * @param minute - Minute (0-59)
 * @param second - Second (0-59.999...)
 * @returns true if valid time, false otherwise
 *
 * @example
 * isValidTime(12, 30, 45)      // true
 * isValidTime(23, 59, 59.999)  // true
 * isValidTime(24, 0, 0)        // false (hour must be 0-23)
 * isValidTime(12, 60, 0)       // false (minute must be 0-59)
 */
declare function isValidTime(hour: number, minute: number, second: number): boolean;
/**
 * Validate a complete CalendarDateTime object
 *
 * Performs comprehensive validation on all components of a calendar date/time.
 * Returns detailed error messages for any invalid components.
 *
 * @param date - CalendarDateTime object to validate
 * @returns ValidationResult with valid flag and array of error messages
 *
 * @example
 * validateCalendarDateTime({
 *   year: 2024, month: 2, day: 29,
 *   hour: 12, minute: 30, second: 45
 * })
 * // { valid: true, errors: [] }
 *
 * @example
 * validateCalendarDateTime({
 *   year: 2023, month: 2, day: 29,
 *   hour: 25, minute: 30, second: 45
 * })
 * // {
 * //   valid: false,
 * //   errors: [
 * //     'Invalid day: February 29 doesn't exist in 2023',
 * //     'Hour must be between 0 and 23, got 25'
 * //   ]
 * // }
 */
declare function validateCalendarDateTime(date: CalendarDateTime): ValidationResult;

/**
 * Time Module
 *
 * Astronomical time calculations including Julian Date, Julian Centuries,
 * Sidereal Time conversions, and Delta T corrections.
 *
 * This module provides the foundational time functions needed for all
 * astronomical and astrological calculations.
 *
 * @module time
 */

declare const index$2_ARCMINUTES_PER_DEGREE: typeof ARCMINUTES_PER_DEGREE;
declare const index$2_ARCSECONDS_PER_DEGREE: typeof ARCSECONDS_PER_DEGREE;
type index$2_CalendarDateTime = CalendarDateTime;
declare const index$2_DAYS_IN_MONTH: typeof DAYS_IN_MONTH;
declare const index$2_DAYS_IN_MONTH_LEAP: typeof DAYS_IN_MONTH_LEAP;
declare const index$2_DAYS_PER_CENTURY: typeof DAYS_PER_CENTURY;
declare const index$2_DEGREES_PER_CIRCLE: typeof DEGREES_PER_CIRCLE;
declare const index$2_DEGREES_PER_HOUR: typeof DEGREES_PER_HOUR;
declare const index$2_GREGORIAN_REFORM_JD: typeof GREGORIAN_REFORM_JD;
declare const index$2_HOURS_PER_DAY: typeof HOURS_PER_DAY;
declare const index$2_J2000_EPOCH: typeof J2000_EPOCH;
type index$2_JulianCenturies = JulianCenturies;
type index$2_JulianDate = JulianDate;
declare const index$2_MINUTES_PER_DAY: typeof MINUTES_PER_DAY;
declare const index$2_MINUTES_PER_HOUR: typeof MINUTES_PER_HOUR;
declare const index$2_MJD_EPOCH: typeof MJD_EPOCH;
declare const index$2_MONTH_NAMES: typeof MONTH_NAMES;
declare const index$2_SECONDS_PER_DAY: typeof SECONDS_PER_DAY;
declare const index$2_SECONDS_PER_HOUR: typeof SECONDS_PER_HOUR;
declare const index$2_SECONDS_PER_MINUTE: typeof SECONDS_PER_MINUTE;
declare const index$2_SIDEREAL_DAY_IN_DAYS: typeof SIDEREAL_DAY_IN_DAYS;
declare const index$2_SIDEREAL_DAY_RATIO: typeof SIDEREAL_DAY_RATIO;
declare const index$2_SOLAR_DAY_IN_SIDEREAL_DAYS: typeof SOLAR_DAY_IN_SIDEREAL_DAYS;
type index$2_SiderealTime = SiderealTime;
type index$2_TimeContext = TimeContext;
type index$2_TimeSystem = TimeSystem;
declare const index$2_UNIX_EPOCH_JD: typeof UNIX_EPOCH_JD;
type index$2_ValidationResult = ValidationResult;
declare const index$2_calendarDate: typeof calendarDate;
declare const index$2_daysInMonth: typeof daysInMonth;
declare const index$2_degreesToHours: typeof degreesToHours;
declare const index$2_deltaT: typeof deltaT;
declare const index$2_formatJulianDate: typeof formatJulianDate;
declare const index$2_formatSiderealTime: typeof formatSiderealTime;
declare const index$2_fractionOfDayToTime: typeof fractionOfDayToTime;
declare const index$2_fromJulianCenturies: typeof fromJulianCenturies;
declare const index$2_fromJulianDate: typeof fromJulianDate;
declare const index$2_gmstRatePerHour: typeof gmstRatePerHour;
declare const index$2_greenwichMeanSiderealTime: typeof greenwichMeanSiderealTime;
declare const index$2_greenwichMeanSiderealTimeAt0h: typeof greenwichMeanSiderealTimeAt0h;
declare const index$2_hoursToDegrees: typeof hoursToDegrees;
declare const index$2_isLeapYear: typeof isLeapYear;
declare const index$2_isValidDate: typeof isValidDate;
declare const index$2_isValidTime: typeof isValidTime;
declare const index$2_julianDate: typeof julianDate;
declare const index$2_localSiderealTime: typeof localSiderealTime;
declare const index$2_timeToFractionOfDay: typeof timeToFractionOfDay;
declare const index$2_toJulianCenturies: typeof toJulianCenturies;
declare const index$2_toJulianDate: typeof toJulianDate;
declare const index$2_ttToUT: typeof ttToUT;
declare const index$2_utToTT: typeof utToTT;
declare const index$2_validateCalendarDateTime: typeof validateCalendarDateTime;
declare namespace index$2 {
  export { index$2_ARCMINUTES_PER_DEGREE as ARCMINUTES_PER_DEGREE, index$2_ARCSECONDS_PER_DEGREE as ARCSECONDS_PER_DEGREE, type index$2_CalendarDateTime as CalendarDateTime, index$2_DAYS_IN_MONTH as DAYS_IN_MONTH, index$2_DAYS_IN_MONTH_LEAP as DAYS_IN_MONTH_LEAP, index$2_DAYS_PER_CENTURY as DAYS_PER_CENTURY, index$2_DEGREES_PER_CIRCLE as DEGREES_PER_CIRCLE, index$2_DEGREES_PER_HOUR as DEGREES_PER_HOUR, index$2_GREGORIAN_REFORM_JD as GREGORIAN_REFORM_JD, index$2_HOURS_PER_DAY as HOURS_PER_DAY, index$2_J2000_EPOCH as J2000_EPOCH, type index$2_JulianCenturies as JulianCenturies, type index$2_JulianDate as JulianDate, index$2_MINUTES_PER_DAY as MINUTES_PER_DAY, index$2_MINUTES_PER_HOUR as MINUTES_PER_HOUR, index$2_MJD_EPOCH as MJD_EPOCH, index$2_MONTH_NAMES as MONTH_NAMES, index$2_SECONDS_PER_DAY as SECONDS_PER_DAY, index$2_SECONDS_PER_HOUR as SECONDS_PER_HOUR, index$2_SECONDS_PER_MINUTE as SECONDS_PER_MINUTE, index$2_SIDEREAL_DAY_IN_DAYS as SIDEREAL_DAY_IN_DAYS, index$2_SIDEREAL_DAY_RATIO as SIDEREAL_DAY_RATIO, index$2_SOLAR_DAY_IN_SIDEREAL_DAYS as SOLAR_DAY_IN_SIDEREAL_DAYS, type index$2_SiderealTime as SiderealTime, type index$2_TimeContext as TimeContext, type index$2_TimeSystem as TimeSystem, index$2_UNIX_EPOCH_JD as UNIX_EPOCH_JD, type index$2_ValidationResult as ValidationResult, index$2_calendarDate as calendarDate, index$2_daysInMonth as daysInMonth, index$2_degreesToHours as degreesToHours, index$2_deltaT as deltaT, index$2_formatJulianDate as formatJulianDate, index$2_formatSiderealTime as formatSiderealTime, index$2_fractionOfDayToTime as fractionOfDayToTime, index$2_fromJulianCenturies as fromJulianCenturies, index$2_fromJulianDate as fromJulianDate, index$2_gmstRatePerHour as gmstRatePerHour, index$2_greenwichMeanSiderealTime as greenwichMeanSiderealTime, index$2_greenwichMeanSiderealTimeAt0h as greenwichMeanSiderealTimeAt0h, index$2_hoursToDegrees as hoursToDegrees, index$2_isLeapYear as isLeapYear, index$2_isValidDate as isValidDate, index$2_isValidTime as isValidTime, index$2_julianDate as julianDate, index$2_localSiderealTime as localSiderealTime, normalizeAngle$1 as normalizeAngle, index$2_timeToFractionOfDay as timeToFractionOfDay, index$2_toJulianCenturies as toJulianCenturies, index$2_toJulianDate as toJulianDate, index$2_ttToUT as ttToUT, index$2_utToTT as utToTT, index$2_validateCalendarDateTime as validateCalendarDateTime };
}

/**
 * Transit Module - Type Definitions
 *
 * Core types for astrological transit calculations.
 *
 * @module transits/types
 *
 * @remarks
 * Transits track when current planetary positions form aspects to natal chart positions.
 * All angles are in degrees (0-360 for longitudes, 0-180 for separations).
 *
 * @see IMPL.md for full implementation specification
 * @see KNOWLEDGE.md Section 9 for aspect foundations
 */

/**
 * Type classification for natal points (affects orb calculation).
 */
type NatalPointType = 'planet' | 'luminary' | 'angle' | 'node' | 'lot' | 'asteroid';
/**
 * A natal chart point that can receive transits.
 *
 * @remarks
 * This represents any point in the natal chart - planets, angles, nodes, etc.
 * The type field is used to determine orb extensions.
 */
interface NatalPoint {
    /** Identifier (e.g., "Sun", "Moon", "ASC", "North Node") */
    name: string;
    /** Ecliptic longitude at birth (0-360°) */
    longitude: number;
    /** Point type for orb calculation */
    type: NatalPointType;
    /** Optional: house placement (1-12) */
    house?: number;
    /** Optional: zodiac sign index (0-11, 0=Aries) */
    signIndex?: number;
}
/**
 * A transiting (current position) celestial body.
 *
 * @remarks
 * Contains the current position and motion of a celestial body.
 * The longitudeSpeed is essential for determining transit phase (applying/separating).
 */
interface TransitingBody {
    /** Body identifier */
    name: string;
    /** Celestial body enum value */
    body: CelestialBody;
    /** Current ecliptic longitude (0-360°) */
    longitude: number;
    /** Daily motion in longitude (degrees/day, negative if retrograde) */
    longitudeSpeed: number;
    /** Whether currently in retrograde motion */
    isRetrograde: boolean;
    /** Optional: current zodiac sign index (0-11) */
    signIndex?: number;
}
/**
 * Phase of a transit aspect.
 *
 * @remarks
 * - 'applying': Aspect is forming (separation decreasing toward exact)
 * - 'exact': Aspect is at or very near exact (deviation < threshold)
 * - 'separating': Aspect is waning (separation increasing from exact)
 */
type TransitPhase = 'applying' | 'exact' | 'separating';
/**
 * A detected transit aspect between a transiting body and natal point.
 *
 * @remarks
 * This represents an active transit - a current planetary position forming
 * an aspect to a natal chart position.
 *
 * @example
 * ```typescript
 * const transit: Transit = {
 *   transitingBody: 'Saturn',
 *   natalPoint: 'Sun',
 *   aspectType: AspectType.Square,
 *   symbol: '□',
 *   aspectAngle: 90,
 *   separation: 88.5,
 *   deviation: 1.5,
 *   orb: 2,
 *   phase: 'applying',
 *   strength: 25,
 *   isRetrograde: false,
 * };
 * ```
 */
interface Transit {
    /** Transiting body name */
    transitingBody: string;
    /** Transiting body enum (for lookups) */
    transitingBodyEnum: CelestialBody;
    /** Natal point being aspected */
    natalPoint: string;
    /** Aspect type */
    aspectType: AspectType;
    /** Aspect symbol (e.g., '☌', '□', '△') */
    symbol: string;
    /** Exact angle for this aspect type (0, 60, 90, 120, 180, etc.) */
    aspectAngle: number;
    /** Current angular separation between transiting and natal positions */
    separation: number;
    /** Deviation from exact aspect (always positive) */
    deviation: number;
    /** Orb used for this detection */
    orb: number;
    /** Transit phase (applying, exact, or separating) */
    phase: TransitPhase;
    /** Transit strength as percentage (100 = exact, 0 = at orb edge) */
    strength: number;
    /** Is transiting body currently retrograde? */
    isRetrograde: boolean;
    /** Is this an out-of-sign (dissociate) aspect? */
    isOutOfSign: boolean;
    /** Julian Date when exact (populated if timing calculated) */
    exactJD?: number;
    /** Calendar date when exact (populated if timing calculated) */
    exactDate?: TransitDate;
}
/**
 * Calendar date representation for transit timing.
 */
interface TransitDate {
    year: number;
    month: number;
    day: number;
    hour: number;
    minute: number;
    second?: number;
}
/**
 * Configuration options for transit calculations.
 *
 * @remarks
 * All options are optional - sensible defaults are used when not specified.
 */
interface TransitConfig {
    /**
     * Aspect types to detect.
     * @default Major aspects (conjunction, sextile, square, trine, opposition)
     */
    aspectTypes?: AspectType[];
    /**
     * Custom orbs per aspect type (overrides defaults).
     * Values in degrees.
     */
    orbs?: Partial<Record<AspectType, number>>;
    /**
     * Bodies to use as transiting.
     * @default All planets + Chiron + True Node
     */
    transitingBodies?: CelestialBody[];
    /**
     * Include house ingress events in results.
     * @default false
     */
    includeHouseIngress?: boolean;
    /**
     * Calculate exact transit times (slower but more info).
     * @default false
     */
    calculateExactTimes?: boolean;
    /**
     * Minimum strength (0-100) to include in results.
     * @default 0 (include all within orb)
     */
    minimumStrength?: number;
    /**
     * Include out-of-sign (dissociate) transits.
     * @default true
     */
    includeOutOfSign?: boolean;
    /**
     * Threshold for 'exact' phase determination (degrees).
     * Transits with deviation below this are marked 'exact'.
     * @default 0.1
     */
    exactThreshold?: number;
}
/**
 * Result of calculating transits for a single moment.
 */
interface TransitResult {
    /** Julian Date of calculation */
    julianDate: number;
    /** Calendar date of calculation */
    date: TransitDate;
    /** All active transits */
    transits: Transit[];
    /** Active house ingresses (if enabled) */
    houseIngresses?: HouseIngress[];
    /** Transits grouped by natal point */
    byNatalPoint: Record<string, Transit[]>;
    /** Transits grouped by transiting body */
    byTransitingBody: Record<string, Transit[]>;
    /** Summary statistics */
    summary: TransitSummary;
    /** Configuration used */
    config: Required<TransitConfig>;
}
/**
 * Summary statistics for transit results.
 */
interface TransitSummary {
    /** Total number of active transits */
    total: number;
    /** Count by aspect type */
    byAspect: Partial<Record<AspectType, number>>;
    /** Count of applying vs separating */
    applying: number;
    separating: number;
    exact: number;
    /** Count of retrograde transiting bodies */
    retrograde: number;
    /** Strongest transit (highest strength) */
    strongest?: Transit;
}
/**
 * A house ingress event (planet entering/exiting a house).
 */
interface HouseIngress {
    /** Transiting body name */
    body: string;
    /** Transiting body enum */
    bodyEnum: CelestialBody;
    /** House being entered */
    house: number;
    /** Previous house (body coming from) */
    previousHouse: number;
    /** Direction of movement */
    direction: 'entering' | 'exiting';
    /** Is body retrograde? (affects interpretation) */
    isRetrograde: boolean;
    /** Julian Date of ingress (when calculated) */
    ingressJD?: number;
    /** Calendar date of ingress */
    ingressDate?: TransitDate;
}
/**
 * Complete timing information for a transit.
 *
 * @remarks
 * This captures the full lifecycle of a transit including:
 * - When it enters orb
 * - All exact passes (may be multiple due to retrograde)
 * - When it leaves orb
 */
interface TransitTiming {
    /** The base transit information */
    transit: Transit;
    /** Julian Date when entering orb */
    enterOrbJD: number;
    /** Calendar date when entering orb */
    enterOrbDate: TransitDate;
    /** Julian Date(s) when exact (multiple if retrograde transit) */
    exactJDs: number[];
    /** Calendar dates when exact */
    exactDates: TransitDate[];
    /** Julian Date when leaving orb */
    leaveOrbJD: number;
    /** Calendar date when leaving orb */
    leaveOrbDate: TransitDate;
    /** Total duration in days */
    durationDays: number;
    /** Number of exact passes (1 = direct only, 3 = typical retrograde) */
    exactPasses: number;
    /** Whether transit includes retrograde period */
    hasRetrogradePass: boolean;
}
/**
 * Parameters for searching transits in a date range.
 */
interface TransitSearchParams {
    /** Start Julian Date */
    startJD: number;
    /** End Julian Date */
    endJD: number;
    /** Natal chart positions to check */
    natalPoints: NatalPoint[];
    /** House cusps (needed for house ingress detection) */
    houseCusps?: number[];
    /** Configuration options */
    config?: TransitConfig;
    /**
     * Search step in days.
     * Smaller = more precise but slower.
     * @default 1 for fast planets, 7 for slow planets
     */
    stepDays?: number;
}
/**
 * Result of searching transits in a date range.
 */
interface TransitSearchResult {
    /** Search parameters used */
    params: TransitSearchParams;
    /** All transit timings found, sorted by first exact date */
    transits: TransitTiming[];
    /** House ingresses found (if enabled) */
    houseIngresses?: HouseIngress[];
    /** Transits grouped by month (YYYY-MM key) */
    byMonth: Record<string, TransitTiming[]>;
    /** Summary statistics */
    summary: {
        /** Total transits found */
        totalTransits: number;
        /** Transit count by aspect type */
        byAspect: Partial<Record<AspectType, number>>;
        /** Transit count by transiting body */
        byBody: Record<string, number>;
        /** Date range covered */
        dateRange: {
            start: TransitDate;
            end: TransitDate;
            days: number;
        };
    };
}
/**
 * Information about a planet's retrograde period.
 */
interface RetrogradePeriod {
    /** Body in retrograde */
    body: CelestialBody;
    /** Julian Date when retrograde begins (station retrograde) */
    stationRetroJD: number;
    /** Julian Date when direct motion resumes (station direct) */
    stationDirectJD: number;
    /** Longitude at station retrograde */
    stationRetroLongitude: number;
    /** Longitude at station direct */
    stationDirectLongitude: number;
    /** Duration in days */
    durationDays: number;
}
/**
 * Station point (where planet appears to stop moving).
 */
interface StationPoint {
    /** Body stationing */
    body: CelestialBody;
    /** Type of station */
    type: 'station-retrograde' | 'station-direct';
    /** Julian Date of station */
    jd: number;
    /** Longitude at station */
    longitude: number;
    /** Calendar date */
    date: TransitDate;
}
/**
 * Error thrown when transit calculation fails.
 */
declare class TransitCalculationError extends Error {
    step: string;
    details?: unknown | undefined;
    constructor(message: string, step: string, details?: unknown | undefined);
}
/**
 * Error thrown when transit search parameters are invalid.
 */
declare class TransitSearchError extends Error {
    field: string;
    value: unknown;
    constructor(message: string, field: string, value: unknown);
}

/**
 * Transit Module - Constants
 *
 * Default orbs, body lists, and other constants for transit calculations.
 *
 * @module transits/constants
 *
 * @remarks
 * Transit orbs are typically tighter than natal orbs because:
 * 1. Transits are time-sensitive (exact timing matters)
 * 2. Wider orbs would show too many simultaneous transits
 * 3. Traditional practice uses 1-3° for most transits
 *
 * @see IMPL.md for rationale and sources
 */

/**
 * Default orbs for transit aspects (in degrees).
 *
 * @remarks
 * These are tighter than natal orbs. Major aspects get 2-3°,
 * minor aspects get 1° or less. These can be overridden via config.
 *
 * Source: Traditional astrological practice, consolidated from
 * Robert Hand "Planets in Transit" and other standard references.
 */
declare const DEFAULT_TRANSIT_ORBS: Readonly<Record<AspectType, number>>;
/**
 * Major aspect types for quick filtering.
 */
declare const MAJOR_TRANSIT_ASPECTS: readonly AspectType[];
/**
 * All supported transit aspect types.
 */
declare const ALL_TRANSIT_ASPECTS: readonly AspectType[];
/**
 * Orb extension for luminaries (Sun, Moon) as natal or transiting body.
 * Added to base orb when either body is a luminary.
 */
declare const LUMINARY_ORB_EXTENSION = 1;
/**
 * Orb extension for chart angles (ASC, MC, DSC, IC) as natal point.
 * Angles are considered highly sensitive points.
 */
declare const ANGLE_ORB_EXTENSION = 1;
/**
 * Orb extension for slow outer planets (Saturn through Pluto).
 * Their transits last longer and are given slightly wider orbs.
 */
declare const OUTER_PLANET_ORB_EXTENSION = 0.5;
/**
 * All celestial bodies that can transit.
 *
 * @remarks
 * Excludes calculated points like Part of Fortune which don't "transit"
 * in the traditional sense (they're derived from other positions).
 */
declare const TRANSITING_BODIES: readonly CelestialBody[];
/**
 * Default transiting bodies (most commonly used).
 * Excludes Moon (too fast for most analyses) and True Node.
 */
declare const DEFAULT_TRANSITING_BODIES: readonly CelestialBody[];
/**
 * Fast-moving planets (inner planets + Sun).
 * Their transits are brief and may need smaller time steps.
 */
declare const FAST_PLANETS: readonly CelestialBody[];
/**
 * Slow-moving planets (outer planets + Chiron).
 * Their transits last longer and may have multiple passes due to retrograde.
 */
declare const SLOW_PLANETS: readonly CelestialBody[];
/**
 * Planets that commonly go retrograde (affects transit duration).
 */
declare const RETROGRADE_PLANETS: readonly CelestialBody[];
/**
 * Luminary bodies (for orb extension checks).
 */
declare const LUMINARIES: readonly CelestialBody[];
/**
 * Average daily motion for each body in degrees per day.
 *
 * @remarks
 * Used for:
 * - Estimating search step sizes
 * - Approximating transit duration
 * - Phase determination edge cases
 *
 * Values from Meeus "Astronomical Algorithms" and empirical observation.
 * Negative value for nodes indicates typical retrograde motion.
 */
declare const AVERAGE_DAILY_MOTION: Readonly<Record<CelestialBody, number>>;
/**
 * Typical transit duration in days for a 3° orb conjunction.
 *
 * @remarks
 * These are approximate and vary based on retrograde motion.
 * A retrograde transit can last 3-5x longer than the direct estimate.
 *
 * Formula: duration ≈ (2 × orb) / average_daily_motion
 */
declare const TYPICAL_TRANSIT_DURATION_DAYS: Readonly<Record<CelestialBody, number>>;
/**
 * Default search step in days for fast planets.
 */
declare const FAST_PLANET_SEARCH_STEP = 1;
/**
 * Default search step in days for slow planets.
 * Can use larger steps because transits last longer.
 */
declare const SLOW_PLANET_SEARCH_STEP = 3;
/**
 * Search step for Moon transits (in days).
 * Moon moves ~13°/day so needs sub-daily precision.
 */
declare const MOON_SEARCH_STEP = 0.25;
/**
 * Maximum iterations for binary search when finding exact times.
 */
declare const MAX_BINARY_SEARCH_ITERATIONS = 50;
/**
 * Tolerance for exact time finding (degrees).
 * ~0.36 arcseconds, more than sufficient for astrology.
 */
declare const EXACT_TIME_TOLERANCE = 0.0001;
/**
 * Default threshold for marking a transit as "exact" phase (degrees).
 * Transits with deviation less than this are labeled 'exact'.
 */
declare const DEFAULT_EXACT_THRESHOLD = 0.1;
/**
 * Speed threshold for considering a planet "stationary" (degrees/day).
 * Planets moving slower than this are treated as stationary.
 */
declare const STATIONARY_SPEED_THRESHOLD = 0.001;
/**
 * Display names for celestial bodies.
 */
declare const BODY_NAMES: Readonly<Record<CelestialBody, string>>;
/**
 * Aspect symbols for transit formatting.
 */
declare const ASPECT_SYMBOLS: Readonly<Record<AspectType, string>>;
/**
 * Aspect angles in degrees.
 */
declare const ASPECT_ANGLES: Readonly<Record<AspectType, number>>;
/**
 * Default transit configuration.
 */
declare const DEFAULT_TRANSIT_CONFIG: {
    readonly aspectTypes: readonly AspectType[];
    readonly transitingBodies: readonly CelestialBody[];
    readonly includeHouseIngress: false;
    readonly calculateExactTimes: false;
    readonly minimumStrength: 0;
    readonly includeOutOfSign: true;
    readonly exactThreshold: 0.1;
};

/**
 * Transit Detection Module
 *
 * Core algorithms for detecting transits between transiting planets
 * and natal chart positions.
 *
 * @module transits/transit-detection
 *
 * @remarks
 * A transit is detected when a transiting body's longitude forms
 * an aspect (angular relationship) with a natal point's longitude
 * within the configured orb tolerance.
 *
 * @see IMPL.md Section 5.1 for algorithm details
 */

/**
 * Calculate the shortest angular separation between two longitudes.
 *
 * @param long1 - First longitude (0-360°)
 * @param long2 - Second longitude (0-360°)
 * @returns Separation in degrees (0-180°)
 *
 * @example
 * ```ts
 * angularSeparation(10, 350); // 20° (not 340°)
 * angularSeparation(90, 180); // 90°
 * ```
 */
declare function angularSeparation(long1: number, long2: number): number;
/**
 * Calculate the signed angular difference between two longitudes.
 * Positive if long2 is "ahead" of long1 (in direct motion direction).
 *
 * @param long1 - First longitude (0-360°)
 * @param long2 - Second longitude (0-360°)
 * @returns Signed difference (-180 to +180°)
 */
declare function signedAngularDifference(long1: number, long2: number): number;
/**
 * Normalize an angle to 0-360° range.
 *
 * @param angle - Any angle in degrees
 * @returns Normalized angle (0-360°)
 */
declare function normalizeAngle(angle: number): number;
/**
 * Get the zodiac sign index (0-11) from a longitude.
 *
 * @param longitude - Ecliptic longitude (0-360°)
 * @returns Sign index (0=Aries, 11=Pisces)
 */
declare function getSignIndex(longitude: number): number;
/**
 * Get the effective orb for a transit aspect.
 *
 * @param aspectType - The aspect type
 * @param natalPoint - The natal point being aspected
 * @param transitBodyEnum - The transiting body
 * @param config - Transit configuration (for custom orbs)
 * @returns Effective orb in degrees
 *
 * @remarks
 * The effective orb is the base orb plus any extensions for:
 * - Luminaries (Sun, Moon) as natal or transiting body
 * - Angles (ASC, MC) as natal point
 * - Outer planets (Saturn through Pluto) as transiting body
 */
declare function getEffectiveOrb(aspectType: AspectType, natalPoint: NatalPoint, transitBodyEnum: CelestialBody, config?: TransitConfig): number;
/**
 * Determine the phase of a transit (applying, exact, or separating).
 *
 * @param transitLongitude - Current longitude of transiting body
 * @param natalLongitude - Longitude of natal point
 * @param transitSpeed - Daily motion of transiting body (degrees/day)
 * @param aspectAngle - The exact aspect angle (0, 60, 90, 120, 180)
 * @param deviation - Current deviation from exact aspect
 * @param exactThreshold - Threshold for marking as 'exact'
 * @returns Transit phase
 *
 * @remarks
 * - Applying: The aspect is forming (deviation is decreasing)
 * - Exact: The aspect is at or very near exact
 * - Separating: The aspect is waning (deviation is increasing)
 *
 * The determination depends on:
 * 1. Whether the planet is direct or retrograde
 * 2. Whether the current separation is less than or greater than the aspect angle
 */
declare function getTransitPhase(transitLongitude: number, natalLongitude: number, transitSpeed: number, aspectAngle: number, deviation: number, exactThreshold?: number): TransitPhase;
/**
 * Calculate transit strength as a percentage.
 * 100% = exact, 0% = at orb edge.
 *
 * @param deviation - Deviation from exact aspect in degrees
 * @param orb - Orb size in degrees
 * @returns Strength as percentage (0-100)
 */
declare function calculateTransitStrength(deviation: number, orb: number): number;
/**
 * Check if an aspect is out-of-sign (dissociate).
 *
 * @param transitLongitude - Transiting body longitude
 * @param natalLongitude - Natal point longitude
 * @param aspectAngle - The aspect angle
 * @returns True if the aspect crosses sign boundaries unexpectedly
 *
 * @remarks
 * An out-of-sign aspect occurs when two bodies are within orb of an aspect
 * but their signs don't match the expected relationship.
 * Example: Sun at 29° Aries, Saturn at 1° Capricorn = 88° (within square orb)
 * but Aries-Capricorn is not a square relationship (should be Aries-Cancer).
 */
declare function isOutOfSign(transitLongitude: number, natalLongitude: number, aspectAngle: number): boolean;
/**
 * Detect if a transit aspect exists between a transiting body and natal point.
 *
 * @param natalPoint - The natal point to check
 * @param transitingBody - The transiting body
 * @param config - Transit configuration
 * @returns Detected transit or null if no transit within orb
 *
 * @example
 * ```ts
 * const transit = detectTransit(
 *   { name: 'Sun', longitude: 280.37, type: 'luminary' },
 *   { name: 'Saturn', body: CelestialBody.Saturn, longitude: 282.5, longitudeSpeed: 0.03, isRetrograde: false }
 * );
 * // Returns conjunction transit if Saturn is within orb of natal Sun
 * ```
 */
declare function detectTransit(natalPoint: NatalPoint, transitingBody: TransitingBody, config?: TransitConfig): Transit | null;
/**
 * Find all aspects between a transiting body and natal point.
 * (A body might form multiple aspects, e.g., if orbs overlap)
 *
 * @param natalPoint - The natal point
 * @param transitingBody - The transiting body
 * @param config - Configuration
 * @returns Array of detected transits (usually 0 or 1, rarely 2)
 */
declare function findAllTransits(natalPoint: NatalPoint, transitingBody: TransitingBody, config?: TransitConfig): Transit[];
/**
 * Get current positions of transiting bodies from ephemeris.
 *
 * @param jd - Julian Date
 * @param bodies - Bodies to get positions for
 * @returns Array of transiting body data
 */
declare function getTransitingBodies(jd: number, bodies?: readonly CelestialBody[]): TransitingBody[];
/**
 * Detect all active transits between multiple bodies and natal points.
 *
 * @param natalPoints - Array of natal chart positions
 * @param transitingBodies - Array of current transiting positions
 * @param config - Transit configuration
 * @returns Array of all detected transits
 */
declare function detectAllTransits(natalPoints: NatalPoint[], transitingBodies: TransitingBody[], config?: TransitConfig): Transit[];
/**
 * Group transits by natal point.
 *
 * @param transits - Array of transits
 * @returns Object mapping natal point name to its transits
 */
declare function groupByNatalPoint(transits: Transit[]): Record<string, Transit[]>;
/**
 * Group transits by transiting body.
 *
 * @param transits - Array of transits
 * @returns Object mapping body name to its transits
 */
declare function groupByTransitingBody(transits: Transit[]): Record<string, Transit[]>;
/**
 * Generate summary statistics for transits.
 *
 * @param transits - Array of transits
 * @returns Summary statistics
 */
declare function summarizeTransits(transits: Transit[]): TransitSummary;
/**
 * Convert Julian Date to TransitDate.
 *
 * @param jd - Julian Date
 * @returns Calendar date representation
 */
declare function jdToTransitDate(jd: number): TransitDate;
/**
 * Calculate all transits at a specific moment.
 *
 * @param natalPoints - Natal chart positions
 * @param jd - Julian Date to calculate transits for
 * @param config - Configuration options
 * @returns Complete transit result
 *
 * @example
 * ```ts
 * const natalPoints = [
 *   { name: 'Sun', longitude: 280.37, type: 'luminary' },
 *   { name: 'Moon', longitude: 223.32, type: 'luminary' },
 *   { name: 'ASC', longitude: 101.65, type: 'angle' },
 * ];
 *
 * const result = calculateTransits(natalPoints, 2460665.0);
 *
 * for (const transit of result.transits) {
 *   console.log(`${transit.transitingBody} ${transit.symbol} ${transit.natalPoint}`);
 * }
 * ```
 */
declare function calculateTransits(natalPoints: NatalPoint[], jd: number, config?: TransitConfig): TransitResult;
/**
 * Format a transit for display.
 *
 * @param transit - The transit to format
 * @returns Human-readable string
 *
 * @example
 * ```ts
 * formatTransit(transit); // "Saturn □ Sun (1.5°, 25%, applying)"
 * ```
 */
declare function formatTransit(transit: Transit): string;
/**
 * Get transits to a specific natal point.
 *
 * @param result - Transit result
 * @param natalPointName - Name of natal point
 * @returns Array of transits to that point
 */
declare function getTransitsToPoint(result: TransitResult, natalPointName: string): Transit[];
/**
 * Get transits from a specific transiting body.
 *
 * @param result - Transit result
 * @param bodyName - Name of transiting body
 * @returns Array of transits from that body
 */
declare function getTransitsFromBody(result: TransitResult, bodyName: string): Transit[];
/**
 * Filter transits by aspect type.
 *
 * @param transits - Array of transits
 * @param aspectType - Aspect type to filter for
 * @returns Filtered transits
 */
declare function filterByAspectType(transits: Transit[], aspectType: AspectType): Transit[];
/**
 * Filter transits by phase.
 *
 * @param transits - Array of transits
 * @param phase - Phase to filter for
 * @returns Filtered transits
 */
declare function filterByPhase(transits: Transit[], phase: TransitPhase): Transit[];
/**
 * Get only applying transits.
 *
 * @param transits - Array of transits
 * @returns Only applying transits
 */
declare function getApplyingTransits(transits: Transit[]): Transit[];
/**
 * Get only separating transits.
 *
 * @param transits - Array of transits
 * @returns Only separating transits
 */
declare function getSeparatingTransits(transits: Transit[]): Transit[];
/**
 * Get the strongest transit in a list.
 *
 * @param transits - Array of transits
 * @returns Strongest transit or undefined if empty
 */
declare function getStrongestTransit(transits: Transit[]): Transit | undefined;

/**
 * Transit Timing Module
 *
 * Functions for calculating exact transit times using binary search
 * and determining when transits enter/leave orb.
 *
 * @module transits/transit-timing
 *
 * @remarks
 * This module provides precise timing calculations for transits:
 * - Find the exact moment a transiting body perfects an aspect
 * - Determine when a transit enters/leaves the orb
 * - Handle multiple exact passes due to retrograde motion
 *
 * The core algorithm uses binary search (bisection method) for
 * numerical precision, achieving ~0.36 arcsecond accuracy.
 *
 * @see IMPL.md Section 5.2 for algorithm details
 */

/**
 * Calculate the signed deviation from an exact aspect.
 *
 * @param transitLongitude - Current longitude of transiting body
 * @param natalLongitude - Longitude of natal point
 * @param aspectAngle - Target aspect angle (0, 60, 90, 120, 180)
 * @returns Signed deviation from exact (negative = before exact, positive = after)
 *
 * @remarks
 * This is used by binary search to detect sign changes that indicate
 * the exact aspect crossing. The sign of the deviation tells us which
 * side of exact we are on.
 */
declare function calculateSignedDeviation(transitLongitude: number, natalLongitude: number, aspectAngle: number): number;
/**
 * Check if an aspect crossing exists in a time window.
 *
 * @param body - Transiting body
 * @param natalLongitude - Natal point longitude
 * @param aspectAngle - Target aspect angle
 * @param startJD - Start of search window
 * @param endJD - End of search window
 * @returns True if there's a crossing in this window
 */
declare function hasCrossingInWindow(body: CelestialBody, natalLongitude: number, aspectAngle: number, startJD: number, endJD: number): boolean;
/**
 * Find the exact time when a transit aspect becomes perfect using binary search.
 *
 * @param body - Transiting celestial body
 * @param natalLongitude - Natal point longitude (0-360°)
 * @param aspectAngle - Target aspect angle (0, 60, 90, 120, 180, etc.)
 * @param startJD - Start of search window (Julian Date)
 * @param endJD - End of search window (Julian Date)
 * @param tolerance - Angular tolerance in degrees (default: 0.0001°)
 * @returns Julian Date of exact aspect, or null if no crossing in window
 *
 * @remarks
 * Uses binary search (bisection method) to find the moment when the
 * transit becomes exact within the specified tolerance.
 *
 * Algorithm:
 * 1. Check if there's a zero-crossing in the window
 * 2. Bisect the interval, keeping the half with the crossing
 * 3. Repeat until deviation is within tolerance
 *
 * @example
 * ```ts
 * // Find exact Saturn conjunction to natal Sun
 * const exactJD = findExactTransitTime(
 *   CelestialBody.Saturn,
 *   280.37, // natal Sun longitude
 *   0,      // conjunction = 0°
 *   2460665, // start JD
 *   2460695  // end JD (30 days later)
 * );
 * ```
 */
declare function findExactTransitTime(body: CelestialBody, natalLongitude: number, aspectAngle: number, startJD: number, endJD: number, tolerance?: number): number | null;
/**
 * Find multiple exact times in a window (for retrograde transits).
 *
 * @param body - Transiting body
 * @param natalLongitude - Natal point longitude
 * @param aspectAngle - Target aspect angle
 * @param startJD - Start of search window
 * @param endJD - End of search window
 * @param scanStep - Days between scans (default: based on body speed)
 * @returns Array of Julian Dates for each exact aspect
 *
 * @remarks
 * A slow outer planet can make 3 (or more) exact passes over a natal
 * point due to retrograde motion. This function scans the entire window
 * and finds all crossings.
 */
declare function findAllExactTimes(body: CelestialBody, natalLongitude: number, aspectAngle: number, startJD: number, endJD: number, scanStep?: number): number[];
/**
 * Find when a transit enters the orb.
 *
 * @param body - Transiting body
 * @param natalLongitude - Natal point longitude
 * @param aspectAngle - Target aspect angle
 * @param orb - Orb size in degrees
 * @param searchStartJD - Start of search (before expected entry)
 * @param searchEndJD - End of search
 * @returns Julian Date when entering orb, or null if not found
 *
 * @remarks
 * Searches backward from the exact time (or forward from search start)
 * to find when the deviation first becomes less than the orb.
 */
declare function findOrbEntry(body: CelestialBody, natalLongitude: number, aspectAngle: number, orb: number, searchStartJD: number, searchEndJD: number): number | null;
/**
 * Find when a transit exits the orb.
 *
 * @param body - Transiting body
 * @param natalLongitude - Natal point longitude
 * @param aspectAngle - Target aspect angle
 * @param orb - Orb size in degrees
 * @param searchStartJD - Start of search (after exact)
 * @param searchEndJD - End of search
 * @returns Julian Date when exiting orb, or null if not found
 */
declare function findOrbExit(body: CelestialBody, natalLongitude: number, aspectAngle: number, orb: number, searchStartJD: number, searchEndJD: number): number | null;
/**
 * Calculate transit timing from explicit natal longitude.
 *
 * @param body - Transiting body
 * @param natalLongitude - Natal point longitude
 * @param aspectAngle - Target aspect angle
 * @param orb - Orb to use
 * @param searchStartJD - Search window start
 * @param searchEndJD - Search window end
 * @returns Transit timing or null if not found
 *
 * @example
 * ```ts
 * // Find timing for Saturn square natal Sun
 * const timing = findTransitTiming(
 *   CelestialBody.Saturn,
 *   280.37, // natal Sun
 *   90,     // square
 *   3,      // 3° orb
 *   2460000,
 *   2461000
 * );
 *
 * if (timing) {
 *   console.log(`Enters orb: ${timing.enterOrbDate.year}-${timing.enterOrbDate.month}`);
 *   console.log(`${timing.exactPasses} exact pass(es)`);
 *   console.log(`Duration: ${timing.durationDays.toFixed(1)} days`);
 * }
 * ```
 */
declare function findTransitTiming(body: CelestialBody, natalLongitude: number, aspectAngle: number, orb: number, searchStartJD: number, searchEndJD: number): Omit<TransitTiming, 'transit'> | null;
/**
 * Estimate when the next aspect will occur.
 *
 * @param body - Transiting body
 * @param natalLongitude - Natal point longitude
 * @param aspectAngle - Target aspect angle
 * @param fromJD - Starting Julian Date
 * @returns Estimated Julian Date of next aspect, or null
 *
 * @remarks
 * This provides a rough estimate (within a few days) for when an aspect
 * will occur. Use findExactTransitTime for precision.
 */
declare function estimateNextAspect(body: CelestialBody, natalLongitude: number, aspectAngle: number, fromJD: number): number | null;
/**
 * Get the search step size appropriate for a body.
 *
 * @param body - Celestial body
 * @returns Recommended step in days
 */
declare function getSearchStepForBody(body: CelestialBody): number;
/**
 * Format a transit timing result for display.
 *
 * @param timing - Transit timing result
 * @returns Human-readable summary
 */
declare function formatTransitTiming(timing: TransitTiming | Omit<TransitTiming, 'transit'>): string;

/**
 * House Ingress Module
 *
 * Functions for detecting when transiting planets enter and exit natal houses.
 *
 * @module transits/house-ingress
 *
 * @remarks
 * A house ingress occurs when a transiting planet crosses a house cusp.
 * This module provides:
 *
 * - **Ingress Detection**: Determine if a planet has crossed a cusp
 * - **Next Ingress Finding**: When will the next house change occur
 * - **Ingress Timeline**: All house changes in a date range
 *
 * House ingresses are significant in transit analysis as they show when
 * planetary energies shift from one life area to another.
 *
 * @see IMPL.md Section 5.4 for algorithm details
 */

/**
 * Detect if an ingress occurred between two positions.
 *
 * @param houseCusps - Array of 12 house cusp longitudes
 * @param currentLongitude - Current position of the body
 * @param previousLongitude - Previous position of the body
 * @param body - The celestial body (for naming)
 * @param isRetrograde - Whether the body is in retrograde motion
 * @returns HouseIngress if a cusp was crossed, null otherwise
 *
 * @remarks
 * This compares the house placement at two times to determine if
 * a house boundary was crossed. It handles both direct and retrograde
 * motion, as well as the 12→1 house wraparound.
 *
 * @example
 * ```ts
 * const cusps = [0, 30, 60, 90, 120, 150, 180, 210, 240, 270, 300, 330];
 * const ingress = detectHouseIngress(cusps, 35, 25, CelestialBody.Saturn, false);
 * // Returns: { body: 'Saturn', house: 2, previousHouse: 1, direction: 'entering', isRetrograde: false }
 * ```
 */
declare function detectHouseIngress(houseCusps: number[], currentLongitude: number, previousLongitude: number, body: CelestialBody, isRetrograde: boolean): HouseIngress | null;
/**
 * Check if a house ingress occurs between two Julian Dates.
 *
 * @param body - Celestial body to check
 * @param houseCusps - Array of 12 house cusp longitudes
 * @param startJD - Start of time window
 * @param endJD - End of time window
 * @returns HouseIngress with timing info if found, null otherwise
 */
declare function checkIngressInWindow(body: CelestialBody, houseCusps: number[], startJD: number, endJD: number): HouseIngress | null;
/**
 * Find when a body will next enter a different house.
 *
 * @param body - Celestial body
 * @param houseCusps - Array of 12 house cusp longitudes
 * @param fromJD - Starting Julian Date
 * @param maxSearchDays - Maximum days to search (default: 365)
 * @returns HouseIngress with timing, or null if not found
 *
 * @example
 * ```ts
 * const nextIngress = findNextIngress(
 *   CelestialBody.Saturn,
 *   houseCusps,
 *   currentJD
 * );
 *
 * if (nextIngress) {
 *   console.log(`Saturn enters house ${nextIngress.house} on ${nextIngress.ingressDate?.year}`);
 * }
 * ```
 */
declare function findNextIngress(body: CelestialBody, houseCusps: number[], fromJD: number, maxSearchDays?: number): HouseIngress | null;
/**
 * Find when a body will enter a specific house.
 *
 * @param body - Celestial body
 * @param houseCusps - House cusp longitudes
 * @param targetHouse - House to find entry to (1-12)
 * @param fromJD - Starting Julian Date
 * @param maxSearchDays - Maximum days to search
 * @returns HouseIngress or null if not found
 */
declare function findIngressToHouse(body: CelestialBody, houseCusps: number[], targetHouse: number, fromJD: number, maxSearchDays?: number): HouseIngress | null;
/**
 * Find all house ingresses for a body in a date range.
 *
 * @param body - Celestial body
 * @param houseCusps - House cusp longitudes
 * @param startJD - Start of search range
 * @param endJD - End of search range
 * @returns Array of all ingresses, sorted by date
 *
 * @example
 * ```ts
 * const ingresses = calculateAllIngresses(
 *   CelestialBody.Mars,
 *   houseCusps,
 *   jdStart,
 *   jdEnd
 * );
 *
 * for (const ing of ingresses) {
 *   console.log(`Mars enters house ${ing.house} on ${ing.ingressDate?.year}-${ing.ingressDate?.month}`);
 * }
 * ```
 */
declare function calculateAllIngresses(body: CelestialBody, houseCusps: number[], startJD: number, endJD: number): HouseIngress[];
/**
 * Find all ingresses for multiple bodies.
 *
 * @param bodies - Array of celestial bodies
 * @param houseCusps - House cusp longitudes
 * @param startJD - Start of search range
 * @param endJD - End of search range
 * @returns Array of all ingresses, sorted by date
 */
declare function calculateAllIngressesForBodies(bodies: CelestialBody[], houseCusps: number[], startJD: number, endJD: number): HouseIngress[];
/**
 * Get the current house position for a body.
 *
 * @param body - Celestial body
 * @param houseCusps - House cusp longitudes
 * @param jd - Julian Date
 * @returns House number (1-12)
 */
declare function getBodyHouse(body: CelestialBody, houseCusps: number[], jd: number): number;
/**
 * Get positions and house placements for multiple bodies.
 *
 * @param bodies - Array of celestial bodies
 * @param houseCusps - House cusp longitudes
 * @param jd - Julian Date
 * @returns Map of body name to house number
 */
declare function getBodyHouses(bodies: CelestialBody[], houseCusps: number[], jd: number): Map<string, number>;
/**
 * Format a house ingress for display.
 *
 * @param ingress - The house ingress
 * @returns Human-readable string
 */
declare function formatHouseIngress(ingress: HouseIngress): string;
/**
 * Group ingresses by house.
 *
 * @param ingresses - Array of ingresses
 * @returns Object mapping house number to ingresses
 */
declare function groupIngressesByHouse(ingresses: HouseIngress[]): Record<number, HouseIngress[]>;
/**
 * Group ingresses by body.
 *
 * @param ingresses - Array of ingresses
 * @returns Object mapping body name to ingresses
 */
declare function groupIngressesByBody(ingresses: HouseIngress[]): Record<string, HouseIngress[]>;

/**
 * Retrograde Transit Module
 *
 * Functions for handling retrograde motion in transits, including
 * detecting multiple exact passes and finding station points.
 *
 * @module transits/retrograde-transits
 *
 * @remarks
 * Retrograde motion is crucial for transit analysis because:
 *
 * - A single transit can have 3+ exact passes (direct → retrograde → direct)
 * - The retrograde pass is often the most intense
 * - Station points (where the planet appears to stop) are highly significant
 *
 * This module provides:
 * - **Station Detection**: Find when planets station retrograde/direct
 * - **Multiple Pass Detection**: Track all exact aspects during a transit
 * - **Retrograde Period Classification**: Determine motion phase
 *
 * @see IMPL.md Section 4.6 for specification
 * @see KNOWLEDGE.md for astronomical background on retrograde
 */

/**
 * Motion state of a celestial body.
 */
type MotionState = 'direct' | 'retrograde' | 'stationary-retrograde' | 'stationary-direct';
/**
 * Classify the motion state of a body at a given time.
 *
 * @param body - Celestial body
 * @param jd - Julian Date
 * @returns Motion state
 *
 * @remarks
 * - Direct: Moving forward (positive longitude speed)
 * - Retrograde: Moving backward (negative longitude speed)
 * - Stationary-retrograde: About to turn retrograde (speed ≈ 0, decreasing)
 * - Stationary-direct: About to turn direct (speed ≈ 0, increasing)
 */
declare function classifyMotion(body: CelestialBody, jd: number): MotionState;
/**
 * Check if a body can retrograde.
 *
 * @param body - Celestial body
 * @returns True if body can exhibit retrograde motion
 *
 * @remarks
 * Sun and Moon never retrograde. The lunar nodes are always retrograde
 * (mean) or mostly retrograde (true). All planets Mercury through Pluto
 * retrograde.
 */
declare function canRetrograde(body: CelestialBody): boolean;
/**
 * Check if a body is currently retrograde.
 *
 * @param body - Celestial body
 * @param jd - Julian Date
 * @returns True if retrograde
 */
declare function isRetrograde(body: CelestialBody, jd: number): boolean;
/**
 * Find station points (where planet appears to stop) in a date range.
 *
 * @param body - Celestial body
 * @param startJD - Start of search range
 * @param endJD - End of search range
 * @returns Array of station points
 *
 * @remarks
 * A station occurs when the planet's longitude speed crosses zero.
 * - Station Retrograde: Speed goes from positive to negative
 * - Station Direct: Speed goes from negative to positive
 *
 * @example
 * ```ts
 * const stations = findStationPoints(CelestialBody.Mercury, jdStart, jdEnd);
 * for (const station of stations) {
 *   console.log(`Mercury ${station.type} at ${station.longitude.toFixed(2)}°`);
 * }
 * ```
 */
declare function findStationPoints(body: CelestialBody, startJD: number, endJD: number): StationPoint[];
/**
 * Find the next station for a body.
 *
 * @param body - Celestial body
 * @param fromJD - Starting Julian Date
 * @param maxSearchDays - Maximum days to search
 * @returns Next station point, or null if not found
 */
declare function findNextStation(body: CelestialBody, fromJD: number, maxSearchDays?: number): StationPoint | null;
/**
 * Find all retrograde periods for a body in a date range.
 *
 * @param body - Celestial body
 * @param startJD - Start of search range
 * @param endJD - End of search range
 * @returns Array of retrograde periods
 *
 * @remarks
 * Each retrograde period is defined by:
 * - Station Retrograde: When retrograde begins
 * - Station Direct: When direct motion resumes
 *
 * @example
 * ```ts
 * const periods = findRetrogradePeriods(CelestialBody.Mercury, jdStart, jdEnd);
 * for (const period of periods) {
 *   console.log(`Mercury Rx from ${period.stationRetroJD} to ${period.stationDirectJD}`);
 *   console.log(`Duration: ${period.durationDays.toFixed(1)} days`);
 * }
 * ```
 */
declare function findRetrogradePeriods(body: CelestialBody, startJD: number, endJD: number): RetrogradePeriod[];
/**
 * Check if a body is currently in a retrograde period.
 *
 * @param body - Celestial body
 * @param jd - Julian Date
 * @returns The retrograde period if currently retrograde, null otherwise
 */
declare function getCurrentRetrogradePeriod(body: CelestialBody, jd: number): RetrogradePeriod | null;
/**
 * Information about a single pass of a transit.
 */
interface TransitPass {
    /** Pass number (1 = first, 2 = retrograde, 3 = direct again) */
    passNumber: number;
    /** Julian Date of exact aspect */
    exactJD: number;
    /** Calendar date of exact aspect */
    exactDate: TransitDate;
    /** Longitude of transiting body at exact */
    longitude: number;
    /** Motion state at this pass */
    motionState: MotionState;
    /** Is this the retrograde pass? */
    isRetrogradePass: boolean;
}
/**
 * Find all exact passes of a transit (accounting for retrograde).
 *
 * @param body - Transiting body
 * @param natalLongitude - Natal point longitude
 * @param aspectAngle - Target aspect angle (0, 60, 90, 120, 180)
 * @param startJD - Start of search range
 * @param endJD - End of search range
 * @returns Array of transit passes
 *
 * @remarks
 * A slow outer planet transit often has 3 passes:
 * 1. First pass (direct) - initial contact
 * 2. Second pass (retrograde) - often most intense
 * 3. Third pass (direct) - final release
 *
 * Fast planets typically have only 1 pass.
 *
 * @example
 * ```ts
 * const passes = findTransitPasses(
 *   CelestialBody.Saturn,
 *   natalSunLongitude,
 *   0, // conjunction
 *   jdStart,
 *   jdEnd
 * );
 *
 * console.log(`Saturn conjunct Sun has ${passes.length} exact pass(es)`);
 * for (const pass of passes) {
 *   console.log(`  Pass ${pass.passNumber}: ${pass.motionState} on ${pass.exactDate.year}-${pass.exactDate.month}`);
 * }
 * ```
 */
declare function findTransitPasses(body: CelestialBody, natalLongitude: number, aspectAngle: number, startJD: number, endJD: number): TransitPass[];
/**
 * Count the expected number of passes for a transit.
 *
 * @param body - Transiting body
 * @param transitDurationDays - Estimated transit duration
 * @returns Expected number of exact passes (1, 3, or 5)
 *
 * @remarks
 * Fast planets (Sun, Moon, Mercury, Venus, Mars) typically have 1 pass.
 * Slow planets (Jupiter through Pluto) may have 3 passes if the transit
 * spans a retrograde period. Very slow planets or very long transits
 * can have 5 passes.
 */
declare function estimatePassCount(body: CelestialBody, transitDurationDays: number): number;
/**
 * Get the "shadow" period before/after retrograde.
 *
 * @param period - Retrograde period
 * @returns Object with pre-shadow and post-shadow JDs
 *
 * @remarks
 * The retrograde "shadow" is the zone the planet transits three times:
 * - Pre-shadow: From when planet first reaches the station-direct degree
 * - Post-shadow: Until planet passes the station-retrograde degree
 */
declare function getRetrogradeShadow(period: RetrogradePeriod): {
    preShadowStartJD: number;
    postShadowEndJD: number;
    shadowZoneStart: number;
    shadowZoneEnd: number;
};
/**
 * Format a retrograde period for display.
 *
 * @param period - Retrograde period
 * @returns Human-readable string
 */
declare function formatRetrogradePeriod(period: RetrogradePeriod): string;
/**
 * Format a station point for display.
 *
 * @param station - Station point
 * @returns Human-readable string
 */
declare function formatStationPoint(station: StationPoint): string;
/**
 * Get summary of all retrograde periods for multiple bodies.
 *
 * @param bodies - Array of celestial bodies
 * @param startJD - Start of search range
 * @param endJD - End of search range
 * @returns Object mapping body name to its retrograde periods
 */
declare function getAllRetrogradePeriods(bodies: CelestialBody[], startJD: number, endJD: number): Map<string, RetrogradePeriod[]>;

/**
 * Transit Search Module
 *
 * Search for transits across a date range, combining detection, timing,
 * and retrograde analysis into comprehensive results.
 *
 * @module transits/transit-search
 *
 * @remarks
 * This module provides the high-level API for finding all transits in a period:
 *
 * - **Date Range Search**: Find all transits between two dates
 * - **Body-Specific Search**: Focus on specific transiting bodies
 * - **Monthly Grouping**: Organize results by calendar month
 * - **Summary Statistics**: Get overview of transit activity
 *
 * @see IMPL.md Section 4.4 for specification
 */

/**
 * Search for all transits in a date range.
 *
 * @param params - Search parameters including date range and natal points
 * @returns Complete search results with timing for each transit
 *
 * @remarks
 * This is the main entry point for transit analysis. It:
 * 1. Scans the date range for all aspect formations
 * 2. Calculates exact times for each transit
 * 3. Groups results by month
 * 4. Provides summary statistics
 *
 * @example
 * ```ts
 * const natalPoints: NatalPoint[] = [
 *   { name: 'Sun', longitude: 280.37, type: 'luminary' },
 *   { name: 'Moon', longitude: 223.32, type: 'luminary' },
 *   { name: 'ASC', longitude: 101.65, type: 'angle' },
 * ];
 *
 * const result = searchTransits({
 *   startJD: julianDate(2025, 1, 1, 0, 0),
 *   endJD: julianDate(2025, 12, 31, 23, 59),
 *   natalPoints,
 * });
 *
 * console.log(`Found ${result.summary.totalTransits} transits in 2025`);
 *
 * for (const [month, transits] of Object.entries(result.byMonth)) {
 *   console.log(`${month}: ${transits.length} transits`);
 * }
 * ```
 */
declare function searchTransits(params: TransitSearchParams): TransitSearchResult;
/**
 * Search transits for a specific body.
 *
 * @param body - Transiting body
 * @param natalPoints - Natal chart points
 * @param startJD - Start of search range
 * @param endJD - End of search range
 * @param config - Optional configuration
 * @returns Array of transit timings
 */
declare function searchTransitsForBody(body: CelestialBody, natalPoints: NatalPoint[], startJD: number, endJD: number, config?: TransitConfig): TransitTiming[];
/**
 * Search transits to a specific natal point.
 *
 * @param natalPoint - Single natal point to check
 * @param startJD - Start of search range
 * @param endJD - End of search range
 * @param config - Optional configuration
 * @returns Array of transit timings
 */
declare function searchTransitsToPoint(natalPoint: NatalPoint, startJD: number, endJD: number, config?: TransitConfig): TransitTiming[];
/**
 * Group transits by calendar month.
 *
 * @param transits - Array of transit timings
 * @returns Object mapping "YYYY-MM" to transits starting that month
 */
declare function groupTransitsByMonth(transits: TransitTiming[]): Record<string, TransitTiming[]>;
/**
 * Group transits by transiting body.
 *
 * @param transits - Array of transit timings
 * @returns Object mapping body name to its transits
 */
declare function groupTransitsByBody(transits: TransitTiming[]): Record<string, TransitTiming[]>;
/**
 * Group transits by natal point.
 *
 * @param transits - Array of transit timings
 * @returns Object mapping natal point name to its transits
 */
declare function groupTransitsByNatalPoint(transits: TransitTiming[]): Record<string, TransitTiming[]>;
/**
 * Group transits by aspect type.
 *
 * @param transits - Array of transit timings
 * @returns Object mapping aspect type to its transits
 */
declare function groupTransitsByAspect(transits: TransitTiming[]): Partial<Record<AspectType, TransitTiming[]>>;
/**
 * Generate summary statistics for search results.
 *
 * @param transits - Array of transit timings
 * @param startJD - Search start date
 * @param endJD - Search end date
 * @returns Summary statistics
 */
declare function generateSearchSummary(transits: TransitTiming[], startJD: number, endJD: number): TransitSearchResult['summary'];
/**
 * Filter transits by minimum strength.
 *
 * @param transits - Array of transit timings
 * @param minStrength - Minimum strength (0-100)
 * @returns Filtered transits
 */
declare function filterByStrength(transits: TransitTiming[], minStrength: number): TransitTiming[];
/**
 * Filter to only retrograde transits.
 *
 * @param transits - Array of transit timings
 * @returns Transits that include a retrograde pass
 */
declare function filterRetrogrades(transits: TransitTiming[]): TransitTiming[];
/**
 * Filter to transits with multiple passes.
 *
 * @param transits - Array of transit timings
 * @returns Transits with 2+ exact passes
 */
declare function filterMultiplePasses(transits: TransitTiming[]): TransitTiming[];
/**
 * Filter to long-duration transits.
 *
 * @param transits - Array of transit timings
 * @param minDays - Minimum duration in days
 * @returns Transits lasting at least minDays
 */
declare function filterByDuration(transits: TransitTiming[], minDays: number): TransitTiming[];
/**
 * Filter transits by body type (fast/slow).
 *
 * @param transits - Array of transit timings
 * @param type - 'fast' or 'slow'
 * @returns Filtered transits
 */
declare function filterByBodySpeed(transits: TransitTiming[], type: 'fast' | 'slow'): TransitTiming[];
/**
 * Get the next significant transit after a date.
 *
 * @param transits - Array of transit timings
 * @param afterJD - Julian Date to search after
 * @returns Next transit, or undefined if none
 */
declare function getNextTransit(transits: TransitTiming[], afterJD: number): TransitTiming | undefined;
/**
 * Get transits currently active at a specific date.
 *
 * @param transits - Array of transit timings
 * @param jd - Julian Date to check
 * @returns Transits active (within orb) at that date
 */
declare function getActiveTransits(transits: TransitTiming[], jd: number): TransitTiming[];
/**
 * Find the most significant transits (longest duration).
 *
 * @param transits - Array of transit timings
 * @param limit - Maximum number to return
 * @returns Top transits by duration
 */
declare function getMostSignificantTransits(transits: TransitTiming[], limit?: number): TransitTiming[];
/**
 * Format a transit timing for display.
 *
 * @param timing - Transit timing
 * @returns Human-readable string
 */
declare function formatTransitTimingDetail(timing: TransitTiming): string;
/**
 * Get a quick overview of upcoming transits.
 *
 * @param natalPoints - Natal chart points
 * @param fromJD - Starting Julian Date
 * @param days - Number of days to search
 * @param config - Optional configuration
 * @returns Search results for the period
 */
declare function getUpcomingTransits(natalPoints: NatalPoint[], fromJD: number, days?: number, config?: TransitConfig): TransitSearchResult;
/**
 * Get transits for a specific month.
 *
 * @param natalPoints - Natal chart points
 * @param year - Year
 * @param month - Month (1-12)
 * @param config - Optional configuration
 * @returns Search results for the month
 */
declare function getTransitsForMonth(natalPoints: NatalPoint[], year: number, month: number, config?: TransitConfig): TransitSearchResult;
/**
 * Get transits for a year.
 *
 * @param natalPoints - Natal chart points
 * @param year - Year
 * @param config - Optional configuration
 * @returns Search results for the year
 */
declare function getTransitsForYear(natalPoints: NatalPoint[], year: number, config?: TransitConfig): TransitSearchResult;

/**
 * Transit Reference Data - Historical Events
 *
 * @remarks
 * Reference data for validating transit calculations against known
 * historical events and astronomical phenomena.
 *
 * SOURCES:
 * - Astrodatabank: https://www.astro.com/astro-databank/
 * - Swiss Ephemeris via pyswisseph 2.10.03
 * - JPL Horizons: https://ssd.jpl.nasa.gov/horizons/
 * - NASA Eclipse Website: https://eclipse.gsfc.nasa.gov/
 *
 * DO NOT modify these values without re-sourcing from authoritative data.
 */

/**
 * Reference transit event for validation
 */
interface ReferenceTransitEvent {
    /** Human-readable name */
    name: string;
    /** Description of the event */
    description: string;
    /** Data source */
    source: string;
    /** Natal position data */
    natal: {
        /** Longitude in degrees (0-360) */
        longitude: number;
        /** For reference: sign and degree */
        zodiacPosition: string;
    };
    /** Transit data */
    transit: {
        /** Transiting body */
        body: CelestialBody;
        /** Aspect type */
        aspectType: AspectType;
        /** Expected exact date */
        exactDate: {
            year: number;
            month: number;
            day: number;
        };
        /** Tolerance in days for date matching */
        dateTolerance: number;
    };
    /** Optional: natal chart source (for celebrity events) */
    natalSource?: {
        name: string;
        roddenRating: 'AA' | 'A' | 'B' | 'C' | 'DD' | 'X';
    };
}
/**
 * Reference station point for validation
 */
interface ReferenceStationPoint {
    /** Description */
    name: string;
    /** Data source */
    source: string;
    /** Celestial body */
    body: CelestialBody;
    /** Station type */
    stationType: 'retrograde' | 'direct';
    /** Expected date */
    date: {
        year: number;
        month: number;
        day: number;
    };
    /** Expected longitude */
    longitude: number;
    /** Zodiac position for reference */
    zodiacPosition: string;
    /** Tolerance in days */
    dateTolerance: number;
}
/**
 * Reference retrograde period for validation
 */
interface ReferenceRetrogradePeriod {
    /** Description */
    name: string;
    /** Data source */
    source: string;
    /** Celestial body */
    body: CelestialBody;
    /** Station retrograde date */
    stationRetro: {
        year: number;
        month: number;
        day: number;
    };
    /** Station direct date */
    stationDirect: {
        year: number;
        month: number;
        day: number;
    };
    /** Expected duration in days */
    expectedDuration: number;
    /** Tolerance in days for duration */
    durationTolerance: number;
}
/**
 * Historical transit events for validation.
 *
 * @remarks
 * These events correlate real-world occurrences with transit patterns.
 * The natal data is from verified sources (Rodden Rating AA when available).
 */
declare const HISTORICAL_TRANSIT_EVENTS: ReferenceTransitEvent[];
/**
 * Known station points for validation.
 *
 * @remarks
 * Mercury stations are frequent and well-documented, making them
 * excellent test cases. Each Mercury retrograde cycle has 2 stations.
 */
declare const REFERENCE_STATION_POINTS: ReferenceStationPoint[];
/**
 * Known retrograde periods for validation.
 *
 * @remarks
 * These periods can validate our retrograde detection algorithm by
 * checking that found periods match expected durations and dates.
 */
declare const REFERENCE_RETROGRADE_PERIODS: ReferenceRetrogradePeriod[];
/**
 * Typical retrograde durations for each planet.
 *
 * @remarks
 * These are average values from astronomical data.
 * Useful for sanity-checking calculated durations.
 */
declare const TYPICAL_RETROGRADE_DURATIONS: Record<CelestialBody, {
    avgDays: number;
    minDays: number;
    maxDays: number;
} | null>;
/**
 * Astronomical events with precise dates for timing validation.
 *
 * @remarks
 * These events have been observed and recorded with high precision.
 * Useful for validating exact time finding algorithms.
 */
declare const ASTRONOMICAL_EVENTS: {
    /**
     * Great Conjunction 2020
     * Jupiter-Saturn conjunction on December 21, 2020
     * Separation: 0°06' (closest since 1623)
     */
    greatConjunction2020: {
        date: {
            year: number;
            month: number;
            day: number;
            hour: number;
            minute: number;
        };
        jupiterLongitude: number;
        saturnLongitude: number;
        separation: number;
        source: string;
    };
    /**
     * Pluto enters Aquarius
     * First ingress: January 20-21, 2024
     */
    plutoEntersAquarius: {
        date: {
            year: number;
            month: number;
            day: number;
        };
        longitude: number;
        source: string;
    };
    /**
     * Spring Equinox 2024
     * Sun at 0° Aries
     */
    springEquinox2024: {
        date: {
            year: number;
            month: number;
            day: number;
        };
        sunLongitude: number;
        source: string;
    };
};

/**
 * Transits Module
 *
 * Calculate astrological transits - when current planetary positions
 * form aspects to natal chart positions.
 *
 * @module transits
 *
 * @remarks
 * Transits are the foundation of predictive astrology. This module provides:
 *
 * - **Transit Detection**: Find active transits at any moment
 * - **Phase Determination**: Applying, exact, or separating
 * - **Strength Calculation**: Linear decay from exact to orb edge
 * - **Orb Handling**: Configurable with extensions for luminaries/angles
 *
 * @example
 * ```typescript
 * import { calculateTransits, type NatalPoint } from 'celestine/transits';
 *
 * const natalPoints: NatalPoint[] = [
 *   { name: 'Sun', longitude: 280.37, type: 'luminary' },
 *   { name: 'Moon', longitude: 223.32, type: 'luminary' },
 *   { name: 'ASC', longitude: 101.65, type: 'angle' },
 * ];
 *
 * // Calculate transits for today
 * const jd = 2460665.0; // December 2025
 * const result = calculateTransits(natalPoints, jd);
 *
 * for (const transit of result.transits) {
 *   console.log(`${transit.transitingBody} ${transit.symbol} ${transit.natalPoint}`);
 *   console.log(`  Strength: ${transit.strength.toFixed(0)}%, ${transit.phase}`);
 * }
 * ```
 *
 * @see IMPL.md for detailed implementation specification
 */

declare const index$1_ALL_TRANSIT_ASPECTS: typeof ALL_TRANSIT_ASPECTS;
declare const index$1_ANGLE_ORB_EXTENSION: typeof ANGLE_ORB_EXTENSION;
declare const index$1_ASPECT_ANGLES: typeof ASPECT_ANGLES;
declare const index$1_ASPECT_SYMBOLS: typeof ASPECT_SYMBOLS;
declare const index$1_ASTRONOMICAL_EVENTS: typeof ASTRONOMICAL_EVENTS;
declare const index$1_AVERAGE_DAILY_MOTION: typeof AVERAGE_DAILY_MOTION;
declare const index$1_BODY_NAMES: typeof BODY_NAMES;
declare const index$1_DEFAULT_EXACT_THRESHOLD: typeof DEFAULT_EXACT_THRESHOLD;
declare const index$1_DEFAULT_TRANSITING_BODIES: typeof DEFAULT_TRANSITING_BODIES;
declare const index$1_DEFAULT_TRANSIT_CONFIG: typeof DEFAULT_TRANSIT_CONFIG;
declare const index$1_DEFAULT_TRANSIT_ORBS: typeof DEFAULT_TRANSIT_ORBS;
declare const index$1_EXACT_TIME_TOLERANCE: typeof EXACT_TIME_TOLERANCE;
declare const index$1_FAST_PLANETS: typeof FAST_PLANETS;
declare const index$1_FAST_PLANET_SEARCH_STEP: typeof FAST_PLANET_SEARCH_STEP;
declare const index$1_HISTORICAL_TRANSIT_EVENTS: typeof HISTORICAL_TRANSIT_EVENTS;
type index$1_HouseIngress = HouseIngress;
declare const index$1_LUMINARIES: typeof LUMINARIES;
declare const index$1_LUMINARY_ORB_EXTENSION: typeof LUMINARY_ORB_EXTENSION;
declare const index$1_MAJOR_TRANSIT_ASPECTS: typeof MAJOR_TRANSIT_ASPECTS;
declare const index$1_MAX_BINARY_SEARCH_ITERATIONS: typeof MAX_BINARY_SEARCH_ITERATIONS;
declare const index$1_MOON_SEARCH_STEP: typeof MOON_SEARCH_STEP;
type index$1_MotionState = MotionState;
type index$1_NatalPoint = NatalPoint;
type index$1_NatalPointType = NatalPointType;
declare const index$1_OUTER_PLANET_ORB_EXTENSION: typeof OUTER_PLANET_ORB_EXTENSION;
declare const index$1_REFERENCE_RETROGRADE_PERIODS: typeof REFERENCE_RETROGRADE_PERIODS;
declare const index$1_REFERENCE_STATION_POINTS: typeof REFERENCE_STATION_POINTS;
declare const index$1_RETROGRADE_PLANETS: typeof RETROGRADE_PLANETS;
type index$1_ReferenceRetrogradePeriod = ReferenceRetrogradePeriod;
type index$1_ReferenceStationPoint = ReferenceStationPoint;
type index$1_ReferenceTransitEvent = ReferenceTransitEvent;
type index$1_RetrogradePeriod = RetrogradePeriod;
declare const index$1_SLOW_PLANETS: typeof SLOW_PLANETS;
declare const index$1_SLOW_PLANET_SEARCH_STEP: typeof SLOW_PLANET_SEARCH_STEP;
declare const index$1_STATIONARY_SPEED_THRESHOLD: typeof STATIONARY_SPEED_THRESHOLD;
type index$1_StationPoint = StationPoint;
declare const index$1_TRANSITING_BODIES: typeof TRANSITING_BODIES;
declare const index$1_TYPICAL_RETROGRADE_DURATIONS: typeof TYPICAL_RETROGRADE_DURATIONS;
declare const index$1_TYPICAL_TRANSIT_DURATION_DAYS: typeof TYPICAL_TRANSIT_DURATION_DAYS;
type index$1_Transit = Transit;
type index$1_TransitCalculationError = TransitCalculationError;
declare const index$1_TransitCalculationError: typeof TransitCalculationError;
type index$1_TransitConfig = TransitConfig;
type index$1_TransitDate = TransitDate;
type index$1_TransitPass = TransitPass;
type index$1_TransitPhase = TransitPhase;
type index$1_TransitResult = TransitResult;
type index$1_TransitSearchError = TransitSearchError;
declare const index$1_TransitSearchError: typeof TransitSearchError;
type index$1_TransitSearchParams = TransitSearchParams;
type index$1_TransitSearchResult = TransitSearchResult;
type index$1_TransitSummary = TransitSummary;
type index$1_TransitTiming = TransitTiming;
type index$1_TransitingBody = TransitingBody;
declare const index$1_angularSeparation: typeof angularSeparation;
declare const index$1_calculateAllIngresses: typeof calculateAllIngresses;
declare const index$1_calculateAllIngressesForBodies: typeof calculateAllIngressesForBodies;
declare const index$1_calculateSignedDeviation: typeof calculateSignedDeviation;
declare const index$1_calculateTransitStrength: typeof calculateTransitStrength;
declare const index$1_calculateTransits: typeof calculateTransits;
declare const index$1_canRetrograde: typeof canRetrograde;
declare const index$1_checkIngressInWindow: typeof checkIngressInWindow;
declare const index$1_classifyMotion: typeof classifyMotion;
declare const index$1_detectAllTransits: typeof detectAllTransits;
declare const index$1_detectHouseIngress: typeof detectHouseIngress;
declare const index$1_detectTransit: typeof detectTransit;
declare const index$1_estimateNextAspect: typeof estimateNextAspect;
declare const index$1_estimatePassCount: typeof estimatePassCount;
declare const index$1_filterByAspectType: typeof filterByAspectType;
declare const index$1_filterByBodySpeed: typeof filterByBodySpeed;
declare const index$1_filterByDuration: typeof filterByDuration;
declare const index$1_filterByPhase: typeof filterByPhase;
declare const index$1_filterByStrength: typeof filterByStrength;
declare const index$1_filterMultiplePasses: typeof filterMultiplePasses;
declare const index$1_filterRetrogrades: typeof filterRetrogrades;
declare const index$1_findAllExactTimes: typeof findAllExactTimes;
declare const index$1_findAllTransits: typeof findAllTransits;
declare const index$1_findExactTransitTime: typeof findExactTransitTime;
declare const index$1_findIngressToHouse: typeof findIngressToHouse;
declare const index$1_findNextIngress: typeof findNextIngress;
declare const index$1_findNextStation: typeof findNextStation;
declare const index$1_findOrbEntry: typeof findOrbEntry;
declare const index$1_findOrbExit: typeof findOrbExit;
declare const index$1_findRetrogradePeriods: typeof findRetrogradePeriods;
declare const index$1_findStationPoints: typeof findStationPoints;
declare const index$1_findTransitPasses: typeof findTransitPasses;
declare const index$1_findTransitTiming: typeof findTransitTiming;
declare const index$1_formatHouseIngress: typeof formatHouseIngress;
declare const index$1_formatRetrogradePeriod: typeof formatRetrogradePeriod;
declare const index$1_formatStationPoint: typeof formatStationPoint;
declare const index$1_formatTransit: typeof formatTransit;
declare const index$1_formatTransitTiming: typeof formatTransitTiming;
declare const index$1_formatTransitTimingDetail: typeof formatTransitTimingDetail;
declare const index$1_generateSearchSummary: typeof generateSearchSummary;
declare const index$1_getActiveTransits: typeof getActiveTransits;
declare const index$1_getAllRetrogradePeriods: typeof getAllRetrogradePeriods;
declare const index$1_getApplyingTransits: typeof getApplyingTransits;
declare const index$1_getBodyHouse: typeof getBodyHouse;
declare const index$1_getBodyHouses: typeof getBodyHouses;
declare const index$1_getCurrentRetrogradePeriod: typeof getCurrentRetrogradePeriod;
declare const index$1_getEffectiveOrb: typeof getEffectiveOrb;
declare const index$1_getMostSignificantTransits: typeof getMostSignificantTransits;
declare const index$1_getNextTransit: typeof getNextTransit;
declare const index$1_getRetrogradeShadow: typeof getRetrogradeShadow;
declare const index$1_getSearchStepForBody: typeof getSearchStepForBody;
declare const index$1_getSeparatingTransits: typeof getSeparatingTransits;
declare const index$1_getSignIndex: typeof getSignIndex;
declare const index$1_getStrongestTransit: typeof getStrongestTransit;
declare const index$1_getTransitPhase: typeof getTransitPhase;
declare const index$1_getTransitingBodies: typeof getTransitingBodies;
declare const index$1_getTransitsForMonth: typeof getTransitsForMonth;
declare const index$1_getTransitsForYear: typeof getTransitsForYear;
declare const index$1_getTransitsFromBody: typeof getTransitsFromBody;
declare const index$1_getTransitsToPoint: typeof getTransitsToPoint;
declare const index$1_getUpcomingTransits: typeof getUpcomingTransits;
declare const index$1_groupByNatalPoint: typeof groupByNatalPoint;
declare const index$1_groupByTransitingBody: typeof groupByTransitingBody;
declare const index$1_groupIngressesByBody: typeof groupIngressesByBody;
declare const index$1_groupIngressesByHouse: typeof groupIngressesByHouse;
declare const index$1_groupTransitsByAspect: typeof groupTransitsByAspect;
declare const index$1_groupTransitsByBody: typeof groupTransitsByBody;
declare const index$1_groupTransitsByMonth: typeof groupTransitsByMonth;
declare const index$1_groupTransitsByNatalPoint: typeof groupTransitsByNatalPoint;
declare const index$1_hasCrossingInWindow: typeof hasCrossingInWindow;
declare const index$1_isOutOfSign: typeof isOutOfSign;
declare const index$1_isRetrograde: typeof isRetrograde;
declare const index$1_jdToTransitDate: typeof jdToTransitDate;
declare const index$1_normalizeAngle: typeof normalizeAngle;
declare const index$1_searchTransits: typeof searchTransits;
declare const index$1_searchTransitsForBody: typeof searchTransitsForBody;
declare const index$1_searchTransitsToPoint: typeof searchTransitsToPoint;
declare const index$1_signedAngularDifference: typeof signedAngularDifference;
declare const index$1_summarizeTransits: typeof summarizeTransits;
declare namespace index$1 {
  export { index$1_ALL_TRANSIT_ASPECTS as ALL_TRANSIT_ASPECTS, index$1_ANGLE_ORB_EXTENSION as ANGLE_ORB_EXTENSION, index$1_ASPECT_ANGLES as ASPECT_ANGLES, index$1_ASPECT_SYMBOLS as ASPECT_SYMBOLS, index$1_ASTRONOMICAL_EVENTS as ASTRONOMICAL_EVENTS, index$1_AVERAGE_DAILY_MOTION as AVERAGE_DAILY_MOTION, index$1_BODY_NAMES as BODY_NAMES, index$1_DEFAULT_EXACT_THRESHOLD as DEFAULT_EXACT_THRESHOLD, index$1_DEFAULT_TRANSITING_BODIES as DEFAULT_TRANSITING_BODIES, index$1_DEFAULT_TRANSIT_CONFIG as DEFAULT_TRANSIT_CONFIG, index$1_DEFAULT_TRANSIT_ORBS as DEFAULT_TRANSIT_ORBS, index$1_EXACT_TIME_TOLERANCE as EXACT_TIME_TOLERANCE, index$1_FAST_PLANETS as FAST_PLANETS, index$1_FAST_PLANET_SEARCH_STEP as FAST_PLANET_SEARCH_STEP, index$1_HISTORICAL_TRANSIT_EVENTS as HISTORICAL_TRANSIT_EVENTS, type index$1_HouseIngress as HouseIngress, index$1_LUMINARIES as LUMINARIES, index$1_LUMINARY_ORB_EXTENSION as LUMINARY_ORB_EXTENSION, index$1_MAJOR_TRANSIT_ASPECTS as MAJOR_TRANSIT_ASPECTS, index$1_MAX_BINARY_SEARCH_ITERATIONS as MAX_BINARY_SEARCH_ITERATIONS, index$1_MOON_SEARCH_STEP as MOON_SEARCH_STEP, type index$1_MotionState as MotionState, type index$1_NatalPoint as NatalPoint, type index$1_NatalPointType as NatalPointType, index$1_OUTER_PLANET_ORB_EXTENSION as OUTER_PLANET_ORB_EXTENSION, index$1_REFERENCE_RETROGRADE_PERIODS as REFERENCE_RETROGRADE_PERIODS, index$1_REFERENCE_STATION_POINTS as REFERENCE_STATION_POINTS, index$1_RETROGRADE_PLANETS as RETROGRADE_PLANETS, type index$1_ReferenceRetrogradePeriod as ReferenceRetrogradePeriod, type index$1_ReferenceStationPoint as ReferenceStationPoint, type index$1_ReferenceTransitEvent as ReferenceTransitEvent, type index$1_RetrogradePeriod as RetrogradePeriod, index$1_SLOW_PLANETS as SLOW_PLANETS, index$1_SLOW_PLANET_SEARCH_STEP as SLOW_PLANET_SEARCH_STEP, index$1_STATIONARY_SPEED_THRESHOLD as STATIONARY_SPEED_THRESHOLD, type index$1_StationPoint as StationPoint, index$1_TRANSITING_BODIES as TRANSITING_BODIES, index$1_TYPICAL_RETROGRADE_DURATIONS as TYPICAL_RETROGRADE_DURATIONS, index$1_TYPICAL_TRANSIT_DURATION_DAYS as TYPICAL_TRANSIT_DURATION_DAYS, type index$1_Transit as Transit, index$1_TransitCalculationError as TransitCalculationError, type index$1_TransitConfig as TransitConfig, type index$1_TransitDate as TransitDate, type index$1_TransitPass as TransitPass, type index$1_TransitPhase as TransitPhase, type index$1_TransitResult as TransitResult, index$1_TransitSearchError as TransitSearchError, type index$1_TransitSearchParams as TransitSearchParams, type index$1_TransitSearchResult as TransitSearchResult, type index$1_TransitSummary as TransitSummary, type index$1_TransitTiming as TransitTiming, type index$1_TransitingBody as TransitingBody, index$1_angularSeparation as angularSeparation, index$1_calculateAllIngresses as calculateAllIngresses, index$1_calculateAllIngressesForBodies as calculateAllIngressesForBodies, index$1_calculateSignedDeviation as calculateSignedDeviation, index$1_calculateTransitStrength as calculateTransitStrength, index$1_calculateTransits as calculateTransits, index$1_canRetrograde as canRetrograde, index$1_checkIngressInWindow as checkIngressInWindow, index$1_classifyMotion as classifyMotion, index$1_detectAllTransits as detectAllTransits, index$1_detectHouseIngress as detectHouseIngress, index$1_detectTransit as detectTransit, index$1_estimateNextAspect as estimateNextAspect, index$1_estimatePassCount as estimatePassCount, index$1_filterByAspectType as filterByAspectType, index$1_filterByBodySpeed as filterByBodySpeed, index$1_filterByDuration as filterByDuration, index$1_filterByPhase as filterByPhase, index$1_filterByStrength as filterByStrength, index$1_filterMultiplePasses as filterMultiplePasses, index$1_filterRetrogrades as filterRetrogrades, index$1_findAllExactTimes as findAllExactTimes, index$1_findAllTransits as findAllTransits, index$1_findExactTransitTime as findExactTransitTime, index$1_findIngressToHouse as findIngressToHouse, index$1_findNextIngress as findNextIngress, index$1_findNextStation as findNextStation, index$1_findOrbEntry as findOrbEntry, index$1_findOrbExit as findOrbExit, index$1_findRetrogradePeriods as findRetrogradePeriods, index$1_findStationPoints as findStationPoints, index$1_findTransitPasses as findTransitPasses, index$1_findTransitTiming as findTransitTiming, index$1_formatHouseIngress as formatHouseIngress, index$1_formatRetrogradePeriod as formatRetrogradePeriod, index$1_formatStationPoint as formatStationPoint, index$1_formatTransit as formatTransit, index$1_formatTransitTiming as formatTransitTiming, index$1_formatTransitTimingDetail as formatTransitTimingDetail, index$1_generateSearchSummary as generateSearchSummary, index$1_getActiveTransits as getActiveTransits, index$1_getAllRetrogradePeriods as getAllRetrogradePeriods, index$1_getApplyingTransits as getApplyingTransits, index$1_getBodyHouse as getBodyHouse, index$1_getBodyHouses as getBodyHouses, index$1_getCurrentRetrogradePeriod as getCurrentRetrogradePeriod, index$1_getEffectiveOrb as getEffectiveOrb, index$1_getMostSignificantTransits as getMostSignificantTransits, index$1_getNextTransit as getNextTransit, index$1_getRetrogradeShadow as getRetrogradeShadow, index$1_getSearchStepForBody as getSearchStepForBody, index$1_getSeparatingTransits as getSeparatingTransits, index$1_getSignIndex as getSignIndex, index$1_getStrongestTransit as getStrongestTransit, index$1_getTransitPhase as getTransitPhase, index$1_getTransitingBodies as getTransitingBodies, index$1_getTransitsForMonth as getTransitsForMonth, index$1_getTransitsForYear as getTransitsForYear, index$1_getTransitsFromBody as getTransitsFromBody, index$1_getTransitsToPoint as getTransitsToPoint, index$1_getUpcomingTransits as getUpcomingTransits, index$1_groupByNatalPoint as groupByNatalPoint, index$1_groupByTransitingBody as groupByTransitingBody, index$1_groupIngressesByBody as groupIngressesByBody, index$1_groupIngressesByHouse as groupIngressesByHouse, index$1_groupTransitsByAspect as groupTransitsByAspect, index$1_groupTransitsByBody as groupTransitsByBody, index$1_groupTransitsByMonth as groupTransitsByMonth, index$1_groupTransitsByNatalPoint as groupTransitsByNatalPoint, index$1_hasCrossingInWindow as hasCrossingInWindow, index$1_isOutOfSign as isOutOfSign, index$1_isRetrograde as isRetrograde, index$1_jdToTransitDate as jdToTransitDate, index$1_normalizeAngle as normalizeAngle, index$1_searchTransits as searchTransits, index$1_searchTransitsForBody as searchTransitsForBody, index$1_searchTransitsToPoint as searchTransitsToPoint, index$1_signedAngularDifference as signedAngularDifference, index$1_summarizeTransits as summarizeTransits };
}

/**
 * Zodiac module constants
 *
 * Reference data for zodiac signs, planetary rulerships, and essential dignities.
 *
 * **Traditional Sources (Pre-1781):**
 * - Claudius Ptolemy, "Tetrabiblos" (c. 160 CE) - Book I, Chapters 18-20
 *   Original doctrine of domiciles, exaltations, and essential dignities
 * - William Lilly, "Christian Astrology" (1647) - Pages 104-115
 *   Comprehensive dignity tables, English Renaissance standard
 *
 * **Modern Additions (Post-Discovery):**
 * - Uranus (discovered 1781): Co-rules Aquarius with Saturn
 * - Neptune (discovered 1846): Co-rules Pisces with Jupiter
 * - Pluto (discovered 1930): Co-rules Scorpio with Mars
 *
 * **Note on Outer Planet Exaltations:**
 * Uranus, Neptune, and Pluto have no universally agreed exaltations in
 * traditional astrology, so they are correctly set to undefined.
 *
 * @see Ptolemy's Tetrabiblos: https://www.sacred-texts.com/astro/ptb/
 * @see Skyscript (modern compilation): https://www.skyscript.co.uk/dignities.html
 * @module zodiac/constants
 */

/**
 * Complete data for all twelve zodiac signs
 *
 * Includes element, modality, polarity, and rulers for each sign.
 *
 * @constant
 */
declare const SIGN_DATA: Record<Sign, SignInfo>;
/**
 * Planetary rulerships (domiciles)
 *
 * Each planet rules one or two signs. Traditional rulerships include Mars
 * ruling both Aries and Scorpio. Modern rulerships assign outer planets
 * to Scorpio (Pluto), Aquarius (Uranus), and Pisces (Neptune).
 *
 * @constant
 * @see Ptolemy's Tetrabiblos, Book I, Chapter 18
 */
declare const PLANETARY_RULERSHIPS: Record<Planet, Sign[]>;
/**
 * Planetary exaltations
 *
 * Each of the seven traditional planets has a sign and specific degree
 * where it is considered "exalted" or honored.
 *
 * **Exact Degrees (from traditional sources):**
 * - Sun in Aries at 19°
 * - Moon in Taurus at 3°
 * - Mercury in Virgo at 15° (but Mercury also RULES Virgo, so Domicile takes precedence)
 * - Venus in Pisces at 27°
 * - Mars in Capricorn at 28°
 * - Jupiter in Cancer at 15°
 * - Saturn in Libra at 21°
 *
 * These degrees are from William Lilly's "Christian Astrology" (1647) and
 * represent the traditional teaching. Modern practice often treats the entire
 * sign as the exaltation zone rather than emphasizing the exact degree.
 *
 * **Modern Planets:**
 * Uranus, Neptune, and Pluto have no universally agreed exaltations in
 * traditional astrology. Various modern sources propose different exaltations,
 * but there is no consensus, so they are correctly set to undefined.
 *
 * @constant
 * @see Ptolemy's Tetrabiblos, Book I, Chapter 19
 * @see William Lilly's Christian Astrology (1647), p. 104-115
 */
declare const EXALTATIONS: Record<Planet, ExaltationData | undefined>;
/**
 * Planet symbols (Unicode)
 *
 * Standard astronomical/astrological symbols for the ten planets.
 *
 * @constant
 */
declare const PLANET_SYMBOLS: Record<Planet, string>;
/**
 * Dignity strength values
 *
 * Traditional point system for essential dignities.
 *
 * @constant
 */
declare const DIGNITY_STRENGTH: {
    readonly DOMICILE: 5;
    readonly EXALTATION: 4;
    readonly PEREGRINE: 0;
    readonly DETRIMENT: -5;
    readonly FALL: -4;
};

/**
 * Planetary dignities
 *
 * Functions for determining essential dignities (domicile, exaltation,
 * detriment, fall, peregrine) of planets in zodiac signs.
 *
 * **Essential Dignity Hierarchy (checked in order):**
 * 1. Domicile (+5 points) - Planet rules the sign
 * 2. Detriment (-5 points) - Planet in sign opposite its rulership
 * 3. Exaltation (+4 points) - Planet exalted in the sign
 * 4. Fall (-4 points) - Planet in sign opposite its exaltation
 * 5. Peregrine (0 points) - No special dignity (default)
 *
 * **Traditional Sources:**
 * - Ptolemy's "Tetrabiblos" (c. 160 CE), Book I, Chapters 18-20
 *   Original dignity system from Hellenistic astrology
 * - William Lilly's "Christian Astrology" (1647), Pages 104-115
 *   Comprehensive dignity tables with exact exaltation degrees
 *
 * **Important Note:**
 * Domicile takes precedence over exaltation. For example, Mercury in Virgo
 * is in domicile (Mercury rules Virgo), not in exaltation, even though
 * Mercury is also exalted in Virgo. This follows traditional doctrine.
 *
 * @see Ptolemy's Tetrabiblos: https://www.sacred-texts.com/astro/ptb/
 * @see Skyscript: https://www.skyscript.co.uk/dignities.html
 * @module zodiac/dignities
 */

/**
 * Get planetary dignity for a planet in a given sign
 *
 * Determines the essential dignity state of a planet based on the sign
 * it occupies. Checks in order: domicile, detriment, exaltation, fall,
 * then defaults to peregrine.
 *
 * **Dignity hierarchy:**
 * 1. Domicile (+5): Planet rules the sign
 * 2. Detriment (-5): Planet in sign opposite its rulership
 * 3. Exaltation (+4): Planet exalted in the sign
 * 4. Fall (-4): Planet in sign opposite its exaltation
 * 5. Peregrine (0): No special dignity
 *
 * @param planet - The planet to evaluate
 * @param sign - The sign the planet is in
 * @param degree - Optional: degree within sign (0-29) for exact exaltation check
 * @returns Dignity information including state, strength, and description
 *
 * @example
 * ```typescript
 * // Mars in Aries: Domicile
 * const marsAries = getPlanetaryDignity(Planet.Mars, Sign.Aries);
 * // marsAries.state = DignityState.Domicile
 * // marsAries.strength = +5
 * ```
 *
 * @example
 * ```typescript
 * // Mars in Libra: Detriment (opposite Aries)
 * const marsLibra = getPlanetaryDignity(Planet.Mars, Sign.Libra);
 * // marsLibra.state = DignityState.Detriment
 * // marsLibra.strength = -5
 * ```
 *
 * @example
 * ```typescript
 * // Mars in Capricorn at 28°: Exaltation at exact degree
 * const marsCap = getPlanetaryDignity(Planet.Mars, Sign.Capricorn, 28);
 * // marsCap.state = DignityState.Exaltation
 * // marsCap.exaltationDegree = 28
 * ```
 *
 * @example
 * ```typescript
 * // Mars in Taurus: Peregrine (no special dignity)
 * const marsTaurus = getPlanetaryDignity(Planet.Mars, Sign.Taurus);
 * // marsTaurus.state = DignityState.Peregrine
 * // marsTaurus.strength = 0
 * ```
 *
 * @public
 */
declare function getPlanetaryDignity(planet: Planet, sign: Sign, degree?: number): Dignity;
/**
 * Check if a planet rules a given sign
 *
 * @param planet - The planet
 * @param sign - The sign
 * @returns True if planet rules the sign
 *
 * @example
 * ```typescript
 * isRuler(Planet.Mars, Sign.Aries);      // true
 * isRuler(Planet.Pluto, Sign.Scorpio);   // true (modern)
 * isRuler(Planet.Mars, Sign.Scorpio);    // true (traditional)
 * isRuler(Planet.Mars, Sign.Taurus);     // false
 * ```
 *
 * @public
 */
declare function isRuler(planet: Planet, sign: Sign): boolean;
/**
 * Check if a planet is exalted in a given sign
 *
 * @param planet - The planet
 * @param sign - The sign
 * @returns True if planet is exalted in the sign
 *
 * @example
 * ```typescript
 * isExalted(Planet.Sun, Sign.Aries);       // true
 * isExalted(Planet.Mars, Sign.Capricorn);  // true
 * isExalted(Planet.Uranus, Sign.Scorpio);  // false (no exaltation)
 * ```
 *
 * @public
 */
declare function isExalted(planet: Planet, sign: Sign): boolean;
/**
 * Check if a planet is in detriment in a given sign
 *
 * @param planet - The planet
 * @param sign - The sign
 * @returns True if planet is in detriment
 *
 * @example
 * ```typescript
 * isDetriment(Planet.Mars, Sign.Libra);     // true (opposite Aries)
 * isDetriment(Planet.Sun, Sign.Aquarius);   // true (opposite Leo)
 * ```
 *
 * @public
 */
declare function isDetriment(planet: Planet, sign: Sign): boolean;
/**
 * Check if a planet is in fall in a given sign
 *
 * @param planet - The planet
 * @param sign - The sign
 * @returns True if planet is in fall
 *
 * @example
 * ```typescript
 * isFall(Planet.Sun, Sign.Libra);      // true (opposite Aries exaltation)
 * isFall(Planet.Mars, Sign.Cancer);    // true (opposite Capricorn exaltation)
 * ```
 *
 * @public
 */
declare function isFall(planet: Planet, sign: Sign): boolean;

/**
 * Sign properties and information
 *
 * Functions for retrieving detailed information about zodiac signs,
 * including elements, modalities, polarities, and rulers.
 *
 * @module zodiac/sign-properties
 */

/**
 * Get complete information about a zodiac sign
 *
 * Returns all properties of a sign including element, modality, polarity,
 * rulers, and degree boundaries.
 *
 * @param sign - The zodiac sign (0-11)
 * @returns Complete sign information
 *
 * @example
 * ```typescript
 * const ariesInfo = getSignInfo(Sign.Aries);
 * // {
 * //   sign: Sign.Aries,
 * //   name: 'Aries',
 * //   symbol: '♈',
 * //   element: Element.Fire,
 * //   modality: Modality.Cardinal,
 * //   polarity: Polarity.Positive,
 * //   ruler: Planet.Mars,
 * //   ...
 * // }
 * ```
 *
 * @example
 * ```typescript
 * // For signs with modern rulers
 * const scorpioInfo = getSignInfo(Sign.Scorpio);
 * // scorpioInfo.ruler = Planet.Pluto (modern)
 * // scorpioInfo.traditionalRuler = Planet.Mars
 * // scorpioInfo.modernRuler = Planet.Pluto
 * ```
 *
 * @public
 */
declare function getSignInfo(sign: Sign): SignInfo;
/**
 * Get sign name from sign enum
 *
 * Convenience function to get just the sign name.
 *
 * @param sign - The zodiac sign (0-11)
 * @returns Sign name as string
 *
 * @example
 * ```typescript
 * getSignName(Sign.Leo);    // 'Leo'
 * getSignName(Sign.Pisces); // 'Pisces'
 * ```
 *
 * @public
 */
declare function getSignName(sign: Sign): string;
/**
 * Get sign symbol (Unicode) from sign enum
 *
 * @param sign - The zodiac sign (0-11)
 * @returns Unicode symbol
 *
 * @example
 * ```typescript
 * getSignSymbol(Sign.Aries);    // '♈'
 * getSignSymbol(Sign.Scorpio);  // '♏'
 * ```
 *
 * @public
 */
declare function getSignSymbol(sign: Sign): string;

/**
 * Zodiac position calculations
 *
 * Core functions for converting ecliptic longitude to zodiac position (sign + degree).
 * Implements the tropical zodiac system where 0° Aries = vernal equinox point.
 *
 * **Algorithm Source:**
 * Based on Swiss Ephemeris `swe_split_deg()` by Dieter Koch and Alois Treindl.
 * Swiss Ephemeris is the authoritative implementation used by professional
 * astrological software worldwide.
 *
 * **Key Algorithm:**
 * ```
 * sign_index = floor(longitude / 30)    // 0-11 for the 12 signs
 * degree_in_sign = longitude % 30        // 0.0-29.999...
 * then convert to degrees, minutes, seconds
 * ```
 *
 * **Edge Cases Handled:**
 * - 360° wraps to 0° Aries
 * - Negative longitudes wrap backward through Pisces
 * - Rounding clamped to prevent sign boundary crossing
 *
 * @see Swiss Ephemeris: https://github.com/aloistr/swisseph
 * @module zodiac/zodiac
 */

/**
 * Convert ecliptic longitude to zodiac position
 *
 * Takes a geocentric ecliptic longitude (0-360°) and determines which
 * tropical zodiac sign it falls in, along with the precise degree,
 * minute, and second within that sign.
 *
 * **Algorithm** (based on Swiss Ephemeris):
 * 1. Normalize longitude to 0-360° range
 * 2. Calculate sign index: `floor(longitude / 30)`
 * 3. Calculate degree within sign: `longitude % 30`
 * 4. Convert decimal degrees to degrees/minutes/seconds
 * 5. Handle rounding edge cases
 *
 * @param longitude - Ecliptic longitude in degrees (0-360, but accepts any value)
 * @returns Complete zodiac position with sign, degree, and formatting
 *
 * @example
 * ```typescript
 * // Simple example
 * const pos = eclipticToZodiac(15.5);
 * // pos.sign = Sign.Aries
 * // pos.degree = 15, pos.minute = 30, pos.second = 0
 * // pos.formatted = "15°30'00\" Aries"
 * ```
 *
 * @example
 * ```typescript
 * // Meeus Example 25.a: Venus on 1992 Oct 13
 * const venus = eclipticToZodiac(217.411111);
 * // venus.sign = Sign.Scorpio
 * // venus.degree = 7, venus.minute = 24, venus.second = 40
 * // venus.formatted = "7°24'40\" Scorpio"
 * ```
 *
 * @example
 * ```typescript
 * // Handles normalization
 * eclipticToZodiac(360);  // 0° Aries (wraps around)
 * eclipticToZodiac(-1);   // 29° Pisces (negative wraps backward)
 * ```
 *
 * @public
 */
declare function eclipticToZodiac(longitude: number): ZodiacPosition;
/**
 * Format a zodiac position with custom options
 *
 * Provides flexible formatting for zodiac positions with various display options.
 *
 * @param position - The zodiac position to format
 * @param options - Formatting options
 * @returns Formatted string
 *
 * @example
 * ```typescript
 * const pos = eclipticToZodiac(15.5125);
 *
 * // Default: full format with seconds
 * formatZodiacPosition(pos);
 * // "15°30'45\" Aries"
 *
 * // Without seconds
 * formatZodiacPosition(pos, { includeSeconds: false });
 * // "15°30' Aries"
 *
 * // Decimal degrees
 * formatZodiacPosition(pos, { decimalDegrees: true });
 * // "15.51° Aries"
 *
 * // With symbol instead of name
 * formatZodiacPosition(pos, { useSymbol: true });
 * // "15°30'45\" ♈"
 *
 * // Longitude only (no sign)
 * formatZodiacPosition(pos, { includeSign: false });
 * // "15°30'45\""
 * ```
 *
 * @public
 */
declare function formatZodiacPosition(position: ZodiacPosition, options?: {
    includeSeconds?: boolean;
    includeSign?: boolean;
    useSymbol?: boolean;
    decimalDegrees?: boolean;
}): string;

/**
 * Zodiac module - Public API
 *
 * Zodiac sign calculations, sign properties, and planetary dignities
 * for the tropical zodiac system.
 *
 * @module zodiac
 *
 * @example
 * ```typescript
 * import { eclipticToZodiac, getSignInfo, getPlanetaryDignity, Sign, Planet } from '@celestine/zodiac';
 *
 * // Convert ecliptic longitude to zodiac position
 * const position = eclipticToZodiac(217.411111);
 * // position.sign = Sign.Scorpio
 * // position.formatted = "7°24'40\" Scorpio"
 *
 * // Get sign properties
 * const ariesInfo = getSignInfo(Sign.Aries);
 * // ariesInfo.element = Element.Fire
 * // ariesInfo.modality = Modality.Cardinal
 *
 * // Check planetary dignity
 * const marsAries = getPlanetaryDignity(Planet.Mars, Sign.Aries);
 * // marsAries.state = DignityState.Domicile
 * // marsAries.strength = +5
 * ```
 */

declare const index_DIGNITY_STRENGTH: typeof DIGNITY_STRENGTH;
type index_Dignity = Dignity;
type index_DignityState = DignityState;
declare const index_DignityState: typeof DignityState;
declare const index_EXALTATIONS: typeof EXALTATIONS;
type index_Element = Element;
declare const index_Element: typeof Element;
type index_ExaltationData = ExaltationData;
type index_FormatOptions = FormatOptions;
type index_Modality = Modality;
declare const index_Modality: typeof Modality;
declare const index_PLANETARY_RULERSHIPS: typeof PLANETARY_RULERSHIPS;
declare const index_PLANET_SYMBOLS: typeof PLANET_SYMBOLS;
type index_Planet = Planet;
declare const index_Planet: typeof Planet;
type index_Polarity = Polarity;
declare const index_Polarity: typeof Polarity;
declare const index_SIGN_DATA: typeof SIGN_DATA;
type index_Sign = Sign;
declare const index_Sign: typeof Sign;
type index_SignInfo = SignInfo;
type index_ZodiacPosition = ZodiacPosition;
declare const index_eclipticToZodiac: typeof eclipticToZodiac;
declare const index_formatZodiacPosition: typeof formatZodiacPosition;
declare const index_getPlanetaryDignity: typeof getPlanetaryDignity;
declare const index_getSignInfo: typeof getSignInfo;
declare const index_getSignName: typeof getSignName;
declare const index_getSignSymbol: typeof getSignSymbol;
declare const index_isDetriment: typeof isDetriment;
declare const index_isExalted: typeof isExalted;
declare const index_isFall: typeof isFall;
declare const index_isRuler: typeof isRuler;
declare namespace index {
  export { index_DIGNITY_STRENGTH as DIGNITY_STRENGTH, type index_Dignity as Dignity, index_DignityState as DignityState, index_EXALTATIONS as EXALTATIONS, index_Element as Element, type index_ExaltationData as ExaltationData, type index_FormatOptions as FormatOptions, index_Modality as Modality, index_PLANETARY_RULERSHIPS as PLANETARY_RULERSHIPS, index_PLANET_SYMBOLS as PLANET_SYMBOLS, index_Planet as Planet, index_Polarity as Polarity, index_SIGN_DATA as SIGN_DATA, index_Sign as Sign, type index_SignInfo as SignInfo, type index_ZodiacPosition as ZodiacPosition, index_eclipticToZodiac as eclipticToZodiac, index_formatZodiacPosition as formatZodiacPosition, index_getPlanetaryDignity as getPlanetaryDignity, index_getSignInfo as getSignInfo, index_getSignName as getSignName, index_getSignSymbol as getSignSymbol, index_isDetriment as isDetriment, index_isExalted as isExalted, index_isFall as isFall, index_isRuler as isRuler };
}

export { ALL_ASPECTS$1 as ALL_ASPECTS, type Aspect, type AspectBody, type AspectPattern, AspectType, type BirthData, type CalendarDateTime, CelestialBody, type Chart, type ChartAngles, type ChartAspects, type ChartHouses, type ChartOptions, type ChartPlanet, type ChartSummary, DEFAULT_ORBS, type Dignity, DignityState, Element, type HouseData, type HouseSystem, J2000_EPOCH, MAJOR_ASPECTS$1 as MAJOR_ASPECTS, Modality, type NatalPoint, PatternType, Planet, type PlanetPosition, type ProgressedAngles$1 as ProgressedAngles, type ProgressedAspect, type ProgressedChart, type ProgressedPlanet, type ProgressionBirthData, type ProgressionConfig, type ProgressionType, Sign, type Transit, type TransitConfig, type TransitResult, type ZodiacPosition, index$7 as aspects, calculateAngles, calculateAspects, calculateChart, calculateHouseCusps, calculateHouses, calculatePlanets, calculateProgressedPositions, calculateProgression, calculateSolarArc, calculateTransits, index$6 as chart, deltaT, detectAspect, eclipticToZodiac, index$5 as ephemeris, findPatterns, formatChart, formatProgressedChart, formatTransit, formatZodiacPosition, fromJulianDate, getAllPositions, getAvailableHouseSystems$1 as getAvailableHouseSystems, getMoonPosition, getPlanetaryDignity, getPosition, getProgressedMoonReport, getSignInfo, getSunPosition, getTransitsToPoint, greenwichMeanSiderealTime, index$4 as houses, localSiderealTime, obliquityOfEcliptic, index$3 as progressions, index$2 as time, toJulianDate, index$1 as transits, validateBirth, index as zodiac };
